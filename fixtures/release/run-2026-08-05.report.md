# Release report — spec 14 §2 acceptance gates

## Provenance

| Field | Value |
| --- | --- |
| v2 commit | `9e8c062` |
| Host | aarch64-macos |

## Gate summary

| Gate | Verdict |
| --- | --- |
| quality | PASS |
| memory-quality | PASS |
| latency | baseline only, never gated |
| resources | baseline only, never gated |
| reliability | PASS |
| consistency | PASS |
| sharing | PASS |
| idempotency | PASS |
| rebuild | PASS |

**Overall: PASS**

## Quality (search)

Hit@1=0.5918 Hit@3=0.8367 Hit@5=0.8367 MRR=0.7007 (v1: 0.5918/0.7959/0.8367/0.6963)

## Memory quality (router)

Precision=0.6757 Recall=0.5682 F1=0.6173

## Latency (baseline)

| Scenario | p50 ms | p95 ms |
| --- | --- | --- |
| warm search | 112.185 | 701.828 |
| reconcile: one file | 37.954 | 40.380 |
| reconcile: branch checkout (10 files) | 171.094 | 173.676 |

## Resources (baseline)

| Metric | Value |
| --- | --- |
| Idle RAM (min/mean/max/last, bytes) | 19906560 / 19906560 / 19906560 / 19906560 |
| state.sqlite bytes | 1413120 |
| cache.sqlite bytes | 5128192 |
| shard dir bytes | 1709465 |
| bytes/symbol (545 occurrences) | 15139.04 |
| embedding cache budget ratio | 0.0008 (1652736 / 2147483648 bytes) |
| source/worktree byte ratio | 0.2347 (496069 / 2113957 bytes) |

## Test citations

### Reliability — PASS

Commands:

- `cargo test -p local-rag-store --features failpoints`
- `cargo test -p local-rag-hook --features failpoints`
- `cargo test -p local-rag-projection --features failpoints`

Named tests:

- local-rag-projection::fault_matrix / fault_matrix_coverage (F1-F12, spec 05 §10)
- local-rag-hook::kill_matrix (S1-S2, spec 07 §7)
- local-rag-store::spool_kill_matrix (S3-S8, spec 07 §7)

Spec refs:

- docs/specification/14-acceptance-and-testing.md §2 (reliability)

### Consistency — PASS

Commands:

- `cargo test -p local-rag-search --features failpoints`

Named tests:

- local-rag-search::generation_mixing / switch_concurrency / switch_failpoint_load (G09)

Spec refs:

- docs/specification/14-acceptance-and-testing.md §2 (consistency)

### Sharing — PASS

Commands:

- `cargo test -p local-rag-index --features failpoints`

Named tests:

- local-rag-index::persist / reconcile (structural sharing, G05)

Spec refs:

- docs/specification/14-acceptance-and-testing.md §2 (sharing)

### Idempotency — PASS

Commands:

- `cargo test -p local-rag-store --features failpoints`
- `cargo test -p local-rag-index --features failpoints`

Named tests:

- local-rag-store::memory_op (idempotency-key replay, G14)
- local-rag-index::reconcile / persist (retry produces no duplicate content, G05)

Spec refs:

- docs/specification/14-acceptance-and-testing.md §2 (idempotency)

### Rebuild — PASS

Commands:

- `cargo test -p local-rag-store --features failpoints`
- `cargo test -p local-rag-projection --features failpoints`

Named tests:

- local-rag-store::fts_validate / fts_corruption (validate-on-open / rebuild, G08)
- local-rag-projection::rebuild_faults / fault_matrix (dense rebuild-on-doubt, G07/G08)

Spec refs:

- docs/specification/14-acceptance-and-testing.md §2 (rebuild)

