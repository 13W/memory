{"hookSpecificOutput":{"additionalContext":"local-rag is not installed, so this session has no memory, no hybrid code search and no observation capture: the tools are absent, and nothing was recalled or recorded. The plugin never downloads anything — obtaining the native binaries is the npm package's job. Fix: `npm i -g @13w/memory`. Offline or air-gapped: set `LOCAL_RAG_BIN_DIR` to a directory of prebuilt binaries instead.","hookEventName":"SessionStart"}}
