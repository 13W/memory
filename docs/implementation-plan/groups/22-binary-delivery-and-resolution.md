# Группа 22 — Binary delivery и executable-based resolution (post-v0)

Пятая группа, открытая после закрытия `T00–T17` (`G17: PASS`) по явному продуктовому решению
владельца от 2026-08-25; само решение записывается в `docs/adr/0013-binary-delivery-via-release-assets.md`
карточкой `T22-01` — до неё ссылки на этот ADR опережающие. Не переоткрывает ни один гейт `G00–G21`.
Цель: плагин перестаёт ходить в сеть и ищет **исполняемый файл**, а не npm-пакет; `@13w/memory`
становится единственным npm-пакетом и сам получает нативные бинарники из GitHub release assets;
пять platform-пакетов удаляются; `libonnxruntime` переезжает в установщик первого запуска
`local-rag`, туда же, где уже живут веса моделей.

Ссылки: ADR-0013 (создаётся `T22-01`); ADR-0005 (прецедент verify-before-trust и «эталон — вкомпилированный каталог»);
spec 01 §1/§2 `[FIXED]`; spec 10 §5 `[FIXED policy]`; spec 11 §3.1 `[FIXED]` (always exit 0),
§3.2, §5; spec 12 §1; spec 13 §1/§2/§4 (включая as-built T17-03, T19-03 и D-055);
`plugin/bin/local-rag-mcp-launcher.js`; `plugin/hooks/hooks.json`; `npm/memory/`;
`crates/models/src/{install.rs,fetch.rs,onnx.rs}`; `crates/local-rag-proxy/src/connect.rs:55`;
`crates/xtask/src/dist_ort.rs`; `dist-workspace.toml`; `.github/workflows/release.yml`;
D-028, D-029, D-033, D-035, D-055, D-084.

Формат карточек — по `TASK-TEMPLATE.md`, структура группы — как в группах 18/20/21. Один таск —
одна итерация — один коммит.

## Диагноз (контекст для исполнителя — прочитать до начала)

1. **Нормативно зафиксированный канал доставки не реализован.** `13 §1:7-11` `[FIXED]` объявляет
   «per-platform packages as `optionalDependencies`». Фактически: `bin/` пяти пакетов лежит в
   `.gitignore:43-47` и наполняется руками (`cargo build --release`, `cargo zigbuild`,
   `cargo xtask dist-ort`); ни скрипта, ни xtask-подкоманды, ни CI-шага, который бы это делал, в
   репозитории нет. В npm-реестр не публиковалось ничего: `npm publish`, `NPM_TOKEN`,
   `registry.npmjs` дают ноль совпадений вне полей `publishConfig`. Все шесть пакетов на `0.0.0`,
   тулинга бампа версий нет. У `win32-x64` бинарников нет вовсе (D-029, D-033), а `local-rag-tui`
   отсутствует во всех пяти, хотя bin `local-rag-dashboard` объявлен.
2. **Незафиксированный канал, наоборот, работает.** `.github/workflows/release.yml` (сгенерирован
   `cargo-dist`) по тегу собирает пять таргетов и заливает в GitHub Release архивы по бинарю ×
   таргету с `.sha256`-сайдкарами и `dist-manifest.json`. Релиз `0.0.0` существует и содержит
   34 ассета. То есть группа не строит новый канал, а перестаёт поддерживать мёртвый.
3. **`libonnxruntime` есть ТОЛЬКО в platform-пакетах.** ADR-0005 выбрал `ort`/`load-dynamic`:
   `crates/models/src/onnx.rs:326 bundled_ort_dylib_path` ищет библиотеку рядом с исполняемым
   файлом, иначе — типизированная `OnnxError::Runtime`. Проверено по `dist-manifest.json` релиза
   `0.0.0`: в архивах лежит **только** исполняемый файл. Удаление пакетов без переноса ORT ломает
   индексацию на любой чистой машине. Это главный технический риск группы, и он закрывается
   `T22-15` до `G22`, а не «когда-нибудь».
4. **`.tar.xz` нечем распаковать из Node.** В `node:zlib` есть gzip и inflate и не будет xz.
   `crates/xtask/src/dist_ort.rs` шеллится в системный `tar` и в собственном doc-комментарии
   оговаривает, что это допустимо для dev-инструмента, а не для того, что обязано работать офлайн
   в проде. На минимальных Linux-образах GNU tar зовёт внешний `xz` и падает с `xz: Cannot exec`.
   Поэтому `T22-07` меняет формат архива в `dist-workspace.toml`, а не учит продуктовый код
   шеллиться.
5. **Контракт обёртка↔бинарь не semver-safe** (`PROGRESS.md:1390`: «версия lockstep `"0.0.0"`, не
   semver-range»), а владелец выбрал «всегда `latest`». Отсюда два обязательства, а не одно:
   манифест установки хранит `packageVersion` и при несовпадении делает установку «отсутствующей»
   (обновление обёртки инвалидирует старые бинарники автоматически), и — отдельно — ADR-0013
   обязан прямо сказать, что сверка с `.sha256` из того же релиза защищает от повреждения и
   подмены на транспорте, но **не** от скомпрометированного релиза. Формулировка ADR-0005
   («verified against the **compiled-in catalog**») к `latest` неприменима, и делать вид, что
   применима, нельзя.
6. **`pnpm link --global` не запускает `postinstall`.** Поэтому локальный сценарий владельца
   обязан работать через резолв, а не через установку: пакет, лежащий внутри чекаута, обнаруживает
   это сам и берёт `target/release`. Та же ветка — защита от того, чтобы `npm install` внутри
   чекаута затёр закоммиченные заглушки.
7. **`resolve_daemon_binary_path` ищет демона рядом с собой** (`connect.rs:55`, имя ровно
   `local-rag`/`local-rag.exe`), и `bundled_ort_dylib_path` использует ту же конвенцию. Поэтому
   пути в `bin`-карте обязаны совпадать с каноническими именами нативных файлов — это структурная
   гарантия co-location, а не косметика.
8. **Бюджеты производительности мерились не на том, что исполняется.** Записанные 5 мс для хука —
   это голый exec симлинка, а `hooks.json` на холодном кэше исполнял совсем другую строку. Новый
   замер берёт строку целиком. Число вырастет; это повышение честности, а не регрессия, и текст
   поправки обязан сказать, что числа несравнимы.

Порядок работ: регистрация (`T22-00`), затем нормативная авторизация (`T22-01` ADR, `T22-02`
rev 7) — до неё ни одна правка `[FIXED]`-текста не легальна, — затем спека (`T22-03`, `T22-04`),
затем три независимые ветки: npm-пакет (`T22-05`…`T22-11`), плагин (`T22-12`…`T22-14`) и
ORT/релиз (`T22-15`…`T22-17`). Ветки не зависят друг от друга и могут идти в любом порядке.

## T22-00 — Регистрация scope

- **Зависит от:** —.
- **Спецификация:** `TRACEABILITY.md` (правило «новая scope → ADR + группа»); `CLAUDE.md`
  (deviation workflow).
- **Результат:** этот файл, секция `## 22` в `PROGRESS.md`, шесть строк `D-102`…`D-107` в
  `DEVIATIONS.md` (статус `open`, корректирующие задачи названы), абзац под таблицей Gate results,
  называющий устаревающие строки трасс `G17`/`G19`, строки `01`/`10`/`11`/`12`/`13` в
  `TRACEABILITY.md` → `G22`.
- **В scope:** только документация; все перекрёстные ссылки обязаны резолвиться.
- **Не в scope:** сам ADR-0013 и пятый абзац-прецедент в `TRACEABILITY.md` — они в `T22-01`;
  любые правки `[FIXED]`-текста; любой код.
- **Тесты:** нет.
- **Приёмка:** перечисленные файлы существуют и внутренне непротиворечивы; каждая ссылка либо
  резолвится на существующий файл, либо явно помечена как опережающая и названа карточкой, которая
  её создаёт; ни одна карточка `T22-01+` не считается начатой до этого.
- **Evidence:** строка `T22-00` в «Task evidence» (`PROGRESS.md`).

## T22-01 — ADR-0013: доставка бинарников и резолв по исполняемому файлу

- **Зависит от:** `T22-00`.
- **Спецификация:** ADR-0005 (форма и планка); `TRACEABILITY.md` (список прецедентов).
- **Результат:** `docs/adr/0013-binary-delivery-via-release-assets.md` в форме Nygard, на
  английском, плюс пятый абзац-прецедент в `TRACEABILITY.md`.
- **В scope:** четыре связанных решения — (1) релизные ассеты вместо platform-пакетов;
  (2) модель доверия при `latest`, названная своими словами вместе с обязательством проверять
  GitHub artifact attestation; (3) резолв по исполняемому файлу вместо резолва npm-пакета;
  (4) ORT как артефакт первого запуска. Отклонённые альтернативы записываются, включая
  «`.mcp.json` зовёт `local-rag-proxy` напрямую» (убивает known-dirs восстановление и превращает
  «не установлено» в непрозрачный `ENOENT`) и «пин по версии пакета».
- **Не в scope:** правки спеки и `idea.md` — это `T22-02`…`T22-04`.
- **Тесты:** нет.
- **Приёмка:** ADR называет все четыре решения и их цену; абзац-прецедент в `TRACEABILITY.md`
  резолвится на существующий файл.

## T22-02 — `idea.md` rev 7 и сверка цитат

- **Зависит от:** `T22-01`.
- **Спецификация:** `docs/specification/README.md:47` («rev 6 decisions win **until a rev 7
  exists**»); `docs/implementation-plan/README.md:5`.
- **Результат:** `docs/idea.md` становится rev 7; все документы, ссылающиеся на «rev 6», обновлены.
- **В scope:** `idea.md:1` (заголовок), `:4-9` (абзац-дельта, называющий что именно меняет rev 7),
  `:16`, `:596-605` (глава 11 «Дистрибуция»), `:628` (строка тулинга — проверить, изменилась ли);
  `CLAUDE.md:15-16` и `:22`; `docs/specification/README.md:3/:15/:46-49`;
  `docs/implementation-plan/README.md:5`.
- **Не в scope:** нормативный текст спеки (`T22-03`/`T22-04`); «Веса не в npm» остаётся истинным
  и не трогается.
- **Тесты:** нет.
- **Приёмка:** `rg 'rev 6'` не даёт совпадений, утверждающих действующую ревизию; глава 11 больше
  не обещает `optionalDependencies`. Закрывает нормативную половину `D-102`.

## T22-03 — Спека: что доставляется

- **Зависит от:** `T22-02`.
- **Спецификация:** spec 01 §1/§2; spec 13 §1; spec 10 §5.
- **Результат:** нормативный текст описывает один npm-пакет и релизные ассеты.
- **В scope:** `01-overview.md:11-13` `[FIXED]` (предложение о дистрибуции), `:20-21` (пять
  таргетов остаются, но как таргеты релизных ассетов); `13 §1:7-11` (буллет
  `optionalDependencies`), `:12` (build tooling — проверить, что `cargo-dist`/`cargo-zigbuild`
  по-прежнему верны), `:13-19` (бинарники «per platform package» → «per release asset»);
  `10 §5:433` («bundling … per platform package … owned by T17-03») и `:461` («Excluding weights
  from the npm packages is T17-01's packaging test»); as-built T17-03 `13:131-157` помечается
  замещённым, но **не удаляется**.
- **Не в scope:** получение/доверие/обновление (`T22-04`); код.
- **Тесты:** нет.
- **Приёмка:** `D-102` переводится в `resolved`; спека нигде не обещает platform-пакет.

## T22-04 — Спека: получение, доверие, обновление, «не установлено»

- **Зависит от:** `T22-02`.
- **Спецификация:** spec 13 §2/§4; spec 12 §1; spec 11 §3.1/§3.2/§5.
- **Результат:** написан весь нормативный текст, которого сегодня нет.
- **В scope:** `13 §2:121-129` `[FIXED list]` — буллеты «resolution under pnpm/npm/yarn layouts»
  и «clear, actionable error when the platform package is missing» переформулированы под модель
  исполняемых файлов, требование внятной ошибки **усиливается**; сам контракт резолва
  (упорядоченный список кандидатов, `LOCAL_RAG_BIN_DIR`, «плагин не качает никогда»);
  `13 §4:212-228` — триггер обновления без npm плюс инвариант co-location как требование, а не
  случайность; `12 §1` — новый абзац о сетевом egress для бинарей и ORT в форме заметки T11-06
  (публичные байты внутрь, пользовательский контент наружу не уходит) с честной моделью доверия;
  `11 §3.2` — поведение при отсутствии бинаря с явной оговоркой, что уведомление не является
  recall-выводом и потому не нарушает `[FIXED]` «Empty recall ⇒ no output at all» из `11 §5:632`;
  as-built T19-03 `13:38-63` и as-built D-055 `13:65-95` помечаются замещёнными.
- **Не в scope:** `11 §3.1` `[FIXED]` (семь событий, always exit 0, ≤200 мс) — **не меняется**;
  карточка обязана записать это явно, чтобы гейт не переоткрывал вопрос.
- **Тесты:** нет.
- **Приёмка:** `D-105`, `D-106`, `D-107` переводятся в `resolved`.

## T22-05 — Чистые основания npm-пакета

- **Зависит от:** `T22-03`, `T22-04`.
- **Спецификация:** `13 §1` (таргеты), ADR-0013.
- **Результат:** модули без ввода-вывода, полностью покрытые тестами, до того как что-либо
  открывает сокет.
- **В scope:** `npm/memory/src/platform.js` — сохранить пять поддерживаемых ключей и
  `win32-arm64` в deferred, удалить `platformPackageName`, добавить `exeSuffix(platform)` и
  `TARGET_TRIPLES` ровно по `dist-workspace.toml`; новый `src/release.js`; переписанный
  `src/errors.js` (`formatNotInstalledError`, `formatSourceCheckoutNotBuiltError`,
  `formatChecksumMismatchError`, `formatAssetAbsentError` и прочие).
- **Не в scope:** сеть, файловая система, удаление platform-пакетов.
- **Тесты:** `platform.test.js` (пять таргетов, deferred, round-trip триплетов против литерального
  списка из `dist-workspace.toml`); `release-urls.test.js` — `parseSha256Sidecar` на реальной форме
  `<64 hex> *<имя>`, на форме с двумя пробелами, с хвостовым переводом строки, на чужом имени, на
  63 символах и не-hex; `parseTagFromLocation` на реальной строке `Location`, на строке с query и
  на несовпадающей; `errors.test.js` — каждое сообщение называет ключ/ассет/оба дайджеста и даёт
  одну исполняемую команду.
- **Приёмка:** `cd npm/memory && node --test test/*.test.js` зелёный; ни один новый модуль не
  требует сети.

## T22-06 — Сетевой шов

- **Зависит от:** `T22-05`.
- **Спецификация:** `12 §1` (as-built из `T22-04`); ADR-0013 (модель доверия).
- **Результат:** единственный модуль, открывающий сокет, и локальный сервер-фикстура, на котором
  живут все дальнейшие тесты.
- **В scope:** `src/http.js` — `resolveLatestTag` (читает `location` первого хопа, не следуя за
  ним), `httpGetToString` с жёстким потолком, `httpGetToFile` с хэшированием на лету; редиректы
  до пяти, межхостовые (GitHub уводит на `release-assets.githubusercontent.com`); ретраи только на
  сетевых ошибках и 5xx/429 и **никогда** на несовпадении дайджеста; таймауты на соединение и на
  простой между чанками, не на общую длительность; прокси через `CONNECT` с
  `LOCAL_RAG_HTTPS_PROXY`/`npm_config_https_proxy`/`npm_config_proxy`/`HTTPS_PROXY`/`ALL_PROXY`,
  обход по `NO_PROXY`/`npm_config_noproxy`, `npm_config_cafile` читается в `ca`.
  `Authorization` не выставляется никогда — репозиторий публичный. `npm_config_strict_ssl=false`
  не поддерживается сознательно.
  `test/helpers/fixture-server.js` — `node:http` на `127.0.0.1:0` с инъекцией отказов.
- **Не в scope:** установка на диск, распаковка.
- **Тесты:** `http-proxy.test.js` — локальный CONNECT-прокси перед фикстурой, `HTTPS_PROXY` и
  `npm_config_proxy` соблюдаются, `NO_PROXY` обходит; редирект-цепочка отдаёт тег; ретрай на
  `500`-затем-`200`; отсутствие ретрая на неверном дайджесте. TLS в scope не входит — записать в
  шапке файла.
- **Приёмка:** ни один тест не резолвит `github.com`.

## T22-07 — Формат архива

- **Зависит от:** `T22-06`.
- **Спецификация:** ADR-0013; `13 §1:12` (build tooling).
- **Результат:** релизные архивы распаковываются встроенными средствами Node на всех платформах.
- **В scope:** `dist-workspace.toml` получает `unix-archive = ".tar.gz"` и
  `windows-archive = ".zip"`; `src/archive.js` — `extractSingleMember(archive, expectedName, dest)`
  на `zlib` плюс чтение одного tar-члена и одного zip-члена; отвергается член с абсолютным путём,
  с `..` или с чужим именем, а также архив более чем с одним членом.
- **Не в scope:** перевыпуск релиза (`T22-17`); шелл-аут в системный `tar` — он рассмотрен и
  отклонён, причина записывается в doc-комментарии модуля.
- **Тесты:** `archive-extract.test.js` — извлечение одного члена из `.tar.gz` и из `.zip`;
  отказ на многочленном, абсолютном и `..`-члене.
- **Приёмка:** распаковка не зависит от `PATH` и от наличия `xz` в системе.

## T22-08 — Установка: замок и verify-before-trust

- **Зависит от:** `T22-07`.
- **Спецификация:** `crates/models/src/install.rs` (форма, пункт в пункт); D-084 (урок
  освобождения замка); spec 10 §5 `[FIXED policy]` (форма атомарной загрузки).
- **Результат:** установка идемпотентна, резюмируема без журнала и безопасна при гонке.
- **В scope:** `src/lock.js` — `withInstallLock` на `O_EXCL`, кража протухшего замка по мёртвому
  pid или по возрасту, double-checked проверка манифеста после захвата, освобождение только если
  владельцем всё ещё являемся мы; `src/install.js` — `.part` во временном каталоге на **той же**
  файловой системе, `fsync` до сверки дайджеста, `rename`, `fsync` каталога, манифест
  `.local-rag-install.json` **последним**; режим `0755` с явной пометкой, почему он отличается от
  `0600` в `install.rs`; опциональные ассеты (`local-rag-tui` отсутствует в релизе `0.0.0`)
  записываются как `absent`, а не роняют установку; `scripts/install.js` как отдельная точка
  входа, в том числе `--background`.
- **Не в scope:** резолв (`T22-09`), шимы и `postinstall` (`T22-10`).
- **Тесты:** `install-verify.test.js` — счастливый путь (манифест последним, режим, тег записан,
  временный каталог убран); несовпадение дайджеста (ничего не установлено, нет `.part`, нет
  манифеста, оба дайджеста в сообщении); прерывание между ассетами (манифеста нет → повтор
  доводит → второй повтор no-op); 404 на опциональном ассете; 404 на обязательном.
  `install-concurrency.test.js` — два **реальных** процесса на одном каталоге, по логу
  сервера-фикстуры скачал ровно один; протухший замок крадётся; живой замок заставляет
  хук-путь выйти немедленно.
- **Приёмка:** прерывание на любом шаге лечится повторным запуском.

## T22-09 — Резолв и обнаружение чекаута

- **Зависит от:** `T22-08`.
- **Спецификация:** `13 §2` (контракт резолва из `T22-04`); `crates/core/src/paths/` (порядок
  env для каталога данных).
- **Результат:** `src/locate.js` вместо `src/resolve.js`; `pnpm link --global` из чекаута отдаёт
  `target/release` без сети и без `postinstall`.
- **В scope:** порядок `LOCAL_RAG_BIN_DIR` (не проваливается дальше и не качает) → чекаут →
  `<pkg>/bin` → кэш `<dataDir>/local-rag/bin/<triple>` → «не установлено»; `manifestValid` со
  сверкой `packageVersion`; `sourceCheckoutRoot()` через `realpathSync` с проверкой, что
  `npm/memory` найденного дерева — **тот же самый** пакет; `installInfo()` как поверхность
  диагностики. Переименование файла намеренное: устаревшие вызовы обязаны падать громко.
- **Не в scope:** удаление platform-пакетов (`T22-11`).
- **Тесты:** `locate-order.test.js` — каждый уровень бьёт следующий; отсутствующий
  `LOCAL_RAG_BIN_DIR` это типизированная ошибка, а не проваливание; каталог без манифеста не
  доверенный; манифест с чужим `packageVersion` не доверенный; `.exe` при подставленной
  `platform:"win32"`. `source-checkout-detection.test.js` — синтетическое дерево, `target/release`
  бьёт `target/debug`, скачиваний ноль по логу фикстуры, почти-совпадающее дерево **не**
  опознаётся, симлинкованная (pnpm) раскладка резолвится так же.
- **Приёмка:** `D-107` закрыт кодом (нормативно закрыт в `T22-04`).

## T22-10 — Шимы, подмена, `postinstall`

- **Зависит от:** `T22-09`.
- **Спецификация:** ADR-0013; `13 §1` (`bin`-карта); `connect.rs:55` (инвариант co-location).
- **Результат:** после установки на `PATH` лежит нативный бинарь, а не Node-шим.
- **В scope:** `bin`-карта, где пути совпадают с каноническими именами нативных файлов, а
  `local-rag-mcp`/`local-rag-dashboard` остаются устаревшими алиасами на один релиз; четыре
  JS-заглушки; `src/shim.js` с тремя режимами (хук — fail-open и **детач**-починка вместо
  синхронного скачивания; MCP и dashboard — синхронно, диагностика только в stderr);
  `src/replace-shims.js` — `linkSync` + `renameSync`, **никогда** `writeFileSync` поверх (pnpm
  жёстко линкует файлы из общего стора), пропуск на win32 и под yarn, отключение
  `LOCAL_RAG_NO_BIN_REPLACE`; `scripts/postinstall.js`, всегда выходящий 0, с ветками
  «пропущено», «`LOCAL_RAG_BIN_DIR`», «чекаут», «неподдерживаемая платформа», «некуда писать»;
  `scripts/prepack.js`, отказывающийся паковать пакет с нативной нагрузкой, манифестом или
  замком в `bin/`; `files` и `scripts` в `package.json`; `os`/`cpu` намеренно **не** добавляются.
- **Не в scope:** удаление platform-пакетов (`T22-11`).
- **Тесты:** `shim-behavior.test.js` — проброс argv и кода возврата, stdout не тронут; хук-шим
  выходит 0 на любом отказе, MCP-шим выходит 1 на том же наборе. `bin-replacement.test.js`
  (POSIX) — после `postinstall` файл побайтово равен отданному фикстурой и исполняемый; **жёсткая
  ссылка, снятая до установки, сохраняет исходные байты шима** (исключает класс «порча
  pnpm-стора»); предсозданный симлинк `.bin/` по-прежнему резолвится; `LOCAL_RAG_NO_BIN_REPLACE=1`
  оставляет шим, и он всё равно находит нативный бинарь. `package-contents.test.js` переписан на
  один пакет с отравленной копией, `npm pack --dry-run` обязан **упасть** через `prepack`.
- **Приёмка:** установка в контейнере без `xz` и отдельно с `--ignore-scripts` даёт рабочие
  команды.

## T22-11 — Снос platform-пакетов

- **Зависит от:** `T22-10`.
- **Спецификация:** `13 §1` (as-built из `T22-03`).
- **Результат:** в репозитории остаётся один npm-пакет.
- **В scope:** удаление `npm/memory-{darwin-arm64,darwin-x64,linux-x64,linux-arm64,win32-x64}/`
  целиком; `optionalDependencies` из `npm/memory/package.json`; строка `npm/memory-*/bin/` из
  `.gitignore`; корневой `package.json` переводится на `{"dependencies": {"@13w/memory":
  "file:npm/memory"}}`, `package-lock.json` перегенерируется; удаление четырёх мёртвых тестов
  (`resolve-layout-matrix`, `resolve-supported-platforms`, `resolve-missing-package`,
  `resolve-dashboard-binary`) вместе с их предметом; `src/binary-cache.js` удаляется вместе с
  кэш-тиром плагина.
- **Не в scope:** правки плагина (`T22-12`/`T22-13`).
- **Тесты:** весь набор `npm/memory` зелёный без удалённых файлов; `rg 'memory-darwin|memory-linux|memory-win32'`
  не даёт совпадений вне истории и спеки.
- **Приёмка:** `npm pack --dry-run` в `npm/memory` даёт один пакет ожидаемого состава.

## T22-12 — Лаунчер плагина: резолв исполняемого файла

- **Зависит от:** `T22-04`.
- **Спецификация:** `13 §1/§2` (контракт резолва); `13 §1:19` (бюджет p95).
- **Результат:** лаунчер не знает ни про npm-пакеты, ни про `npx`, ни про кэш.
- **В scope:** удаление `platformPackageName`, `npmGlobalNodeModules`, `globalNodeModulesAnchor`,
  `cachedProxyPath`, `tier1`, `tier2`, `tier3` и трёхтирового doc-комментария; добавление чистой
  `candidateBinDirs({env, platform, execPath})` и `resolveBinary`; `runChildAndExit` сохраняется
  **дословно** (это `[FIXED list]` про проброс сигналов); ветка отказа — stdout байт в байт пуст,
  stderr называет `npm i -g @13w/memory` и `LOCAL_RAG_BIN_DIR`, список кандидатов только под
  `LOCAL_RAG_DEBUG=1`, выход 1; тестовый шов `LOCAL_RAG_TEST_BIN_DIRS` **заменяет весь** список.
- **Не в scope:** хуки (`T22-13`); зависимость от `npm/memory/src` любого рода — её больше нет.
- **Тесты:** `mcp-launcher-resolution.test.js` вместо `mcp-launcher-tiers.test.js` — попадание в
  PATH; попадание в известный каталог при урезанном PATH (случай launchd); `LOCAL_RAG_BIN_DIR`
  бьёт PATH; PATH бьёт известные каталоги (через `POISON_BINARY_SRC`, чтобы неверный выбор падал
  громко); ничего не нашлось → stdout пуст, stderr совпадает, код ≠ 0; проброс SIGTERM и смерть
  внука; чистые пер-платформенные тесты `candidateBinDirs`. Перезамер `p95 < 100 мс` на любом
  разрешённом пути **и** на неразрешённом.
- **Приёмка:** `plugin/test/*` больше не импортирует ничего из `npm/memory` (вендорить `tmp.js` и
  `proc.js` в `plugin/test/helpers/`).

## T22-13 — Хук-резолвер, семь команд и сигнал «не установлено»

- **Зависит от:** `T22-04`.
- **Спецификация:** `11 §3.1` `[FIXED]` (семь событий, always exit 0); `11 §3.2` (as-built из
  `T22-04`); `13 §1:15` (бюджет хука).
- **Результат:** хуки резолвятся без Node и без сети, а отсутствие сервера доходит до Клода.
- **В scope:** `plugin/bin/local-rag-resolve-hook.sh` — POSIX `sh` только на встроенных командах,
  скан и `exec`, код 127 при промахе; шесть событий получают `… || true`; `SessionStart`
  складывает сигнал внутрь себя (при успехе stdout принадлежит бинарю, при 127 бинарь не печатал
  ничего — уведомление единственный писатель, второй записи не нужно);
  `plugin/hooks/not-installed.json` — golden-файл в том же конверте, что печатает
  `crates/local-rag-hook/src/recall.rs::print_hook_output`, без единой интерполяции.
- **Не в scope:** лаунчер MCP (`T22-12`).
- **Тесты:** все семь строк через `sh -c` с отравленным окружением — выход 0 в каждом случае;
  SessionStart без бинаря даёт байт-точный golden; SessionStart с бинарём — уведомление
  отсутствует; остальные шесть при отсутствии бинаря молчат; `hooks-list.test.js` теряет ассерт
  про `|| npx `; перезамер `< 50 мс` на всей строке целиком в трёх случаях (PATH, последний
  известный каталог, промах).
- **Приёмка:** `D-103` закрыт кодом; ни одна строка `hooks.json` не содержит `npx`.

## T22-14 — Паритет резолверов, доказательство отсутствия сети, синхронизация доков

- **Зависит от:** `T22-12`, `T22-13`.
- **Спецификация:** `13 §2`; `CONTRIBUTING.md`; `plugin/README.md`.
- **Результат:** два резолвера доказуемо согласованы, и ни один путь не может уйти в сеть.
- **В scope:** режим `--print-candidates` у шелл-скрипта и экспорт `candidateBinDirs()` из JS,
  тест сравнивает упорядоченные списки на одном окружении; тест «сети не бывает» — `PATH` из
  временного каталога с ядовитыми шимами `npx`/`npm`/`curl`, трогающими файл-маркер, и утверждение,
  что маркер не появился (инверсия нынешнего `helpers/fake-npx.js`); расширение
  `no-writes-in-sample-repo.test.js` утверждением, что `${CLAUDE_PLUGIN_DATA}` остаётся пустым;
  `CONTRIBUTING.md:344-470` и `plugin/README.md:9-24` приводятся к как-построено, включая честную
  замену чисел бюджетов.
- **Не в scope:** новая функциональность.
- **Тесты:** перечислены в scope.
- **Приёмка:** утверждение «плагин ничего не качает» проверяется тестом, а не комментарием.

## T22-15 — ORT в установщик первого запуска

- **Зависит от:** `T22-03`.
- **Спецификация:** `10 §5` `[FIXED policy]`; `13 §2:129` + as-built `:131-157`; D-028 (запрет
  на неявный поиск `ort`).
- **Результат:** `libonnxruntime` доступна на чистой машине без platform-пакетов.
- **В scope:** рантайм-каталог ORT, порождённый из `crates/xtask/src/dist_ort.rs::ORT_ASSETS`
  (единственный источник истины, пин URL и SHA-256 сохраняется полностью); установка через
  `AssetFetcher`/`HttpFetcher` в форме `install.rs`; `resolve_ort_source` получает третий источник
  между override и «рядом с exe»; запрет D-028 сохраняется дословно.
- **Не в scope:** поверхность CLI (`T22-16`); смена версии ORT.
- **Тесты:** порядок резолва как чистая функция на всех четырёх комбинациях; установка с
  подставленным `AssetFetcher` (дайджест сходится / не сходится); отсутствие библиотеки даёт
  типизированную ошибку, а не зависание.
- **Приёмка:** `D-104` закрыт; индексация работает на дереве без `npm/`.

## T22-16 — Поверхность `init`/`doctor` и судьба `xtask dist-ort`

- **Зависит от:** `T22-15`.
- **Спецификация:** `11 §6` (CLI); `10 §5`.
- **Результат:** пользователь может поставить ORT явно и увидеть, что именно установлено.
- **В scope:** установка ORT в `local-rag init`; `doctor` сообщает наличие и происхождение
  библиотеки, а также разрешённый тег и источник бинарников из `installInfo()`;
  `crates/xtask/src/dist_ort.rs` либо выводится из обращения, либо получает doc-комментарий,
  указывающий на нового владельца заботы, — молча ссылаться на удалённый каталог нельзя.
- **Не в scope:** новые проверки, не связанные с доставкой.
- **Тесты:** CLI-тесты на обе поверхности.
- **Приёмка:** `local-rag doctor` на чистой машине объясняет, чего не хватает и как это починить.

## T22-17 — Релизный пайплайн и контракт имён ассетов

- **Зависит от:** `T22-10`, `T22-15`.
- **Спецификация:** ADR-0013; `13 §1:12`.
- **Результат:** релиз производит ровно то, что потребляет установщик.
- **В scope:** проверка `dist-workspace.toml` и `.github/workflows/release.yml` после смены формата
  архива; новый тег, в котором есть `local-rag-tui`; фиксация контракта имён и checksum-сайдкаров
  в спеке как as-built; проверка GitHub artifact attestation, если ADR-0013 её принял.
- **Не в scope:** публикация `@13w/memory` в реестр — это отдельное решение владельца.
- **Тесты:** тест, сверяющий `TARGET_TRIPLES` с литеральным списком `dist-workspace.toml`.
- **Приёмка:** установщик, направленный на настоящий новый релиз, ставит все четыре бинарника.

## G22 — Сверка доставки и резолва

- **Зависит от:** все карточки группы.
- **Спецификация:** spec 13 §1/§2/§4; spec 01 §1/§2; spec 12 §1; spec 11 §3.1/§3.2.
- **Результат:** `PASS`, `PASS after D-NNN` или `BLOCKED` в таблице Gate results.
- **В scope:** перечитать названные разделы; построить трейс «требование → код → тест», включая
  ту часть трасс `G17`/`G19`, которую эта группа объявила устаревшей; проверить каждый затронутый
  `[FIXED]`/`[SPEC]`/`[OPEN]`; убедиться, что `D-102`…`D-107` все `resolved`; прогнать оба
  набора (`npm/memory` и `plugin`) и `cargo xtask ci`.
- **Не в scope:** переоткрытие `G17`/`G19` — прецедент групп 18–21 однозначен.
- **Тесты:** все тесты группы плюс живая приёмка из плана.
- **Приёмка:** трейс воспроизводим, evidence дописан в `PROGRESS.md`.
