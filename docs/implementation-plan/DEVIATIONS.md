# Журнал отклонений и уточнений

Записи добавляются, но не удаляются. До начала следующей плановой задачи любое `open`
отклонение должно быть исправлено или переведено в `blocked` решением владельца.

| ID | Найдено в задаче | Требование | Наблюдаемое расхождение | Решение/корректирующая задача | Статус | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| D-001 | T01-04 | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD) | Тест T01-03 `concurrent_migrators_apply_exactly_once` недетерминирован: два worker-потока гоняются за однократным переводом свежей БД в WAL (`raw_conn`, `PRAGMA journal_mode=WAL` требует эксклюзивной перезаписи заголовка → `SQLITE_BUSY` под нагрузкой). Проигравший паникует в `raw_conn` до `Barrier::wait(2)` → второй worker навсегда блокируется на барьере, `--workspace`/`--test-threads` виснет. Обнажено добавленным в T01-04 тест-бинарником `migrate_resumable` (параллельная нагрузка). Продуктовый код (flock L1) корректен. | D-001: пред-инициализировать БД в WAL в главном потоке до spawn (открытие уже-WAL БД идемпотентно; остаётся только целевая гонка за migration lock). Без ослабления ассерта. | resolved | коммит T01-04 (та же правка); `migrate.rs:194` concurrent-тест 40/40 прогонов зелёные (`--exact ... --test-threads=1`), ранее ~4/5 виснул/паниковал |
| D-002 | T02-04 | «Do not weaken tests» (CLAUDE.md); source-lint T01-05 `no_writable_cross_db_attach` охраняет 03 §1.4 (нет writable cross-DB `ATTACH`) | Линт `tests/cache.rs::no_writable_cross_db_attach` матчил `line.to_ascii_lowercase().contains("attach")` — подстрока без учёта регистра. Введённая T02-04 spec-именованная операция `repo attach` (04 §7) — `pub fn attach`, `AttachError`, `NotReattachable` в `src/registry/resolve.rs`/`mod.rs`/`lib.rs` — даёт ложные срабатывания: тест падал с 8 «нарушениями», ни одно из которых не является SQL `ATTACH`. Продуктовый инвариант не нарушен (реального writable-ATTACH нет). | D-002: сузить линт до SQL-ключевого слова через хелпер `contains_sql_attach(line) = line.contains("ATTACH")` (SQL в крейте — заглавными по строгой конвенции; ловит и многострочный `execute_batch`), Rust-идентификатор `attach` больше не матчится. Инвариант сохранён (любой uppercase SQL `ATTACH` без маркера `// cross-db: read-only` по-прежнему flagged), не ослаблен. Regression: `source_lint_targets_sql_attach_not_the_rust_identifier` (positive: `"ATTACH DATABASE …"`/`ATTACH 'file' AS aux`; negative: `pub fn attach(`, `AttachError::…`, `NotReattachable(…)`). | resolved | коммит T02-04 (та же правка); `cargo test -p local-rag-store --test cache` 11 OK (было 9 + regression), `--test resolve` 11 OK |
| D-003 | T03-01 (при финальном `cargo xtask ci`) | «Тесты детерминированы: без сети, wall-clock race и зависимости от home» (CLAUDE.md; README DoD) | Тест `tests/cache.rs::recreate_is_idempotent_on_retry` (T01-05, группа 01, gated `PASS`) изредка падает под пиковой параллельной нагрузкой `cargo test --workspace`: helper `cache_meta` делает `.ok()` на `SELECT value FROM cache_meta …`, превращая transient `SQLITE_BUSY` (read-коннект открывается сразу после recreate + WAL-checkpoint) в `None` → assert `Some(UUID_A)` падает. Дефект — в тест-helper'е (глотает transient-ошибку), НЕ в нормативном поведении продукта: recreate (T01-05) синхронен и корректен, `state.sqlite` не затрагивается. T03-01 не изменил ни строки cache-кода/cache-теста (0 строк в `crates/store/src/cache/*`, `tests/cache.rs`) — лишь добавил тест-бинарник `tests/code.rs`, рост параллельной нагрузки проявил латентный флап. Эмпирика: base `bb683c9` (без T03-01) 0/20, current `6b684ff` (с T03-01) 0/20, исходно 1 падение во время `xtask ci` (шаг `test --workspace` при добавочном contention от `--features failpoints`). | D-003: сделать read-helper'ы в `tests/cache.rs` (в первую очередь `cache_meta`) устойчивыми к transient `SQLITE_BUSY` — не глотать ошибку через `.ok()`, а различать «нет строки» (`QueryReturnedNoRows` → `None`) и занятость коннекта (bounded busy-retry/явный busy_timeout на read-conn → значение); добавить regression. Продуктовый cache-код НЕ трогается (он корректен). Владелец решил зафиксировать без немедленного фикса; выполнить до гейта G03 (флап workspace-теста может изредка ронять полный прогон гейта). | resolved | коммит `D-003: cache read-helper transient-busy robustness` (та же правка); только тест-код `tests/cache.rs` (продуктовый cache-код не тронут): `cache_meta`/`probe_present` больше не глотают ошибку — `read_optional` возвращает `None` строго на `QueryReturnedNoRows`, ретраит transient busy/locked (вкл. `BUSY_SNAPSHOT`, который `busy_timeout` не пережидает) на свежем коннекте (без sleep), «no such table» → легитимный `false`, любая иная ошибка → громкая паника. Regression: `read_helper_classifiers_discriminate` (классификаторы `is_transient`/`is_no_such_table` через `ffi::Error::new`: `SQLITE_BUSY`/`BUSY_SNAPSHOT`/`LOCKED` → transient; no-such-table/QueryReturnedNoRows/generic → не transient) + `read_helpers_distinguish_present_from_absent` (present→Some, absent-key→None, absent-table→false, present→true). `cargo test -p local-rag-store --test cache` 12 OK (было 10 + 2 regression); `cargo xtask ci` = all checks passed; стресс `cargo test --workspace` 20/20 без падений (было 1 падение до фикса) |

| D-004 | T06-03 (при исследовании перед реализацией) | Карточка T06-03 (`groups/06-retention-and-gc.md:20-25`) vs `[FIXED]` roadmap-порядок (spec 15 §1) + owning-карточки T07-04 / T13-05 | T06-03 «orphan/quarantine/spool housekeeping» секвенирована в группе 06, но её три цели производятся подсистемами более поздних групп, которых ещё нет: **projection/shard + quarantine** (`crates/projection` = 6-строчный stub, нет `worktree_projection_state`, нет quarantine-кода) — группа 07 (T07-04) + shard-strategy группа 09/10; **spool/cursor** (`local-rag-hook` = stub, нет `spool_import_cursor`) — группа 13 (T13-05, карточка почти дословно дублирует spool-часть T06-03: «fully committed absent sessions eligible after 14d», «uncommitted retained», fake-clock, repeated sweep). Это противоречит `[FIXED]` roadmap-порядку 15 §1 (retention pin-roots = шаг 6; shard strategy = шаг 10; spool = шаг 14 — строятся *после* retention) и дублирует T13-05 (spool-GC) / T07-04 (quarantine). Нормативное поведение спеки (05 §8, 07 §6) НЕ нарушено — оно реализуется в своих группах; расхождение — в очерёдности **плана**, не в коде. | D-004 (owner decision — split): реализовать сейчас **только** orphan-shard-dir sweep (единственная часть с готовым фундаментом: layout `projection/<worktree_id>` `core/paths/mod.rs:277,282` + таблица `worktree` существуют; 0 новой схемы; spec 05 §8 «Orphan shard directories (no worktree row): GC'd at startup sweep») + startup/periodic-framework (идемпотентная, dry-run-способная, resumable sweep-функция; wiring — daemon группа 15). Отложить в owning-карточки (не backlog — уже в плане): quarantine-rotation ≤2 циклов → **T07-04**; grace-destroy `removing`/`detached` worktree (требует будущей миграции `removed_at`, которой нет в таблице `worktree`) → **группа 07/09 shard lifecycle** (spec 05 §8); spool-GC 14 дней/committed-cursor → **T13-05**. Корректирующая задача = сама редуцированная T06-03 (нового кода сверх неё не требуется); disposition зафиксирована в этой строке, реализация — в коммите T06-03. G06 перечитает 05 §8/07 §6 и подтвердит, что отложенное указывает на owning-карточки. | resolved | коммит T06-03 (та же правка). Реализован `local_rag_store::housekeeping` (`sweep_orphan_shard_dirs` чистое FS-ядро + `run_orphan_shard_sweep` DB-обёртка над `registry::all_worktree_ids`), тесты: `--lib housekeeping::` 7 OK + `--test housekeeping` 4 OK (orphan удалён / worktree-backed вкл. detached/removing сохранены / dry-run / идемпотентность). Отложенные тест-кейсы карточки (quarantine rotation, partial cursor retention, 14-day spool) НЕ реализованы — принадлежат T07-04/T13-05. `cargo xtask ci` = all checks passed |

| D-005 | T07-03 (при финальном `cargo xtask ci`) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD) | `crates/projection/tests/fake_faults.rs` (T07-01, группа 07, gated `PASS`) изредка падает под `cargo test --features failpoints` (параллельные потоки одного бинарника): `serial()` — мьютекс вокруг process-global failpoint-реестра (`local_rag_test_support::failpoint::global()`) — держали только 2 из 7 тестов (`head_is_last_operation_error_leaves_no_head`, `head_is_last_operation_survives_sigabrt` — те, что сами вызывают `arm`/`disarm` `projection.fake.write_head`). Остальные 5 (`inspect_reports_loaded_state`, `corrupt_drop_point_is_visible_after_reopen`, `corrupt_remove_head_is_visible_after_reopen`, `corrupt_swap_point_yields_equal_count_different_set`, `corrupt_overwrite_head_makes_shard_unopenable`) вызывают обычный `shard.write_head(...)` без ожидания failpoint, но т.к. реестр процесс-глобальный, попадание в окно `Action::Error`-armed другого потока превращает их обычный `write_head` в `Err(Backend("failpoint: write_head"))` → спурious panic на `.expect("head")`. T07-03 не менял этот файл — расхождение обнаружено при первом чистом прогоне `cargo xtask ci` после T07-03 (новый `switch_faults.rs` не пересекается с этим процессом: интеграционные тест-бинарники — отдельные процессы с собственным `OnceLock`, гонка целиком внутри `fake_faults.rs`). Эмпирика: 10 последовательных прогонов `cargo test -p local-rag-projection --features failpoints --test fake_faults` до фикса — 6/10 падали (разные тесты каждый раз: `corrupt_overwrite_head_makes_shard_unopenable`, `corrupt_drop_point_is_visible_after_reopen`, `corrupt_remove_head_is_visible_after_reopen`, `corrupt_swap_point_yields_equal_count_different_set`, иногда вместе с `inspect_reports_loaded_state`). Нормативное поведение фейка (T07-01) не нарушено — дефект чисто в тест-изоляции. | D-005: расширить `let _serial = serial();` на все 5 незащищённых тестов (не только те, что сами `arm`/`disarm`) — единственный корректный инвариант с process-global registry: «пока ЛЮБОЙ failpoint потенциально armed, НИ ОДНА операция над `FakeShard` в этом бинарнике не должна выполняться параллельно», а не только те тесты, что сами вооружают failpoint. Обновлён doc-comment `serial()` (явно требует guard от каждого теста файла). Продуктовый `fake`-код (`crates/projection/src/fake.rs`) не тронут — дефект только в тесте. | resolved | коммит T07-03 (тот же коммит, файл `crates/projection/tests/fake_faults.rs`, 5 однострочных вставок `let _serial = serial();` + doc-comment). Верификация: `cargo test -p local-rag-projection --features failpoints --test fake_faults` 7 OK ×8 последовательных прогонов подряд (было ~60% падений); полный `cargo xtask ci` зелёный |

| D-006 | T08-04 (при планировании, до написания кода) | Spec 06 §4 `[FIXED]`: «FTS rebuild/degrade decision» — strong-проверка «catches an identical count with a differing ID set that a bare count comparison would miss» (прямая аналогия `Divergence::ManifestMismatch`, `crates/projection/src/validate.rs`); cheap-проверка запускается «per search» и обязана ловить расхождение `occurrence_count`. | `open_and_validate_fts` (T08-03, `crates/store/src/cache/validate.rs`) берёт ОБА значения — `actual_occurrence_count` (cheap/strong count-предикат) и recomputed-manifest (strong-предикат) — из `state.sqlite` (`code::occurrence_count_for_generation`/`occurrence_ids_for_generation`: неизменное, per-generation ожидание), а НЕ из фактического текущего содержимого `fts_doc`/`fts_occurrences` в `cache.sqlite` (то, что как раз валидируется). Поскольку generation иммутабельна после `projection_ready` (structural sharing), это ожидание никогда не меняется, пока сама generation та же — то есть ОБЕ проверки (не только strong/manifest) полностью слепы к прямой порче/удалению строк `fts_doc`/`fts_occurrences`, пока `fts_projection_head` и `state.sqlite` не тронуты; обнаруживается только смена generation (`GenerationMismatch`) или прямая порча полей самой `fts_projection_head`. Дословный сценарий «equal occurrence count, different ID set» сегодня не ловится вообще. Ни один из 7 тестов `crates/store/tests/fts_validate.rs` (T08-03, группа 08, gated `PASS`) не ловит эту дыру — все либо меняют generation, либо портят поля самой `fts_projection_head` напрямую (что ловится корректно любым пересчётом, включая баг-версию, — поэтому дефект и проскочил при T08-03). Дизайн-прецедент правильного подхода уже существовал в этой же кодовой базе: dense-проекция (`crates/projection/src/rebuild.rs:463-468`) recompute'ит `shard_point_count`/`computed_manifest` из РЕАЛЬНОГО `shard.point_ids()`/`point_count()`, никогда из ожидания `state.sqlite`. | D-006: два новых cache-side ридера `cache::fts_doc_occurrence_count`/`cache::fts_doc_occurrence_ids` (`crates/store/src/cache/fts.rs`, реэкспортированы с корня крейта, читают фактические строки `fts_doc` по `worktree_id`). `open_and_validate_fts` переписан: валидационный вход (count для cheap/strong, occurrence-ids для strong-manifest) берётся из НИХ; `code::occurrence_count_for_generation` остаётся, но только для оценки стоимости rebuild (`should_rebuild_synchronously`) — отдельный вопрос («сколько будет стоить rebuild», не «доверять ли кэшу») — читается отдельной переменной, ПОСЛЕ подтверждения расхождения, не смешивается с валидационным чтением (это смешение и породило баг). Чистые предикаты `validate_fts_cheap`/`validate_fts_strong` не меняются — баг был только в том, что им передавал оркестратор. Regression: новый тест в `crates/store/tests/fts_validate.rs` — напрямую подменяет один `fts_doc.occurrence_id` (count не меняется), доказывает `Cheap` остаётся `Valid` (сознательно слеп по дизайну spec 06 §4 `[SPEC split]`), а `Strong` теперь корректно ловит и чинит. `docs/specification/06-reconcile-and-fts.md` §4: точечно исправлена фраза T08-03-заметки, описывавшая баговую реализацию как верную; добавлена новая as-built заметка (D-006). | resolved | коммит `D-006: cache-sourced FTS validation input (fix state-vs-cache count/manifest source)` |

| D-007 | G09 (при сверке spec 05 §8 в трейсе `требование → код → тест`) | Spec 05 §8 `[FIXED]` «Shard lifecycle follows registry lifecycle»: «remove/detach: grace period `[SPEC: 7 days]`, then destroy» — нормативное v0-поведение, не deferred/post-v0 scope | Требование не реализовано **и осталось без владеющей карточки**. Цепочка деферала обрывается ровно на этом гейте: D-004 (`resolved`) отложил grace-destroy в «группу 07/09 shard lifecycle» (нужна миграция `removed_at`, которой не было); группа 07 закрылась гейтом `PASS`, реализовав из деферала только quarantine rotation (T07-04); T09-02 явно отказался — `crates/projection/src/manager.rs:74-77` «needs a `removed_at` migration that does not exist yet», отложив «to group 07/09 shard lifecycle in general, not this specific task». `TRACEABILITY.md:12` даёт для spec 05 первичные гейты G07/G09/G10, из которых G10 — backend-spike (lifecycle не трогает), то есть после `PASS` на G09 ни одна карточка плана не владеет этим требованием. Фактическое состояние кода на момент находки: в таблице `worktree` (`crates/store/src/registry/mod.rs:154`) нет ни `removed_at`, ни `detached_at`, ни `state_changed_at`; `transition_worktree_state` (`crates/store/src/registry/worktree.rs`) меняет только `state` и не ставит никакой отметки времени — измерить grace-период физически нечем. Нарушения уже реализованного поведения нет (destroy просто не существует), нарушен запрет CLAUDE.md/README: «Нельзя переносить отклонение в backlog… Допустимо отложить только явно deferred/post-v0 scope». | D-007 (решение владельца — реализовать сейчас, а не переприязывать к более поздней карточке): forward-only **миграция 5** `worktree_state_clock` (`registry::SCHEMA_V5`) — `ALTER TABLE worktree ADD COLUMN state_changed_at INTEGER NOT NULL DEFAULT 0` + backfill `= last_seen_at` (не `0`, иначе каждый существующий шард мгновенно eligible); `create_worktree` штампует `created_at`; `transition_worktree_state(.., now_ms)` перештамповывает **только при эффективном переходе** (`from != to`), сохраняя контракт «self-transition — идемпотентный no-op»: иначе retry-цикл бесконечно продлевал бы жизнь обречённому шарду. Чистый предикат `shard_destroy_due` + `expired_shard_ids` + FS-функция `sweep_expired_shard_dirs` + DB-обёртка `run_expired_shard_sweep(db, layout, now_ms, grace_ms, dry_run)` в `crates/store/src/housekeeping.rs` рядом с orphan-sweep T06-03 (общий обход вынесен в приватный `sweep_shard_dirs`, чтобы консервативная обработка не-директорий/не-UTF-8/отсутствующего каталога не разъехалась). `SHARD_DESTROY_GRACE_MS` = 7 дней (`[SPEC]`-значение, не `[FIXED]`), но бюджет — явный параметр вызова, поэтому тесты используют фейковые часы без сна. Scope-граница зафиксирована явно: уничтожается **шард**, а не строка `worktree` (04 §7 «deleted after shard/spool/GC cleanup» требует ещё spool-очистки — группа 13) и не открытый handle (`ShardManager::remove`, wiring — группа 15, тот же деферал, что уже несёт orphan-sweep). Spec-амендменты `[SPEC]`: 03 §2.1 (колонка в DDL + as-built заметка), 05 §8 (as-built заметка). | resolved | коммит `G09: Сверка concurrency model (spec 02 §5-6, 04 §8, 05 §2/§8, 06 §3, 09 §1, 14 §4) [PASS after D-007]` (та же правка). Продуктовый код: `crates/store/src/{registry/mod.rs,registry/worktree.rs,registry/resolve.rs,migrate/mod.rs,housekeeping.rs,lib.rs}`. Тесты: `cargo test -p local-rag-store --lib housekeeping::` 15 OK (было 7 — +8 D-007: truth-table состояний × бюджета, future-stamp, zero-grace, отбор id, 7-дневная константа, expired-sweep removal/dry-run/идемпотентность); `--test housekeeping` 10 OK (было 4 — +6 интеграционных: destroyed detached+removing vs retained active, ретенция до дедлайна и destroy ровно на нём, reattach сбрасывает бюджет, self-transition не продлевает, dry-run→real→idempotent, непересечение двух sweep'ов); `--test worktree` 14 OK (было 12 — +2: штамп при создании, движение часов только на эффективном переходе вкл. отказ на illegal); `--test migrate` 9 OK (было 8 — +1: `migration_5_adds_and_backfills_the_worktree_state_clock`, применяется к стору, поднятому ровно до v4, иначе backfill не проверяется). Обновлены (не ослаблены) 3 существовавших инвариант-ассерта под новую as-built схему: `tests/worktree.rs::worktree_has_no_path_derived_identity_column` (7→8 колонок; все запреты path-производных колонок сохранены), `tests/migrate.rs::state_db_open_bootstraps_and_is_idempotent` и `tests/registry.rs::migration_produces_exact_registry_schema` (набор миграций v1..v4 → v1..v5). `cargo xtask ci` = all checks passed |

| D-008 | T11-03 (при планировании, до написания кода) | Карточка T11-03 (`groups/11-embeddings-and-model-spaces.md:20-25`): «in-process default implements Embedder» + spec 10 §1 `[FIXED]` «Embeddings run **in-process**: `fastembed` (ONNX Runtime) or `Candle`» | Карточка читается как «связать реальный ONNX/candle-рантайм», но в T11-03 такому провайдеру физически нечего загружать: веса и их установка — scope **T11-06** (spec 10 §5 `[FIXED policy]`: `local-rag init --download-models`, checksum-манифест, `.part → fsync → rename → .ok`), коммитить веса запрещено (`CLAUDE.md`), а DoD требует тестов без сети (`README.md`; карточка T11-06 отдельно оговаривает «Network tests use local fixture server only»). Связывание `ort`/`fastembed`/`candle` сейчас дало бы код, который не может выполнить ни одного инференса ни в одном тесте, плюс `ort`'s default-фича `download-binaries` качает бинарники ORT на этапе сборки, что ломает `CONTRIBUTING.md`'s «after the initial `cargo fetch`, the full check runs **offline**»; сам ORT-бандлинг спека проверяет «before the final CI matrix» (10 §5, 13 §1 — группа 17), т.е. после T11. Нормативного нарушения нет: 10 §1 требует, чтобы локальный бэкенд был **рабочим дефолтом** и чтобы remote/Ollama были строго опциональны — оба свойства выполнимы без ML-SDK. | D-008 (owner decision — split): T11-03 поставляет **весь** провайдерный слой (`crates/embed`: `[FIXED]`-trait `Embedder`, типы `EmbedRequest`/`Vector`/`EmbedError`, ordered pool с primary/fallback + retry по v1-контракту, центральный policy guard + `ErrorCode::PolicyBlockedRemote`, сверка `key()` с реестром представлений) и **ADR-0004**, закрывающий embedding-половину O3 (выбор дефолтной модели по quality/license/platform evidence). In-process дефолт реализован как детерминированный `HashingEmbedder` под собственным bootstrap `model_id` (`local-hashing-v1`) — он не может быть спутан с продовой моделью, т.к. `model_id` входит в шестиполевой `RepresentationKey`. Отложенное получает **явного владельца**, а не backlog: карточка **T11-06** расширена пунктом «ONNX-провайдер выбранной ADR-0004 модели + `require_model_assets`-контракт `.ok`» (правка `groups/11-embeddings-and-model-spaces.md`), консьюмерская половина этого контракта (`local_rag_embed::require_model_assets`, типизированный `ModelAssetsMissing`, без единого сетевого вызова) реализована уже сейчас. G11 обязан подтвердить, что T11-06 несёт этот пункт. | resolved | коммит T11-03 (та же правка). Структурная гарантия: `crates/embed/tests/offline_smoke.rs::the_crate_declares_no_network_or_model_dependency` — линт манифеста (`reqwest`/`hyper`/`ureq`/`ort`/`fastembed`/`candle`/`ollama`/`hf-hub`/… запрещены в `[dependencies]`, все зависимости обязаны начинаться с `local-rag-`); `git diff --stat Cargo.lock` пуст — ни одной новой внешней зависимости. **Подтверждение исполнения (T11-06, 2026-07-26):** owning-карточка доставила отложенное — `crates/models` (`OnnxEmbedder` через `ort`+`load-dynamic`, инсталлятор spec 10 §5) и ADR-0005; провайдер ходит через `require_model_assets`, сборка остаётся offline (`cargo build --offline -p local-rag-models` — OK), реальный инференс на ORT 1.27 прогнан локально (768d, unit-норма, детерминизм). Линт `crates/embed` не ослаблен: тяжёлые зависимости изолированы в отдельный крейт |

| D-009 | T11-03 (при финальном `cargo xtask ci`) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD) | `crates/store/tests/cache.rs::recreate_is_idempotent_on_retry` (T01-05, группа 01, gated `PASS`) падает под нагрузкой с `SqliteFailure(SystemIoFailure, extended_code: 522 = SQLITE_IOERR_SHORT_READ, "disk I/O error")` — и **не** лечится ретраем: 16 подряд свежих read-only коннектов видят одно и то же состояние (`cache.sqlite = 4096` байт — пустой заголовок, `-wal = 0`, `-shm` отсутствует), т.е. данные реально отсутствуют, а не «занят снапшот» (в отличие от D-003, где ретрай на свежем коннекте помогает). Корень — в **тесте**, а не в продукте: блок `{ let db = open_cache(...); write_probe(&db).await; }` полагается на то, что после выхода из scope инстанс полностью закрыт, но `CacheDb::drop` асинхронен по конструкции (writer живёт на detached-потоке; graceful drain — забота демона, spec 02 §4.3/T15). Тест тут же делает `remove_file` на `cache.sqlite`/`-wal`/`-shm` и открывает новый инстанс по тому же пути; поскольку SQLite открывает `-wal`/`-shm` **по имени**, teardown старого соединения попадает по сайдкарам, которые уже принадлежат новому инстансу → его seed-транзакция теряется, читатель получает SHORT_READ. Обнаружено при первом полном `cargo xtask ci` после T11-03 (новый крейт `local-rag-embed` добавил 5 тест-бинарников → выше параллелизм); T11-03 не изменил ни строки в `crates/store/src/cache/*`. Продуктовое поведение не нарушено: writer-соединение того же инстанса видит корректный binding (проверено диагностикой), а в продукте один процесс не удаляет БД из-под собственного открытого инстанса. | D-009: дать `CacheDb` **ожидающее** закрытие — `CacheWriter::spawn` возвращает `JoinHandle`, `CacheDb` его хранит, `CacheDb::close(self)` закрывает очередь и join'ит writer-поток (документировано: блокируется, пока жив хотя бы один клон `CacheWriter`; обычный `drop` остаётся асинхронным, как и был). Все пять reopen-блоков `tests/cache.rs` закрываются через `db.close()`. Ассерты **не тронуты** — оригинальные read-only проверки (`cache_meta`/`probe_present`) сохранены дословно, ослабления нет. Regression: `close_lets_the_path_be_unlinked_and_recreated_immediately` — 8 раундов «open → write → close → unlink → reopen», каждый раунд читается независимым read-only коннектом (одного раунда недостаточно: он проходил бы и с гонкой). | resolved | коммит T11-03 (та же правка). Продуктовый код: `crates/store/src/cache/{mod.rs,writer.rs}` (+`close`, `JoinHandle`). Атрибуция фикса измерена, а не предположена — стресс 16 параллельных процессов × 8 потоков одного тест-бинарника: **без** `close()` перед unlink — 6/16 процессов падают (в одном из прогонов 8/16, 10/16); **с** `close()` — 0/16 (подтверждено дважды, в т.ч. с сохранённой read-only проверкой). `cargo test -p local-rag-store --test cache` — 13 OK (было 12 + regression); `cargo xtask ci` — all checks passed |

| D-010 | T11-04 (при добавлении feature `failpoints` в `crates/embed`) | «Do not weaken tests» (CLAUDE.md); source-lint T11-03 `tests/offline_smoke.rs::the_crate_declares_no_network_or_model_dependency` охраняет 10 §1 + D-008 (в `crates/embed` нет сетевого клиента и ML-рантайма) | Линт сверял `deps.contains(forbidden)` — подстроку по всему тексту секции `[dependencies]`. T11-04 добавляет в эту секцию легитимную optional dev-зависимость `local-rag-test-support` (та же схема, что в `store`/`index`/`projection`), и линт падает с «must not depend on `ort`»: подстрока `ort` содержится в `test-supp**ort**`. Ровно тот же класс ложного срабатывания, что **D-002** (линт `ATTACH` матчил Rust-идентификатор `attach`). Продуктовый инвариант не нарушен: ни `ort`, ни любой другой ML/сетевой SDK в графе нет (`git diff --stat Cargo.lock` — ни одного нового внешнего источника). | D-010: сузить линт до **имён** зависимостей — распарсить строки секции в список `name` (до `=`, пропуская комментарии/пустые), затем сравнивать точным `declared.contains(&forbidden)`; позитивная половина («каждая зависимость начинается с `local-rag-`») применяется к тем же именам. Инвариант не ослаблен: реальная `ort = "2"` по-прежнему ловится. Добавлены две проверки: regression `the_dependency_lint_matches_names_not_substrings` (benign-секция с `local-rag-test-support` не даёт хита на `ort`; секция с настоящей `ort` — даёт) и ассерт в самом линте, что `local-rag-test-support` **виден** ему (иначе фикс мог бы «пройти» за счёт фильтрации optional-зависимостей из проверки). | resolved | коммит T11-04 (та же правка); `cargo test -p local-rag-embed --test offline_smoke` — 5 OK (было 4 + regression); `cargo xtask ci` = all checks passed |

| D-011 | G11 (при сверке spec 05 §8 / 10 §4 в трейсе `требование → код → тест`) | Spec 05 §8 `[FIXED]` «Shard lifecycle follows registry lifecycle» + 10 §4 шаг 6 «A → retiring; its cache rows become evictable when no worktree references A» | T11-05 разделил шард по model space (`projection/<worktree_id>/<model_space_id>/`, 05 §2 as-built), но **уборки за миграцией не появилось**: после переезда worktree A→B каталог `projection/<wt>/<A>/` остаётся на диске навсегда. Ни один из двух существующих sweep'ов его не видит — orphan-sweep (T06-03) удаляет только корни **без строки worktree** (worktree жив), expired-sweep (D-007) — только `detached`/`removing` (worktree `active`). На **generation**-оси эквивалентные устаревшие данные вычищает сам switch (05 §5 шаг 3 `delete(existing \ expected)` работает по тому же каталогу); на model-оси такого шага нет **по построению** — именно это делает «until step 4 commits … that worktree still runs A entirely» `[FIXED]` буквально верным. Итог: каждая смена модели навсегда добавляет по одному полному мёртвому шарду на worktree, а 05 §8's disk-budget метрика («soft cap») становится ложной. Владеющей карточки нет: sweep'ы и цепочка дефералов D-004 старше сплита, ни одна карточка групп 12–17 не упоминает shard-каталоги или model spaces (проверено grep'ом по `groups/`). Ровно тот же класс находки, что D-007 на G09. Проверено, что это не lock-гонка: 05 §5 коммитит write-ahead (а с ним `target_model_space_id`) **до** любой backend-мутации (`switch.rs:381` vs `:399`), поэтому целевой каталог отражён в строке раньше, чем создан. | D-011 (решение владельца — фундамент готов, реализовать сразу): `local_rag_store::referenced_model_space_ids` (ридер множества «space, названных строкой `worktree_projection_state` в **любой** колонке» — дословная формулировка 04 §3, применённая per-worktree) + третий sweep рядом с двумя существующими в `housekeeping.rs`: чистое ядро `sweep_unreferenced_space_dirs` и DB-обёртка `run_unreferenced_space_sweep(db, layout, dry_run)`. Консервативность как у сиблингов: корень **без строки** projection-state пропускается целиком (это дело orphan-sweep), кандидаты — только каталоги, non-UTF-8 имена сохраняются, корень worktree никогда не удаляется. Схема не менялась, wiring в демон — группа 15 (тот же деферал, что уже несут два sweep'а). `[SPEC]`-амендменты: 05 §8 (as-built D-011), 10 §4 (шаги 5–6). | resolved | коммит G11 (та же правка). `cargo test -p local-rag-store --lib housekeeping::` 20 OK (было 15; +5 юнитов: переезд A→B, switch in flight держит **оба** буфера, корень-без-строки vs строка-с-пустыми-колонками, dry-run→real→идемпотентность, консервативность/отсутствующий каталог); `--test housekeeping` 14 OK (было 10; +4 интеграционных, в т.ч. `a_migrated_away_model_space_shard_is_reclaimed`, который сперва доказывает, что оба старых sweep'а тут пусты, и `the_three_sweeps_partition_the_work`) |

| D-012 | G11 (при сверке spec 04 §3 в трейсе `требование → код → тест`) | Spec 04 §3: «the default space (`store_settings.default_model_space_id`) MUST be `active`» | Инвариант охранялся только со стороны **указателя**: `set_default_model_space_id` (T11-05) отвергает не-`active` цель. Со стороны **состояния** охраны не было — `transition_model_space` проверяет лишь легальность ребра и (для `projection_ready`) покрытие, поэтому `active → retiring` на пространстве, на которое смотрит `default_model_space_id`, проходил и оставлял стор с не-`active` дефолтом. Последствие не теоретическое и **тихое**: `dormant_migration_target` (05 §8) в этом состоянии возвращает `None` (ветка «дефолт сам и есть непригодный» — защита от петли), то есть dormant-миграция worktree'ев просто перестаёт происходить, вместо громкой ошибки; явный `switch_model_space` на дефолт отказывает `NotEligible`. As-built заметка T11-05 к 10 §4 шаг 5 утверждала, что инвариант «enforced where the value is established» — верно лишь для половины. | D-012 (решение владельца — фундамент готов, реализовать сразу): в `transition_model_space` добавлена проверка — уход **из** `active` для пространства, на которое указывает `default_model_space_id`, отвергается типизированным `ModelSpaceTransitionError::IsDefaultModelSpace` (чтение указателя в той же транзакции, до записи; при отказе мутации нет). Отказ, а не авто-перенаправление: выбор дефолта остаётся явным решением, и это ровно порядок, зафиксированный 10 §4 (шаг 5 `default := B` перед шагом 6 `A → retiring`). Гвард **сознательно сужен** до `from == Active && to != Active`: первая (более широкая) формулировка «любая не-Active цель» ломала путь восстановления — стор, чей указатель уже смотрит на не-active space, не смог бы поднять его обратно (`building → projection_ready → active`), а backfill-тесты T11-04 поймали это немедленно (2 падения). `[SPEC]`-амендмент: 04 §3 (as-built D-012, там же зафиксировано, что правило удаления model space сегодня выполняется вакуумно — кода удаления нет). | resolved | коммит G11 (та же правка). `cargo test -p local-rag-store --test representation` 9 OK (было 5; +4: отказ ретайрить дефолт без мутации, полный порядок шагов 5→6 «сначала переставить дефолт, затем ретайрить старый», не-active дефолт можно вернуть в `active` (regression на сужение гварда), self-transition остаётся no-op); `cargo test -p local-rag-embed --test backfill` 10 OK (ранее 8/10 — те самые 2 падения, поймавшие слишком широкий гвард) |

| D-013 | G11 (при сверке spec 10 §3 в трейсе `требование → код → тест`) | Spec 10 §3 `[SPEC]`: «A model space bundles the representations that must be coherent together (at minimum `code_raw` + `memory` in v0)» | Дефолтный model space, засеянный `SCHEMA_V4` (T07-02), — `active`, но **не имеет ни одной строки** `model_space_representation`. Группа 11 построила весь реестр и API регистрации (`local_rag_embed::register_embedder_representation`, T11-03), но продуктового вызывающего у него нет: на несконфигурированном сторе `required_code_kinds` отказывает `NoCodeRepresentation`, `params_for_model_space` — `NoShardParams`. Отказы громкие (не тихая деградация), нормативное поведение уже реализованного кода не нарушено, но само требование 10 §3 **не покрыто ни одной карточкой**: ни группы 12–17, ни группы 14 не упоминают регистрацию представлений (проверено grep'ом по `groups/`); T12-02 говорит про «active representation **selection**», что предполагает уже существующие строки. | D-013 (решение владельца — требование опирается на подсистему более поздней группы, поэтому фиксируется с **явным именованным владельцем**, прецедент D-008/D-004): сеять миграцией нельзя — `model_id` в шестиполевом ключе есть решение ADR-0004, и зашивание его в DDL и захардкодило бы то, ради миграции чего существует 10 §4, и заставило бы каждый свежий стор заявлять модель, чьи веса могли не скачиваться (10 §5). Владельцы: **`code_raw`-половина → T15-07** (`init` — та же команда, что ставит веса; карточка `groups/15-daemon-interfaces-cli.md` дополнена результатом и тестами), **`memory`-половина → группа 14** (владеет subject-функцией kind'а `memory`; карточка `groups/14-memory.md` дополнена разделом, в т.ч. запретом помечать `memory` как `required` раньше времени — иначе полный `Coverage` недостижим). G15 и G14 обязаны подтвердить исполнение. Кода в G11 не потребовалось. `[SPEC]`-амендмент: 10 §3 (as-built G11/D-013). | resolved (обе половины исполнены — memory: T14-08, code_raw: T15-07; закрытие подтверждено при сверке G17) | коммит G11 (та же правка): `docs/specification/10-models-and-embeddings.md` §3, `groups/15-daemon-interfaces-cli.md` T15-07, `groups/14-memory.md`. Продуктового кода не добавлено — намеренно; исполнение проверяется гейтами G14/G15. **Исполнение memory-половины (T14-08):** `local_rag_store::subjects::memory_entry_subject_keys` + `expected_subject_keys`'s `RepresentationKind::Memory` arm (`crates/store/src/subjects.rs`) — каждая строка `memory_entry` даёт один `EmbeddingKey` через `subject_memory_entry(memory_id, text)`, независимо от `generations` (memory не привязана к генерации) и независимо от recall-eligibility (subject-покрытие — вопрос "что задать эмбеддингом", не "что видит recall"); `local_rag_embed::backfill`'s `memory_index` + новая ветка `SubjectKind::MemoryEntry` в `embed_and_write` читают `memory_entry.text` напрямую. Регистрация `memory` как `required` представления для реального model space по-прежнему не продуктовый код (тот же прецедент, что и `code_raw`: единственные вызовы `set_model_space_representation` в репозитории — тесты/`xtask bench`) — T14-08 закрывает только отсутствие subject-функции, чтобы будущая регистрация (T15-07 или аналог) не наткнулась на `BackfillError::UnsupportedRequiredKind`. Тесты: `cargo test -p local-rag-store --test subjects` (новый `memory_subjects_are_computed_from_every_memory_entry_row`, обновлён `kinds_without_a_subject_function_are_reported` на `RepresentationKind::StructuralDescription`), `cargo test -p local-rag-embed --test backfill` (новый `a_memory_only_run_embeds_every_memory_entry`, обновлён `an_unsupported_required_kind_refuses_the_run`) — все зелёные. `[SPEC]`-амендмент: 03 §4.2 (as-built T14-08). **Исполнение code_raw-половины (T15-07, подтверждено при сверке G17):** `crates/local-rag/src/cli/init.rs::register_code_raw_representation` — production-код (не тест), регистрирует `code_raw` как `required`-представление `DEFAULT_MODEL_SPACE_ID` по состоянию диска (`.ok`-маркер установленных весов, не по CLI-флагу), идемпотентно через `register_representation`'s `ON CONFLICT`; тесты `crates/local-rag/src/cli/init.rs::tests::{after_registration_the_default_space_requires_code_raw, the_registered_key_matches_the_installed_providers_key, params_for_model_space_reports_the_registered_dimensions, repeated_registration_is_idempotent}`. Обе половины D-013 закрыты — расхождение полностью resolved. **Уточнение (D-036, post-v0, найдено живым MCP-dogfood-тестированием):** формулировка выше для memory-половины смешивала T14-08's `subjects`-функцию с продуктовой регистрацией — T14-08 закрывала только первое; сама регистрация `RepresentationKind::Memory` как `required`-представления `DEFAULT_MODEL_SPACE_ID` не выполнялась никаким продуктовым кодом до `D-036` (`crates/local-rag/src/cli/init.rs::register_memory_representation` + daemon/backfill wiring). Эта строка оставлена как историческая запись, не переписана — см. `D-036` для фактического закрытия memory-половины. |

| D-014 | G11 (при перепрогоне opt-in ONNX-evidence по решению владельца) | «Тесты детерминированы… Never invent a successful test result» (CLAUDE.md; README DoD); spec 10 §5 as-built (T11-06): «a no-op install does not reprint» лицензию | `crates/models/tests/install.rs::the_default_catalog_matches_a_real_local_mirror_when_one_is_supplied` (T11-06) устанавливал модель **в** `LOCAL_RAG_TEST_MODEL_HOME`, если тот задан. При первом прогоне (сессия T11-06, пустой каталог) это работало; при повторном — тот же самый каталог уже содержит `.ok`, поэтому `install_model` коротко замыкается (`install.rs:240`, документированный no-op) и тест падает на `assert!(text.contains("Gemma Terms of Use"))` с пустым сообщением. Продукт корректен — дефект в тесте, причём **более серьёзный, чем падение**: короткое замыкание проверяет только наличие маркера `.ok` и не хеширует ни байта, так что не упади ассерт лицензией раньше, тест напечатал бы «RAN: verified 3 files … against the pinned catalog digests», не проверив ни одного digest'а. То есть исход теста зависел от внешнего состояния каталога, а «успех» на предзаполненном сторе был бы ложным. Тот же класс, что D-003/D-005/D-009 (дефект тест-изоляции, продуктовый код не тронут). | D-014: разделить две задачи, которые тест смешивал. (1) **Проверка данных** всегда идёт в свежий `TempHome` — там нечего переиспользовать, поэтому все три файла реально скачиваются и хешируются; ассерт усилен с «downloaded + reused == len» до `downloaded.len() == len` **и** `reused.is_empty()`, что делает ложный «verified» структурно невозможным. (2) **Наполнение общего `LOCAL_RAG_TEST_MODEL_HOME`** (нужного ONNX-тесту) вынесено во вторую половину и там же превращено в позитивную проверку документированной семантики no-op: повторная установка возвращает всё в `reused`, `downloaded` пуст, `bytes_downloaded == 0`, лицензия **не** перепечатывается. Продуктовый код `crates/models` не изменён. | resolved | коммит G11 (та же правка). До фикса: `... --test install the_default_catalog_matches` FAILED на предзаполненном сторе. После: OK за 10.86 s с двумя строками `RAN` — «verified 3 files (314.5 MiB) against the pinned catalog digests» (свежий temp-root) и «shared root … is installed; repeat install is a no-op that reprints nothing». Без `LOCAL_RAG_TEST_MIRROR` по-прежнему громкий `SKIP` (проверено отдельным прогоном), `cargo test -p local-rag-models --test install` — 16 OK |
| D-015 | T12-05 | «runner produces MRR/Recall@5, latency и **per-query diff vs v1**» (карточка T12-05, groups/12) | Пер-запросного среза v1 не существует и получить его нечем. Импортированный артефакт `fixtures/search/baseline/run-embeddinggemma-300m-2026-07-16.json` содержит только агрегаты (`{hit1, hit3, hit5, mrr}` + тайминги); сам раннер v1 (`/opt/soft/local-rag/scripts/benchmark.ts::evaluateModel`) сворачивает ранги в счётчики прямо в цикле (`if (rank === 1) hit1++; … mrr += 1/rank`) и **нигде** не эмитит ранг отдельного запроса. Восстановить его можно было бы только правкой исходников v1 + повторным прогоном на живых Ollama+Qdrant — а T00-01 зафиксировал ровно обратное («v1 source was not modified»), и любая правка обесценила бы сопоставимость с уже записанными числами. | D-015: сузить требование по решению владельца — v2-отчёт даёт **полную пер-запросную детализацию для себя** (ранг, hit/miss, совпавший путь/символ, латентность), а diff против v1 остаётся **на уровне метрик** (Hit@1/3/5, MRR). Формат отчёта резервирует поле под ранги v1: если v1 когда-нибудь перепрогонят с пер-запросным выводом, diff станет ранговым без изменения схемы. | resolved | коммит T12-05 (та же правка); нота в spec 14 §7 и в `fixtures/search/baseline/baseline.md`; тест `report::tests` фиксирует, что отсутствие v1-рангов даёт метрик-уровневый diff, а не пустой |
| D-016 | T12-05 | «quality: MRR not worse than v1 baseline by more than X; Recall@5 ≥ Y» (spec 14 §2 `[FIXED set]`); карточка T12-05 «O2 search thresholds filled only from agreed baseline» | **v0 search качественно ниже v1.** Первый реальный прогон на том же корпусе (`/opt/soft/local-rag` @ `31dfba2`) и той же модели: MRR **0.5646** против v1 **0.6963** (−0.1316, −18.9% относительных), Recall@5 **0.7755** против **0.8367**. Гейт с порогами, выведенными из согласованного v1-бейзлайна (budget 0.03 / floor 0.80), падает по обоим условиям. Пер-ножная диагностика локализует причину: гибрид (0.5646) **лучше обеих своих ног** (dense 0.4939, lexical 0.4374), то есть RRF работает; весь разрыв — в dense-ноге, 0.4939 против v1's 0.6963. Модель, корпус и запросы те же, префиксы задач не применяет ни v1, ни v2 ⇒ разница в том, **какой текст эмбеддится**. Ведущий кандидат: v1 присоединял к каждому чанку его JSDoc/`//`-документацию (`src/indexer/parser.ts::extractDoc`), в адаптерах v2 обработки комментариев нет вовсе — а запросы корпуса как раз естественно-языковые описания. Второстепенные, неизмеренные: квантованные веса (`model_quantized.onnx`) и усечение `MAX_SEQUENCE_TOKENS = 256`. | **Требуется продуктовое решение.** Варианты, ни один из которых не входит в карточку T12-05: (а) включить документацию символа в текст юнита — меняет `content_blob`-деривацию, `normalization_version` и инвалидирует все кэши, то есть отдельная задача в группе 04/06 с новой миграцией; (б) принять просадку для v0 и ослабить пороги осознанно, записав это решение; (в) измерить и устранить вклад квантования/усечения до решения по (а). Пороги в `thresholds.json` намеренно оставлены выведенными из **v1**, а не из регрессировавшего прогона: иначе гейт узаконил бы регрессию. **Развязка (ступень D / D-017): продуктовое решение не понадобилось.** Остаток оказался дефектом провайдера — читался не тот выход графа, — и после его исправления dense-нога даёт 0.7007 против v1's 0.6963. Ни один из вариантов (а)/(б)/(в) не берётся: документацию включать не нужно (гипотеза C измерена и опровергнута), пороги ослаблять не нужно, квантование измерять не нужно (q8 достигает BF16). Остаток разрыва по **дефолтному** режиму перешёл к D-018 — фузии. | resolved | **Ступень 1 (коммит D-016, частично).** Разбор v1 нашёл четыре расхождения, из-за которых сравнивались не совсем одинаковые вещи, и три устранимы. **A — корпус**: v1 индексировал строго `<root>/src/` (92 `.ts` + 4 `.json` = ровно записанные 96 файлов), первый прогон v2 взял весь репозиторий, включая `scripts/benchmark.ts` — файл с 49 запросами дословно, почти идеальное BM25-совпадение с неверным ответом; устранено флагом `--subdir`, корпуса сошлись (93/545 против 96/544), но прирост **всего +0.0034 MRR** — ожидание не подтвердилось. **B — окно**: v1 усекал вход на 3000 символах, v2 на 256 токенах (втрое агрессивнее); поднято до 1024 вместе с `representation_version 1 → 2` (длина усечения не входит в шесть полей ключа, иначе кэш отдавал бы 256-токенные векторы как валидные 1024-токенные); **+0.0102 MRR**. Вместе A+B отыграли 0.0136 из 0.1317 — около десятой части. **C — главное и ещё не измеренное**: v1 никогда не эмбеддил голый код, он строил размеченный конверт `buildEmbedCtx` из пути, типа, имени, докблока, сигнатуры и тела; v2's `code_raw` не несёт ни одного из этих полей. Реализовано как `code_context` (spec 09 §3's `[OPEN]`, который спека и поручала решить бенчмарку), а **не** расширением спана: спан смешал бы идентичность контента с представлением для поиска, потребовал бы `CHUNK_POLICY_VERSION` и полной переиндексации и всё равно не дал бы путь/имя/сигнатуру. Сериализатор и генерационный ридер готовы и покрыты 15 тестами; остаётся проводка subjects/backfill/vectors и измерение. **D — квантование весов** — уравнять нечем, остаточное. **Ступень 2 (коммит D-016 stage C).** Проводка `code_context` доведена до конца (subjects → backfill → vectors → dense-нога → `--dense-kind` в раннере) и **измерена**: MRR **0.5748** против **0.5782** у `code_raw` на том же корпусе, окне и квантовании. **Гипотеза C не подтвердилась** — сдвиг −0.0034 составляет треть от цены одного запроса, переехавшего на позицию, на корпусе в 49 запросов. Конверт меняет не уровень, а баланс: **+0.0612 Hit@3, +0.0204 Hit@5, −0.0408 Hit@1**; на этом корпусе обмен даже пересекает порог — `code_context` проходит `Recall@5 ≥ 0.80` (0.8163), `code_raw` нет (0.7959), — но по MRR промахиваются обе, так что меняется, какое условие падает, а не падает ли гейт. `[OPEN]` spec 09 §3 закрыт **по числам в пользу `code_raw`** (лучше на первой позиции, дешевле как субъект: 538 против 544 субъектов на 545 occurrence); `code_context` остаётся зарегистрированным и searchable представлением, чтобы решение можно было перемерить, а не переписать. Контрольный прогон `code_raw` после всей проводки воспроизвёл A+B ровно (0.5782) — дефолт не сдвинулся. **Итог ступеней A–C: из разрыва 0.1317 объяснено 0.0136; остаётся 0.1181, и из названных кандидатов не измерен ровно один — квантование весов.** Дальнейшая подкрутка на тех же 49 запросах без отложенной выборки не проводится намеренно. **Ступень 3 (коммит D-017).** Остаток объяснён и закрыт: провайдер читал `last_hidden_state` вместо `sentence_embedding`, то есть обученная Dense-голова модели не выполнялась. После исправления dense-нога — **MRR 0.7007** против v1's 0.6963 (Hit@1 совпадает до цифры, Hit@5 совпадает, Hit@3 выше), гейт в `--mode code` **PASS**; ступень D отыграла 0.2068 из 0.2024-го разрыва по ноге. Квантование снято как несущественное: q8 достигает качества BF16, fp16/fp32 не докачивались. Решение 09 §3 переизмерено и подтверждено (`code_raw` 0.7007 против `code_context` 0.6956). **Ступень 4 (коммит D-018).** Последняя часть разрыва — дефолтный режим: гибрид 0.5721 был хуже собственной dense-ноги, потому что невзвешенный RRF складывал её с лексической ногой на 0.4344. Взвешенный RRF с выведенным весом (`d = 2`, `w_lexical = 0.0161`) поднимает дефолт до **MRR 0.7007 при v1 0.6963 — гейт PASS**. **Итог всей цепочки: из разрыва 0.1317 объяснено и закрыто всё**; ни один из вариантов (а)/(б)/(в) не понадобился. | коммиты D-016 (A+B), D-016 stage C и D-017 (ступень D); `fixtures/search/baseline/run-v2-2026-07-26.json` + `.report.md`, пер-ножные `…-code-only.json` / `…-lexical-only.json`, ступени `…-stage-a.json`, `…-stage-b.json`, `…-stage-c-code-raw.json`, `…-stage-c-code-context.json`; таблица и разбор в `fixtures/search/baseline/baseline.md`; решение по представлению — spec 09 §3 as-built note (D-016); воспроизведение: `ORT_DYLIB_PATH=<lib> cargo xtask bench --corpus /opt/soft/local-rag --subdir src --dense-kind code_raw\|code_context [--mode code\|lexical]` |
| D-017 | D-016 (разбор остатка) | «Embeddings run **in-process**… the local backend is the working default» (spec 10 §1 `[FIXED]`) и ADR-0004, выбравший `embeddinggemma-300m` **как модель**, по её опубликованному качеству (MTEB Code 68.76) | **Провайдер эмбеддил не тем выходом графа.** ONNX-экспорт sentence-transformers объявляет два выхода — `last_hidden_state` `[batch, seq, 768]` первым и `sentence_embedding` `[batch, 768]` вторым, — и только второй проходит обученные Dense-модули модели (`st/dense_1` 768→3072, `st/dense_2` 3072→768) после пулинга. `crates/models/src/onnx.rs` брал `outputs.iter().next()`, то есть **всегда первый** (`ort::SessionOutputs::iter()` сохраняет порядок выходов сессии), и mean-пулил его сам. Обученная голова не выполнялась никогда: v2 искал в пространстве, которое модель не обучалась выдавать. Поймать это было нечем — оба выхода 768-мерные, оба нормируются, запрос и документы шли одним и тем же неверным путём, так что пространство оставалось самосогласованным и двигалось только **качество**. Это и есть необъяснённый остаток D-016: dense-нога 0.4939 против v1's 0.6963. | D-017: выбирать выход **по имени** (`POOLED_OUTPUT = "sentence_embedding"`), резолвя его один раз при открытии сессии; mean-пулинг остаётся fallback'ом для графа без такого выхода. Выбор выхода не входит в шесть полей `RepresentationKey`, поэтому `representation_version` идёт `2 → 3` тем же изменением — иначе `embedding_cache` отдавал бы дофиксовые векторы как валидные. | resolved | коммит D-017 (та же правка); dense-нога **MRR 0.4939 → 0.7007** при v1 0.6963 на том же корпусе, окне и квантовании — гейт в `--mode code` **PASS**; независимое подтверждение пространства, а не только счёта: на фиксированной тройке текстов исправленный провайдер воспроизводит геометрию Ollama (cos 0.761/0.191 против 0.755/0.186 у BF16-весов), позиционная версия давала 0.860/0.483; тесты `onnx.rs::select_output` (4 офлайн-теста политики) и утверждение `output_name() == POOLED_OUTPUT` в env-gated реальном прогоне; артефакты `run-v2-2026-07-26-stage-d-*.json` |
| D-018 | D-017 (перемер ступени D) | «quality: MRR not worse than v1 baseline by more than X; Recall@5 ≥ Y» (spec 14 §2 `[FIXED set]`) для **дефолтного** режима поиска; spec 09 §1 «hybrid: both → app-side RRF» | **Невзвешенный RRF теряет качество сильной ноги.** После D-017 dense-нога даёт MRR **0.7007** (гейт PASS), лексическая — **0.4344**, а дефолтный гибрид — **0.5721**, то есть на 0.1286 **хуже собственной dense-ноги**, и гейт падает. Арифметика прямая: `score = Σ 1/(k+rank)`, `k = 60` (spec 09 §4), поэтому документ, найденный обеими ногами на средних рангах, обгоняет документ, который сильная нога поставила первой — `1/61` против `1/61 + 1/80`. Пер-запросно фузия демотирует **15** запросов (13 из них с dense-ранга 1) и поднимает 9; на `code_context` — 14 против 11. Запросы бенчмарка — естественно-языковые описания, худший случай для BM25 по коду, так что лексические голоса здесь в основном шум. Замечание T12-05 «фузия не виновата» описывало две одинаково слабые ноги и на сильной+слабой перестало быть верным. | Требуется отдельная задача с амендментом spec 09 §4 (`[SPEC]`, не `[FIXED]`): взвешенный RRF, либо ограничение вклада ноги по глубине/качеству, либо иная политика фузии. Подбор весов по этим же 49 запросам — переобучение (корпус односоставный, один релевантный документ на запрос), поэтому изменение должно быть принципиальным и проверяться на обеих ногах, а не подгоняться под метрику. | resolved | ступень E (коммит D-018): свип по всем выведенным политикам в **одном** прогоне (корпус индексируется и эмбеддится один раз, кандидаты у всех точек одинаковы): `w=1.0` → MRR 0.5721; `d=50, w=0.446` → 0.6255; `d=20, w=0.238` → 0.6378; `d=10, w=0.129` → 0.6622; `d=5, w=0.0615` → 0.6667; `d=3, w=0.0317` → 0.6905; **`d=2, w=0.0161` → 0.7007, гейт PASS**; `w=0` (dense-нога) → 0.7007. Заранее зафиксированное правило выбора дефолта («наибольшая выведенная глубина, при которой гибрид не ниже собственной dense-ноги») выбирает `d=2`. Контрольный прогон дефолтной конфигурации без флагов: **Hit@1 0.5918 / Hit@3 0.8367 / Hit@5 0.8367 / MRR 0.7007, гейт PASS**. Вторая половина задачи — отсев неселективных термов (`df > N/2`, порог из смены знака IDF в BM25) — на этом корпусе не изменила ничего (лексическая нога 0.4344 с ним и без), потому что при 545 occurrence терму нужно попасть в 273 документа; корректность доказана на насыщенном корпусе в `crates/store/tests/fts_query.rs`. Артефакты: `run-v2-2026-07-27-stage-e-lw*.json`, `…-shallow-lw*.json`, `…-lexical-only.json`, `…-default.json`; ступень D: `run-v2-2026-07-26-stage-d-code-raw.json` (гибрид 0.5721), `…-code-raw-code-only.json` (dense 0.7007, PASS), `…-lexical-only.json` (0.4344), `…-code-context*.json`; пер-запросный разбор и таблица — `fixtures/search/baseline/baseline.md`, раздел «D — какой выход модели читается» |

| D-019 | G13 (при сверке spec 12 §2 в трассе `требование → код → тест`) | Spec 12 §2 (внутри `[FIXED]`-блока «Redaction & caps») `[SPEC]`: «Scanner: rule set (…), versioned `redaction_version` recorded in envelopes» | `local_rag_hook::payload::prepare_payload` (`crates/local-rag-hook/src/payload.rs`) реально вычисляет `redaction_version: scanner.version()` внутри `PreparedPayload::Included`, но `payload_field()` — единственный потребитель `PreparedPayload`, вызываемый из `main.rs::spool_write_pipeline` — извлекает только `bytes`, отбрасывая `redaction_version`/`secrets_found`/`truncation`. Проволочный формат (`local_rag_core::spool::FramePayload`, spec 07 §3) не содержит такого поля; `SCHEMA_V7` (`crates/store/src/observation/mod.rs`, spec 03 §2.5) — `observation_envelope`/`observation_payload` — не содержит такой колонки. Значение вычисляется, покрыто юнит-тестами в изоляции (`payload.rs`/`redaction/mod.rs`), но молча теряется до того, как ему нужно пережить сбой или попасть в БД — противоречит буквальному тексту spec 12 §2. Ни один as-built-комментарий (в отличие, например, от явно отложенного 4 KiB evidence-excerpt cap) не объясняет это как осознанное упрощение. | D-019: `[SPEC]`-пункт, не `[FIXED]` — исправляется тем же гейтом. `FramePayload` (`crates/core/src/spool.rs`) получает новое поле `redaction_version: Option<u32>` (перед `payload`); `local_rag_hook::payload` получает функцию, возвращающую `Some(scanner.version())` для `Included` и `None` для `EnvelopeOnly` (denied-событие не сканируется — значению взяться неоткуда); `main.rs::spool_write_pipeline` прокидывает поле в кадр. `crates/store` получает новую forward-only миграцию версии 8 (`observation_redaction_version`): `ALTER TABLE observation_envelope ADD COLUMN redaction_version INTEGER` (nullable, без backfill — исторические строки и envelope-only события легитимно не имеют значения, в отличие от D-007, где backfill в `0` был бы неверным); `import_batch` прокидывает декодированное значение в `NewObservationEnvelope`. | resolved | коммит `G13: Сверка no-loss ingestion (spec 07, 11 §3-4, 12 §2/§6, 14 §3) [PASS after D-019]` (та же правка). Продуктовый код: `crates/core/src/spool.rs` (+поле), `crates/local-rag-hook/src/{payload.rs,main.rs}` (+`redaction_version_field`, wiring), `crates/store/src/{observation/mod.rs,observation/import.rs,migrate/mod.rs}` (+миграция 8, +колонка, +wiring). Тесты: `cargo test -p local-rag-core --lib spool` 16 OK (было 14 + 2 новых: `frame_payload_redaction_version_is_none_for_envelope_only` + обновлённый golden-byte тест под новое поле); `cargo test -p local-rag-hook --features failpoints` 58 OK (38 lib — было 36, +2: `redaction_version_field_is_none_for_envelope_only`, `redaction_version_field_is_some_scanner_version_for_included`); `cargo test -p local-rag-store --lib observation::` 14 OK (было 13, +1 `insert_envelope_round_trips_redaction_version`); `--test observation_import` 10 OK (было 9, +1 `import_stores_redaction_version_and_null_for_envelope_only`); `--test migrate` 10 OK (было 9, +1 `migration_8_adds_the_envelope_redaction_version_column_with_no_backfill`, плюс обновлённые ассерты `state_db_open_bootstraps_and_is_idempotent` под 8 миграций); `--test registry` 11 OK (обновлён `migration_produces_exact_registry_schema` под v8). `cargo xtask ci` = all checks passed (после однократного `cargo fmt --all` — форматирование новых блоков) |
| D-020 | T14-03 (при планировании, до написания кода) | Spec 04 §5 (внутри `[FIXED]`-блока «Memory entry») prose: «a confirmed hypothesis stays `kind=hypothesis, state=confirmed`… promotion to `fact` happens only via explicit `supersede` — a new `fact` entry with `supersedes_id` pointing at the hypothesis, **which transitions to `superseded`**» | `MemoryState::check_transition` (`crates/store/src/memory/entry.rs`, T14-01) допускает для `Hypothesis` только `(Active,Confirmed)\|(Active,Rejected)\|(Active,Superseded)` — ни одного ребра из `Confirmed` нет вовсе. Подтверждённая гипотеза не может быть повышена в факт никогда, хотя проза 04 §5 буквально описывает именно этот переход («which transitions to superseded» — подлежащее «the hypothesis», уже confirmed). Не поймано T14-01: собственный исчерпывающий unit-тест `check_transition_covers_the_whole_matrix` сверяет код с рукописной копией того же (ошибочного) множества, а `tests/memory.rs::hypothesis_confirm_vs_fact_supersede` доводит гипотезу только до `confirmed` и ни разу не пытается её superseded — контрастный тест (hypothesis может confirm, fact не может, но может supersede), не тест промоушна confirmed-гипотезы. Собственные doc-комментарии `entry.rs` (module doc про promotion, `MemoryState::is_terminal`'s «until it is superseded») уже молчаливо предполагают наличие этого ребра — похоже на недосмотр, не осознанное ограничение. | D-020: добавить ровно `(Confirmed, Superseded)` в ветку `Hypothesis`, не больше — `Confirmed → Rejected` и `Confirmed → Retracted` не имеют текстовой опоры (у `Rejected` нет роли после confirm по прозе; `retracted` вообще не входит в state-set `Hypothesis` по таблице переходов) и не добавляются. Обновить `legal_for()`-зеркало в `check_transition_covers_the_whole_matrix`, добавить регрессионный тест на промоушн confirmed-гипотезы. | resolved | коммит T14-03 (та же правка); `cargo test -p local-rag-store --lib memory::entry:: --test memory` зелёные после фикса, новый тест промоушна confirmed-гипотезы в `superseded` |
| D-021 | G14 (при сверке spec 12 §2 / 03 §2.5 в трассе `требование → код → тест`) | Spec 12 §2 `[SPEC]`: «Size caps everywhere content flows (spool payload 256 KiB, **evidence excerpt 4 KiB**, snippet 8 KiB…)»; spec 03 §2.5: `observation_envelope.short_evidence_excerpt TEXT` | Три отдельных as-built-заметки в уже `PASS`-гейтованных группах явно передают владение этим кэпом группе 14, и ни одна не была подхвачена: `12-security-privacy.md:74-75` (T03-02) — «The 4 KiB evidence-excerpt cap remains **group 14's**, unchanged by this task»; `crates/local-rag-hook/src/payload.rs:43-45` (T13-01, doc-комментарий `PAYLOAD_CAP_BYTES`) — «the former is **group 14's** (memory evidence)»; `07-observations-spool.md:89-90` (T13-02) — «`short_evidence_excerpt` is left `null` at write time — it is not this task's to populate… remaining **group 14's**». Ни одна карточка `groups/14-memory.md` (T14-01…T14-09) этот пункт не несёт — группа закрылась, ни разу его не подобрав. Подтверждено прямой инспекцией: `crates/local-rag-hook/src/main.rs:210` безусловно пишет `short_evidence_excerpt: None`; ни в одном месте репозитория нет константы вида `4 * 1024`/`EXCERPT_CAP`. Поле корректно прокидывается по всему проводу (wire format → `observation_envelope.short_evidence_excerpt` → `WindowObservation.short_evidence_excerpt` → `local_rag_memory::prompt`'s `.unwrap_or("(no excerpt)")`), т.е. ничего не сломано, но router никогда не видит текст evidence, под который спека завела колонку — тот же класс находки, что D-007/D-011 (`[FIXED]`/`[SPEC]`-требование с явно назначенным владельцем, группа закрывается, не подобрав его). | D-021: реализовать сейчас (фундамент готов — схема/wire/import/consumer'ский graceful-degradation fallback уже существуют, недостаёт только сайта заполнения), тем же паттерном, что D-007/D-011/D-013. `crates/local-rag-hook/src/payload.rs`: новая `pub const EXCERPT_CAP_BYTES: usize = 4 * 1024`, новая `pub fn short_evidence_excerpt_field(prepared: &PreparedPayload) -> Option<String>` — зеркало `payload_field`/`redaction_version_field`: `EnvelopeOnly` → `None`; `Included { bytes, .. }` → уже отредактированные `bytes`, урезанные до `EXCERPT_CAP_BYTES` тем же UTF-8-boundary-safe walk-back, что уже реализован инлайн в `prepare_payload` для `PAYLOAD_CAP_BYTES`. `main.rs::spool_write_pipeline` — `short_evidence_excerpt: None` заменено на вызов новой функции. Без нового `{hash, original_size}`: в отличие от payload/snippet-кэпов, это вторичный, чисто для router-промпта срез уже захваченного и уже отслеженного по truncation содержимого, а не авторитетная запись о том, что было захвачено. | resolved | коммит `G14: Сверка memory correctness/quality [PASS after D-021]` (та же правка). Продуктовый код: `crates/local-rag-hook/src/{payload.rs,main.rs}` (+`EXCERPT_CAP_BYTES`, +`short_evidence_excerpt_field`, wiring). Тесты: `cargo test -p local-rag-hook` — новые regression-тесты на under-cap passthrough, ровно-на-границе, over-cap truncation, UTF-8-boundary safety на многобайтовой точке среза, `EnvelopeOnly` остаётся `None`, end-to-end `spool_write_pipeline`→frame-decode доказывает, что реальный excerpt теперь переживает полный round trip вместо `(no excerpt)`-fallback'а. `[SPEC]`-амендменты: `07-observations-spool.md` (excerpt больше не всегда null), `12-security-privacy.md:74-75` (кэп теперь реализован, не только задокументирован) |

| D-022 | T15-01 (при финальном `cargo xtask ci`) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD); D-009 (T11-03) уже зафиксировал ровно этот инвариант | `crates/store/tests/fts_corruption.rs::delete_whole_cache_file_is_fully_restored` (T08-04, группа 08, gated `PASS`) изредка падает под параллельной нагрузкой: `drop(cache)` (комментарий «release the writer thread's connection before deleting files») не ждёт, пока writer-поток `CacheDb` реально закроет соединение — `Drop` асинхронен по конструкции (D-009's находка: writer живёт на detached-потоке, graceful drain — забота демона). Тест тут же удаляет `cache.sqlite`/`-wal`/`-shm` и открывает новый инстанс по тому же пути; SQLite открывает `-wal`/`-shm` **по имени**, так что ещё-не-закрывшийся writer старого инстанса может попасть по сайдкарам уже нового — тот же класс гонки, что D-009 нашёл и исправил в `tests/cache.rs`, просто в другом файле, который не подхватили при том фиксе (T08-04 создан позже T01-05, но `fts_corruption.rs` написан до появления `CacheDb::close()` в D-009 и не обновлён). Обнаружено этой задачей из-за возросшего параллелизма (T15-01 добавила несколько новых тест-бинарников в `local-rag-store`/`local-rag`), тем же способом, что D-003/D-005/D-009 сами были обнаружены. Воспроизведено напрямую: 12 параллельных прогонов `cargo test -p local-rag-store --test fts_corruption -- --test-threads=8` — 4/12 падали на этом тесте. Продуктовый `cache`-код не тронут (сам D-009's `close()` уже существует и корректен — этот тест просто не вызывает его). | D-022: заменить `drop(cache)` на `cache.close()` (уже существующий, D-009's blocking join-the-writer-thread вариант) в `delete_whole_cache_file_is_fully_restored`, дословно тот же фикс, что D-009 применил к пяти блокам `tests/cache.rs`. Ассерты не ослаблены — меняется только момент закрытия соединения перед unlink. | resolved | коммит T15-01 (та же правка); `crates/store/tests/fts_corruption.rs` — 1 строка (`drop(cache)` → `cache.close()`); стресс-прогон 12 параллельных `cargo test -p local-rag-store --test fts_corruption -- --test-threads=8` — 12/12 зелёных (было 4/12 падений до фикса); `cargo test -p local-rag-store --test fts_corruption` — 6 OK |

| D-023 | T15-04 (при написании `crates/local-rag/tests/mcp_memory_tools.rs`) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD); D-022 уже зафиксировал ровно этот инвариант для этой же группы | `local_rag_test_support::TempHome::new()` (`crates/test-support/src/home.rs`) строит корень как `std::env::temp_dir().join("local-rag-tests").join(format!("home-{pid}-{seq}"))` — `std::env::temp_dir()` уважает `$TMPDIR`, длина которого не ограничена ничем нормативным. Реальный демон-тест открывает Unix-domain socket по `StoreLayout::socket_path()` (`.../local-rag/run/daemon.sock`), а `sockaddr_un.sun_path` на macOS ограничен ~104 байтами — в этом окружении `$TMPDIR` уже 49 байт, так что полный путь (106+ байт с реалистичными `pid`/`seq`) регулярно переваливает лимит, и `DaemonHandle::start` падает на `Bind(InvalidInput, "path must be shorter than SUN_LEN")`. Раньше не проявлялось: `next_seq()` — process-global счётчик, и каждый `tests/*.rs`-бинарник до сих пор держал ≤10 реальных демон-тестов в одном процессе, так что `seq` не успевал вырасти настолько, чтобы столкнуть путь за границу; T15-04's `mcp_memory_tools.rs` (17 тестов в одном бинарнике) — первый файл, который выявляет проблему регулярно и невоспроизводимо (какие именно тесты падают, зависит от порядка планировщика потоков). Подтверждено: 2 последовательных прогона `cargo test -p local-rag --test mcp_memory_tools` дали разные наборы из 7 упавших тестов из 17. Продуктовый код (`StoreLayout::socket_path`, реальный `local-rag serve`) не затронут — реальный `LOCAL_RAG_HOME` пользователя короче любого `$TMPDIR` этого класса. | D-023: `TempHome::new()` предпочитает `/tmp` напрямую на Unix (POSIX-гарантированный короткий путь, на macOS — симлинк на `/private/tmp`), в обход `$TMPDIR`, с падением на `std::env::temp_dir()` там, где `/tmp` недоступен, и на не-Unix платформах без изменений. Ассерты не ослаблены — меняется только базовая директория, изоляция/очистка на `Drop` не тронуты. Regression: новый unit-тест в `home.rs`, подтверждающий итоговую длину пути (`TempHome` + `local-rag/run/daemon.sock`) остаётся с запасом под 104-байтной границей при реалистичных `pid`/`seq`. | resolved | коммит T15-04 (та же правка); `crates/test-support/src/home.rs`: `local_temp_root()` (unix/не-unix варианты) + `TempHome::new()` использует её; unit-тесты `a_real_temp_home_socket_path_fits_within_sun_path_len`/`the_bound_holds_for_a_worst_case_pid_and_seq_too`/`a_real_temp_home_is_rooted_under_local_temp_root`; `two_temp_homes_are_isolated` (`tests/harness.rs`) обновлён — сравнивает `a.path().parent() == b.path().parent()` вместо жёсткой сверки с `std::env::temp_dir()`. `cargo test -p local-rag-test-support` — 10 unit + 8 integration + 6 doctest, все OK. Стресс: 5 последовательных прогонов `cargo test -p local-rag --test mcp_memory_tools` — 17/17 зелёных каждый раз (было до 7/17 падений на прогон до фикса) |

| D-024 | T15-06 (при планировании, до написания кода) | `docs/specification/02-architecture.md` §4.3 as-built (T15-01): «continuous consolidation triggering (checkpoint on `Stop`, queue-size threshold, best-effort `SessionEnd`) is T15-06's, per T14-06's own as-built note»; `docs/specification/08-memory.md` §4 as-built (T14-06): «picking an actual default is deferred to whichever task wires the daemon-level trigger (T15-06)»; решающий текст — `docs/specification/07-observations-spool.md` §6 `[FIXED]`: «Consolidation checkpoint on `Stop` and on queue size threshold; best-effort on `SessionEnd`; catch-up of unprocessed observations at daemon startup; a background worker owns all of it» | Собственная карточка T15-06 (`groups/15-daemon-interfaces-cli.md`) несёт только hook-side recall RPC + `additionalContext` — continuous-часть требования (три из четырёх пунктов 07 §6) нигде не описана ни в одной карточке плана. T15-01 реализовала только startup catch-up четверть (`local_rag::daemon::resume`, два one-shot pass'а, запускаются один раз при `DaemonHandle::start`), её собственный module doc уже прямо называет это узкой «crashed runs with expired leases» задачей, не scheduler'ом. Три continuous-условия (checkpoint on Stop / queue-size threshold / best-effort SessionEnd) нигде не реализованы и не покрыты ни одной другой карточкой (grep по `groups/` не находит владельца уже, поскольку T15-06 явно не несёт этот пункт своим собственным текстом) — тот же класс находки, что уже resolved D-007/D-011/D-013/D-021 в этой кодовой базе. | D-024 (owner decision — реализовать сейчас, отдельной задачей от hook recall RPC, обе независимо тестируемы и функциональны): новый continuous background worker `local_rag::daemon::consolidation_trigger` (`crates/local-rag/src/daemon/consolidation_trigger.rs`) — `tokio::time::interval`-driven loop (`[SPEC]` 15s cadence, `main.rs::CONSOLIDATION_POLL_INTERVAL`, не конфиг, тот же прецедент, что `IDLE_POLL_INTERVAL`), спавнится в `DaemonHandle::start` рядом с двумя startup resume pass'ами (тот же `state_db.is_some()` guard), сигнал-затем-await cancellation через отдельную `(oneshot::Sender<()>, JoinHandle<()>)` пару (по образцу `handshake_stop`/`handshake_join`, а не blind-await `resume_handles` — этот loop никогда не завершается сам). Каждый тик: `resume_stale_consolidation_runs` verbatim (сначала — иначе `open_next_run` спуриозно отказывает `Existing` на строку от прошлого тика), затем per-session `import_session_tail` + `open_next_run`+`run_once`, если импорт увидел `Stop`/`SessionEnd` ИЛИ `local_rag_store::pending_backlog` (новый примитив) пересёк `config.memory.consolidation_queue_threshold`. Новые store-примитивы: `pending_backlog` (`crates/store/src/memory/consolidation.rs`) и `ImportBatchReport.{saw_stop,saw_session_end}` (`crates/store/src/observation/import.rs`, выставляются в уже существующем per-frame loop). Новый `JobKind::ConsolidationTrigger` (registry уже `#[non_exhaustive]` именно ради этого расширения — собственный doc `jobs.rs` это предвидел буквально). Новые значения — явно `[SPEC]`, не `[FIXED]`: `consolidation_batch_size = 20`, `consolidation_queue_threshold = 50` (оба в `MemoryConfig`, `[memory]` секция, тот же дом, что `recall_token_budget`) — порог намеренно в 2.5× больше батча, иначе воркер открывал бы run почти на каждом тике с любым backlog. `router_version = "v1"` (не "v0" — единственное реально используемое значение во всех существующих call site'ах). `JobGuard` держится строго внутри одного тика (до, не после, `ticker.tick()`), поэтому воркер, живой между тиками, не блокирует idle-shutdown — `idle.rs` не тронут. Известный, принятый gap: первый тик воркера при старте демона гоняется с T15-01's startup spool-import pass'ом за один и тот же session tail — если startup pass выигрывает гонку, checkpoint-флаг для этого конкретного Stop/SessionEnd пропускается (не просто лишнее чтение), но queue-threshold путь ловит это позже; не инженерится вокруг (не оправдано для boot-only, самовосстанавливающегося разрыва). | resolved | коммит `D-024: daemon continuous consolidation trigger` (та же правка). Продуктовый код: `crates/store/src/{memory/consolidation.rs,memory/mod.rs,observation/import.rs,lib.rs}` (+`pending_backlog`, +`ImportBatchReport.saw_stop/saw_session_end`); `crates/core/src/config/mod.rs` (+`MemoryConfig.consolidation_batch_size/consolidation_queue_threshold`); `crates/local-rag/src/daemon/{consolidation_trigger.rs (новый),jobs.rs,lifecycle.rs,mod.rs,main.rs}`. Тесты: `cargo test -p local-rag-store --lib memory::consolidation::` 16 OK (было 13, +3: `pending_backlog_*`); `--test observation_import` 18 OK (было 14, +4: `import_batch_flags_saw_stop_*`/`saw_session_end_*`/`neither_*`); `cargo test -p local-rag-core --lib config::` 19 OK (было 17, +2: `memory_config_defaults_consolidation_batch_and_threshold`, `memory_section_round_trips_custom_consolidation_values`; `default_matches_spec_toml` обновлён под новые ключи); `cargo test -p local-rag --lib daemon::consolidation_trigger::` 9 OK (новый модуль, 3/3 устойчивых повторных прогона); `cargo test -p local-rag --test consolidation_trigger` 3 OK (новый DaemonHandle-уровневый файл, 3/3 устойчивых повторных прогона); полный `cargo test -p local-rag` — все unit+integration зелёные (StartOptions-литералы в 4 существующих test-файлах расширены новыми полями); `cargo clippy -p local-rag --all-targets --features failpoints -- -D warnings` и `cargo clippy --workspace --all-targets -- -D warnings` чистые; `cargo fmt --all` применён. `cargo xtask ci` — all 17 jobs passed, прогнан дважды подряд для устойчивости (см. PROGRESS.md evidence). `[SPEC]`-амендменты: `docs/specification/02-architecture.md` §4.3, `docs/specification/07-observations-spool.md` §6, `docs/specification/08-memory.md` §4 |

| D-025 | T15-08 (при планировании, до написания кода) | Карточка T15-08 (`groups/15-daemon-interfaces-cli.md:66-70`): «memory review/mutation, **inspect/export/purge**, gc/**doctor**/stats command adapters» | `inspect`/`export`/`purge` и `doctor` не имеют никакого доменного кода в кодовой базе (grep по всему workspace на `purge`/`tombstone`/`audit_ref`/`export`/`doctor` — ноль продуктовых совпадений), и оба явно закреплены за карточками группы 16, которая ещё не начата: T16-02 (`groups/16-security-and-recovery.md:20-23`) — «purge is only hard-delete path and rewrites audit refs to non-sensitive tombstones transactionally»; T16-03 (`:24-27`) — «doctor validates lock/versions/heads/orphans/permissions». Spec 12 §3 (`12-security-privacy.md:129-130`) — единственный нормативный текст про `inspect/export/purge`, одна строка, механика оставлена явно T16-02. Собственные doc-комментарии кода уже предвидят этот разрыв: `crates/store/src/memory/op.rs:822-825` («only an explicit privacy `purge` (12 §5, a later task) hard-removes»), `crates/store/src/retention.rs:50-54` («Memory evidence, audit, and export (groups 14/16) ... do not exist yet»). Тот же класс находки, что D-004/D-007/D-011/D-013/D-021/D-024: карточка обещает адаптеры для функциональности более поздней, ещё не начатой группы. Остальная часть карточки — `memory list|approve|reject|edit|retract|merge|evidence`, `gc [--dry-run]`, `stats` — доменно полностью готова: group 14's op-engine (`crates/store/src/memory/{op,review,entry,stats}.rs`) и T15-04/T15-05's MCP-тулы (`crates/local-rag/src/daemon/mcp/{memory,memory_write}.rs`) уже покрывают весь memory/stats-функционал; шесть housekeeping/retention sweep'ов (`crates/store/src/housekeeping.rs`, `crates/store/src/observation/payload_ttl.rs` — T06-03, D-007, D-011, T13-05, T14-05) публичны, протестированы и не имеют ни одного продуктового вызывающего — `gc` был бы их первой продуктовой проводкой, не новым доменным кодом. | D-025 (owner decision — split немедленно, до написания кода, тот же паттерн, что D-004/D-008/D-020): T15-08 поставляет только CLI-адаптеры `memory`/`gc`/`stats`. `inspect <observation\|memory\|generation>`, `export`, `purge` (и audit-tombstone rewrite за ним) отложены в **T16-02**, которая уже обещает эту функциональность собственным результатом; `doctor` отложен в **T16-03**, которая уже обещает «lock/versions/heads/orphans/permissions» собственным результатом. Обе карточки группы 16 дополнены пунктом «Добавлено D-025», явно называющим их владельцами CLI-проводки этих команд, не только доменного результата (тот же приём, что D-013 применил к карточке T15-07). `memory evidence <id>` (единственный кусок «inspect», у которого уже есть доменная функция `memory_evidence_for` и MCP-прецедент `inspect_memory_evidence`, T15-04) остаётся в T15-08 — в отличие от полной трёхвидовой `local-rag inspect`, ему не нужно ни строчки нового кода. `[SPEC]`-амендмент: 11 §6 (as-built заметка T15-08, тем же стилем, что T15-07/T11-06's собственные заметки в конце секции). | resolved | коммит T15-08 (та же правка) |

| D-026 | G15 (при сверке spec 02 §4/§6, 11, 13 §4 в трассе `требование → код → тест`) | Карточка G15 (`groups/15-daemon-interfaces-cli.md:74-76`): «Execute proxy-daemon-hook end-to-end with daemon down/restart/upgrade, all v0 MCP contract tests» | Три параллельных агента перечитали spec 02 §§1-6, 11 (все шесть секций), 13 §4 против текущего кода. Два независимых трейса (spec-02 и spec-11/13§4) **одинаково** обнаружили: `establish_session` (`crates/local-rag-proxy/src/handshake.rs:140-172` — реальный цикл version-check → `SHUTDOWN_REQUEST` → `wait_for_close` → reconnect → `MAX_UPGRADE_ROUNDS`/`UpgradeLoopExceeded`, spec 13 §4) не имел ни одного теста самого цикла — только его две составляющие (`do_handshake`, `wait_for_close`) были юнит-протестированы изолированно на mock-duplex (`handshake.rs:233-333`). Третий агент (spec-02) отдельно нашёл: ни один существующий two-binary тест (`crates/local-rag-proxy/tests/subprocess.rs`) не проверяет "restart" — только cold-start (демон изначально не запущен); и `startup_error_message`'s `MIGRATION_IN_PROGRESS`-ветка (`crates/local-rag/src/main.rs:189-208`) не имела вообще никакого теста (соседняя `STORE_LOCKED`-ветка покрыта реальным two-process тестом). Четвёртый агент (spec-11) отдельно нашёл: `get_file_context` не имел MCP-dispatch-уровневого контрактного теста (ни happy-path через `tools/call`, ни строки в `mcp_contract.rs`'s таблице ошибок аргументов) — только приватный path-helper и сам `SearchEngine`-метод были протестированы отдельно. Guardrail-проверки (no ambient routing, ingestion never via RPC, indexing isolated from MCP path, no project files) — чисто, нарушений не найдено. `POLICY_BLOCKED_REMOTE` структурно недостижим в v0 (нет ни одного зарегистрированного remote embedder-провайдера, D-008) — не пробел, а as-built факт. L3 (shard-manager map) rank-ordering не имеет отдельного теста, но механизм (`checked_scope_sync`) уже доказан на L1 в `crates/store/tests/lock.rs` — тот же код, вторая проверка не добавляет покрытия. | D-026 (owner decision — исправить сейчас, найдено самим гейтом, тот же паттерн, что D-019/D-021): добавлены четыре теста, продуктовый код не менялся — каждый путь уже вёл себя корректно по спеке, отсутствовало только тестовое покрытие. (1) `crates/local-rag-proxy/tests/subprocess.rs::daemon_version_mismatch_triggers_the_upgrade_flow_and_completes_against_the_new_daemon` — реальный fake-old-daemon (`std::os::unix::net::UnixListener`, отвечает WELCOME с несовпадающей `daemon_version`, читает и поглощает `ShutdownRequest`, закрывается) + реальный спавн нового демона через `spawn_detached_daemon` (round 2), проверяет успешное завершение `establish_session` против совпадающей версии. (2) `..._proxy/tests/subprocess.rs::persistent_version_mismatch_exceeds_the_upgrade_round_budget` — fake-демон постоянно отвечает несовпадающей версией на все `MAX_UPGRADE_ROUNDS` раундов, проверяет `ProxyError::UpgradeLoopExceeded`, exit-код ≠0, диагностика на stderr. (3) `..._proxy/tests/subprocess.rs::a_fresh_proxy_recovers_after_the_daemon_it_used_is_killed` — реальный `SIGKILL` демона (не `SIGTERM`, чтобы оставить осиротевший, непривязанный socket-файл — ровно как spec 02 §4.1/§4.4's as-built заметка описывает), затем свежий процесс proxy успешно переподключается/спавнит новый демон и завершает реальный MCP handshake. (4) `crates/local-rag/src/main.rs`'s новый `#[cfg(test)] mod tests` — `a_not_ready_owner_reports_migration_in_progress`/`a_ready_owner_reports_store_locked`, юнит-тесты чистой функции `startup_error_message` на обеих ветках `owner.ready`. (5) `crates/local-rag/tests/mcp_contract.rs`'s таблица аргумент-ошибок дополнена случаем `get_file_context` без `path`; `crates/local-rag/tests/mcp_tools.rs` получил `get_file_context_returns_the_seeded_occurrence` (реальный seeded/indexed worktree, `tools/call` happy-path). | resolved | коммит `G15: Сверка interface contracts [PASS after D-026]` (та же правка). Продуктовый код не менялся — только тесты. `cargo test -p local-rag-proxy` — 22 OK (было 19, +3: два upgrade-loop теста + restart e2e); `cargo test -p local-rag --bin local-rag` — 15 OK (было 13, +2: `migration_in_progress`/`store_locked` unit-тесты); `cargo test -p local-rag --test mcp_contract` — 8 OK (таблица аргумент-ошибок выросла с 26 до 27 случаев); `cargo test -p local-rag --test mcp_tools` — 4 OK (было 3, +1: `get_file_context`); каждый новый subprocess-тест прогнан ×3 подряд под `cargo test` и `cargo nextest run` без единого падения. `cargo fmt --check`/`cargo clippy --all-targets -- -D warnings` — чисто на обоих затронутых крейтах. Полный `cargo xtask ci` — все 17 job зелёные |

| D-027 | T16-03 (обнаружено smoke-тестом собственной permissions-секции `doctor`'а) | Spec 12 §6 `[FIXED]`: «Store dirs `0700`, files/segments `0600` (POSIX)» | `state.sqlite`/`cache.sqlite` создавались без явного применения режима — `open_state_rw`/`open_cache_rw` (`crates/store/src/{state/open.rs,cache/open.rs}`) делали голый `Connection::open(path)`, полагаясь только на process umask. В отличие от `store.lock` (`daemon::lock::acquire`), spool-сегментов (`local-rag-hook::segment`), файла subagent-counter, файла миграционного лока и backup-файлов миграций — все они уже вызывают `local_rag_core::paths::ensure_file_0600` — два самых часто открываемых файла стора этот вызов никогда не получали. Найдено T16-03's собственной новой permissions-аудит секцией (`StoreLayout::audit_permissions`) на первом реальном ручном прогоне `local-rag doctor` против только что проиндексированного стора: `state.sqlite has mode 0644, expected 0600`. Подтверждено воспроизводимо, не артефакт песочницы: `umask` = `0022` ⇒ `0666 & ~0022 = 0644` — ровно то, что голый `Connection::open()` создаёт без явного mode; `stat` на реальном файле подтвердил `0644`. | D-027: `ensure_file_0600(path)` первой строкой в `open_state_rw`/`open_cache_rw`, тем же идиомом, что уже используют все перечисленные выше managed-файлы — идемпотентно (создаёт при первом открытии на `0600`, owner-проверяет и переасертит `0600` на каждом последующем). Новые варианты `OpenError::Path(PathError)`/`CacheOpenError::Path(PathError)` (оба `#[non_exhaustive]`, обратная совместимость сохранена). Продуктовый инвариант не ослаблен нигде — фикс только устраняет молчаливый пропуск уже существующего механизма. | resolved | коммит T16-03 (та же правка). Продуктовый код: `crates/store/src/{state/open.rs,cache/open.rs}` (+`ensure_file_0600` вызов, +`OpenError::Path`/`CacheOpenError::Path` варианты с Display/`source()`). Тесты: `state_db_open_creates_and_reasserts_state_sqlite_at_0600` (`crates/store/tests/state.rs`) и `cache_db_open_creates_and_reasserts_cache_sqlite_at_0600` (`crates/store/tests/cache.rs`) — оба `#[cfg(unix)]`, оба проверяют свежее создание на `0600` **и** идемпотентное переасертие после ручного расширения до `0644` и повторного открытия. `cargo test -p local-rag-store --test state --test cache` — 21 OK (было 19, +2); полный `cargo test -p local-rag-store` (все lib+integration+doc бинарники, без фильтра) — 0 провалов, регрессий от смены поведения открытия не найдено (в т.ч. никакой существующий тест не symlink'ает `state.sqlite`/`cache.sqlite` — единственный класс, который `ensure_file_0600`'s symlink-swap защита могла бы newly отвергнуть). `cargo check --workspace` — чисто. `cargo fmt --check --all`/`cargo clippy -p local-rag-store -p local-rag-core -p local-rag-projection -p local-rag --all-targets -- -D warnings` — чисто на затронутых крейтах |

| D-028 | T17-03 (обнаружено ручной проверкой реального `local-rag serve`/`init` без `ORT_DYLIB_PATH`, затем повторно — с реальным бандлом на `darwin-x64` под Rosetta) | `crates/models/src/onnx.rs`'s собственный модульный doc-comment (T11-06/D-008): «its absence surfaces as a typed `OnnxError::Runtime` rather than a panic»; `local-rag/src/main.rs::build_query_embedder`'s doc: провайдер, который не открылся, «degrades to `UnavailableEmbedder` rather than failing daemon startup» | `ort` 2.0.0-rc.12's `load-dynamic`-путь не проваливается быстро при **любом** сбое собственной ленивой инициализации: конструирование его же ошибки «не удалось» реэнтерит тот `OnceLock`, в котором уже выполняется эта инициализация, и поток вечно ждёт сам себя на `Once::call_once_force`. Подтверждено **дважды, независимо**, сэмплингом реальных сборок: **(1)** рантайма нет вообще нигде (`ORT_DYLIB_PATH` не задан, рядом с исполняемым файлом ничего не забандлено) — `target/release/local-rag serve` не паниковал и не логировал ошибку, просто завис (`ps` — `SN`, `status` — `not running`) **120+ секунд**; стек застрял в `OnnxEmbedder::open_dir → ort::session::builder::SessionBuilder::new → ort::environment::current → …create_environment::closure → OnceLock::try_init_inner → …call_once_force::closure → ort::setup_api → OnceLock::try_init_inner (вложенный) → …call_once_force::closure → ort::error::Error::new_internal → OnceLock::try_init_inner (тот же хэш монофункции, что и первый кадр) → queue::wait`. **(2)** рантайм найден и путь к нему передан явно (`ort::init_from(path)`), но сам файл не грузится — воспроизведено на T17-03's собственном `darwin-x64`-бандле (ORT v1.20.0, единственный релиз с `osx-x86_64`-архивом) под Rosetta на этой Apple Silicon машине: `codesign -dv` подтвердил, что `libonnxruntime.dylib` v1.20.0 **не подписан вообще** (в отличие от v1.27.0's arm64-сборки, подписанной `UBF8T346G9`), и `local-rag init` под `arch -x86_64` зависал так же — стек застрял внутри `ort::init_from → ort::error::Error::new_internal → …` (тот же реэнтерабельный `OnceLock`, на один уровень глубже). Обе находки — один и тот же корень (любой сбой setup у `ort` реэнтерит его error-path), не два разных бага; ни один из существующих тестов до T17-03 не ловил ни один из двух путей, потому что все они всегда явно задавали заведомо рабочий `ORT_DYLIB_PATH`. | D-028: `crates/models/src/onnx.rs` — **(a)** чистая функция `resolve_ort_source(dylib_path_override, exe) -> Result<PathBuf, String>` решает («используй override», «используй бандл рядом с `exe`», «ничего нет — типизированная ошибка») **до** обращения к `ort`; **(b)** `ensure_ort_initialized()` (memoized в `OnceLock<Result<(), String>>` — результат нужен каждому вызывающему, не только первому) сама вызывает `ort::init_from(path)` на пути от (a) **вместо того**, чтобы полагаться на `ort`'s собственный ленивый разбор `ORT_DYLIB_PATH` внутри `Session::builder()` — то есть первое обращение к `ort` в процессе всегда происходит под этим модулем's контролем, а не где-то ещё; **(c)** сам вызов `ort::init_from` бежит на отдельном потоке-«сторожевом псе» (`run_with_timeout`, 10 секунд — реальные загрузки по G11 занимают 300–800 мс) — таймаут превращает зависший поток в типизированную ошибку и **сознательно** оставляет тот один поток висеть навсегда (у Rust нет thread-kill; `OnceLock` гарантирует, что за жизнь процесса это происходит максимум один раз). Продуктовый инвариант не ослаблен: рантайм всё ещё резолвится из `ORT_DYLIB_PATH` (приоритет) или бандла рядом с исполняемым файлом; «не удалось» теперь всегда — быстрая типизированная ошибка в разумный срок, а не бессрочный зависон. **Известный остаток, не закрытый этой правкой**: почему конкретно v1.20.0's `osx-x86_64` не грузится под Rosetta (сам факт «не подписан» не проверен как достаточная причина — не было реальной Intel-машины, чтобы отличить «не подписан → не грузится нигде» от «не подписан → не грузится только под Rosetta») остаётся не выясненным; watchdog делает это безопасным сбоем (`search_code` остаётся `lexical_only`), но не делает `darwin-x64`'s dense-ногу доказанно рабочей — см. evidence T17-03 в `PROGRESS.md`. | resolved | коммит T17-03 (та же правка). Продуктовый код: `crates/models/src/onnx.rs` (`resolve_ort_source`, `ensure_ort_initialized`, `init_ort_with_timeout`, `run_with_timeout`, вызов `?` в `open_dir`). Тесты (все новые, все чистые unit — без мутации `ORT_DYLIB_PATH`, без сети, без гонок): `neither_an_override_nor_a_bundled_file_is_a_typed_error_not_a_hang`, `an_explicit_override_always_wins_over_a_bundled_file`, `no_override_falls_back_to_the_bundled_file_when_present`, `no_bundled_dylib_beside_the_executable_resolves_to_none`, `a_dylib_beside_the_executable_is_found_by_its_platform_name`, `run_with_timeout_returns_a_fast_result`, `run_with_timeout_gives_up_on_a_hung_closure_without_waiting_for_it` (искусственно зависающее замыкание на канале без отправителя — доказывает, что сторож реально ограничивает ожидание, а не доверяет замыканию) — `cargo test -p local-rag-models --lib onnx::` — **15 OK**, 0 failed; `cargo fmt -p local-rag-models --check`/`cargo clippy -p local-rag-models --all-targets -- -D warnings` — чисто. Регрессия против G11's рабочего пути не сломана: `ORT_DYLIB_PATH=/opt/homebrew/lib/libonnxruntime.dylib LOCAL_RAG_TEST_MODEL_HOME=~/.local/share/local-rag-bench/local-rag cargo test -p local-rag-models --release --test onnx real_inference -- --nocapture` → тот же реальный инференс (768 dims, cos(same)=0.761 > cos(diff)=0.191). Сквозная проверка на реальных npm-бандлах (`npm/memory-<platform>/bin/`, модель установлена, `ORT_DYLIB_PATH` снят): **darwin-arm64** — `local-rag init` 0.85 с, реальная регистрация `code_raw`; `local-rag index` реального фикстур-файла → «indexed 2 files (63 occurrences)…embedded 62 subjects (0 failed); dense +63/-0»; `local-rag doctor` → «clean», `dense=Valid`; `local-rag-hook spool-write` → реальный сегмент на диске; полный MCP handshake через `local-rag-proxy` (cold-spawn демона) → `initialize`+`tools/list` (17 инструментов) отвечены корректно. **darwin-x64 под Rosetta** — было: зависание 120+ с; стало: `local-rag init` завершается за ~12 с (10-секундный таймаут + оверхед) с чистым сообщением «loading the ONNX Runtime did not finish within 10s…», `search_code` остаётся `lexical_only`, процесс не виснет |

| D-029 | T17-03 (запланировано и согласовано с владельцем до начала работы — см. `AskUserQuestion` в этой же сессии) | Карточка T17-03: «signed/checksummed artifacts for darwin x64/arm64, linux x64/arm64, win32 x64; ORT/tree-sitter/SQLite/chosen backend smoke; win32-arm64 explicitly excluded» | Три независимых, но однокоренных пробела, все — недостижимы локально на этой машине (macOS arm64, без Docker/QEMU, без реального Windows-хоста): **(1) Код-signing** — ни Apple Developer ID (notarization), ни Windows Authenticode сертификат не сконфигурированы; `dist build`/`dist plan` подтверждают, что генерируемый `.github/workflows/release.yml` не содержит подписывающего шага (его некому включить без сертификата). **(2) Реальный прогон сгенерированной GitHub Actions CI matrix** — `git remote` теперь реально существует (`origin` → `https://github.com/13W/memory.git`, создан в этой же сессии через `gh`), но ничего не запушено (CLAUDE.md: «Do not push unless the user asks», явного запроса не было), поэтому ни один реальный `windows-latest`/`macos-latest`/`ubuntu-latest`-раннер, которых требует workflow, ни разу не выполнялся. **(3) `win32-x64` build/smoke целиком** — `cargo-zigbuild` документированно не поддерживает Windows-таргеты вообще («Currently only Linux and macOS targets are supported», проверено WebFetch на официальной документации до начала T17-03), значит `x86_64-pc-windows-msvc` нельзя собрать с этой машины никаким доступным инструментом из `[FIXED]`-списка spec 13 §1 (`cargo-dist, cargo-zigbuild`); нужен либо реальный `windows-latest`-раннер (через тот же п.2), либо отдельный MSVC-кросс-тулчейн (`cargo-xwin` и т.п.), явно вне scope этой карточки. `npm/memory-win32-x64/bin/` остаётся пустым — задокументированный блок, не тихий пробел (сравнить с уже наполненными `npm/memory-{darwin-arm64,darwin-x64,linux-x64,linux-arm64}/bin/`, T17-03). Дополнительно: реальное (не build-only) исполнение `linux-x64`/`linux-arm64` тоже недостижимо (нет Docker/QEMU) — те два таргета верифицированы только структурно (`file` подтверждает верный ELF-формат/архитектуру каждого бинарника и `libonnxruntime.so`), без реального инференса; в отличие от п.1–3 это не отдельная находка, а тот же корень («нет второй реальной машины/раннера под этот таргет»). | Не корректирующая задача — продуктовый/инфраструктурный вопрос, требующий решения владельца (сертификаты, разрешение на push, и/или выделенный CI-раннер), а не инженерного выбора. Зафиксировано как `blocked`, а не тихо пропущено или подделано как «пройдено»: `T17-03` в `PROGRESS.md` отмечается `[x]` за реально достигнутый и проверенный scope (4 из 5 таргетов: собраны, чек-суммированы, структурно/где возможно рантайм-верифицированы; артефакт-манифест реально прогнан через `dist build`/`dist manifest`), evidence-строка прямо называет эти три блокировки и ссылается на эту запись — не выдаётся за полное закрытие карточкиного «Результата» дословно (`win32 x64` из «Результата» не достигнут). | Не корректирующая задача — продуктовый/инфраструктурный вопрос, требующий решения владельца (сертификаты, разрешение на push, и/или выделенный CI-раннер), а не инженерного выбора. **Владелец решил (G17, 2026-08-05): запросить push в `origin` и реальный tagged-release прогон**, чтобы закрыть пп. (2)/(3) окончательно, оставив (1) явно принятой границей v0 (unsigned artifacts). | resolved | `PROGRESS.md`'s строка T17-03 (тот же коммит) + G17 evidence (2026-08-05). **(2) реальный прогон сгенерированной CI-матрицы — resolved**: `git push -u origin master` + `git tag 0.0.0` + `git push origin 0.0.0` реально запустили `.github/workflows/release.yml` на настоящих `windows-latest`/`macos-latest`/`ubuntu-latest` GitHub-раннерах (не заглушка, не dry-run) — 4 прогона `Release`/`ci` за сессию, полные логи получены через `gh run view`/`gh api .../logs`. **(3) `win32-x64` build/smoke — частично resolved, частично передано**: исходная причина блокировки (`cargo-zigbuild` не поддерживает Windows) стала неактуальной — реальный `windows-latest`-раннер компилирует нативно, `cargo-zigbuild` не участвует вовсе. Реальная попытка сборки нашла и закрыла **D-033** (Unix-only IPC/entropy — `local-rag-hook`/`local-rag-proxy`/`local-rag-core` теперь чисто компилируются под `x86_64-pc-windows-msvc`, подтверждено `cargo check --target` локально) и как побочный эффект **D-034** (transient `consolidation_runner`-тест race, впервые проявившийся на реальном раннере). Полный `local-rag.exe` (демон) по-прежнему не линкуется на win32-x64 — новая, более глубокая, отдельно диагностированная причина (`llama-cpp-sys-2` CRT-символы не резолвятся при линковке), зафиксирована и передана как **D-035** (`blocked`, владелец решил не чинить в этой сессии). Linux x64/arm64 real-exec (не build-only) остаётся недостижимым без Docker/QEMU — не переоткрыто, тот же корень, что и раньше, не блокирует. **(1) код-signing — по-прежнему явно отсутствует, принятая граница v0** (unsigned artifacts на всех платформах; сертификаты не предоставлены и не запрошены в этой сессии) — не скрыто, не выдано за решённое. **Финальное обновление (третий реальный CI-прогон, тег `0.0.0`, run 31006707246, после фиксов D-033 round 2): «`local-rag.exe` по-прежнему не линкуется» выше — устарело, исправлено в D-035's собственной записи.** `local-rag.exe`/`local-rag-hook.exe`/`local-rag-proxy.exe` реально скомпилировались, слинковались и упакованы на win32-x64 (`build-local-artifacts (x86_64-pc-windows-msvc)` = `success`), весь релизный pipeline завершился (`build-global-artifacts`/`host`/`announce` = `success`), реальный публичный GitHub Release `0.0.0` создан со всеми 15 checksummed-артефактами по 5 таргетам (`https://github.com/13W/memory/releases/tag/0.0.0`). Тот же прогон + повторные `ci/master`/`ci/0.0.0` (полный `cargo xtask ci`, 18/18 job) — оба зелёные, включая ранее флейкавший `consolidation_runner`-тест (D-034), что эмпирически подтверждает фикс. **D-029 закрыт полностью**: (1) явно принятая граница v0 (сертификаты), (2) и (3) — реально подтверждены сквозным зелёным прогоном всех пяти таргетов на настоящих GitHub-раннерах. |

| D-030 | T17-04 (при планировании, до написания кода) | Spec 11 §4 `[FIXED concern, mechanism [SPEC]]`: «a newer hook binary writing a newer format than the running daemon supports is a reportable incompatibility, not silent loss» | `crates/local-rag/src/daemon/lifecycle.rs::spawn_spool_resume` вызывал `resume_spool_import(...).await;` без `let`-привязки — `Vec<(String, Result<ImportOutcome, ImportError>)>`, включая `ImportOutcome.stalled_on` (T13-04, `crates/store/src/observation/import.rs`), полностью отбрасывался. Сигнал вычислялся корректно на каждом startup resume pass, но не долетал никуда: ни строки в лог, ни поля в `doctor`, ни любого иного видимого оператору следа — ровно «silent loss», прямо запрещённый уже существующим `[FIXED concern]`. Найдено при планировании T17-04 (карточка требует тест «incompatible new hook warning»), не входит в уже названный scope самой карточки — отдельная находка. | D-030: (a) `spawn_spool_resume` теперь обрабатывает результат каждой сессии — `stalled_on`/`Err` выводятся в stderr демона немедленно (`eprintln!`, тот же паттерн, что уже использует `main.rs`); (b) новая read-only `local_rag_store::diagnose_spool_tail(read, layout, session_id)` (`crates/store/src/observation/import.rs`) переиспользует общий decode-walk (`decode_pending_tail`, выделен из `import_session_tail` рефакторингом без изменения поведения) — не импортирует ничего и не двигает курсор, поэтому её можно звать в любой момент. Заведена в `local-rag doctor` новой секцией `spool` (`crates/local-rag/src/cli/doctor.rs`: `DoctorReport.spool`, `SpoolFinding`, `SpoolSessionFinding`) — по одной строке на известную spool-сессию (`ok`/`STALLED: <reason>`/`error: <reason>`), участвует в `is_clean()`. Продуктовый инвариант декодирования не ослаблен: `decode_pending_tail` — дословно то же тело цикла, что было в `import_session_tail` раньше. | resolved | коммит T17-04 (та же правка). Продуктовый код: `crates/store/src/observation/import.rs` (+`decode_pending_tail`, +`diagnose_spool_tail`, реэкспорт из `observation/mod.rs` и `lib.rs`), `crates/local-rag/src/daemon/lifecycle.rs` (`spawn_spool_resume`), `crates/local-rag/src/cli/doctor.rs` (`spool` section). Тесты: `cargo test -p local-rag-store --test observation_import` — новый `diagnose_spool_tail_matches_import_session_tail_without_advancing_the_cursor` (15 OK, было 14); `cargo test -p local-rag --test cli_doctor` — новый `spool_stall_is_reported_alongside_a_healthy_session` (18 OK, было 17); `cargo test -p local-rag --test serve_subprocess` — новый `a_stalled_spool_session_is_reported_on_stderr_not_silently_dropped`, реальный демон-процесс, реальный `SIGTERM`, реальный stderr (2 OK, было 1). `[SPEC]`-амендмент: 11 §4 (as-built D-030/T17-04) |

| D-031 | T17-04 (при финальном `cargo xtask ci`, полный прогон 18 job) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD) | `crates/local-rag-hook/tests/recall_rpc.rs::unreachable_daemon_prints_nothing`/`::timeout_daemon_that_accepts_but_never_responds_prints_nothing` (T15-06, группа 15, gated `PASS`) падают только под полной параллельной нагрузкой `cargo xtask ci` (18 конкурентных job): первый ассертит `elapsed < 1s`, второй — `elapsed < 2s`, вокруг **реального** сабпроцесса (`local-rag-hook`, реальный `fork`/`exec`). Оба бюджета — не бюджет самого recall RPC (`RECALL_BUDGET = 300ms`, `crates/local-rag-hook/src/recall.rs:88`; для `unreachable_daemon` соединение вообще падает мгновенно, `ECONNREFUSED`, бюджет не расходуется), а произвольные абсолютные wall-clock окна вокруг spawn/scheduling реального процесса. Под пиковой конкуренцией планировщика (18 тест-бинарников одновременно) фактический elapsed — `2.46s`/`2.77s`, что уже превышает оба порога. Ни T17-04, ни любая другая задача в этой сессии не меняла `local-rag-hook`; изолированный прогон (`cargo test -p local-rag-hook --test recall_rpc`, без конкуренции) — 9/9 OK. Замечено, что оба порога (1s/2s) существенно уже, чем уже принятая в этой же кодовой базе норма для аналогичных real-subprocess тестов (`crates/local-rag/tests/serve_subprocess.rs`/`crates/local-rag-proxy/tests/subprocess.rs` дают `Duration::from_secs(20)` и больше на реальный daemon spawn именно ради устойчивости к нагрузке планировщика) — тот же класс находки, что D-001/D-003/D-005/D-009 (тест с недостаточным запасом относительно легитимной wall-clock-вариации под пиковой параллельной нагрузкой). | D-031: расширить верхние границы обоих тестов до `Duration::from_secs(10)` (было 1s/2s) — та же по порядку величина, что уже даёт остальной набор real-subprocess тестов. Нижняя граница второго теста (`>= 200ms`) не менялась — она и доказывает «дождался бюджета, а не мгновенный отказ». Инвариант не ослаблен: реальный баг (например, регрессия, возвращающая безусловное ожидание/retry-цикл на много секунд) по-прежнему ловится с большим запасом; изменена только устойчивость к легитимной OS-scheduling-вариации под пиковой нагрузкой, не то, что тест проверяет по существу. | resolved | коммит T17-04 (та же правка). `cargo test -p local-rag-hook --test recall_rpc` — 9 OK (без изменений в количестве/семантике тестов, только два порога); повторный полный `cargo xtask ci` — все 18 job зелёные |

| D-032 | T17-05 (при написании `release_report::resources`/`latency`'s собственных real-model тестов) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD) | `crates/xtask/src/bench/run.rs::tempdir` (T12-05, группа 12, gated `PASS`) namespace'ит временный стор только по `std::process::id()` — безопасно ровно пока `cargo xtask bench` вызывается как единственный процесс-на-запуск (как оно и было единственным вызывающим до этой задачи). T17-05 впервые вызвал вынесенный из этого же файла `build_indexed_store` из двух независимых тестов (`release_report::resources`/`::latency`), которые `cargo test` реально гоняет параллельными потоками **одного** процесса — оба потока получили один и тот же `std::process::id()`, то есть один и тот же путь временного стора. Реально воспроизведено дважды подряд: `UNIQUE constraint failed: repository.repo_id` (два конкурентных `create_repository` в то, что стало одной and той же `state.sqlite`) и, отдельно, `projection switch: no vector for occurrence …` (одна попытка `switch()` увидела вектора/генерацию другого теста). Дефект — в переиспользуемой `tempdir()`, не в новом `build_indexed_store`/`resources`/`latency` коде как таковом. | D-032: `tempdir()` теперь уникален **на вызов**, не только на процесс — статический `AtomicU64`-счётчик, добавленный к pid в имя каталога (`local-rag-bench-<pid>-<call>`). Поведение `cargo xtask bench`'s единственного реального вызывающего не меняется (первый вызов в процессе — `call == 0`, та же семантика «убрать протухший каталог от прежнего запуска под тем же pid»); инвариант не ослаблен, только устранена гонка при нескольких вызовах в одном процессе. | resolved | коммит T17-05 (та же правка). `cargo test -p xtask -- --nocapture release_report::` (реальные веса, `ORT_DYLIB_PATH` этого хоста) — 6/6 OK одновременно (было 4/6, 2 реальных падения на конкурентной гонке); стабильно на повторном прогоне |
| D-033 | G17 (при реальном прогоне релизной CI-матрицы, запущенном для резолюции D-029) | Spec 13 §1 `[FIXED]`: пять таргетов, включая `win32 x64`, в обязательном v0-скоупе (win32-arm64 — единственный явно deferred per CLAUDE.md architecture guardrails) | Реальный `windows-latest`-раннер (первый когда-либо реальный прогон, D-029) нашёл, что `win32-x64` **не компилируется вообще** — не инструментальная проблема (`cargo-zigbuild` не поддерживает Windows, уже известно и зафиксировано D-029), а настоящая ошибка компиляции кода: `crates/local-rag-hook/src/recall.rs:72` безусловно импортирует `std::os::unix::net::UnixStream` (`error[E0433]: cannot find unix in os`). Разведка вглубь (после локального `cargo check --target x86_64-pc-windows-msvc`, где найдены доступны все 5 таргетов rustup) показала, что это симптом куда более широкого пробела: весь IPC-транспорт демона безусловно завязан на Unix-only типы без `#[cfg(unix)]` — `crates/local-rag/src/daemon/lifecycle.rs` (`tokio::net::UnixListener::bind`, шаг 4 старта демона), `crates/local-rag/src/daemon/handshake.rs` (`tokio::net::{UnixListener,UnixStream}`, `serve_connections`/`handle_connection`), `crates/local-rag/src/daemon/shutdown.rs`'s `ShutdownSignal` (уже был `#[cfg(unix)]`, но `lifecycle::run`/`wait_for_shutdown_trigger` — нет), `crates/local-rag-proxy/src/connect.rs` (`tokio::net::UnixStream`, `connect_or_spawn`), `crates/local-rag-proxy/src/handshake.rs` (`tokio::net::unix::{OwnedReadHalf,OwnedWriteHalf}`, `EstablishedSession`/`establish_session`), `crates/local-rag-proxy/src/relay.rs` (`relay()` принимает `ShutdownSignal` напрямую), `crates/local-rag-proxy/src/main.rs` (`run()`). Отдельная, независимая находка того же прогона: `crates/core/src/identity/uuidv7.rs`'s `SystemUuidV7` (durable-ID entropy source, `/dev/urandom`) была `#[cfg(unix)]`-only с явным doc-комментарием «Unix-only for now» — но используется безусловно в `local-rag/src/{main.rs, cli/init.rs, cli/index.rs, cli/rebuild.rs}`, то есть ломала бы компиляцию `local-rag` на Windows независимо от IPC-пробела, и (в отличие от демона) эти CLI-команды по дизайну T15-07 не используют демон/сокет вообще — обязаны реально работать на Windows, не просто «не падать при компиляции». `daemon::probe`/`cli::service`/`cli::status` уже показывали правильный паттерн (`#[cfg(unix)] { ... } #[cfg(not(unix))] { ... }` / `#[cfg(windows)] pub use ...`) — пробел был в его неполном применении, не в отсутствии прецедента. | D-033 (владелец-решение — минимальный фикс сейчас, полная Windows named-pipe IPC реализация — отдельный будущий X-NNN, не часть этой правки): IPC-транспорт (демон/hook/proxy) `#[cfg(unix)]`-гейтирован по образцу уже существующего в кодовой базе паттерна (`daemon::probe`); Windows-ветки — типизированный немедленный отказ **до** любого побочного эффекта, переиспользуя уже существующие типы ошибок (`DaemonStartupError::Bind`/`ProxyError`), без изобретения новых вариантов; чистые/протокольные хелперы (`local-rag-hook::recall`'s JSON/framing-функции, `local-rag-proxy::transport`/`relay::relay`'s generic-over-`AsyncBufRead+AsyncWrite` тело, `daemon::handshake`'s `RequestHandler` trait/`HandshakeContext`/`read_hello`/`read_bounded_line`/`write_message`) остаются платформонезависимыми — гейтировано только то, что буквально типизировано на `Unix{Listener,Stream}`. `SystemUuidV7` — **не** заглушка (трейт `UuidSource::next_uuid` infallible, эти ID реально нужны на Windows для `init`/`index`/`rebuild`): реализован настоящий `#[cfg(windows)]` источник энтропии через прямой FFI на `BCryptGenRandom` (`bcrypt.dll`, `BCRYPT_USE_SYSTEM_PREFERRED_RNG`, ноль новых crate-зависимостей) — Windows-аналог `/dev/urandom`, не деградация. Проверено локально: `cargo check --target x86_64-pc-windows-msvc -p local-rag-core -p local-rag-hook -p local-rag-proxy` — чисто, 0 ошибок (только ожидаемые `unused`/`unsafe_code`-warnings на мёртвом Windows-arm коде и FFI); `-p local-rag` **не может** быть локально проверен тем же способом — `libsqlite3-sys`/`ring`/`zstd-sys`'s C build.rs требует настоящих Windows SDK заголовков, которых на этой (macOS) машине нет, тот же класс ограничения, что и D-029's cargo-zigbuild — Rust-уровневые правки (`daemon/{lifecycle,handshake,mod}.rs`) верифицированы построчным ручным review по тому же паттерну, доказанному компилятором в `core`/`hook`/`proxy`, и повторным реальным CI-прогоном (см. D-029 evidence). Полный локальный `cargo test --workspace` (native, Unix) — регрессии проверены до коммита. | resolved | коммит G17 (та же правка). Правки: `crates/local-rag-hook/src/recall.rs`, `crates/local-rag-proxy/src/{connect,handshake,relay,main}.rs`, `crates/local-rag/src/daemon/{lifecycle,handshake,mod}.rs`, `crates/core/src/identity/{uuidv7,mod}.rs`. `cargo check --target x86_64-pc-windows-msvc -p local-rag-core -p local-rag-hook -p local-rag-proxy` — 0 ошибок; `cargo test --workspace` (native) — без регрессии (см. G17 evidence-строку); повторный реальный tagged-release прогон на `windows-latest` — см. D-029 evidence. Полная функциональная Windows named-pipe IPC (owner-only ACL, spec 12 §6) остаётся будущим X-NNN, не решается здесь — `local-rag serve`/`local-rag-proxy` на Windows сегодня компилируются и сразу типизированно отказывают, не более. **Второй раунд (тот же коммит, найдено повторным реальным release-прогоном после первого раунда правок):** первый проход пропустил два места. (1) `crates/local-rag/src/daemon/lifecycle.rs`'s `use super::probe::SocketLivenessProbe;` не был гейтирован (`error[E0432]: unresolved import`, реальный `windows-latest` лог) — исправлено `#[cfg(unix)]` по тому же паттерну. (2) `crates/local-rag/src/cli/watch.rs` безусловно использует `local_rag::daemon::ShutdownSignal` — но это отдельная, не завязанная на демон-IPC команда (`local-rag watch`, T15-07), которая обязана реально работать на Windows, не просто компилироваться (тот же принцип, что уже применён к `SystemUuidV7`, не тот же принцип, что к самому IPC-транспорту). Вместо заглушки — реальная `#[cfg(windows)]`-реализация `ShutdownSignal` через `tokio::signal::ctrl_c()` (кроссплатформенный в самом tokio, оборачивает `SetConsoleCtrlHandler` на Windows) — Unix-версия (SIGTERM+Ctrl-C) не тронута, `ShutdownSignal` теперь безусловный экспорт в `daemon/mod.rs`. Оба фикса верифицированы локально: `cargo check --target x86_64-pc-windows-msvc -p local-rag-core -p local-rag-hook -p local-rag-proxy` по-прежнему 0 ошибок; `local-rag` сам — тот же class ограничения (нет Windows SDK локально), ждёт третьего реального CI-прогона. |
| D-034 | G17 (реальный `ci`-прогон, тот же win32-x64-валидационный push, что нашёл D-033) | «Тесты детерминированы: без сети, wall-clock race…» (CLAUDE.md; README DoD) | Первый когда-либо реальный прогон полного `cargo xtask ci` на настоящем GitHub Actions `ubuntu-latest` (этот репозиторий никогда раньше не пушился) нашёл, что `crates/store/tests/consolidation_runner.rs::lease_renews_on_cadence_while_the_generator_runs` (T14-06, группа 14, gated `PASS`) падает: `lease_until must have moved forward by ~45s of virtual time, got Some(120000)` (т.е. лизинг не продлился к отметке virtual-time 45s, хотя цикл продления — каждые 30s). Тест уже документированно использует `#[tokio::test(start_paused = true)]` (paused virtual time, auto-advance при простое) специально для детерминизма. Убедительно подтверждено как **transient CI-раннер-специфичный флейк, не детерминированный баг продукта**: один и тот же коммит дал РАЗНЫЕ результаты в параллельных прогонах `ci.yml` (запущенных одновременно push-ом одного и того же коммита на `tag 0.0.0` и на `master`) — 4 прогона подряд на неизменном коде: success/fail/success/fail. Корень (вероятный, основан на документированном поведении tokio `time::pause`): `run_once`'s продление лизинга (`crates/store/src/memory/runner.rs:640-646`) уходит через `StateWriter`'s выделенный **реальный** OS-поток (canonical, `daemon::shutdown`'s собственный doc: «writer thread stays detached», никогда не инлайнится на async-задаче) — это НЕ чистое ожидание таймера, а cross-thread channel round-trip. Auto-advance-эвристика paused-clock отслеживает только зарегистрированные таймеры и планировщик, а не то, что задача блокирована именно на внешнем (не-таймерном) канале — на быстрой/незагруженной локальной машине reply приходит раньше, чем executor вообще успевает счесть себя "простаивающим", поэтому гонка никогда не проявлялась локально (0/20 изолированных + 0/15 под искусственной CPU-нагрузкой прогонов); на contended 2-vCPU CI-раннере под параллельной nextest-нагрузкой auto-advance может продвинуть virtual-clock probe'а (следующий зарегистрированный таймер, 45s) раньше, чем реальный ответ writer-потока физически долетел, — ровно наблюдаемый симптом (`got Some(120000)` = дозаписи ещё не было видно). Локально не воспроизведено ни разу целенаправленно (35 попыток), но 2 независимых реальных CI-прогона на идентичном коде дают прямое эмпирическое подтверждение недетерминизма именно здесь (не где-то ещё в 1983-тестовом наборе). Продуктовый код (`run_once`'s фактическая cadence продления) корректен — расхождение только в тесте, который делает одноразовую точечную проверку вместо устойчивой к этой конкретной cross-thread-race конструкции. | D-034: `probe_fut` в `lease_renews_on_cadence_while_the_generator_runs` переписан с одноразового `sleep(45_000)` + point-in-time assert на bounded poll-цикл (`sleep(5_000)` шагами, до 160_000ms virtual-time — с запасом за пределы 150s generator-sleep, чтобы truly-never-renewed лизинг всё ещё падал, а не тихо проходил). Каждая итерация даёт auto-advance ещё один шанс: и продвигает virtual-clock мимо 30s-дедлайна продления, и — что важно для races — позволяет пройти дополнительному РЕАЛЬНОМУ времени между итерациями для завершения ещё не долетевшего cross-thread write, тем же классом фикса, что D-003 уже применял к транзиентному `SQLITE_BUSY` в read-хелперах (не глотать гонку молча, дать её пережить устойчивым ретраем, не ослабляя итоговый assert). Ассерт не ослаблен: по-прежнему требует `lease_until > original_lease_until`, по-прежнему паникует громко при истинном отсутствии продления. | resolved | коммит G17 (та же правка). `crates/store/tests/consolidation_runner.rs::lease_renews_on_cadence_while_the_generator_runs` — `cargo test -p local-rag-store --test consolidation_runner lease_renews_on_cadence_while_the_generator_runs -- --exact` — `ok` локально (0.01–0.02s, virtual-time путь не изменился в быстром случае); полный `cargo test -p local-rag-store --test consolidation_runner` — без регрессии остальных тестов файла. Реальное подтверждение отсутствия повторного флейка на CI — см. G17 evidence-строку (повторный tagged-release прогон). |
| D-035 | G17 (реальный релизный CI-прогон на `windows-latest`, после фикса D-033) | Spec 13 §1 `[FIXED]`: `win32 x64` в обязательном v0-скоупе; spec 10 §1 `[FIXED]`: генерация работает in-process через `llama.cpp` (ADR-0006) | После фикса D-033 (IPC-транспорт компилируется) реальный `windows-latest`-раннер упёрся в **линковку**, не компиляцию: `libllama_cpp_sys_2-*.rlib` даёт 19 unresolved external symbols при линковке (`__imp_tolower`, `__imp_strncpy`, `__imp__wfopen`, `__imp_clock`, `__imp__aligned_malloc`/`_free`, `__imp_strerror`, `__imp_fread`, `__imp__fseeki64`/`_ftelli64`, `__imp___stdio_common_vsscanf`, `__imp_toupper`, `__imp_ldexp`, `__imp_lroundf`, `__imp_expm1f`/`fmaxf`/`fminf`/`truncf`/`erff` — все стандартные MSVC CRT/math-функции), `link.exe` завершается с exit code 1120 (`LNK1120: 19 unresolved externals`). Симптом воспроизведён дважды в двух реальных прогонах: сначала на `xtask.exe` (та же C++-зависимость через `local-rag-generate`, xtask'а собственный `bench`/`memory-bench`), затем структурно ожидаемо ударит и по `local-rag.exe` самому (тот же `llama-cpp-sys-2` — транзитивная зависимость через `local-rag-generate`, реальный in-process генератор демона, ADR-0006), поскольку `local-rag`'s компиляция ещё не дошла до стадии линковки к моменту находки (упала раньше на отдельной Rust-ошибке, уже исправленной). Корень — конфигурация CMake-сборки `llama-cpp-sys-2` (vendored C/C++ через `cmake`+`bindgen`, ADR-0006's собственный disclosed cost) под конкретно этим MSVC/`cargo-dist`-окружением на `windows-latest`: похоже на несовпадение `/MD` vs `/MT` рантайм-линковки либо отсутствие стандартных импорт-библиотек (`ucrt.lib`/`legacy_stdio_definitions.lib`/`msvcrt.lib`) в финальной линк-команде — диагностика не завершена, корень не подтверждён однозначно. Это не Rust-уровневый баг и не cfg-пробел (в отличие от D-033) — типичная для vendored-native-C++-crate кросс-платформенная сборочная проблема, требующая итеративной отладки против реального Windows-toolchain/линкера, которого на этой (macOS) машине нет — тот же класс ограничения, что и у D-029/D-033, но на уровень глубже (не `rustc`, а `link.exe`/CMake-генерируемые объектники). | D-035 (владелец-решение — задокументировать как известный пробел, не чинить вслепую): диагностика и фикс линковки `llama-cpp-sys-2` на win32-x64 — отдельная будущая задача с неопределённым числом итераций через реальный CI (~10–15 минут на итерацию, без гарантии быстрого решения; нужен либо реальный доступ к Windows-toolchain для локальной итерации, либо явный CMake/MSVC-эксперт-ревью `llama-cpp-sys-2`'s build.rs). `local-rag-hook`/`local-rag-proxy` (не зависят от `local-rag-generate`) этой проблемы не имеют — их фикс (D-033) остаётся полным и достаточным. `local-rag` (демон, зависит от `local-rag-generate`/`llama-cpp-sys-2`) сегодня по-прежнему не может быть полностью собран под win32-x64 — тем же по духу образом, что D-029's сертификаты подписи: известная, названная, не скрытая граница. | resolved (пересмотрено — см. «Обновление» в конце этой ячейки) | коммит G17 (та же правка, evidence зафиксирован без code fix — решение владельца не чинить сейчас). `crates/xtask` (реальный прогон, `windows-latest`) — `LNK1120`, 19 unresolved externals, ссылки в `libllama_cpp_sys_2-*.rlib` объектниках (`llama-vocab.obj`, `ggml-backend-reg.obj`, `llama.cpp.obj`, `ggml.c.obj` ×3, `llama-mmap.obj`, `gguf.obj` ×3, `llama-model-loader.obj`, `llama-chat.obj`, `ggml-quants.obj` ×2, `quants.c.obj`, `unary-ops.obj` ×3, `ops.obj` ×2) — полный лог сохранён в evidence вложении задачи. Разблокировка требует одного из: (a) владелец/исполнитель с доступом к реальной Windows-машине итеративно чинит линковку `llama-cpp-sys-2` под MSVC; (b) явное продуктовое решение — `local-rag`'s win32-x64 remains CI-build-only с задокументированным линк-разрывом до отдельного фикса, не блокирует остальной v0-релиз (тот же прецедент, что D-029 для code-signing). **Обновление (тот же коммит после push, реальный третий CI-прогон, тег `0.0.0`, run 31006707246): прогноз «структурно ожидаемо ударит и по local-rag.exe» не подтвердился.** Тот же прогон, тот же `link.exe`, тот же `libllama_cpp_sys_2-*.rlib`: `xtask.exe` **снова** `LNK1120` (19 unresolved externals, дословно те же символы), но `local-rag.exe`/`local-rag-hook.exe`/`local-rag-proxy.exe` **успешно** скомпилировались, слинковались и упакованы — `build-local-artifacts (x86_64-pc-windows-msvc)` = `success`, весь релизный pipeline (`build-global-artifacts`/`host`/`announce`) = `success`, реальный публичный GitHub Release `0.0.0` создан (`https://github.com/13W/memory/releases/tag/0.0.0`, 15 артефактов + checksums по всем 5 таргетам, включая `local-rag-x86_64-pc-windows-msvc.zip`/`local-rag-hook-x86_64-pc-windows-msvc.zip`/`local-rag-proxy-x86_64-pc-windows-msvc.zip`). Точная причина расхождения xtask-vs-product-binaries не диагностирована (не исследовалось дальше — не блокирует), но эмпирика однозначна и дважды воспроизведена (run 31000860816 и run 31006707246, идентичный символ-набор оба раза): **линковка ломается только для `xtask.exe`**, dev-only инструмента, который не входит ни в один npm platform-пакет и не поставляется. **Статус пересмотрен: `blocked` → `resolved`** — v0-релиз (spec 13 §1's пять таргетов, реальные подписываемые артефакты) не заблокирован; правки код не требуется. Отдельно остаётся зафиксированным как известный (не скрытый) минорный гэп: `cargo xtask bench`/`memory-bench` не соберётся на реальной Windows-машине разработчика — вне v0-релиз-скоупа, кандидат на будущий X-NNN только если станет реальной проблемой для контрибьютора. |
| D-036 | post-v0 (найдено при живом MCP-dogfood-тестировании через реальный npm/platform-бинарник `local-rag-proxy` + `local-rag serve`, первый настоящий `remember`→`recall` round-trip против непустого стора после релиза `0.0.0`) | Spec 10 §3 `[SPEC]`: «A model space bundles the representations that must be coherent together (at minimum `code_raw` + `memory` in v0)»; spec 08 §6 `[FIXED pipeline]` / `15-roadmap.md` §2 MVP v0 scope: «recall v0 (FTS + brute-force cosine)» — dense memory recall — это заявленный v0-скоуп, не deferred | Живой `remember` затем `recall` на реальном сторе стабильно возвращает `dense_degraded: "no_representation"` даже после рестарта демона с полностью рабочим embedding-провайдером (`search_code(mode="code")` в это же время работает штатно). Причина подтверждена чтением кода, не догадкой: единственный продуктовый (не тестовый) вызывающий `register_representation` во всём workspace — `crates/local-rag/src/cli/init.rs::register_code_raw_representation`, и он регистрирует исключительно `RepresentationKind::CodeRaw`; `rg` по всем `crates/*/src` подтверждает, что каждый вызов `set_model_space_representation(.., RepresentationKind::Memory, ..)` в репозитории — тест или `xtask bench`, ни одного продуктового. Код сам это признаёт as-built: `crates/local-rag/src/daemon/query_embedder.rs:9-18` — «no `memory`-kind `RepresentationKey` has ever been registered in production… group 14 closed without doing it… closing it is out of this task's own scope». Это прямо противоречит собственной итоговой диспозиции `D-013` в этом же файле — «resolved (обе половины исполнены — memory: T14-08, code_raw: T15-07; закрытие подтверждено при сверке G17)»: тело той же строки `D-013` цитирует T14-08 как закрывающую только subject-функцию («продуктовая регистрация memory… по-прежнему не продуктовый код… закрытие — работа будущей задачи (T15-07 или аналог)»), а evidence T15-07 в `PROGRESS.md` независимо повторяет тот же отказ («closing this half is a future task's job», «work of a future task owning group 14's model-axis registration») — работа так никем и не подобрана, и гейт `G17`, подтвердивший закрытие `D-013`, это противоречие не поймал. Итог для реального пользователя: `remember`/`recall`'s dense-плечо навсегда `no_representation` в любом настоящем запуске v0.0.0, вне зависимости от числа накопленных записей памяти или времени работы демона — это не временная деградация, а тупик, поскольку ключ регистрации никем никогда не создаётся. | D-036 (реализовать — фундамент полностью готов, недостаёт только продуктовой проводки, тот же класс, что уже closed D-007/D-011/D-021/D-024): **Результат** — `local-rag init [--download-models]` регистрирует `RepresentationKind::Memory` для `DEFAULT_MODEL_SPACE_ID` тем же идемпотентным disk-state-gated паттерном, что уже `register_code_raw_representation`, и демон реально подключает `local_rag_memory::recall::dense::QueryEmbedder` (сегодня всегда `UnavailableEmbedder`, `crates/local-rag/src/daemon/query_embedder.rs`'s собственный doc это подтверждает) — симметрично уже существующему `EmbedderQueryAdapter` для `local_rag_search::QueryEmbedder`. **В scope**: (a) регистрация memory-представления в `cli/init.rs` (или общий с `code_raw` хелпер), (b) реальный `MemoryDenseBackend`/`QueryEmbedder` в daemon-composition (`MemoryContext`), (c) regression-тест — реальный `remember`→`recall` round-trip с рабочим эмбеддером возвращает dense-хит, не `no_representation`, (d) аннотация (не переписывание) диспозиции `D-013` — явная перекрёстная ссылка на переоткрытие memory-половины. **Не в scope**: ANN-бэкенд для memory, что-либо в `code_raw`-пути (уже работает), reranker. **Тесты**: unit-тест регистрации по образцу уже существующих `cli/init.rs::tests::{after_registration_the_default_space_requires_code_raw,...}`, integration-тест на демон-уровне для реального dense recall hit. **Приёмка**: `local-rag init --download-models` печатает регистрацию обоих представлений; MCP `recall` на реальном сторе с совпадающей по смыслу записью возвращает dense-хит (не `no_representation`) для непустого запроса. | resolved | коммит `D-036: продуктовая регистрация memory-representation` (та же правка); строка `D-036` в `PROGRESS.md`'s «Task evidence» |
| D-037 | post-v0 (тот же живой dogfood-сеанс, что D-036) — MCP-сервер и демон были живьём подключены/автозапущены ДО `local-rag init --download-models`; после установки модели `search_code(mode="code")` продолжал отказывать `INDEX_UNAVAILABLE`/`no embedding provider is configured`, пока демон не был вручную перезапущен | Буквального нарушенного `[FIXED]`/`[SPEC]`-текста нет — зафиксировано как продуктовый gap, не conformance-дефект: спецификация нигде не обещает и нигде явно не отказывается от hot-reload эмбеддинг-провайдера после старта демона. Но 11 §2's `initialize`'s server `instructions` (`crates/local-rag/src/daemon/mcp/instructions.rs`) безусловно рекомендуют вызывающему «hybrid (default)… use it unless you have a reason not to», не оговаривая случай «демон стартовал раньше, чем модель была установлена» — тот самый случай, где эта рекомендация ведёт к молчаливо более слабому lexical-only результату без явного предупреждения вызывающему до первого реального запроса. | Живьём подтверждено чтением кода: `crates/local-rag/src/main.rs::build_query_embedder` (строки 187-216) читает `.ok`-маркер модели и открывает `OnnxEmbedder` **один раз** при `run_serve`, результат — единственный `Arc<dyn QueryEmbedder>`, вживлённый в `SearchEngine`/`MemoryContext` на весь lifetime процесса; ни файлового watcher'а на `models/<id>/.ok`, ни периодической переоценки, ни какого-либо канала «представление появилось — пересобери провайдера» нигде в `daemon/` нет. Единственный способ подхватить модель, установленную после старта демона — полный `local-rag restart` (сам по себе создающий D-038). Живой прогон в этой сессии: демон поднят автоспавном proxy в 09:32, `local-rag init --download-models` завершился в 09:42, `search_code(mode="code")` всё ещё возвращал `INDEX_UNAVAILABLE` в 10:03 — только `local-rag restart` исправил. | D-037 (владелец-решение нужно перед реализацией — не однозначный conformance-фикс): кандидат-подход — демон периодически (тот же класс, что уже существующий `CONSOLIDATION_POLL_INTERVAL`-паттерн, `crates/local-rag/src/main.rs:172`) или лениво на следующий `search_code`/`recall`-запрос перепроверяет `.ok`-маркер/представление и, если провайдер сейчас `UnavailableEmbedder`, а модель уже установлена, пересобирает `QueryEmbedder` без полного рестарта процесса (тот же паттерн, что T15-07's уже существующий `build_query_embedder`, но вызываемый снова, не один раз при `run_serve`). Альтернатива, если владелец решит не чинить сейчас: явно задокументировать в CLI-выводе `local-rag init --download-models` («changes take effect after `local-rag restart`») — сегодня этого предупреждения нет, значит текущее поведение тихо удивляет оператора. **Не в scope**: любая другая причина рестарта демона (upgrade-flow уже покрыт). **Тесты** (если выбран hot-reload-путь): integration-тест — демон стартует без модели → `search_code` lexical_only → модель устанавливается на диск живьём (без рестарта процесса) → следующий (или N-й) запрос переходит на dense/hybrid без падения соединения. | resolved | коммит `D-037: lazy query-embedder reload (no restart after late model install)` (та же правка). Владелец выбрал ленивую пересборку по требованию вместо background-poll таймера: `daemon::query_embedder::LazyQueryEmbedder<T>` — трёхзначный `ProviderProbe<T>` (`NotInstalled`/`Unusable`/`Ready(Arc<T>)`) за `std::sync::RwLock` (тот же примитив, что уже используется в крейте — `arc_swap`/`parking_lot` не добавлялись); `.ok`-маркер перепроверяется только пока состояние `NotInstalled`, только на dense-пути (`mode="lexical"` стоимости не несёт), double-checked locking исключает повторные пробы. Продуктовые конструкторы `code_query_embedder`/`memory_query_embedder` (`daemon/query_embedder.rs`) заменили одноразовые `main.rs::build_query_embedder`/`build_memory_query_embedder`; `instructions.rs` не тронут — рекомендация становится верной без оговорок. Новый e2e `crates/local-rag/tests/embedder_late_install.rs` (2 теста, реальный демон + реальный MCP/UDS wire protocol, фикстурный провайдер вместо ONNX): `search_code` — `lexical_only` → маркер появляется → `degraded: null` с непустыми результатами, без рестарта; `recall` — `dense_degraded: "embed_failed…"` → маркер → `dense_degraded: null`. Плюс 4 unit-теста в `query_embedder.rs` (поздний провайдер подхватывается на обеих сторонах; открытый провайдер не перепробуется; неоткрывшийся не переоткрывается; пустой стор деградирует, не паникует). `cargo fmt --all -- --check` OK; `cargo clippy -p local-rag --all-targets -- -D warnings` OK; `cargo test -p local-rag` — все бинари зелёные (0 failed); `cargo test -p local-rag --test embedder_late_install` 2/2 OK отдельно проверено. `docs/specification/10-models-and-embeddings.md` — as-built `[SPEC]`-заметка D-037. |
| D-038 | post-v0 (тот же живой dogfood-сеанс, что D-036/D-037) — `local-rag restart`, выполненный для применения D-037's обходного пути, оборвал уже установленное MCP-соединение этой сессии; инструменты `mcp__memory__*` пропали из сессии до ручного `/mcp` | Буквального нарушенного `[FIXED]`/`[SPEC]`-текста нет — spec 02 §4.2's upgrade-flow (`ShutdownRequest`→`wait_for_close`→`connect_or_spawn`, `MAX_UPGRADE_ROUNDS=2`) описывает узкий кейс: **новый** proxy при хендшейке обнаруживает несовпадение версии демона и сам инициирует переключение до того, как `relay()` вообще начался. Нигде не заявлено (и явно не отвергнуто) требование «уже установленная, работающая MCP-сессия переживает независимо инициированный рестарт демона (`local-rag stop`/`restart`, crash, OOM-kill) без вмешательства пользователя» — зафиксировано как продуктовый gap. | Подтверждено чтением кода и живым воспроизведением: `crates/local-rag/src/cli/service.rs::stop_running_daemon` (строки 88-104) шлёт голый `SIGTERM` по pid из `store.lock`, в обход HELLO/WELCOME/`ShutdownRequest` протокола целиком — уже подключённые proxy-процессы не получают никакого уведомления. `crates/local-rag-proxy/src/relay.rs::relay` — одноразовый `tokio::select!`-проброс stdin↔UDS без какой-либо reconnect-логики; когда демон исчезает, `relay()` просто возвращает управление, и `main.rs::run()`/`run_proxy()` завершает процесс (`std::process::exit`). Upgrade-retry-цикл (`handshake::establish_session`, `MAX_UPGRADE_ROUNDS=2`) целиком живёт **до** `relay()` и никогда не вызывается повторно после того, как relay уже начался — реконнект для уже-в-разгаре сессии архитектурно недостижим сегодняшним кодом, не просто нереализован. Живое наблюдение этой сессии: после `local-rag restart` все 17 `mcp__memory__*` tool-вызовов начали падать `No such tool available` до выполнения пользователем `/mcp`. | D-038 (владелец-решение нужно перед реализацией — затрагивает proxy↔daemon протокол, не изолированный баг): кандидат-подход — обобщить уже существующий upgrade-retry-паттерн на «relay зафиксировал EOF» как ещё один триггер (наравне с версионным mismatch на хендшейке), т.е. `relay()` при обнаружении закрытия UDS-соединения (не по собственному shutdown-сигналу прокси) переходит в тот же `connect_or_spawn`+повторный HELLO/WELCOME путь вместо немедленного `exit`, и восстанавливает `RequestContext` (тот же `session_id`/`worktree_root`, зафиksированные при первом запуске) на новом соединении — по духу той же гарантии, что уже даёт upgrade-flow, обобщённой на любой источник обрыва, не только версионный. Требует продуктового решения о поведении во время реконнект-окна (буферизовать ли входящий MCP-запрос от Claude Code, или отвечать транспортной ошибкой, которую сам MCP-клиент уже умеет ретраить) — не техническая деталь, а протокольное расширение spec 02 §4.2. **Не в scope**: сохранение in-flight запроса, начатого до обрыва (proxy не хранит состояния — 11 §1's «holds no project state» — реконнект новой сессии, не resume прежней). **Тесты** (после решения владельца): subprocess-тест по образцу уже существующих `local-rag-proxy/tests/subprocess.rs` — реальный демон убит `SIGTERM` в разгаре `relay()`, `local-rag-proxy` реконнектится к свежеспавненному демону без завершения процесса, следующий MCP tool-call проходит без вмешательства клиента. | resolved | коммит `D-038: proxy reconnects on daemon EOF, no restart-time session loss` (та же правка). Владелец выбрал reconnect-on-EOF без буферизации in-flight запроса (альтернатива из карточки D-038). `relay::relay` разделён на внешний reconnect-цикл и внутренний `relay_connection` (одно соединение); триггеры «демон исчез» — EOF от UDS, ошибка чтения (кроме `InvalidData`, framing-нарушение самим демоном — реконнект его повторил бы) и неудачная запись запроса (EPIPE, то же событие с другой стороны). Реконнект переиспользует дословно `handshake::establish_session` (`connect_or_spawn` + HELLO/WELCOME) с теми же `socket_path`/`daemon_binary`/`SessionParams`, что и первое подключение (`relay::DaemonEndpoint`); бюджет `MAX_RECONNECT_ROUNDS = MAX_UPGRADE_ROUNDS` (2) считает подряд идущие безрезультатные попытки, сбрасывается при первом отвеченном запросе на новой сессии — `ProxyError::ReconnectLoopExceeded`, если демон не поднимается или рвёт соединение сразу. In-flight запрос **не буферизуется**: `PendingRequests` хранит id уже отправленных неотвеченных запросов и на обрыве выдаёт на stdout чистую JSON-RPC транспортную ошибку (`-32000`) вместо вечного ожидания — вне scope карточки, MCP-клиент ретраит сам. Побочная находка при реализации: демон — child-процесс прокси; раньше прокси умирал вместе с ним, теперь живёт дальше — вышедший демон оставался бы зомби, а зомби отвечает на `kill(pid,0)` как живой (spec 02 §4.4's store-lock ownership check) — исправлено `connect.rs::reap_exited_daemons` (`waitpid(-1, WNOHANG)` перед каждым спавном, `libc` уже был объявленной зависимостью крейта, новых зависимостей не добавлено). `stop_running_daemon`/`ShutdownRequest`-протокол не тронуты (вне scope). Новые тесты `tests/subprocess.rs`: `a_live_proxy_reconnects_after_its_daemon_is_restarted_mid_session` (реальный демон убит `SIGTERM` в разгаре `relay()`, прокси сам поднимает замену, следующий `tools/list` проходит по тому же stdin/stdout) и `a_request_in_flight_when_the_daemon_dies_fails_cleanly_and_the_session_continues` (fake-демон рвёт соединение без ответа → `-32000` с тем же `id` в пределах таймаута, затем реконнект и следующий вызов отвечает). Плюс unit-тесты в `relay.rs` (`a_request_in_flight_when_the_daemon_vanishes_gets_a_transport_error_on_stdout`, `an_answered_request_is_no_longer_pending`, `a_notification_is_never_pending_because_it_is_never_answered`). `cargo fmt -p local-rag-proxy --check` OK; `cargo clippy -p local-rag-proxy --all-targets -- -D warnings` OK; `cargo test -p local-rag-proxy` 31/31 OK (21 unit + 2 cli_version + 8 subprocess); `cargo test -p local-rag-proxy --features failpoints --test subprocess -- --test-threads=1` 9/9 OK (вкл. cross-binary upgrade-тест, не регрессировал). |
| D-039 | post-v0 (тот же живой dogfood-сеанс, что D-036/037/038) — живой `search_code(name_pattern="register_repr")` вернул `register_embedder_representation` (не начинается с `register_repr`); решающий тест `name_pattern="repr_register"` (слова переставлены, заведомо невозможный литеральный префикс) дал **побайтово идентичный** набор результатов | Caller-facing контракт `search_code`'s `name_pattern`, дважды: MCP tool schema (`crates/local-rag/src/daemon/mcp/tools.rs:70-72`) — «Keep only units whose local or qualified name **starts with this prefix**»; server `instructions` (`crates/local-rag/src/daemon/mcp/instructions.rs:62`) — «name_pattern filters by **identifier prefix**». Оба буквально описывают single-string prefix-of-whole-identifier семантику. | Реализация (`crates/store/src/cache/fts_query.rs:54-71`, `fts_match_expression_from_terms`) — сознательный и протестированный `[SPEC]`: `name_pattern` токенизируется тем же `tokenize_identifier`, что и индексируемые имена, и превращается в FTS5-выражение `({name qualified_name} : (tok1* AND tok2* AND …))` — AND независимых по позиции/порядку префиксов ПО КАЖДОМУ слову-токену, не префикс всей строки целиком. Golden-тест `multi_token_pattern_ands_every_prefix` (тот же файл) буквально фиксирует это на примере `"extractImp"` → `("extractimp"* AND "extract"* AND "imp"*)`. Сам механизм корректен и работает как спроектирован (подтверждено `register_code`-контрольным тестом — 15/15 совпадений точных) — расхождение не в поведении, а в том, что caller (человек или LLM-агент), читающий только `description`/`instructions` (единственный контракт, видимый вызывающему через MCP-протокол), не может узнать, что порядок и позиция слов внутри `name_pattern` не имеют значения, а частичное слово где-то в середине идентификатора («repr» внутри «representation») пройдёт фильтр — эмпирически подтверждённый источник введения в заблуждение (использовано в этой самой сессии для формирования неверного ожидания). Spec 09 §1's «prefix-tokenized on local_name/qualified_name» ближе к истине, но тоже не проговаривает AND/порядок-независимость явно и не является caller-facing текстом (не видно вызывающему инструмент, только читателю спеки). | D-039 (текстовая правка, без изменения поведения — тот же класс, что уже closed D-021 в части «поле существовало, текст не поспевал»): переписать `tools.rs`'s `name_pattern` schema `description` и `instructions.rs`'s соответствующее предложение так, чтобы явно называть механизм — «matches when the pattern's own words (split the same way identifiers are — snake_case/camelCase/kebab-case) each prefix-match a word somewhere in the name, independent of order» (или короче, но не «starts with»/«identifier prefix» без оговорки). **В scope**: только эти два caller-facing текста + при желании синхронизация формулировки spec 09 §1's одной строки. **Не в scope**: поведение `fts_match_expression_from_terms`/`lexical_leg` — не трогать, уже корректно и протестировано. **Тесты**: не поведенческие — юнит-тест на новый текст не нужен (это строковый литерал, не логика); приёмка — ручная сверка нового текста против golden-тестов `fts_query.rs` (текст должен описывать ровно то, что тесты доказывают). | resolved | коммит `D-039: search_code name_pattern caller-facing text fix` (та же правка). Изменены только caller-facing тексты — `tools.rs` JSON schema `description`, `instructions.rs` абзац `Modes:` — плюс as-built уточнение в `docs/specification/09-search.md` §1 (T12-01 note, «"Prefix-tokenized" is per token, not per string»); `fts_match_expression_from_terms`/`lexical_leg` не тронуты. `cargo fmt --all -- --check` OK; `cargo check -p local-rag --all-targets` OK, без warnings; `cargo test -p local-rag-store --lib fts_query` 22/22 OK (вкл. `multi_token_pattern_ands_every_prefix`); `cargo test -p local-rag --test mcp_contract` 8/8 OK (`initialize_returns_server_identity_and_instructions`/`tools_list_advertises_all_seventeen_v0_tools` подтверждают неизменность контракта форм). |
| D-040 | post-v0 (тот же живой dogfood-сеанс, что D-036–039) — живой `give_feedback` вызов записал `observation_envelope` (`session_id = 019fd635-3a9e-7f2f-a7a7-a5db736389d5`, реальный MCP-session), `spool/`-директория стора пуста (0 сессий, подтверждено `ls`) — прямым SQL-чтением `state.sqlite` подтверждено, что запись существует и не покрыта никаким consolidation run | 11 §2's собственный текст (MCP tool description, `crates/local-rag/src/daemon/mcp/tools.rs`, `give_feedback`): «writes an observation envelope directly… feeds the next consolidation pass» `[SPEC]` — обещание, что каждая записанная через `give_feedback` observation рано или поздно попадёт в consolidation. Spec 07 §6 `[FIXED]`: «a background worker owns all of it» (continuous consolidation triggering) — без оговорки «кроме сессий без spool-активности». | Причина подтверждена чтением кода, не догадкой: `crates/local-rag/src/daemon/consolidation_trigger.rs:142` — единственный продуктовый вызывающий `open_next_run` (т.е. единственный, кто когда-либо ОТКРЫВАЕТ новый consolidation run, не только реанимирует зависший) — итерирует исключительно по `known_spool_sessions(layout)`; эта функция (`crates/store/src/observation/import.rs:486-504`) — буквально `fs::read_dir(layout.spool_dir())`, список директорий `spool/<session_id>/`, которые создаёт только hook-путь (`local_rag_hook::spool_write_pipeline`). `give_feedback` (`crates/local-rag/src/daemon/mcp/memory_write.rs`, T15-05's собственная as-built заметка 11 §2) — прямая вставка в `observation_envelope`, **в обход spool целиком** («daemon-internal writes are exempt from the spool-only constraint»), поэтому её `session_id` никогда не появляется на диске как директория и, следовательно, никогда не входит в `known_spool_sessions()`'s список. Единственный другой продуктовый вызывающий `run_once` — `crates/local-rag/src/daemon/resume/consolidation.rs` (startup-резюме) — вызывает только `retry_run`/`stale_runs`, никогда `open_next_run`, т.е. реанимирует уже открытые (упавшие) runs, но не открывает новые для непокрытого backlog'а — не альтернативный путь. Итог: любая `give_feedback`-запись от сессии, ни разу не писавшей в spool (структурно верно для **любого** MCP-клиента без Claude Code hooks-плагина, включая эту тестовую сессию), навсегда остаётся вне охвата consolidation — не «пока не наберётся backlog», а буквально никогда ни при каком объёме backlog'а, поскольку сама сессия структурно невидима триггеру. | D-040 (владелец-решение нужно перед реализацией — затрагивает границу spool-only/daemon-internal write, не изолированный баг): кандидат-подход — `known_spool_sessions` (или его вызывающий в `consolidation_trigger_tick`) расширить до объединения spool-директорий **и** различных `session_id`, реально присутствующих в ещё непокрытом (`pending_backlog > 0`) диапазоне `observation_envelope`, — например новый `local_rag_store::sessions_with_pending_backlog(read)` (SQL `SELECT DISTINCT session_id FROM observation_envelope WHERE …`), объединённый с `known_spool_sessions` перед циклом. **В scope**: (a) новый store-примитив/расширение существующего запроса, (b) wiring в `consolidation_trigger_tick`, (c) regression-тест — `give_feedback`-only сессия (без единого spool-сегмента) реально доводится до `Ran(RunOutcome::Applied(_))`/`Failed(_)` тем же тестовым стилем, что уже существующие `consolidation_trigger.rs::tests`. **Не в scope**: сам spool-путь (уже корректен), lease/retry-механика (не трогать). **Тесты**: новый unit-тест в `consolidation_trigger.rs`, зеркалящий `a_stop_event_triggers_immediately`, но сеющий envelope напрямую (как `stale_run_recovery_runs_before_the_new_run_pass_in_the_same_tick` уже делает) вместо spool-сегмента, без единой spool-директории на диске. **Приёмка**: живой `give_feedback` без предшествующей hook-активности реально доходит до открытого `consolidation_run` в разумное число тиков (наблюдаемо `local-rag doctor`/прямым чтением `consolidation_run`). | resolved | коммит `D-040: consolidation trigger covers spool-bypassing sessions` (та же правка). Новая `crates/store/src/memory/consolidation.rs::sessions_with_pending_backlog` — group-by-session SQL по `observation_envelope`/`processing_cursor` (тот же критерий, что `pending_backlog`, для всех сессий разом, за один проход по индексу `envelope_session(session_id, received_seq)`), реэкспортирована через `memory/mod.rs`/`lib.rs` рядом с `pending_backlog`. `daemon/consolidation_trigger.rs::consolidation_trigger_tick` объединяет `known_spool_sessions(layout)` с `sessions_with_pending_backlog(&db.open_read()?)` через `BTreeSet` (дедуп) перед циклом, открывающим runs — `known_spool_sessions`/lease-retry-механика не тронуты. Regression `a_session_with_no_spool_directory_is_still_consolidated`: сессия `sess-fb` с тремя `McpFeedback`-envelope, вставленными прямым SQL ровно как `give_feedback` (без единой spool-директории на диске, `queue_threshold` вместо `Stop`/`SessionEnd`-триггера) реально доводится до `Ran(RunOutcome::Applied(_))` за один тик, курсор сдвигается (`pending_backlog == 0`); подтверждено регрессией — при временно отключённой union-строке тест падает. Плюс `sessions_with_pending_backlog_lists_only_sessions_past_their_own_cursor`/`..._is_empty_with_no_envelopes` в `consolidation.rs`. `cargo fmt --all -- --check` OK (весь workspace); `cargo clippy -p local-rag-store -p local-rag --all-targets -- -D warnings` OK; `cargo test -p local-rag-store --lib memory::consolidation` 15/15 OK; `cargo test -p local-rag --lib daemon::consolidation_trigger` 10/10 OK. |
| D-041 | post-v0 (тот же живой dogfood-сеанс, что D-036–040) — агент с полным доступом к MCP-инструментам, прочитавший текущие `SERVER_INSTRUCTIONS`, пропускал `recall`/`remember` часами, дефолтясь на файловый workflow из `CLAUDE.md`; подтверждено прямым raw-protocol-пробником (в обход MCP-клиента) после rebuild+restart демона | `T17-02`'s собственный заявленный результат (`PROGRESS.md`): «MCP instructions deliver RECALL→SEARCH_CODE→THINK→ACT→REMEMBER» — обещание поведенческого эффекта, не просто присутствия текста | Существующий unit-тест (`instructions_deliver_the_recall_search_code_think_act_remember_cycle`) проверяет только присутствие строки-стрелочной цепочки в тексте — не поведенческое соответствие. Живое наблюдение показало: advisory-тон («Working loop: recall before you act…») и порядок абзацев (`Which tool` с «search_code — start here» шёл **до** working-loop абзаца, буквально противоречия recall-first) не производили заявленный эффект — агент с полным контекстом всё равно не вызывал `recall` первым | D-041 (текстовая правка, без изменения диспетчер-логики/поведения инструментов — тот же класс, что уже closed D-039): переписать `SERVER_INSTRUCTIONS` (`instructions.rs`) на явный, loss-framed, action-triggered тон («Working loop, not optional: call recall before your first file read, grep, or search this session… Skipping it means re-deriving facts and decisions this project already paid once to store»); переставить `Working loop`-абзац **перед** `Which tool`-абзацем, устраняя противоречие «search_code — start here» vs recall-first; добавить короткую усиливающую фразу в каждое из 3 caller-facing описаний инструментов (`tools.rs`: `search_code`, `recall`, `remember`). **В scope**: ровно эти два файла, тот же класс правки. **Не в scope**: рантайм-гейтирование (например, отказ выполнить `search_code` без предварительного `recall`) — текст остаётся advisory, только сильнее сформулирован; другие инструменты/описания. **Тесты**: не поведенческие (строковые литералы, не логика) — `instructions_deliver_the_recall_search_code_think_act_remember_cycle` остаётся зелёным (стрелочная подстрока `RECALL → SEARCH_CODE → THINK → ACT → REMEMBER` сохранена дословно в новом тексте); `initialize_returns_server_identity_and_instructions` (`mcp_contract.rs`, `.contains("search_code")`) остаётся зелёным. Явное ограничение: агентная поведенческая комплаенс-проверка не автоматизируется юнит-тестом (эмпирический вывод, не unit-проверяемое свойство) — уже так для `T17-02`'s исходного теста, D-041 не ухудшает и не изображает несуществующее покрытие. **Приёмка**: `cargo fmt --all -- --check` чисто; `cargo check -p local-rag --all-targets` без warnings; `cargo test -p local-rag --lib daemon::mcp::instructions` зелёный; `cargo test -p local-rag --test mcp_contract` зелёный | resolved | коммит `D-041: caller-facing MCP instructions rewritten hard-gated (recall-first)` (та же правка). Изменены только `crates/local-rag/src/daemon/mcp/instructions.rs` (`Working loop`-абзац переставлен перед `Which tool`, оба переписаны в action-triggered тон, стрелочная цепочка сохранена дословно) и `crates/local-rag/src/daemon/mcp/tools.rs` (усиливающая фраза в `search_code`/`recall`/`remember` description). `cargo fmt --all -- --check` OK; `cargo check -p local-rag --all-targets` OK, без warnings; `cargo test -p local-rag --lib daemon::mcp::instructions` OK; `cargo test -p local-rag --test mcp_contract` OK. |
| D-042 | T19-02 (при планировании, по прямому требованию карточки — `groups/19-mcp-adoption.md`'s собственный текст запрещает решать этот вопрос молча) | Spec 11 §5 `[FIXED]`: «Empty recall ⇒ no output at all» | T19-02 добавляет tool-routing трейлер к непустому recall-блоку — самому сильному каналу адопции (рядом с точкой принятия решения на каждом ходу). Но ровно там, где адопция хуже всего — первые сессии нового пользователя/репозитория, когда память ещё пуста — `[FIXED]`-правило запрещает выводить вообще что-либо, включая сам трейлер: пустой recall остаётся `""`, без исключений. Это не расхождение с уже реализованным (T19-02 корректно сохраняет `[FIXED]` как есть — trailer добавлен строго после `entries.is_empty()`-guard'а, не обходит его), а явно поднятый карточкой продуктовый вопрос: не нужен ли нудж на роутинг инструментов именно в этом худшем-для-адопции случае. | Не корректирующая задача с кодом — продуктовое решение, требующее либо принятия текущей границы, либо новой design revision, меняющей `[FIXED]`-текст 11 §5 (аналогично тому, как retention K/T стало X-001 после явного решения владельца). До решения: `[FIXED]` не трогается, T19-02 не реализует нудж на пустом recall. Тот же паттерн disposition, что уже несёт `T19-06` этой же группы (`groups/19-mcp-adoption.md`) — блокировано, зафиксировано, не блокирует переход к следующим задачам группы 19. | blocked | T19-02 (та же правка, тот же коммит) — see spec `11-interfaces.md` §5 as-built note (T19-02) и `PROGRESS.md`'s T19-02 evidence |

Допустимые статусы: `open`, `fixing`, `resolved`, `blocked`. Для `[OPEN]` решения здесь
ссылаются на ADR/spike, но не превращают вопрос в deviation, если реализация ещё не нарушена.
