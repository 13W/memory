# Прогресс имплементации

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` завершено, `[!]` заблокировано. Одновременно
может быть `[~]` только одна задача, если карточка явно не разрешает независимую работу.
Gate следующей группы нельзя начинать до `PASS` предыдущего.

## 00 — Контракт разработки и baseline

- [x] T00-01 Импортировать v1 behavioral fixtures и зафиксировать baseline inventory
- [x] T00-02 Создать Rust workspace, quality commands и CI smoke
- [x] T00-03 Создать общий fixture/failpoint test harness
- [x] G00 Сверка foundations и testing contract

## 01 — Миграции и SQLite foundation

- [x] T01-01 Реализовать разрешение store/config путей и permissions
- [x] T01-02 Реализовать state DB open policy и bounded writer
- [x] T01-03 Реализовать forward-only migration runner
- [x] T01-04 Добавить resumable/destructive migration mechanics
- [x] T01-05 Реализовать cache DB binding и recreation
- [x] D-001 Устранить недетерминизм concurrent-migrator теста (WAL-init race)
- [x] G01 Сверка migration/storage foundation

## 02 — Реестр repository/worktree

- [x] T02-01 Реализовать canonical path и hash/ID primitives
- [x] T02-02 Добавить registry schema и repository operations
- [x] T02-03 Добавить stable worktree operations и path history
- [x] T02-04 Реализовать attach/move/non-git resolution
- [x] T02-05 Реализовать config merge и data-policy ordering
- [x] D-002 Сузить source-lint no_writable_cross_db_attach до SQL-ключевого слова
- [x] G02 Сверка identity и registry

## 03 — Exact source и skip policy

- [x] T03-01 Добавить code-storage DDL и repositories
- [x] T03-02 Реализовать file classification и skip reasons
- [x] T03-03 Реализовать exact source_blob/file_revision reuse
- [x] T03-04 Реализовать normalized text cache regeneration
- [x] D-003 Устранить недетерминизм cache read-helper под нагрузкой (transient SQLITE_BUSY)
- [x] G03 Сверка source-blob invariant

## 04 — Парсинг и path-independent units

- [x] T04-01 Закрыть O4 и оформить ADR первого набора языков
- [x] T04-02 Реализовать parser fingerprint и parser abstraction
- [x] T04-03 Реализовать первый язык и parser fixtures
- [x] T04-04 Реализовать второй язык и parser fixtures
- [x] T04-05 Реализовать третий язык, если выбран ADR
- [x] T04-06 Реализовать deterministic parsed-unit persistence
- [x] G04 Сверка parsing identity

## 05 — Generations и strict reconcile

- [x] T05-01 Реализовать generation/occurrence schema и state transitions
- [x] T05-02 Реализовать authoritative tree scan и gitignore semantics
- [x] T05-03 Реализовать generation builder и structural sharing
- [x] T05-04 Реализовать reconcile scheduler/triggers
- [x] T05-05 Реализовать retry/failure handling
- [x] G05 Сверка reconcile и generation invariants

## 06 — Retention и GC

- [x] T06-01 Реализовать pin-root calculation
- [x] T06-02 Реализовать batched generation/source sweep
- [x] T06-03 Реализовать orphan/quarantine/spool housekeeping (split — orphan-shard sweep; quarantine/grace/spool отложены D-004)
- [x] D-004 Разрешить расхождение очерёдности: quarantine→T07-04, grace-destroy→07/09, spool-GC→T13-05
- [x] G06 Сверка rebuildability и retention

## 07 — Fake projection и протокол переключения

- [x] T07-01 Определить ProjectionStore contract и fake backend
- [x] T07-02 Добавить two-axis projection state guards
- [x] T07-03 Реализовать desired-set write-ahead switch
- [x] D-005 Устранить недетерминизм fake_faults.rs (общий failpoint-регистр без serial() на части тестов)
- [x] T07-04 Реализовать validate-on-open и rebuild
- [x] T07-05 Реализовать F1–F12 fault matrix
- [x] G07 Сверка projection correctness

## 08 — FTS materialized view

- [x] T08-01 Добавить FTS schema, preprocessing и manifest
- [x] T08-02 Реализовать FTS build/delta и head-last commit
- [ ] T08-03 Реализовать validation, degradation и rebuild
- [ ] T08-04 Добавить FTS corruption/staleness tests
- [ ] G08 Сверка FTS consistency

## 09 — Locking и shard lifecycle

- [ ] T09-01 Реализовать lock hierarchy и per-worktree coordination
- [ ] T09-02 Реализовать ref-counted shard LRU
- [ ] T09-03 Реализовать snapshot/read-lock search skeleton
- [ ] T09-04 Проверить concurrent switches и generation mixing
- [ ] G09 Сверка concurrency model

## 10 — Выбор dense backend

- [ ] T10-01 Подготовить воспроизводимый spike harness и corpus
- [ ] T10-02 Адаптер и измерения brute-force backend
- [ ] T10-03 Адаптер и измерения usearch backend
- [ ] T10-04 Адаптер и измерения Qdrant Edge backend
- [ ] T10-05 Сравнить, выбрать backend и оформить ADR
- [ ] G10 Сверка отсутствия преждевременной backend coupling

## 11 — Embeddings и model spaces

- [ ] T11-01 Реализовать representation/model-space registry
- [ ] T11-02 Реализовать embedding cache integrity и eviction
- [ ] T11-03 Реализовать local embedder provider pool
- [ ] T11-04 Реализовать resumable coverage backfill
- [ ] T11-05 Реализовать per-worktree model switch
- [ ] T11-06 Реализовать model asset installer
- [ ] G11 Сверка model migration

## 12 — Hybrid code search

- [ ] T12-01 Реализовать lexical leg и фильтры
- [ ] T12-02 Интегрировать выбранный dense backend
- [ ] T12-03 Реализовать RRF и deterministic response
- [ ] T12-04 Реализовать source_blob snippets/context/overview
- [ ] T12-05 Запустить 49-query baseline/gates
- [ ] G12 Сверка search v0

## 13 — Spool и observations

- [ ] T13-01 Реализовать redaction/caps перед записью
- [ ] T13-02 Реализовать segment writer и hook fail-open
- [ ] T13-03 Реализовать frame reader и identity semantics
- [ ] T13-04 Реализовать transactional importer/cursor
- [ ] T13-05 Реализовать payload TTL и spool GC
- [ ] T13-06 Реализовать S1–S8 kill matrix
- [ ] G13 Сверка no-loss ingestion

## 14 — Durable memory

- [ ] T14-01 Добавить memory DDL и legal state transitions
- [ ] T14-02 Реализовать базовый transactional memory-op engine
- [ ] T14-03 Реализовать lifecycle/edit memory operations
- [ ] T14-04 Реализовать merge memory operation
- [ ] T14-05 Реализовать candidates и review operations
- [ ] T14-06 Реализовать consolidation lease/cursor runner
- [ ] T14-07 Реализовать local router и quality fixture set
- [ ] T14-08 Реализовать recall relevance и safe formatting
- [ ] G14 Сверка memory correctness/quality

## 15 — Daemon, protocol, MCP и CLI

- [ ] T15-01 Реализовать store lock и daemon lifecycle
- [ ] T15-02 Реализовать versioned proxy protocol/handshake
- [ ] T15-03 Реализовать MCP code query tools
- [ ] T15-04 Реализовать MCP status и memory read tools
- [ ] T15-05 Реализовать MCP memory/write tools
- [ ] T15-06 Реализовать hook recall RPC и additionalContext
- [ ] T15-07 Реализовать service/index/registry CLI
- [ ] T15-08 Реализовать memory/privacy/diagnostic CLI
- [ ] G15 Сверка interface contracts

## 16 — Security, privacy и recovery operations

- [ ] T16-01 Централизовать remote policy enforcement
- [ ] T16-02 Реализовать inspect/export/purge и audit tombstones
- [ ] T16-03 Реализовать doctor/rebuild/degraded diagnostics
- [ ] T16-04 Добавить adversarial and ownership suite
- [ ] G16 Сверка security/privacy

## 17 — Distribution и release gates

- [ ] T17-01 Создать npm launcher и platform packages
- [ ] T17-02 Создать Claude Code plugin registration
- [ ] T17-03 Настроить cargo-dist/zigbuild platform CI
- [ ] T17-04 Проверить upgrade/migration/offline install flows
- [ ] T17-05 Запустить полный acceptance/resource/latency suite
- [ ] G17 Финальная сверка v0

## Evidence

### Gate results

Заполняется только после выполнения gate: `PASS`, `PASS after D-NNN` или `BLOCKED`.

| Gate | Результат | Ссылка на evidence/report |
| --- | --- | --- |
| G00 | PASS | строка G00 в «Task evidence» + трейс «G00 — трейс требование → artifact/test» ниже |
| G01 | PASS | строка G01 в «Task evidence» + трейс «G01 — трейс требование → artifact/test» ниже |
| G02 | PASS | строка G02 в «Task evidence» + трейс «G02 — трейс требование → artifact/test» ниже |
| G03 | PASS | строка G03 в «Task evidence» + трейс «G03 — трейс требование → artifact/test» ниже |
| G04 | PASS | строка G04 в «Task evidence» + трейс «G04 — трейс требование → artifact/test» ниже |
| G05 | PASS | строка G05 в «Task evidence» + трейс «G05 — трейс требование → artifact/test» ниже |
| G06 | PASS | строка G06 в «Task evidence» + трейс «G06 — трейс требование → artifact/test» ниже |
| G07 | PASS | строка G07 в «Task evidence» + трейс «G07 — трейс требование → artifact/test» ниже |
| G08 | — | — |
| G09 | — | — |
| G10 | — | — |
| G11 | — | — |
| G12 | — | — |
| G13 | — | — |
| G14 | — | — |
| G15 | — | — |
| G16 | — | — |
| G17 | — | — |

### Task evidence

Добавляйте строки без удаления истории:

| ID | Commit/PR | Проверки | Результат/артефакт | Исполнитель/дата |
| --- | --- | --- | --- | --- |
| T08-02 | коммит `T08-02: FTS build/delta и head-last commit (spec 06 §2/§4)` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib` 81 OK (79 старых + новые `code::membership::tests::occurrences_for_fts_joins_scopes_and_orders`/`occurrences_for_fts_rejects_corrupt_unit_kind` join-ридер юнит-тесты); `cargo test -p local-rag-store --test fts_materialize` 5 OK (default features — `fail_before_head_rolls_back_whole_tx` корректно `#[cfg(feature = "failpoints")]`-gated, без фичи no-op); `cargo test -p local-rag-store --test fts_materialize --features failpoints` 6 OK (`a_to_b_add_rename_delete_reflects_in_materialized_fts` — rename переиспользует file_revision/unit_id, occurrence_id новый; delete/add отражены; full-replace не merge; `all_unit_kinds_present_are_materialized_agnostically` — все 5 unit_kind, вкл. 2 сегодня нигде не производимых парсером (`config_section`/`text_section`), вставлены напрямую — материализатор агностичен; `fail_before_head_rolls_back_whole_tx` — armed `cache:fts_before_head`, вся транзакция откатывается, ПРЕДЫДУЩАЯ успешная generation A остаётся валидной побайтово (не просто «пусто»), чистый retry сходится к B; `no_valid_head_before_first_successful_build`; `evicted_normalized_text_is_recomputed_and_recached` — прямая порча `normalized_text_cache` строки, повторный materialize чинит через `delete_normalized_text`+recompute (реальный баг найден и исправлен в разработке — `insert_normalized_text`'s `ON CONFLICT` трогает только `last_used_at`, не текст); `repeated_build_is_byte_identical`) ×8 последовательных прогонов подряд без падений (SERIAL-guard tokio Mutex сериализует все тесты файла — process-global `cache:fts_before_head` failpoint, тот же класс риска что D-005); `cargo clippy -p local-rag-store --all-targets -- -D warnings` и то же с `--features failpoints` чисты; `cargo fmt --check` чист; `cargo test -p local-rag-store` (весь крейт, default) все test-result блоки OK; `cargo xtask ci` = all checks passed; guardrail: `git diff --stat Cargo.lock` пуст (0 новых зависимостей); grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml`+корневой = NONE | **Generation materializer `local_rag_store::materialize_fts` (spec 06 §2/§4) — первое наполнение данными FTS-представления, построенного в T08-01.** Новый join-ридер `code::occurrences_for_fts` (`crates/store/src/code/membership.rs`, первый multi-table JOIN в кодовой базе: `generation_unit_occurrence ⋈ parsed_unit ⋈ content_blob`) — типизированный `FtsSourceRow`, corrupt `unit_kind` → `FromSqlConversionFailure` (не тихий default). `materialize_fts` (`crates/store/src/cache/fts.rs`): (1) вне транзакции читает occurrences через новый ридер; (2) вне транзакции резолвит normalized-text каждого различного `blob_id` через `normalized_text_cache` (`get_normalized_text`+`verify_cached_text`), собирая список нуждающихся в recompute; (3) вне транзакции пересчитывает недостающие/битые блобы из `state.sqlite`'s `source_blob` (срез `[span_start,span_end)` — `blob_id` per-unit, не per-файл), с defensive-проверкой `derived.blob_id == blob_id` (иначе типизированная `BlobMismatch`, не тихая подмена под чужим ключом); (4) ОДНА cache-транзакция: `delete_normalized_text`+`insert_normalized_text` для recompute-блобов → удаление старых `fts_doc`/`fts_occurrences` строк worktree → вставка нового набора (rowid — локальный `MAX(fts_rowid)+1` счётчик, НЕ `last_insert_rowid()` — тот connection-global и был бы испорчен вставками в `normalized_text_cache` в той же транзакции) → **последним** upsert `fts_projection_head`. Полная замена (delete-all-then-insert-all), не построчный diff — обосновано тем, что `occurrence_id` включает `generation_id` (spec 03 §1.2), поэтому ID двух generation ВСЕГДА не пересекаются (в отличие от dense-проекции, где diff экономит дорогой пересчёт эмбеддингов — у FTS такой стоимости нет). `body` = сырой `normalized_text` (без токенизации T08-01-функциями — spec 09 §2 явно не включает body в app-side-токенизируемые колонки); `signature` всегда пуст (`tokenize_signature(&[])` — не подключено, вне scope); `qualified_name` читается как есть (сегодня всегда `NULL`). Новый типизированный ридер `read_fts_projection_head`/`FtsProjectionHeadRow` (чистый row-accessor, без интерпретации валидности — та остаётся T08-03). Новый failpoint `cache:fts_before_head` (calька `cache:after_delete`), `#[cfg(feature = "failpoints")]`. **Реальный дефект, найден и исправлен в разработке (до коммита, тот же класс что D-003/D-005):** первая версия материализатора звала `insert_normalized_text` напрямую для recompute-блобов; тест `evicted_normalized_text_is_recomputed_and_recached` поймал, что `ON CONFLICT` этой функции трогает только `last_used_at` (T03-04 предполагал «конфликтующая строка = идентичный контент», что неверно для порченной строки) — испорченный текст не чинился. Исправлено: `delete_normalized_text` перед `insert_normalized_text` (без-op если строки не было, гарантированно чинит если была). **As-built `[SPEC]`** заметка добавлена в `06-reconcile-and-fts.md` §4 (после существующего описания FTS rebuild recipe) — описывает full-replace-обоснование, single-cache-tx реализацию, rowid-стратегию, delete-before-insert-фикс, body/signature/qualified_name решения; `[FIXED]` текст секции не тронут. `crates/index` НЕ тронут (материализатор — standalone функция, wiring в reconcile-driver — вне scope, аналогично тому как `switch()`/`rebuild()` T07 не были сразу провязаны). Отклонений сверх исправленного в разработке дефекта не обнаружено; `DEVIATIONS.md` D-001…D-005 остаются `resolved`; историческое evidence не переписывалось | Claude Sonnet 5 / 2026-07-22 |
| T08-01 | коммит `T08-01: FTS schema, preprocessing и manifest (spec 03 §4.3, 09 §2)` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib cache::fts::` 11 OK (tokenizer golden tables `tokenize_identifier`/`tokenize_path`/`tokenize_qualified_name`/`tokenize_signature` вкл. Unicode-кейсы `café`/CJK/эмодзи и NFC-эквивалентность decomposed/precomposed; `fts_manifest_hash` golden + order/duplicate-independence + `binds_the_worktree`/`binds_the_generation` как два отдельных теста + `every_occurrence_id_changes_the_hash`); `cargo test -p local-rag-store --test fts` 8 OK (`fts5_module_is_compiled_in` через `sqlite_compileoption_used('ENABLE_FTS5')`, `fts_tables_exist_after_open`, `fts_doc_by_wt_index_exists`, `fts_doc_rowid_round_trips_as_fts_occurrences_rowid`, `fts_doc_occurrence_id_is_unique`, `bm25_weights_favor_the_declared_column_order`, `bm25_weight_order_is_actually_sensitive` (перевёрнутые веса меняют порядок — ловит транспонированный аргумент), `bm25_rewards_multi_column_hits`); `cargo test -p local-rag-store --test cache --test normalized_text` 12 OK + 6 OK (регрессия после бампа `CACHE_SCHEMA_VERSION` 2→3, вкл. починенный хардкод в `normalized_text.rs:122`); `cargo clippy -p local-rag-store --all-targets --all-features -- -D warnings` чисто; `cargo test -p local-rag-store --features failpoints` зелёный (231 тест, 0 failed, суммарно по всем test-result блокам); `cargo test --workspace` — 62 test-result блока, 0 failed; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/projection `--features failpoints`); guardrail: `git diff --stat Cargo.lock` = 1 строка (`casefold` — внутреннее ребро `local-rag-store`, пакет уже резолвлен через `local-rag-core`, 0 новых внешних источников); `rusqlite`/`libsqlite3-sys` **не менялись** — FTS5 уже безусловно включён `bundled`-сборкой (`-DSQLITE_ENABLE_FTS5` в `libsqlite3-sys build.rs`, нет отдельной cargo-фичи `fts5` ни у `rusqlite`, ни у `libsqlite3-sys` — проверено чтением исходников пакета); grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml`+корневой `Cargo.toml` = NONE | **FTS materialized-view schema + versioned lexical preprocessing + manifest identity (spec 03 §4.3, 09 §2), фундамент группы 08 — без наполнения данными (T08-02), без validate-on-open/rebuild (T08-03), без corruption suite (T08-04).** Новые/изменённые модули: `crates/store/src/cache/open.rs` — `FTS_DOC_DDL`/`FTS_OCCURRENCES_DDL`/`FTS_PROJECTION_HEAD_DDL` (дословно spec 03 §4.3), `CACHE_SCHEMA_VERSION` 2→3, wiring в `seed_binding` (одна транзакция, all-or-nothing — как `cache_meta`/`normalized_text_cache`); `crates/store/src/cache/fts.rs` (новый) — `LEXICAL_SCHEMA_VERSION`/`TOKENIZER_VERSION` (обе `1`), `tokenize_identifier`/`tokenize_path`/`tokenize_qualified_name`/`tokenize_signature` (единый character-class boundary scan: hard-delimiter на любом не-alphanumeric символе + 4 case/digit-правила — letter→digit, digit→letter, lower→upper, конец акронима с lookahead на следующую lowercase-букву; разбиение ДО lowering через `casefold::simple_fold`; «слитный» токен эмитится только если атом без внутренней пунктуации — иначе удвоение term frequency в bm25; path/qualified_name разбиваются на компоненты ДО атомарного разбора, чтобы решение о fusion принималось для каждого компонента отдельно, а не глобально по всей строке), `fts_manifest_hash` (переиспользует существующий `Domain::FtsManifest`, sorted+deduplicated occurrence ids, БЕЗ оси `model_space_id` — FTS generation-scoped only, в отличие от dense-проекции). Явные, задокументированные упрощения (не молчаливые пропуски): digit-boundary splitting не задан спекой явно — сознательный выбор; путь-компонент не разбивается дополнительно на «стем/расширение» (`barBaz.rs` даёт `bar`,`baz`,`rs`, но не `barbaz`); `tokenize_signature` не делает fusion вообще (фрагмент сигнатуры — не самостоятельный поисковый термин); `qualified_name=None` (сегодня всегда так на реальных данных, `crates/index/src/reconcile/build.rs:453,498`) → `""`. `crates/index` НЕ тронут (плюмбинг сырого текста сигнатуры из tree-sitter-адаптеров — вне scope). Golden-фикстуры реализованы как inline `#[cfg(test)]` таблицы (не новая fixture-семья) — обоснование: `fixtures/` существует для fidelity к v1, у токенизатора нет v1-аналога (чисто v2 логика), прямой прецедент — `content_blob_id`/`projection::manifest_hash` golden-тесты тоже inline. `CONTRIBUTING.md`: расширена область `casefold` (`crates/core` → `crates/core, crates/store`); строка `rusqlite` дополнена явной заметкой «FTS5 уже доступен через `bundled`, отдельной фичи нет» (без изменения самой фичи). **As-built `[SPEC]`** заметки добавлены в `03-data-model.md` (после §4.3) и `09-search.md` §2 (после bm25-весов), описывающие ровно то, что реализовано выше; `[FIXED]`-текст обеих секций не тронут. Отклонений не обнаружено; `DEVIATIONS.md` D-001…D-005 остаются `resolved`; историческое evidence не переписывалось | Claude Sonnet 5 / 2026-07-22 |
| G07 | этот коммит (gate-сверка + `PROGRESS.md` evidence в одном коммите; продуктовый код и тесты НЕ тронуты) | узкие тесты группы 07: `cargo test -p local-rag-projection` (default features) — `fake_shard.rs` 9 OK, `rebuild.rs` 7 OK, `switch.rs` 8 OK, `fault_matrix_coverage.rs` 2 OK, lib 26 OK, `fake_faults.rs`/`fault_matrix.rs`/`switch_faults.rs`/`rebuild_faults.rs` 0 (gated за `failpoints`, ожидаемо); `cargo test -p local-rag-projection --features failpoints` — весь крейт зелёный: lib 26 OK, `fake_faults.rs` 7 OK, `fake_shard.rs` 9 OK, **`fault_matrix.rs` 9 OK (F2–F10)**, `fault_matrix_coverage.rs` 2 OK (12/12 деклараций `fixtures/fault/matrix.json` ↔ Rust-реестр сходятся 1:1), `rebuild.rs` 7 OK, **`rebuild_faults.rs` 1 OK (F11)**, `switch.rs` 8 OK, **`switch_faults.rs` 1 OK (F1)** — суммарно F1–F12 = 12/12 named tests, каждый проверяет и детекцию, и идемпотентный rebuild; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/projection `--features failpoints`); `git status --short` пуст (working tree чист); guardrail T10: `git diff --stat Cargo.lock` пуст (0 новых зависимостей), grep `crates/*/Cargo.toml`+корневой `Cargo.toml` на qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss = NONE | Сверка G07 (перечитаны spec 04 §1/§2 целиком, 05 §1–§10 целиком, 14 §2/§3, опора 03 §1.2/§1.4/§2.2): построен трейс «требование → код → тест» (блок «G07 — трейс…» ниже), расхождений код↔спека НЕ обнаружено. Явные deliverables карточки G07 подтверждены: (a) **fault-detection matrix 12/12** — `fault_matrix_coverage.rs::every_declared_fault_row_has_exactly_one_executable_test` мехнически сверяет `fixtures/fault/matrix.json` против `FAULT_MATRIX_COVERAGE`-реестра, драйфа нет; (b) **two-axis serialization** — `ProjectionStatus::check_transition` (полная 4×4 truth-table, T07-02) + `check_invariants` (`BothAxesMovedAtOnce` отклоняет одновременный сдвиг обеих осей, NULL-active инициализация — не нарушение) удостоверены truth-table тестами `projection_state.rs` (наследуются из T07-02, не изменялись); `switch::commit_switch` — единственный писатель обеих осей — двигает только одну ось за вызов (`model_axis_only_switch_leaves_generation_active`, `generation_transitions_in_the_same_commit`); (c) **no durable-barrier assumption** — F5/F9/F10 доказывают, что `status='clean'` НЕ является proof физической durability бэкенда: post-clean WAL-потеря/swallowed-flush-failure детектируются только на СЛЕДУЮЩЕМ `open_and_validate`, никогда не в момент `switch()`/`write_head` (F9's narrative point: `switch()` возвращает `Ok`, расхождение всплывает только позже); (d) **validate on every fake reopen** — `open_and_validate` (`rebuild.rs:426`) прогоняет `validate` (`validate.rs:83`) на КАЖДОМ открытии без исключений (daemon-start/LRU-reopen/post-crash все проходят один и тот же путь — нет отдельного "trusted" пути открытия); (e) **recovery only from active canonical state** — `begin_rebuild` (`rebuild.rs:215`) явно очищает `target_*` и `rebuild()` всегда таргетит `active_generation_id`/`active_model_space_id` из `state.sqlite`, никогда не резюмирует прерванный `target` switch (`stale_head_tuple_triggers_rebuild`, `bootstrap_before_any_switch_is_no_active_tuple` — нет active tuple ⇒ `NoActiveTuple`, шард не трогается); (f) **backend-specific dependency запрещена до PASS G10** — `git diff Cargo.lock` пуст, `crates/projection`/`crates/store` не тянут ни один dense/model SDK; единственный backend — `local_rag_projection::fake::FakeProjectionStore` (T07-01), рабочий, но backend-нейтральный. `[FIXED]`/`[SPEC]`/`[OPEN]` осмотрены: spec 05's principle («dense projection — всегда untrusted cache», «no durable barrier … only detectability») реализован буквально; spec 04 §2 FSM diagram (`clean⇄updating`, `dirty→rebuilding→clean`, `rebuilding→dirty`) воспроизведена 1:1 в `check_transition` (T07-02, регрессия не тронута этим гейтом); spec 05 §1 «backend chosen at roadmap step 11 `[OPEN]`» и §9 «thresholds `[OPEN]`, step-11 spike» НЕ закрыты молча — `optimize()` в fake — пустой no-op stub, ни один тест не имитирует реальный threshold-policy (закрывается T10). Отложено by-design (seam на месте, указывает на owning-карточки): dormant-worktree model migration (05 §8, «offline worktree мигрирует на default space при следующем open») — нет production wiring (нет теста `dormant_*`/`*_migrates_to_default*` в `crates/projection`), требует daemon open-path (группа 15) + реальный model-space registry (T11-01); grace-destroy `removing`/`detached` worktree (05 §8, нужна миграция `removed_at`) — группа 07/09 per D-004, НЕ эта карточка (T07-04/T07-05 её не покрывали, gate это подтверждает, не расширяет scope); generation-mixing под конкурентной нагрузкой (14 §4 «assert every result set's occurrences belong to exactly one generation») и real per-worktree WRITE lock (05 §5 step 3) — T09-01/T09-03, real dense backend hybrid search — группа 12; per-worktree read lock над полным hybrid pipeline (guardrail CLAUDE.md) — вне scope (нет search-пути до T10/T12). Gate НЕ писал ни продуктового кода, ни новых тестов — все 70 тестов группы 07 (26 lib + 44 integration через 8 test-бинарников под `--features failpoints`) уже существовали. Отклонений не обнаружено; `DEVIATIONS.md` D-001…D-005 остаются `resolved`; историческое evidence не переписывалось. Итог: **PASS** — группа 08 разблокирована | Claude Sonnet 5 / 2026-07-22 |
| T07-05 | коммит `T07-05: F1-F12 fault matrix (spec 05 §10)` (строка evidence в том же коммите) | `cargo test -p local-rag-projection --test fault_matrix --features failpoints` 9 OK ×5 последовательных прогонов (F2..F10, ноль флапов — сериализация на `tokio::sync::Mutex` держится); `cargo test -p local-rag-projection --test fault_matrix_coverage` 2 OK (без `--features failpoints` — читает `fixtures/fault/matrix.json`, сверяет 12/12 id против Rust-реестра); `cargo test -p local-rag-projection --test switch_faults --features failpoints` 1 OK (F1 расширен: `open_and_validate` после краха → `NoActiveTuple` (bootstrap, нет active tuple для rebuild), после retry-конвергенции → `Valid`); `cargo test -p local-rag-projection --features failpoints` (весь крейт) 3× подряд — 0 падений; `cargo xtask ci` = all checks passed ×2 подряд (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/projection `--features failpoints`); guardrail T10: `git diff Cargo.lock` = только внутреннее ребро `tokio` feature `sync` (уже резолвленный пакет, 0 новых внешних источников), grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml` = NONE | **F1–F12 fault-detection matrix полностью executable (spec 05 §10 `[FIXED]`); GAP-06 (F-половина) закрыт.** Один новый production-seam: `crates/projection/src/switch.rs` — `projection.switch.before_commit` (fires между landed shard-write и финальным `state.sqlite` commit; F4 — единственная точка, не покрытая существующими `fake.rs`-seams) + `SwitchError::Failpoint(&'static str)` (калька `index::build::BuildErrorKind::Failpoint`). Новые тесты: `crates/projection/tests/fault_matrix.rs` (F2 — второй switch, upsert падает, head остаётся stale от switch #1; F3 — write_head падает после upsert+delete, head op_id ≠ projection_op_id; F4 — новый seam, head tuple=target≠active; F5/F10 — общий helper, truncate `points` до пусто, PointCountMismatch, F10 = «то же самое» дословно по спеке; F6 — `Corruption::DropPoint` + честная заметка про «intact catalog» (fake меняет count вместе с manifest, в отличие от гипотетического бэкенда со «stale catalog»); F7 — `Corruption::RemoveHead`; F8 — `Corruption::SwapPoint`, end-to-end компаньон к чистому unit-тесту T07-04; F9 — тот же механизм что F6/F8, но нарратив «switch() вернул Ok, лишь следующий open_and_validate находит расхождение»); каждый тест проверяет ОБА требования карточки — детекцию сигнала И идемпотентный rebuild до `Valid`. Расширен существующий F1-тест (`switch_faults.rs`, T07-03) добавлением `open_and_validate` до и после retry. **Реестр покрытия** `crates/projection/tests/fault_matrix_coverage.rs` (default features, БЕЗ failpoints — работает в любом `cargo test`): dependency-free JSON-скан `fixtures/fault/matrix.json` (калька `crates/index/tests/language_coverage.rs`, без `serde_json` — не одобрена для этого крейта) + статическая Rust-таблица `FAULT_MATRIX_COVERAGE: &[(&str,&str)]` (F-id → «файл::тест»), тест `every_declared_fault_row_has_exactly_one_executable_test` сверяет множества id 1:1 — это и есть «reusable artifact»: расхождение (переименованный тест, правка фикстуры) валится громко. Фикстура `fixtures/fault/matrix.json` и её схема (`status` заблокирован на `"declarative"`) НЕ тронуты (как и задумано — экзекьютабельность живёт в Rust-тестах, не в самой декларативной фикстуре). **Обнаружено при разработке (не отдельная deviation, тот же класс что D-005, исправлено в этом же коммите до commit):** `established()`-хелпер в `fault_matrix.rs` изначально минтил свой собственный `SeqUuidV7::new()` внутри — второй `switch()` в тесте (со своим отдельным свежим `SeqUuidV7`) получал ИДЕНТИЧНЫЙ первый op_id (последовательность зависит только от числа вызовов, не от инстанса) → F2/F3 ложно падали («left != right» с одинаковыми UUID). Исправлено: `established()` принимает `uuids` от вызывающего теста (единый счётчик на весь тест). Также 8/9 тестов файла первоначально гонялись без `serial()`-подобной защиты процесс-глобального failpoint-реестра — идентичный D-005 паттерн, тот же файл в разработке; исправлено добавлением `static SERIAL: tokio::sync::Mutex<()>` (async-aware — `std::sync::Mutex` через `.await` сам по себе баг, `clippy::await_holding_lock`) и `let _serial = SERIAL.lock().await;` в каждом тесте; добавлена фича `sync` к dev-dependency `tokio` в `crates/projection/Cargo.toml`. Верифицировано ×5 последовательных прогонов без падений. **As-built `[SPEC]`** (см. 05-projection.md as-built note T07-05): F1/F11/F12 переиспользуют существующие тесты (F1 расширен); F6 честно фиксирует расхождение с «intact catalog» формулировкой спеки; F9/F10 — тот же механизм что F6/F8/F5 под другим нарративом (спека сама говорит «F10 = same as F5»). `fixtures/manifest.json` GAP-06 обновлён: F-половина done, S1-S8 (T13-06) остаётся open. Отклонений сверх исправленных в разработке (тот же класс, до коммита) не обнаружено; `DEVIATIONS.md` D-001…D-005 остаются `resolved`; историческое evidence не переписывалось | Claude Sonnet 5 / 2026-07-22 |
| T07-04 | коммит `T07-04: validate-on-open and rebuild (spec 05 §6/§7)` (строка evidence в том же коммите) | `cargo test -p local-rag-projection --lib` 26 OK (17 старых + 9 новых `validate::tests::*`: `fully_consistent_state_is_valid`, `not_clean_fires_alone` (3 статуса), `active_projected_mismatch_fires`, `missing_head_fires`, `op_id_mismatch_fires`, `head_tuple_mismatch_fires`, `point_count_mismatch_fires`, **`manifest_mismatch_fires_on_equal_count_different_ids`** (F8: равный count, разный ID-набор → `ManifestMismatch`, НЕ `PointCountMismatch`), `earlier_predicate_wins_over_later_ones`); `cargo test -p local-rag-projection --test rebuild` 7 OK (`bootstrap_before_any_switch_is_no_active_tuple`; `valid_shard_stays_valid_and_second_open_is_a_true_no_op` — 2× подряд, `FakeVectors` call-log пуст; `unopenable_shard_is_quarantined_and_rebuilt` — corrupt `points` без failpoints (калька `fake_shard.rs::open_on_corrupt_points_file_errors`), quarantine-каталог сохранён для диагностики; `quarantine_rotation_keeps_at_most_two` — 3 quarantine-события подряд → ровно `QUARANTINE_RETENTION=2` каталогов выживает; `stale_head_tuple_triggers_rebuild` — openable-shard destroy (не quarantine); `missing_vector_during_rebuild_leaves_status_rebuilding_and_no_partial_shard` — `RebuildError::MissingVector`, `status='rebuilding'`, свежий шард пуст (0 points, нет head); `cause_is_recorded_for_diagnostics`); `cargo test -p local-rag-projection --test rebuild_faults --features failpoints` 1 OK (`crash_during_rebuild_leaves_rebuilding_and_retry_converges` — F11: armed существующий `projection.fake.upsert`, status остаётся `Rebuilding`, retry сходится к `Valid`, `point_count=2`); `cargo test -p local-rag-projection --features failpoints` (весь крейт) зелёный ×4 последовательных прогона (проверка на D-005-класс флапов — не найдено); `cargo xtask ci` = all checks passed ×2 подряд (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/projection `--features failpoints`); guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей — переиспользованы `local-rag-store`/`tokio` из T07-03), grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml` = NONE | **Validate-on-open + единственный recovery-путь rebuild (spec 05 §6/§7 `[FIXED]`), плюс quarantine-rotation половина §8, отложенная сюда D-004.** Новые модули: `crates/projection/src/validate.rs` (чистый `Divergence` enum + `validate(row, head, shard_point_count, shard_manifest_hash) -> Option<Divergence>` — 5 предикатов spec 05 §6 в заданном порядке, first-match-wins как `check_invariants`; манифест — self-consistency пересчёт `point_ids()` под ЗАЯВЛЕННЫМ в head tuple, независимо от `HeadTupleMismatch`); `crates/projection/src/rebuild.rs` (`open_and_validate` — точка входа: читает row, пробует `store.open`, гонит `validate`, чинит через `rebuild` при любом расхождении; `rebuild` — три ОТДЕЛЬНЫЕ гвардированные tx `mark_dirty`→`begin_rebuild`→`finish_rebuild`, т.к. FSM разрешает только `Dirty→Rebuilding` напрямую (04 §2), а не `Clean/Updating/Rebuilding→Rebuilding`; destroy/quarantine границы — quarantine ТОЛЬКО при `RebuildCause::Unopenable` (F12, нет `ShardHandle` чтобы `destroy`), иначе `ShardHandle::destroy`; `quarantine_shard`/`rotate_quarantine` — чистое FS-ядро (калька `housekeeping.rs`), `<worktree_id>-<uuidv7>` имя → лексикографическая сортировка = хронологический порядок, хранит ≤`QUARANTINE_RETENTION=2`; missing-vector реюзает T07-03 `VectorSource`, собирает ВСЕ векторы в `Vec` до первого `upsert` → partial-set никогда не пишется, ошибка оставляет `status='rebuilding'` нетронутым для retry). Rebuild — ВСЕГДА полный destroy/quarantine-then-recreate (никогда diff против существующего шарда — это fast-path `switch`), что делает retry после краха тривиально идемпотентным (детерминированные point ID + чистая derivation из state.sqlite). **As-built `[SPEC]`** (см. 05-projection.md as-built note T07-04): 3-tx структура вместо 1 псевдокода; quarantine/destroy граница = unopenable-only; `QUARANTINE_RETENTION=2` + UUIDv7-suffix ordering; missing-vector через существующий seam вместо реального `embedding_cache` (T11-02). Отклонений не обнаружено; `DEVIATIONS.md` D-001…D-005 остаются `resolved`; историческое evidence не переписывалось | Claude Sonnet 5 / 2026-07-22 |
| T07-03 + D-005 | коммит `T07-03: desired-set write-ahead switch (spec 05 §5)` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib code::membership` 7 OK (+ новый `occurrence_ids_for_generation_scopes_and_sorts`); `cargo test -p local-rag-projection --lib` 17 OK (12 старых + 5 новых `expected::tests::*`: пустая генерация, cross-product occ×kind, смена model_space/generation меняет каждый point id, `expected_point_ids` согласован с `expected_points`); `cargo test -p local-rag-projection --test switch` 8 OK (`add_change_delete_point_sets`; `unchanged_vectors_not_recomputed`; `retry_from_arbitrary_partial_fake_set_converges_and_is_idempotent`; `final_tuple_is_exact`; `generation_transitions_in_the_same_commit`; `model_axis_only_switch_leaves_generation_active`; `unknown_worktree_is_a_typed_write_ahead_error`; `missing_vector_is_typed_and_leaves_status_updating`); `cargo test -p local-rag-projection --test switch_faults --features failpoints` 1 OK (`backend_error_leaves_detectable_updating` — armed existing `projection.fake.upsert`, no new failpoint names); `cargo test -p local-rag-projection --features failpoints` (весь крейт) зелёный, вкл. починенный `fake_faults.rs` 7 OK ×8 последовательных прогонов (см. D-005); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/projection `--features failpoints`); guardrail T10: `git diff Cargo.lock` = только внутренние рёбра (`local-rag-store` path-dep + уже резолвленный `tokio` — 0 новых внешних источников), grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml` = NONE | **Desired-set write-ahead switch (spec 05 §5 `[FIXED]`), первая зависимость `local-rag-projection → local-rag-store`.** Новые модули: `crates/projection/src/expected.rs` (`expected_point_ids`/`expected_points`, spec 05 §4 — читает новый `local_rag_store::occurrence_ids_for_generation` через `occurrence_by_gen` индекс, кросс-произведение с `REQUIRED_REPRESENTATION_KINDS`); `crates/projection/src/switch.rs` (`switch()` — write-ahead→desired-set reconcile→commit, `VectorSource` seam, типизированные `SwitchError`/`SwitchCommitError`). Шаги: (1) `write_ahead` — гвардированная SQLite-tx, `status→updating`, target-tuple, новый `projection_op_id`, active/projected копируются verbatim; (2) reconcile — `expected \ existing` апсертится (вектор запрашивается ТОЛЬКО для отсутствующих точек — доказывает «unchanged vectors not recomputed»), `existing \ expected` удаляется, head пишется последним; (3) `commit_switch` — **pre-flight** обоих generation-state переходов (`GenerationState::check_transition` на target→Active и, если применимо, prev→Retiring) чистыми проверками ДО первой записи в tx, затем guarded `write_projection_state`(→Clean), затем сами `transition_generation`×(1-2) + `set_current_generation` — все в одной tx («generation transition in same final tx», отклонение на любом шаге не оставляет частичной мутации). Retry = повторный вызов `switch()` с тем же target (Updating→Updating — легальный self-transition из T07-02; `existing` каждый раз читается заново из шарда — «no command-log replay»). **As-built `[SPEC]`** (см. 05-projection.md as-built note T07-03): required-kinds захардкожены `{code_raw,code_context}` (T11-01 ещё не существует, v0 и так сводится к этой паре); vectors — через новый `VectorSource` seam вместо ещё не построенного `embedding_cache` (T11-02); `∪ changed` реализовано как ∅ (`ShardHandle` не даёт vector-read-back, upsert идемпотентен); per-worktree WRITE lock — ответственность вызывающего (T09-01). **D-005 (обнаружено этим же прогоном `xtask ci`, НЕ в T07-03-коде):** `fake_faults.rs` (T07-01) флапал под `--features failpoints` — `serial()`-мьютекс вокруг process-global failpoint-реестра держали только 2 из 7 тестов; остальные 5 (обычный `write_head` без ожидания failpoint) могли попасть под чужой armed `Action::Error`. Исправлено добавлением `let _serial = serial();` во все 5 тестов + уточнён doc-comment; продуктовый `fake.rs` не тронут. Верифицировано 8×0-падений подряд (было ~60% флапов). Отклонений сверх D-005 не обнаружено; `DEVIATIONS.md` D-001…D-004 остаются `resolved`; историческое evidence не переписывалось | Claude Sonnet 5 / 2026-07-22 |
| T07-02 | коммит `T07-02: two-axis projection state guards (migration v4)` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib registry::projection_state` 7 OK (`schema_v4_seeds_the_default_model_space`; `projection_status_round_trips`; `check_transition_covers_the_whole_matrix` — 4×4 truth-table: legal set + идемпотентный self + всё прочее illegal; `clean_invariants`/`updating_invariants`/`dirty_and_rebuilding_carry_no_tuple_invariant` — truth table инвариантов вкл. `BothAxesMovedAtOnce` и init-из-NULL; `projection_state_rejects_corrupt_status` → `FromSqlConversionFailure`); `cargo test -p local-rag-store --test projection_state` 7 OK (`init_creates_clean_empty_row`; `updating_requires_a_target_…` (target-required-updating, rollback); `clean_requires_no_target_and_active_equals_projected`; **`simultaneous_generation_and_model_target_is_rejected`** (write-ahead из active=(genA,default) в target=(genB,msB) → typed `BothAxesMovedAtOnce`, clean-состояние сохранено); `illegal_status_transition_is_typed_error` (clean→rebuilding); `unknown_worktree_is_typed_error`; `happy_path_one_axis_switch_walks_clean_updating_clean`); `--test migrate` 8 OK (`state_db_open_bootstraps_and_is_idempotent` расширен до v4: `model_space`/`worktree_projection_state` созданы, seed active `default` через `default_model_space_id`, 4-я строка `(4,"projection")`, reopen=4); `--test registry` 11 OK (`migration_produces_exact_registry_schema` до `[v1,v2,v3,v4]`); `--test schema_audit` 5 OK (новые таблицы авто-проходят: только identity-колонки, нет path/fingerprint); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/projection `--features failpoints`); guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей — чистый Rust + `rusqlite`), grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml` = NONE | **Two-axis projection deployment state + минимальный model-registry seed (spec 03 §2.2, FSM 04 §2, one-axis 04 §8/05 §5 `[FIXED]`).** Новый submodule `crates/store/src/registry/projection_state.rs` (реэкспорт через `registry/mod.rs` → `lib.rs`). **Миграция v4** `SCHEMA_V4` (`migrate::ALL` 4-й элемент `Migration::sql(4,"projection",…)`, non-destructive atomic-apply, frozen-once-shipped): `CREATE TABLE model_space` + `worktree_projection_state` (DDL дословно §2.2, одноколоночные FK); seed дефолта — фиксированный `DEFAULT_MODEL_SPACE_ID='00000000-0000-7000-8000-000000000001'`, `display_name='default'` (совпадает с config `ModelsConfig::default_model_space`), `state='active'` (04 §3 «default MUST be active»), `created_at=updated_at=0` (epoch-sentinel — детерминизм миграции), + `store_settings.default_model_space_id`. **Guard-слой** (идиома `registry/generation.rs`): `ProjectionStatus{Clean,Updating,Dirty,Rebuilding}`+`as_str`/`from_db`; pure `check_transition` (legal = self ∪ {clean→updating, updating→clean, clean→dirty, updating→dirty, dirty→rebuilding, rebuilding→clean, rebuilding→dirty}); pure `check_invariants` (truth table: clean⇒target NULL ∧ active==projected; updating⇒target-tuple ∧ op_id заданы; **one-axis** — ось «движется» только если active-значение non-NULL и ≠ target, значит инициализация из NULL — не запрещённый одновременный сдвиг); `ProjectionStateRow` + read `projection_state` (corrupt status → typed `FromSqlConversionFailure`); guarded `write_projection_state(tx, wt, ProjectionStateChange, now_ms) -> rusqlite::Result<Result<(),ProjectionStateError>>` (nested-Result: read→check_transition→check_invariants→UPDATE; inner Err = no mutation); `insert_projection_state` (init clean-empty row); `default_model_space_id` read. Типизированные ошибки `IllegalProjectionTransition`/`ProjectionInvariantViolation`/`ProjectionStateError` (Display+source). **As-built `[SPEC]`:** v4 создаёт только `model_space`+`worktree_projection_state` (representation/model_space_representation + canonical RepresentationKey + coverage + model_space build-FSM → T11-01, её FK не нужны здесь); фиксированный default id; seed `created_at=0`; one-axis трактует NULL-active как init. **Границы (owning-карточки):** switch-оркестрация (write-ahead↔commit desired-set reconcile) → T07-03; validate-on-open + rebuild + mark_dirty/begin_rebuild → T07-04; F1–F12 → T07-05; representation registry → T11-01. `[FIXED]` (двухосевой state, one-axis) не изменён. Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001…D-004 остаются `resolved`); историческое evidence не переписывалось | Claude Opus 4.8 / 2026-07-22 |
| T07-01 | коммит `T07-01: ProjectionStore contract + persistent fake backend` (строка evidence в том же коммите) | `cargo test -p local-rag-projection` = lib 12 OK (identity goldens `projection_point_id_is_exact_golden` `2f3497a5…55f0` / `manifest_hash_is_exact_golden` `b428ec08…26a1` — оба крест-проверены против `domain::hash(Domain::ProjectionPoint/Manifest,…)`; `manifest_is_independent_of_order_and_duplicates`; `manifest_binds_the_tuple`; `head_derives_count_and_manifest_from_the_set`; `representation_kind_changes_the_point_id`; contract enum/ord/error; fake hex round-trip/reject/dot) + `tests/fake_shard.rs` 9 OK (`fresh_shard_is_empty_with_no_head`, `reopen_preserves_points_and_head`, `upsert_is_idempotent_by_point_id`, `delete_is_idempotent`, `manifest_is_independent_of_upsert_order`, `search_is_deterministic_top_k`, `upsert_rejects_wrong_dimensionality`, `destroy_removes_the_shard_directory`, `open_on_corrupt_points_file_errors`); `cargo test -p local-rag-projection --features failpoints` = + `tests/fake_faults.rs` 7 OK (`head_is_last_operation_error_leaves_no_head` — injected `Action::Error` в seam `projection.fake.write_head` перед записью head → reopen без head, points целы, retry сводит; `head_is_last_operation_survives_sigabrt` — реальный `SIGABRT` в дочернем процессе перед write_head, reopen: points целы/head отсутствует; `inspect_reports_loaded_state`; `corrupt_drop_point`/`remove_head`/`swap_point`/`overwrite_head`); `cargo test -p local-rag-core --lib identity::uuidv7` 8 OK (+ `from_str_round_trips_display`/`from_str_rejects_malformed`); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store/index/**projection** `--features failpoints`); guardrail T10: `git diff Cargo.lock` = единственная вставка — path-крейт `local-rag-test-support` в deps `local-rag-projection` (**0 внешних источников**), grep `qdrant/usearch/onnxruntime/ort/candle/ollama/hnsw/faiss/reqwest/hyper/tonic/tokenizers` по `crates/*/Cargo.toml` = NONE | **Backend-нейтральный контракт проекции + персистентный fake (spec 05 §1 `[FIXED abstraction, signatures [SPEC]]`, §3/§4/§10; 03 §1.2).** Крейт `crates/projection` (был 6-строчный stub) — dep только `local-rag-core`; `local-rag-test-support` как optional за фичей `failpoints` + `[dev-dependencies]` (калька store/index). Модули: `contract` (traits `ProjectionStore`/`ShardHandle` дословно 05 §1 + типы), `identity` (`projection_point_id`/`manifest_hash`/`head` поверх `core::identity::domain`, домены `ProjectionPoint`/`ProjectionManifest` уже определены), `fake` (`FakeProjectionStore`/`FakeShard` — рабочий backend до T10, не только test-double). **Fake persistence:** pure-std, два файла на шард в `projection/<worktree_id>/` — `points` (отсортированные `point_id_hex\tvec_le_f32_hex`, детерминированная функция *множества*) и `head` (`key=value`, пишется ТОЛЬКО `write_head`, строго после мутаций точек), атомарно temp+rename; `open` грузит без валидации (validate-on-open = T07-04), парс-ошибка → `Corrupt` (F12-сигнал); brute-force `search`; `upsert` проверяет `dimensions`. **As-built `[SPEC]`** (форма типов — свобода дизайна карточки): `ProjectionPoint{point_id,vector}`, `ShardParams{dimensions}`, `DenseQuery{vector,k}`, `ScoredPoint{point_id,score}`, `PROJECTION_SCHEMA_VERSION=1`; поля манифеста = tuple (`worktree_id`,`generation_id`,`model_space_id`) ‖ point ids sorted-bytewise + dedup (set-семантика 05 §4), `op_id` исключён (per-op nonce, проверяется отдельно 05 §6). **Поддерживающая правка core:** `impl FromStr for Uuid` (+`UuidParseError`) в `core::identity::uuidv7` — нужен для round-trip UUID-полей head при reopen; чистый, протестирован, `[FIXED]` не тронут. **failpoints-контроли:** seams `projection.fake.{upsert,delete,write_head}` + `FakeShard::inspect`/`corrupt(Corruption)` за фичей `failpoints`; `xtask ci` и `CONTRIBUTING.md` дополнены прогоном projection под фичей. **Границы scope (owning-карточки, D-004):** `worktree_projection_state` two-axis guards → T07-02; write-ahead switch + `expected_point_ids(state.sqlite)` + dep `local-rag-store` → T07-03; validate-on-open + rebuild → T07-04; матрица F1–F12 → T07-05; реальный dense-backend/SDK — не ранее T10. Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001…D-004 остаются `resolved`); историческое evidence не переписывалось | Claude Opus 4.8 / 2026-07-22 |
| G06 | этот коммит (gate-сверка + doc-precision правка `retention.rs` — 1 строка комментария, поведение не изменено — + `PROGRESS.md` evidence в одном коммите; логика и тесты НЕ тронуты) | узкие тесты группы 06: `cargo test -p local-rag-store --lib retention::` 16 OK (13 mark-фазы T06-01 + 3 sweep-константы T06-02: `sweep_batch_ceiling_is_500`/`sweep_report_total_and_is_empty`/`record_maps_each_phase_to_its_own_field`); `--test retention` 12 OK (5 mark + 7 sweep: `pinned_rows_survive`, `orphan_graph_fully_removed`, `shared_revision_retained_until_final_ref`, `external_revision_pin_retains_shared_content`, `dry_run_mutates_nothing`, `deep_unit_tree_deleted_leaf_first_across_batches`, `empty_sweep_is_a_noop`); `--features failpoints --test retention_sweep` 2 OK (`interruption_between_batches_resumes` + `resumable_hard_kill_via_sigabrt` — реальный `SIGABRT` mid-sweep в дочернем процессе); `--lib housekeeping::` 7 OK + `--test housekeeping` 4 OK (orphan снесён / worktree-backed вкл. `detached`/`removing` сохранены `tests/housekeeping.rs:107-113` / dry-run / идемпотентность); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store `--features failpoints` → clippy+test index `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей), grep `crates/*/Cargo.toml` на qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss = NONE | Сверка G06 (перечитаны spec 05 §8, 06 §5, 07 §6, опора 03 §3/§2.1/§4.4, 15 O6): построен трейс «требование → код → тест» (блок «G06 — трейс…» ниже), расхождений код↔спека НЕ обнаружено. Явные deliverables карточки G06 подтверждены: (a) **pin-root truth table** полна — состояния active/building/projection_ready/retiring/failed, K/T boundaries + union, lease expiry (`> now`), external refs, worktree isolation, детерминизм mark (`retention.rs:773-1042` 13 unit + `tests/retention.rs` 5 integ); (b) **crash sweep tests** — between-batch resume + hard `SIGABRT` через failpoint `retention.sweep.between_batches` (`retention_sweep.rs`); (c) **transaction batch ceiling `[SPEC ≤500 rows/tx]`** — `SWEEP_BATCH_ROWS=500` (`retention.rs:314`), портируемый `DELETE … WHERE rowid IN (… LIMIT ?1)`, одна committed-транзакция на батч, leaf-first `parsed_unit`, delete-order = 06 §5 verbatim (`orphan_graph_fully_removed`); (d) **orphan shard sweep** 05 §8 — `sweep_orphan_shard_dirs`/`run_orphan_shard_sweep`, orphan = нет строки `worktree` (`detached`/`removing` сохранены). `[FIXED]`/`[SPEC]`/`[OPEN]` осмотрены: `retiring` GC/audit-only, never routing (03 §2.1) — candidate-guard свипает только `retiring`/`failed`, `active`/`building` защищены; rebuildability `[FIXED]` (03 §4.4) — sweep трогает только canonical `state.sqlite`, shard/FTS уходят через desired-set; **metrics-driven checkpoint/VACUUM policy** (`[FIXED]` 06 §5 / 03 §3, числа `[SPEC]`) — драйвер отложен by-design в daemon (группа 15): в `retention`/`housekeeping` checkpoint/VACUUM НЕ вызывается, VACUUM-by-schedule отсутствует (нет преждевременного нарушения; seams `StateWriter::queue_capacity/available_slots`, `SweepReport`/`ShardSweepReport` counts); **O6 (K/T) НЕ закрыт** — читается из `[storage].retired_generations_keep`/`_ttl_h`, дефолты `2`/`168 h` provisional/не нормативны (closed by usage metrics; шаг 6 от O6 не зависит — 15 §1 «Steps 1–7 depend on no open question»). Отложено by-design в owning-карточки (D-004, `resolved`): ExternalPins/leases → группы 14/16, startup/periodic wiring + `now_ms`-supplier → daemon 15, quarantine-rotation → T07-04, grace-destroy + `removed_at` → 07/09, spool-GC (>14д absent AND committed cursor, 07 §6) → T13-05. Наблюдение (non-blocking, НЕ отклонение): `setup_scratch`/`plan_sweep` материализуют scratch-набор одной транзакцией без ≤500-потолка — потолок нормативно относится к sweep-**DELETE**-батчам, а temp-таблицы connection-local и не пишут в `state.sqlite` WAL; кандидат на будущую оптимизацию, `[FIXED]` не нарушен. Единственная правка кода — doc-precision в `setup_scratch` (`retention.rs`): комментарий про «read-only connection» приведён в соответствие со спекой 06 §5 (as-built note T06-02, п.5) — dry run на единственном writer-коннекте, мутация замкнута в `temp` (read-only conn `query_only` не создаёт temp-таблиц); поведение не изменено, `dry_run_mutates_nothing` зелёный. Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001…D-004 остаются `resolved`); историческое evidence не переписывалось. Итог: **PASS** — группа 07 разблокирована | Claude Opus 4.8 / 2026-07-22 |
| T06-03 | коммит `T06-03: orphan shard housekeeping (split; D-004)` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib housekeeping::` 7 OK (чистое FS-ядро: `orphan_removed_and_live_retained`, `dry_run_reports_without_removing`, `repeated_sweep_is_idempotent`, `non_directory_entries_are_left_alone`, `missing_projection_dir_is_empty`, `all_live_removes_nothing`, `non_utf8_dir_name_is_retained` (best-effort: APFS/macOS отвергает не-UTF-8 имена `EILSEQ` → skip, на Linux CI проверяет ветвь)); `cargo test -p local-rag-store --test housekeeping` 4 OK (`orphan_shard_swept_worktree_backed_retained` — orphan-каталог снесён, shard'ы live-worktree вкл. `detached`/`removing` сохранены (доказывает «orphan = нет строки», grace-destroy отложен); `dry_run_reports_without_removing`; `repeated_sweep_is_idempotent`; `empty_projection_is_a_noop`); регрессия после рефактора `store_wide_pinned`: `--lib retention::` 16 OK, `--test retention` 12 OK, `--features failpoints --test retention_sweep` 2 OK; clippy `-D warnings` чист для store в обеих конфигурациях (default + `--features failpoints`, `--all-targets`); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store `--features failpoints` → clippy+test index `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей — чистый `std::fs` + `rusqlite`) | **Orphan shard-dir sweep = реализованная часть T06-03 (split, spec 05 §8; остальное отложено D-004).** Новый модуль `crates/store/src/housekeeping.rs` (sibling `retention`), реэкспорт через `lib.rs`. Публично: `sweep_orphan_shard_dirs(projection_dir, live: &BTreeSet, dry_run) -> io::Result<ShardSweepReport>` (чистое FS-ядро, идиома «pure core + thin DB reader») + `run_orphan_shard_sweep(db, layout, dry_run) -> Result<_, HousekeepingError>` (читает live-множество через новый `registry::all_worktree_ids`, свипает `layout.projection_dir()`); типы `ShardSweepReport { removed: Vec<String> (sorted), retained, dry_run }`(+`is_empty`)/`HousekeepingError { Open/Sqlite/Io }`. **Правило:** удалить `projection/<name>`, где `<name>` не `worktree_id` ни одной строки `worktree` (любого состояния). Консервативно: только директории (не-файлы/симлинки сохранены), не-UTF-8 имя (worktree_id всегда валидный UUID) сохранено. `read_dir` NotFound → пустой отчёт. **Идемпотентность/resume:** повторный прогон пересчитывает live из живой БД → orphan'ов нет; прерывание лечится повтором. **Общий reader** `registry::all_worktree_ids(conn) -> Vec<String>` (ORDER BY, детерминизм) в `registry/worktree.rs`; `retention::store_wide_pinned` отрефакторён на него (behavior-preserving, регрессия T06-02 зелёная). **Startup/periodic-триггер** — daemon (группа 15); поставлена вызываемая sweep-функция (seam, как observability T05-05). **Границы (D-004):** quarantine-rotation → T07-04; grace-destroy `removing`/`detached` (нужен `removed_at`) → группа 07/09; spool-GC 14д/committed → T13-05; каталог `quarantine/` не тронут; 0 преждевременной projection/spool-схемы (T10). Отклонение D-004 зарегистрировано и `resolved`; `DEVIATIONS.md` дополнен (D-001/D-002/D-003 без изменений); историческое evidence не переписывалось | Claude Opus 4.8 / 2026-07-21 |
| D-004 | коммит T06-03 (та же правка) | (см. проверки строки T06-03: `--lib housekeeping::` 7 OK, `--test housekeeping` 4 OK, регрессия retention 16+12+2 OK, `cargo xtask ci` + offline = passed) | **Разрешение расхождения очерёдности плана (см. `DEVIATIONS.md` D-004).** Найдено при исследовании перед T06-03: карточка T06-03 (группа 06) секвенирована раньше подсистем, что она убирает — projection/quarantine (группа 07 / T07-04), shard-strategy (09/10), spool/cursor (группа 13 / T13-05, почти дословный дубль spool-части), — вопреки `[FIXED]` roadmap-порядку 15 §1 (retention pin-roots = шаг 6; shards = шаг 10; spool = шаг 14). Решение владельца — **split**: реализована orphan-shard sweep-часть (готовый фундамент: layout + таблица `worktree`), а quarantine-rotation → T07-04, grace-destroy+`removed_at` → 07/09, spool-GC → T13-05 (owning-карточки, не backlog). Нормативное поведение спеки (05 §8, 07 §6) не нарушено — реализуется в своих группах. Статус `resolved` (disposition = редуцированная T06-03; отдельного кода не требует). G06 подтвердит, что отложенное указывает на owning-карточки | Claude Opus 4.8 / 2026-07-21 |
| T06-02 | коммит `T06-02: batched generation/source sweep` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test retention` 12 OK (5 mark-фазы T06-01 не тронуты + 7 sweep: `pinned_rows_survive` — active + last-K и их контент целы, retiring-кандидат и осиротевший граф удалены; `orphan_graph_fully_removed` — полностью незакреплённое retiring-поколение с полным графом (membership/skips/occurrences/edges/unresolved/вложенные units/revision/blob) снесено до 0 строк по каждой таблице; `shared_revision_retained_until_final_ref` — `file_revision` под двумя поколениями (одно active, одно retiring) выживает после первого sweep (active ссылается) и удаляется только после ретайра последнего держателя; `external_revision_pin_retains_shared_content` — `ExternalPins::referenced_file_revisions` держит revision, хотя поколение снесено; `dry_run_mutates_nothing` — `plan_sweep` даёт `candidate_generations`+ненулевой `would_delete`, снимок `COUNT(*)` всех канонических таблиц до/после идентичен, а реальный `run_sweep` затем удаляет ровно `would_delete`; `deep_unit_tree_deleted_leaf_first_across_batches` — цепочка из 5 `parsed_unit` (root←…←leaf) при `batch_rows=1` удаляется leaf-first без нарушения self-FK `parent_unit_id`; `empty_sweep_is_a_noop` — sweep на all-active store = пустой отчёт, строки целы); `cargo test -p local-rag-store --lib retention::` 16 OK (13 mark + 3 новых: `sweep_batch_ceiling_is_500`, `sweep_report_total_and_is_empty`, `record_maps_each_phase_to_its_own_field` — 9 фаз в фикс. порядке, каждая в своё поле); `cargo test -p local-rag-store --features failpoints --test retention_sweep` 2 OK (`interruption_between_batches_resumes` — `Action::Error` после первого закоммиченного батча (`batch_rows=1`): частичный прогресс на диске (1 из 3 edges снесён, поколение цело) → `Err(SweepError::Interrupted)`; disarm + повторный `run_sweep` → полный sweep; третий прогон = no-op (идемпотентность); `resumable_hard_kill_via_sigabrt` — реальный `SIGABRT` mid-sweep в дочернем процессе (модель power-loss): закоммиченный до краха батч выживает (2 edges), resume в свежем процессе досводит sweep, all-swept); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store `--features failpoints` → clippy+test index `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; clippy `-D warnings` чист для store в обеих конфигурациях (default + `--features failpoints`, `--all-targets`); guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей — чистый Rust, реюз `rusqlite`/existing code-inserts) | **Batched generation/source sweep = мутирующая sweep-фаза retention/GC (spec 06 §5 `[FIXED]`, 03 §3 `[SPEC ≤500 rows/tx]`).** Расширяет `crates/store/src/retention.rs` (обе половины mark+sweep теперь в одном модуле, module-doc обновлён), без новой DDL/миграции (читает замороженную схему), реэкспорт через `lib.rs`. Публично: `run_sweep(db, params, external, now_ms)`/`run_sweep_with_batch(.., batch_rows)` (async, драйвит единый bounded writer), `plan_sweep(db, ..)` (dry-run), типы `SweepReport`(per-table u64 + `total`/`is_empty`)/`SweepPlan`(`candidate_generations`+`would_delete`)/`SweepError`(`Write`/gated `Interrupted`), `SWEEP_BATCH_ROWS=500`. **Store-wide pin:** объединение `pinned_generation_roots` по всем worktree ∪ `external.referenced_generations`. **Кандидат = `state ∈ {retiring,failed}` AND not pinned** — state-guard защищает конкурентно строящееся `building`/`active` поколение от устаревшего pin-снимка (04 §1: retiring/failed — GC-цели). **Delete order** (spec §5, + FK при `foreign_keys=ON`): `resolved_graph_edge`→`generation_unit_occurrence`→`generation_file`/`skipped_file`→`generation`→(reachability closure)→`unresolved_reference`→`parsed_unit`→`file_revision`→`content_blob`. **Reachability closure:** revision осиротел ⟺ ни один *выживший* (non-candidate) `generation_file` на него не ссылается и он не в `external.referenced_file_revisions` (= «shared revision retained until final ref»); blob сносится когда на него не ссылается ни один выживший `parsed_unit`. **Batch ≤500:** `DELETE … WHERE rowid IN (SELECT rowid … WHERE <pred> LIMIT ?1)` — портируемо, без `SQLITE_ENABLE_UPDATE_DELETE_LIMIT`; `parsed_unit` — **leaf-first** (в батче лишь строки, на которые не ссылается ни один ещё-не-удалённый orphan-unit как `parent_unit_id`) → self-FK цел на границе каждого statement даже когда дерево пересекает батчи. **Resumable без progress-таблицы:** каждый батч — своя commit-транзакция, sweepable-множества пересчитываются из живой БД на каждом вызове → прерывание (`Error`/`SIGABRT`) лечится повторным `run_sweep` (удаления монотонны, уже удалённое не матчит). Scratch-множества (pinned/pinned_rev/candidates/orphan_rev) — в connection-local `temp`-таблицах (не часть `state.sqlite`). **dry-run `plan_sweep`:** та же setup+подсчёт в ОДНОЙ writer-транзакции, трогающей только `temp`-схему (read-only коннекты `query_only` — temp-таблицы создать не могут), → ни канонической строки, ни main-WAL-фрейма. **Границы:** shard/FTS-строки снятых поколений НЕ трогаются (исчезают через desired-set 05 §8); orphan shard/quarantine/spool — T06-03; memory/audit/export/lease — группы 14/16 (пустой seam `ExternalPins`); `[OPEN] O6` не закрыт (K/T провизорны). failpoint seam `retention.sweep.between_batches` gated за фичей `failpoints` (калька `build.rs`). `[SPEC]`-amendment spec 06 §5 (as-built sweep). Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются `resolved`); историческое evidence не переписывалось | Claude Opus 4.8 / 2026-07-21 |
| T06-01 | коммит `T06-01: pin-root calculation (retention mark phase)` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib retention::` 13 OK (табличные, чистые: `state_roots_pin_active_building_and_target_only` — pin `{active,building,projection_ready}`, retiring/failed не по состоянию; `last_k_boundary_keeps_highest_numbers` — K=0→∅, K=2 из 5→два старших номера, K≥count→все; `window_boundary_is_inclusive_lower_edge` — `created_at ≥ now−T` инклюзивно (floor 900: 900 in, 899 out); `oversized_window_saturates_and_pins_all` — `window_ms=i64::MAX` не underflow'ит; `k_and_t_are_a_union` — вне last-K но в окне пиннится и наоборот; `only_unexpired_leases_pin` — `lease_until_ms>now` пинит, `==now`/`<now` нет; `lease_pins_generation_absent_from_slice`; `external_references_pin_regardless_of_state` — external ссылка пинит failed; `failed_is_never_pinned_by_retention` — регрессия решения (K=99/T=MAX не пинят failed); `referenced_file_revisions_pass_through`; `mark_is_order_independent` — перестановка входа → идентичный `BTreeSet`; `params_from_default_storage_config` K=2/T=168h→`window_ms=604_800_000`; `from_storage_config_saturates_absurd_ttl` u64::MAX→i64::MAX) + `--test retention` 5 OK (`db_roots_match_pure_mark_and_cover_state_roots` — DB-путь == чистый `mark_pins` на тех же строках; `last_k_is_isolated_per_worktree` — K применяется per-worktree, пины WT-A не включают WT-B; `window_over_created_at_from_real_rows`; `lease_pins_over_db_path`; `empty_worktree_pins_nothing` + `from_storage_config` wiring); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store `--features failpoints` → clippy+test index `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей — чистый Rust, реюз `rusqlite`/`local_rag_core`) | **Pin-root calculation = чистая mark-фаза retention/GC (spec 06 §5 `[FIXED]`, только вычисление pinned-корней; батчевый sweep = T06-02).** Новый top-level модуль `crates/store/src/retention.rs` (sibling `registry`/`code`), read-only, без новой DDL/миграции (читает замороженную схему). Чистое ядро `mark_pins(gens, params, external, now_ms) -> PinRoots` (без I/O, явный `now_ms: i64`, отсортированные `BTreeSet` → детерминизм; идиома `Debouncer`/`check_transition`) + тонкие DB-читатели `generation_meta_for_worktree`/`pinned_generation_roots` (guarded read → pure compute; изоляция `WHERE worktree_id=?1`). Типы `GenerationMeta`/`RetentionParams`(+`from_storage_config`: K как есть, T ч→мс saturating)/`JobLease`/`ExternalPins`(Default=пусто)/`PinRoots`, реэкспорт через `lib.rs`. **Pin-набор:** (a) `{active,building,projection_ready}` безусловно; (b) среди `retiring` — union «последние K по номеру» ∪ «`created_at ≥ now−T`»; (c) `external.referenced_generations`; (d) не истёкшие leases (`lease_until_ms>now`). **`[SPEC]`-решения (амендмент 06 §5):** `failed` **не** пиннится retention'ом (спец-список именует только «retired»=retiring; 04 §1: retiring/failed — GC-цели; shared-контент failed выживает через ссылки active — 06 §2); окно T меряется по `created_at` (нет `retired_at`; добавление колонки — миграция, вне scope mark-фазы); K/T — union; seam'ы memory/audit/export/lease пусты по умолчанию (группы 14/16). `[OPEN] O6` НЕ закрыт: K/T читаются из `[storage].retired_generations_keep`/`retired_generations_ttl_h` (провизорные 2/168 ч, не нормативны). **Scope-guard:** без sweep/мутаций и без замыкания generation→file_revision (T06-02); без новой DDL/конфига; 0 новых зависимостей (T10). Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются `resolved`); историческое evidence не переписывалось | Claude Opus 4.8 / 2026-07-21 |
| G05 | этот коммит (gate-сверка + `PROGRESS.md` evidence в одной правке; продуктовый код и тесты НЕ тронуты) | узкие тесты группы 05: `cargo test -p local-rag-store --test generation` 9 OK (T05-01), `--lib registry::generation::` 3 OK + `code::membership::` 6 OK (3 occurrence_id + 3 skip/edge); `cargo test -p local-rag-index --test scan` 14 OK (T05-02), `--test reconcile` 9 OK (T05-03), `--test scheduler` 6 OK (T05-04), `--lib reconcile::` 17 OK (schedule 13 / watcher 4) + `scan::` 3 OK (stat_cache); `cargo test -p local-rag-index --features failpoints --test reconcile_failure` 4 OK (T05-05); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store `--features failpoints` → clippy+test index `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей), grep `crates/*/Cargo.toml`+`xtask` на qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss = NONE; git-free: grep `crates/index/src/{scan,reconcile}` на git2/gix/libgit/`Command::new` = NONE | Сверка G05 (перечитаны spec 04 §1, 06 §1–3/§6, 14 §2/§4/§5, опора 03 §1.2/§2.4): построен трейс «требование → код → тест» (блок «G05 — трейс…» ниже), расхождений код↔спека НЕ обнаружено — as-built заметки спеки (04 §1 T05-01, 06 §1 T05-02/T05-04, 06 §2 T05-03, 03 §2.4 T05-01 + forward-ref T05-05 в 04 §1 / 06 §1) сверены построчно с кодом. Явные deliverables карточки G05 подтверждены: (a) A→B fixture-семейства + retries + concurrent scans прогнаны (`reconcile` 9 / `reconcile_failure` 4 / `scheduler` 6); (b) **structural sharing** `[FIXED gate]` — `editing_one_file_shares_untouched_units` / `retry_produces_no_duplicate_content` (`build.rs:290` pre-check); (c) **strict-overflow** — `overflow_forces_strict_immediately` + `fast_trusts_full_match_but_strict_rehashes_all` + `reconcile_mode_fast_uses_cache_strict_rehashes`; (d) **immutable active snapshots** — `cancellation_leaves_active_generation_valid` / `read_failure_marks_failed_and_leaves_prior_untouched` / `failpoint_at_each_build_phase_marks_failed_and_leaves_active`; (e) **retiring/failed не роутятся** — `active_generations` отдаёт только `state='active'` (`generation.rs:289`) + `one_active_invariant`; (f) **projection activation НЕ имитируется** — `complete_build_reaches_projection_ready_without_activating` (`current_generation`=None, active пуст). `[FIXED]`/`[SPEC]`/`[OPEN]` осмотрены: watcher=hint/reconcile=truth, watcher-overflow→strict, structural-sharing acceptance, retiring never routed, «no occurrences» skip-invariant, deterministic IDs stable — все as-built; `[SPEC]`-константы `DEBOUNCE_MS=500`/`PERIODIC_MS=6h`/`RETRY_BACKOFF_*` вне `core::Config`; `[OPEN]` не закрыты молча (retention K/T 06 §5 — группа 06; `[BASELINE]` числа 14 §2 — post-v1). Отложено by-design (seam на месте): projection switch/`worktree_projection_state` (05 §5 — группа 07), read-lock/hybrid generation-mixing (06 §3, 14 §4 — группы 09/12), live `spawn_watcher` в CI (группа 15), Retention/GC (06 §5 — группа 06). Gate НЕ писал ни features, ни gate-hardening тестов. Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются `resolved`); историческое evidence не переписывалось. Итог: **PASS** — группа 06 разблокирована | Claude Opus 4.8 / 2026-07-21 |
| T05-05 | коммит `T05-05: retry/failure handling` (строка evidence в том же коммите) | `cargo test -p local-rag-index --lib reconcile::schedule::` 13 OK (+4 backoff: `backoff_grows_on_consecutive_failures` (base→2×→4×→cap `RETRY_BACKOFF_MAX_MS`), `success_resets_backoff`, `backoff_floors_an_automatic_retry` (fail@0→floor 1000; FsChange@100 due@600, но `take_due(600)`=None, fires@1000), `manual_bypasses_backoff` (Manual сбрасывает floor → fires mid-backoff, counter сохранён=1)); `cargo test -p local-rag-index --features failpoints --test reconcile_failure` 4 OK (`failpoint_at_each_build_phase_marks_failed_and_leaves_active` — все 3 фазы `after_allocate`/`persist_file`/`before_finalize`: gen→`failed`, seeded A остаётся Active + `active_generations`==[A] + current=A, failed/retiring не маршрутизируются; `retry_after_failure_builds_valid_generation_without_duplicates` — fail@finalize (контент персистнут: rev/unit ≥2)→retry `projection_ready`, counts `file_revision`/`content_blob`/`parsed_unit` неизменны (structural sharing reuse=2), failed gen всё ещё `failed`, active пуст; `driver_records_failure_and_backs_off` — flush-fail → `failures` watch=Some (counter=1, backoff_until>0, generation_id=Some, last_error содержит "failpoint"), failed gen `failed` + active пуст; `driver_reports_success_as_no_failure` — healthy flush → `failures`=None + ровно 1 generation); регресс feature OFF: `--test reconcile` 9 OK, `--test scheduler` 6 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test store `--features failpoints` → **новые** clippy+test index `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: `git diff Cargo.lock` пуст (optional `local-rag-test-support` — path-крейт, 0 новых external sources), grep qdrant/usearch/onnx/candle/ollama/hnsw/faiss/reqwest/hyper/tonic по `crates/*/Cargo.toml`+`xtask` = NONE | **Retry/failure handling (spec 04 §1 `building → failed`, spec 06 §2/§3, T05-05).** Три слоя, вписаны в раскол T05-04 (чистый движок ↔ async драйвер) и паттерн failpoints из `crates/store`. **`build.rs`:** унифицирован путь пометки `failed` — теперь ЛЮБАЯ пост-allocate ошибка (включая сбой финального `building → projection_ready`, ранее оставлявший генерацию висеть в `building`) прогоняется через один best-effort `→ failed` (spec 04 §1 «error in reconcile/switch»); под фичей `failpoints` — 3 именованных фазовых сайта `reconcile.build.{after_allocate,persist_file,before_finalize}` (gated `BuildErrorKind::Failpoint(&'static str)` + gated Display-ветка), которые тесты вооружают `Action::Error`. **`schedule.rs` (чистый `Debouncer`):** `[SPEC]`-константы `RETRY_BACKOFF_BASE_MS=1000`/`RETRY_BACKOFF_MAX_MS=5min` (index-крейт, как `DEBOUNCE`/`PERIODIC`, вне `core::Config`), `backoff_delay(n)=min(BASE·2^(n-1), MAX)` (saturating, exponent clamp); поля `consecutive_failures`/`retry_backoff_until` + `record_failure`/`record_success`/аксессоры; `next_wake`/`take_due` применяют backoff-пол к автоматическому ретраю (fs/git/periodic/startup/overflow), `Manual` (reindex) сбрасывает пол (явный форс), counter снимается только успехом. **`driver.rs`:** `run` больше НЕ глотает исход — `run_and_observe` сворачивает его в наблюдаемость: успех → `record_success` + watch `None`; сбой → `record_failure` + типизированный `ReconcileFailure { generation_id: Option, last_error, consecutive_failures, backoff_until_ms }` в watch `Some`; `ReconcileHandle.failures: watch::Receiver<Option<ReconcileFailure>>` — seam наблюдаемости (async-mutex сериализует failpoint-тесты). **Хранение (решение владельца):** `generation → failed` персистится; `last_error`/counter/backoff — **in-memory наблюдаемость**. Таблицу `worktree_projection_state` (spec 04 §1/§2, spec 05) НЕ создаём — скоуп группы 07/T07-02 (TRACEABILITY 05 Projection→G07; G05 запрещает имитировать projection; forward-only миграции). Запись `last_error` в БД выполнит группа 07, потребив `ReconcileFailure.last_error`. **Границы:** без активации/projection switch, без projection-state DDL (группа 07); без read-lock/hybrid-search. **CI/фича:** `crates/index` получил optional `local-rag-test-support` + feature `failpoints` (калька store); xtask `run_ci` + `CONTRIBUTING.md` расширены на index-failpoints clippy+test шаги. `[SPEC]`-amendment: build.rs docs + backoff-константы. Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются `resolved`); историческое evidence не переписывалось | Claude Opus 4.8 / 2026-07-21 |
| T05-04 | коммит `T05-04: reconcile scheduler/triggers + notify watcher` (строка evidence в том же коммите) | `cargo test -p local-rag-index --lib reconcile::` 13 OK (**Layer-1 pure engine** `reconcile::schedule::tests`: `debounce_resets_on_each_fs_event` (t=0→wake 500, t=300→wake 800, due@800 не @799), `coalescing_collapses_burst_into_one` (3×FsChange@0/100/200 → один `take_due`@700, второй None), `manual_bypasses_debounce` (Manual → immediate Strict), `overflow_forces_strict_immediately` (Fs@0 затем Overflow@100 → immediate Strict), `fs_event_never_delays_a_pending_manual`, `startup_periodic_manual_git_coalesce_into_one_strict` (Strict wins, один reconcile), `periodic_self_injects_every_interval` (backstop self-inject + re-arm), `git_head_is_dropped_for_non_git_worktrees` (§6), `scan_mode_selection_table`; **watcher mapping** `reconcile::watcher::tests`: `rescan→WatcherOverflow`, `path→FsChange`, `.git-path→GitHead` только на git-worktree иначе FsChange (§6), nested `.git`) + `cargo test -p local-rag-index --test scheduler` 6 OK (**`driver_runs_a_reconcile_on_shutdown_flush`** — FsChange→shutdown → ровно 1 `projection_ready` generation, `active_generations` пуст; **`concurrent_triggers_make_one_next_generation`** — 8×FsChange coalesce → ровно 1 `generation`-строка; **`reconcile_mode_fast_uses_cache_strict_rehashes`** — cold Fast(reused=0)→warm Fast(reused≥2,hashed=0)→Strict(reused=0,rehashes all) = overflow/periodic/startup path 06 §1 `[FIXED]`; **`cancellation_leaves_active_generation_valid`** — seed active gen A через легальные transitions → drop reconcile-future через biased `select!{ready(())}` → A остаётся Active + `active_generations`==[A] + current pointer=A; затем completed reconcile → new gen `projection_ready`, A всё ещё Active/current (builder не активирует, disjoint row set); `load_worktree_meta_reads_root_kind_and_prune_roots` (root/kind/is_git, unknown→None); `nested_prune_roots_lists_same_repo_sibling_under_root` → `[".worktrees/wt2"]`) + регресс `--test reconcile` 9 OK / `--test scan` 14 OK (T05-03 builder не сломан после ужесточения `uuids` bound); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); cross-platform guard: `cargo check -p notify --target x86_64-unknown-linux-gnu` OK (inotify+mio Linux backend компилируется; полный ubuntu-билд с C-тулчейном — GitHub CI); dep-audit: `cargo tree -p notify -e no-dev` на darwin+linux, allowlist-строки совпадают с `Cargo.lock` (только **добавления**, без removals/downgrades) | **Reconcile scheduler/triggers + live `notify` watcher (spec 06 §1).** Two-layer split для детерминизма при живом (недетерминированном) watcher. **Layer 1 — `crates/index/src/reconcile/schedule.rs` (чистый, без async/tokio):** `Debouncer` — движок, параметризованный явным монотонным `now_ms: i64` (зеркало `build_generation(.., now_ms)`/`uuidv7_from`). `record`/`next_wake`/`take_due`/`take_pending`: coalescing в один pending, эскалация режима (`Strict` побеждает), сброс 500ms quiet-window на каждый debounced-триггер, self-inject периодики; git-триггеры отбрасываются для non-git (§6). `[SPEC]`-константы `DEBOUNCE_MS=500`/`PERIODIC_MS=6h` (`ScheduleConfig`) — в index-крейте, НЕ в `core::Config` (spec 02 §3.1 + `default_matches_spec_toml` заморожены). Taxonomy: Startup/Periodic/Manual/WatcherOverflow → bypass+Strict (Manual = «reindex» force, Overflow = mandatory immediate `[FIXED]`); FsChange/GitHead → debounced+Fast. **Layer 2 — `driver.rs` (tokio):** `reconcile_once` (scan → build_generation, синхронный scan inline как в build.rs); `WorktreeReconciler::run` — `biased` `select!`-цикл (timer vs trigger), один reconcile in-flight на worktree, awaited inline = write-сторона per-worktree L2-lock (spec 02 §5) структурно без явного RwLock (read-сторона/projection switch — позже); триггеры во время build буферизуются → coalesce в ≤1 follow-up (concurrent → ровно одно поколение); graceful shutdown флашит запланированный reconcile. Cancellation drop-safe на границе tx state-writer'а (spec 02 §4.3): dropped future оставляет лишь заброшенное `building`/`projection_ready` поколение (disjoint row set, никогда не активируется) → активное поколение цело. `WorktreeMeta`/`load_worktree_meta`/`nested_prune_roots` компонуют мету из store-readers; `prune_roots` = same-repo вложенные worktree (global cross-repo finder отложен); `CaseSensitivity` подаётся out-of-band (не персистится). **Watcher — `watcher.rs` (`notify`, тонкий):** `WatchEvent`-индирекция + чистая `watch_event_to_trigger` (тестируется); live `spawn_watcher` (notify::Event→WatchEvent) исключён из CI (недетерминированный тайминг). Билдер T05-03 `uuids` seam ужесточён до `&(dyn UuidSource + Send + Sync)` (behavior-preserving, чтобы reconcile-future был `Send`-spawnable). **Границы:** stops at `projection_ready` — без активации/projection switch (группа 07; G05 запрещает обходную активацию в продакшн-коде); без typed failure/`last_error`/backoff (T05-05 — переиспользует лишь существующий `building→failed`); без read-lock/hybrid-search. **Зависимости (по решению владельца — wire runtime+notify сейчас):** `tokio` в `crates/index` повышен до runtime-deps (`rt`/`macros`/`sync`/`time`; +`tokio-macros`, `pin-project-lite` уже был); новый `notify 8.2.0` (`default-features=false`, `macos_fsevent`) — CONTRIBUTING allowlist: amended `tokio`-строка + новая `notify`-строка (транзитивы для darwin/linux + lock-only kqueue/windows-*/wasi перечислены, лицензии). `[SPEC]`-amendment spec 06 §1 (as-built scheduler/движок/taxonomy/watcher/driver/registry). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-21 |
| T05-03 | коммит `T05-03: generation builder + structural sharing` (строка evidence в том же коммите) | `cargo test -p local-rag-index --test reconcile` 9 OK (**`editing_one_file_shares_untouched_units`** — gen2 `revisions_reused==1` (b.rs) / `revisions_created==1` (a.rs), ровно +1 `file_revision`, юниты b.rs не дублированы — гейт 06 §2.1 `[FIXED]`; **`rename_shares_content_but_changes_occurrences`** — rename a→b: `revisions_reused==1`, 0 новых `file_revision`, те же `unit_id`, occurrence_id disjoint (a.rs↔b.rs); `deleted_file_is_absent_from_next_generation`; **`read_failure_marks_failed_and_leaves_prior_untouched`** — новый c.rs удалён после scan → `fs::read` NotFound → gen `failed`, предыдущий gen `projection_ready`, `active_generations` пуст; **`retry_produces_no_duplicate_content`** — повторный build → `revisions_reused==2`, `file_revision`/`content_blob`/`parsed_unit` count не вырос, 2 поколения; **`complete_build_reaches_projection_ready_without_activating`** — `projection_ready` + `active_generations` пуст + `current_generation` None (T05-03 не активирует); `unsupported_language_file_is_deferred` — `.md`/`.json` → `files_deferred==2`, не в `generation_file`/`skipped_file`; `supported_extension_binary_content_is_skipped` — `.rs` с NUL → `skipped_file(binary)`; `huge_file_is_skipped_without_reading` → `skipped_file(huge)`) + `cargo test -p local-rag-store --test code` 14 OK (новый `parsed_units_for_revision_lists_units_in_span_order` — все юниты в span-порядке независимо от insertion order, empty для неизвестной ревизии) + регресс `--test scan`/`--test persist`/`--test parse_fixtures`/`--lib` без изменений; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); guardrail T10: `git diff Cargo.lock` **пуст** (0 новых зависимостей); схема не менялась (`--test migrate` `store_version==3`, `--test schema_audit` зелёные) | **Generation builder со structural sharing (ядро reconcile 06 §2).** Новый модуль `crates/index/src/reconcile/{mod.rs,build.rs}`: async `build_generation(db, worktree_id, root, manifest, cfg, scanner, uuids: &dyn UuidSource, now_ms) -> Result<BuildOutcome, BuildError>`. Потребляет `ScanManifest` (T05-02), строит поколение N+1: allocate (`building`, tx0) → per-entry per-file tx → `building→projection_ready` (tx_final, только если полно); при ошибке `building→failed` (best-effort), active не трогается. **Structural sharing:** pre-check `file_revision_id_by_content_key(content_hash, parser_fingerprint)` (read-conn, оптимизация) — `Some` → reuse ревизии+юнитов без чтения/парсинга (occurrences из нового reader `parsed_units_for_revision`); `None` → `fs::read` + `classify` (пустой gitignore — ignored уже отсеян сканером) → `Skipped`→`skipped_file` либо `Indexed`→`prepare_source`+`parser_for(lang).parse`+`create_or_reuse_file_revision`(TOCTOU абсорбирует Created/Reused)+`persist_parse_output`+`insert_generation_file`+occurrences (`occurrence_id=H(gen,path,unit)`, `qualified_name`/`context_hash`=None). **Критично:** IO/CPU (read/classify/prepare/parse) вне tx-closure (writer — единственный поток, `Send+'static`); внутрь tx только DB-операции. Huge (`content_hash=None`) → `skipped_file(huge)` без чтения. None-language (`select_language`=None) → **defer** (`files_deferred`, не index/не skip) — universal путь позже (задокументировано в `select_language`), нет `SkipReason` для «unsupported». Retry=свежее поколение; контент дедуплицируется `create_or_reuse_*` → без дублей. Маленькие добавки: `parse::parser_for(LanguageId)->Box<dyn LanguageParser>` (production-фабрика), `store::parsed_units_for_revision(conn, rev)->Vec<String>` (span-ordered). Тест-double `SeqUuidV7` (реализует `UuidSource`) — локально в тесте (`test-support` намеренно бездепендентный, не тронут). **Границы:** без активации/projection switch/`current_generation`/`worktree_projection_state` (группа 07; G05 запрещает обходную активацию); без typed failure/`last_error`/backoff (T05-05 — здесь только `failed`-transition); без scheduler (T05-04); без universal-индексации/occurrence enrichment. `[SPEC]`-amendment spec 06 §2 (as-built builder + deferral). 0 новых зависимостей (T10), схема не менялась. Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-21 |
| T05-02 | коммит `T05-02: authoritative tree scan + advisory stat cache` (строка evidence в том же коммите) | `cargo test -p local-rag-index --test scan` 14 OK (`nested_gitignore_and_negation` — вложенные `.gitignore` deeper-wins + `!`-негация, `.gitignore`-файлы индексируются, манифест отсортирован; `symlinks_are_excluded_and_not_followed` #[cfg(unix)] — file/dir/dangling симлинки исключены, dir-симлинк не следуется; `delete_absent_and_rename_moves_content_hash` two-scan — delete→absent, rename→тот же `content_hash` на новом пути; **`stat_mismatch_escalates_to_hashing`** — seeded несовпадающий `StatKey`+фейк-хеш → Fast ре-хеширует реальный контент; **`fast_trusts_full_match_but_strict_rehashes_all`** — seeded истинный `StatKey`+неверный хеш: Fast доверяет (advisory-риск), Strict игнорирует кэш и кладёт верный; `strict_reused_stats_reports_reuse_zero` — cold(2 hashed/0 reused)→warm Fast(0/2)→Strict(2/0); **`non_git_and_git_modes_produce_the_same_manifest`** — дерево с `.gitignore` как NonGit(`require_git(false)`) == как Main(с `.git`), §6 parity; `git_directory_is_never_indexed` — `.git/**` исключён при `hidden(false)`; `prune_roots_excludes_nested_worktree_subtree`; `huge_files_keep_an_entry_with_no_hash` — >cap → `content_hash=None`, байты не читаются; `empty_tree`→пусто; `nonexistent_root`/`a_file_root`→Err; `manifest_is_deterministic_across_scans` — два Strict-скана равны + warm-Fast равен) + `--lib scan::` 3 OK (`StatCache`: reuse требует полного совпадения+известный `file_id`, unknown-id никогда не reuse, record overwrites); регресс `--lib classify::` / `--test classify` без изменений; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); guardrail T10: `git diff Cargo.lock` **пуст** (0 новых зависимостей — `ignore` уже был); guardrail git: grep `crates/index/src/scan/` на git2/gix/libgit/Command = NONE | **Authoritative gitignore-aware сканер дерева (шаги 1–2 reconcile-пайплайна 06 §2).** Новый модуль `crates/index/src/scan/` (`mod.rs`+`stat_cache.rs`): `scan(root, WorktreeKind, CaseSensitivity, max_file_size_bytes, ScanMode, prune_roots, &mut StatCache) -> io::Result<(ScanManifest, ScanStats)>` через `ignore::WalkBuilder`. Возвращает **канонически отсортированный** (`(normalized_path, display_path)`) манифест кандидатов; каждый несёт `normalized_path`/`display_path` (через `identity::path::normalize_relative`, НИКОГДА durable id — 01 §5.1), `size` и `content_hash: Option` (store-`H(file_content)`, сравним с `file_revision.content_hash`). `ScanMode::{Fast,Strict}`: Fast сверяет advisory `StatCache` (`StatKey=(mtime,size,file_id)`; reuse ⇔ все три равны И `file_id` известен — unknown inode = doubt → re-hash, 06 §1); **Strict** (watcher-overflow/cold-start seam) игнорирует кэш и хеширует всё. Два skip-гейта здесь: `ignored` (native-pruning walk → игнорируемые файлы **отсутствуют** в манифесте, без `skipped_file`-строки) и stat-only `huge` (`content_hash=None`, байты не читаются). **Детерминизм-guards (критично):** `git_global(false)`+`parents(false)` (нет утечки `$HOME`/над-корневых ignore), `follow_links(false)`+только regular-files (симлинки/fifo/сокеты исключены), внутренний `.git` подрезается безусловно (при `hidden(false)`). NonGit → `require_git(false)` (`.gitignore` чтится без `.git` → §6 parity). Опциональный `prune_roots` (через `filter_entry`) исключает вложенные зарегистрированные worktree — корни подаёт T05-04 из реестра (по решению владельца — seam добавлен сейчас). Манифест не зависит от mtime/состояния кэша → чистая функция байт дерева. **Границы scope:** без content-based classify (`lfs`/`binary`/`encoding`/`secret`), без `skipped_file`/`file_revision`/generation-записи, без diff A→B/structural sharing (всё T05-03); без watcher/`notify`/debounce (T05-04); без `IndexConfig.excludes` (06 §2.2: «only size cap configurable in v0» — конфиг не тронут). `ignore` `WalkBuilder` доступен без доп-features (единственная feature — `simd-accel`); **0 новых зависимостей** (T10 держится), 0 git-зависимости (guardrail T15). `[SPEC]`-amendment'ы: spec 06 §1 (as-built scanner/StatCache/ScanMode/детерминизм-guards), classify doc (`classify/gitignore.rs`+`classify/mod.rs`: ignored теперь native-pruning, ignored-ветка `classify()` — defense-in-depth). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-21 |
| T05-01 | коммит `T05-01: generation lifecycle + deterministic occurrence_id` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib` 37 OK (новые unit: `registry::generation::{generation_state_round_trips, check_transition_covers_the_whole_matrix (полная 5×5 матрица — легальный набор + идемпотентные self + все нелегальные, вкл. `active→failed`/`building→active`), generation_state_rejects_corrupt_enum}`; `code::membership::{occurrence_id_matches_domain_hash_golden (64-hex golden `79e86eca…91c5` + равенство прямому `hash(Domain::OccurrenceId,…)`), occurrence_id_is_retry_and_order_independent, occurrence_id_field_boundaries_are_unambiguous}`) + `--test generation` 9 OK (`allocation_is_monotone_per_worktree`, `allocation_numbers_are_per_worktree`, **`concurrent_allocation_yields_unique_numbers`** — 24 конкурентных `tokio::spawn` allocation'ов на один worktree → номера ровно `{1..=24}`, ни одного UNIQUE-конфликта, set-проверка т.к. присвоение недетерминировано, **`allocate_unknown_worktree_rejected`** (FK-rollback), `illegal_transition_is_typed_error_and_rolls_back` (building→active → typed `Illegal`, state неизменен), `unknown_generation_transition_is_typed_error`, `happy_path_walk_succeeds` (building→projection_ready→active→retiring), **`cross_worktree_current_generation_rejected`** (allocate+transition→active под WT-A/WT-B, затем `set_current_generation(WT-B, gen-A)` → composite-FK reject, откат), **`one_active_invariant`** (позитив: retire N + promote N+1 в одной tx → `active_generations`==[N+1]; неваку­умный негатив: promote N+2 без retire → len==2)) + tripwire'ы схемы (DDL не менялась): `--test code` 13 OK, `--test migrate` 8 OK (`store_version==3`, без `ChecksumDrift`), `--test schema_audit` 5 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей — чистый Rust) | **Жизненный цикл поколения + детерминированный `occurrence_id` (без новой DDL/миграции — вся схема уже в `SCHEMA_V2`/`SCHEMA_V3`).** Новый модуль `crates/store/src/registry/generation.rs`: `GenerationState{Building,ProjectionReady,Active,Retiring,Failed}` (as_str/from_db) + чистая `check_transition` (легальный набор spec 04 §1: `building→projection_ready`, `building→failed`, `projection_ready→active`, `projection_ready→failed`, `active→retiring` + идемпотентные self; `active→failed`/`building→active` нелегальны) + `allocate_generation(tx,wt,gen_id,now)->i64` (per-worktree монотон `MAX(number)+1` над всеми состояниями, рождается `building`; неизвестный wt → FK-reject; `UNIQUE(worktree_id,generation_number)` — структурный backstop, корректность гонки держится single-writer'ом spec 03 §3) + `transition_generation` (guarded read-then-write, typed `GenerationTransitionError{UnknownGeneration,Illegal}`, no-mutation на reject, corrupt enum → `FromSqlConversionFailure` — зеркало `transition_worktree_state`) + readers `generation_state`/`active_generations` (последний возвращает только `state='active'` → `retiring`/`failed` **никогда** не роутятся, spec 04 §1 `[FIXED]`). Компаундный switch (retire N + promote N+1 + `worktree_projection_state` в одной tx, 05 §5) **отложен** (T05-03/группа 07; G05 запрещает имитировать активацию обходным update) — инвариант «ровно один active» держится процедурно порядком switch'а и доказан тестом (позитив+негатив). `crates/store/src/code/membership.rs`: свободная `occurrence_id(generation_id,normalized_path,unit_id)->String = H(occurrence_id: …)` через `identity::domain::hash` в порядке полей §1.2 (все три — text/already-hex → exact-bytes); зависит только от своего кортежа → стабилен под retry/reconcile и независим от порядка вставки (§1.2 `[FIXED]`). `insert_occurrence`/`NewOccurrence` **не тронуты** (verbatim-контракт T03-01); конструктор `deterministic` не добавлялся (owned `String` vs `&'a str` — builder T05-03 сам биндит). Реэкспорты через `registry/mod.rs`+`code/mod.rs`+`lib.rs`. `[SPEC]`-amendment'ы: spec 04 §1 (as-built lifecycle: allocate/легальный набор/guarded transition/процедурный one-active/`active_generations` не роутит retiring-failed), spec 03 §2.4 (as-built `occurrence_id` derivation). Scope-guard: **без** generation builder/structural sharing (T05-03), scheduler/triggers (T05-04), retry/failure routing (T05-05); 0 новых зависимостей (T10 держится тривиально). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-21 |
| G04 | этот коммит (gate-сверка + `PROGRESS.md` evidence в одной правке; продуктовый код и тесты НЕ тронуты) | узкие тесты группы 04: `cargo test -p local-rag-index --lib parse::` 60 OK; `--test parse_fixtures` 6 OK, `--test persist` 7 OK, `--test parse_identity` 2 OK, `--test language_coverage` 4 OK, `--test classify` 2 OK; `cargo test -p local-rag-store --test code` 13 OK, `--test schema_audit` 5 OK; `cargo test -p xtask --test adr_links` 4 OK; `python3 fixtures/tooling/validate.py` = OK (`parser/index.json` **25** кейсов: 9 TS + 8 JS + 8 Rust; категории syntax/error/empty/unicode; ids уникальны, no backend keys); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: grep всех `crates/*/Cargo.toml`+`xtask/Cargo.toml` на qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss = NONE, `git diff Cargo.lock` пуст (0 новых зависимостей) | Сверка G04 (перечитаны spec 03 §2.3/§2.3.1/§2.4, 06 §2.1, 14 §1/§5, 15 O4/O7 + ADR-0001/ADR-0002): построен трейс «требование → код → тест» (блок «G04 — трейс…» ниже), расхождений код↔спека НЕ обнаружено. Явные deliverables карточки G04 подтверждены: (a) **весь parser-корпус (25 кейсов) прогнан дважды, байт-идентичен** — `parser_output_is_byte_identical_on_reparse` re-парсит каждый кейс через свежий parser-instance для всех 3 языков (spec 14 §5), плюс `cargo xtask ci` прогнан дважды (обычный + offline), `test --workspace` зелёный оба раза; (b) **span против точного blob** — `byte_spans_are_exact`/`unicode_spans_are_byte_offsets` (unit), `parsed_unit_span_must_be_ordered` (CHECK `span_end>=span_start`), round-trip `every_produced_unit_kind_is_searchable` персистит spans в реальный `state.sqlite` над точным `source_blob`; (c) **системный schema/code audit** — `schema_audit` 5 OK (content-shared `file_revision`/`content_blob`/`parsed_unit` без path/context/generation-колонок; path-колонки только на ledger/membership; ни один FK не таргетит path-производный hash) + `persist::shared_rows_have_no_path_or_context` расширяет audit на `unresolved_reference`; вручную сверён `SCHEMA_V3` (`code/mod.rs:89`) — три content-shared таблицы path-free, `syntax_locator TEXT … NO path`. Coverage языков полон: TS/JS/Rust каждый ≥4 кейса, все категории. `[FIXED]`/`[SPEC]`/`[OPEN]` осмотрены: O4 RESOLVED (ADR-0001), O7 SyntaxLocator-часть RESOLVED (ADR-0002) / graph-часть остаётся `[OPEN]` (`resolved_graph_edge.edge_kind`, не закрыта молча), `source_blob` `[FIXED]`-инвариант цел, `[SPEC]`-amendment'ы 03 §2.3.1/§2.4 = as-built. Gate **не** писал ни features, ни gate-hardening тестов — все требуемые gate-проверки уже покрыты группой 04. Отложено by-design (seam на месте): generation-builder/occurrence_id/reconcile (group 05); универсальные kinds `config_section`/`text_section` (universal-file путь групп 05/08, вне O4-набора языков — задокументировано T04-01); graph-семантика O7 (design follow-up). Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются `resolved`); историческое evidence не переписывалось. Итог: **PASS** — группа 05 разблокирована | Claude Opus 4.8 / 2026-07-21 |
| T04-06 | коммит `T04-06: deterministic parsed-unit persistence` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test code` = 13 OK (было 11 + **`create_or_reuse_parsed_unit_reuses_by_natural_key`** (Created→Reused по natural key при другом candidate id; `parsed_unit_id_by_natural_key` Some/None; ровно 1 строка) + **`delete_unresolved_references_for_revision_is_scoped`** (удаляет только свою ревизию, сохраняя кратность `./x`×2; возвращает счётчик)) + `cargo test -p local-rag-index --test persist` = 7 OK (**`persist_is_idempotent_under_retry_with_no_duplicates`** — 2-й persist `created=0`/`reused=n`, те же `unit_ids`, без дублей; **`persisted_row_set_is_input_order_independent`** — reversed-units → идентичный natural-key set; `content_blobs_are_deduped`; `references_carry_file_source_and_kind` — import/type_import/reexport с `source_unit_id`=file unit, retry не дублирует; **`rollback_leaves_no_partial_graph`** — намеренный `Err` в tx → 0 units/refs/blobs, затем чистый persist = полный граф; `parents_link_to_persisted_ids`; `shared_rows_have_no_path_or_context` = spec 14 §5 schema-audit через `pragma_table_info`) + `--test parse_fixtures` = 6 OK (round-trip `every_produced_unit_kind_is_searchable` **отрефакторен** на `persist_parse_output` + occurrences из возвращённых `unit_ids`) + регресс `--lib parse::` 60 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; **T10 guardrail** grep = NONE; `git diff Cargo.lock` **пуст** (0 новых зависимостей — чистый код) | **Детерминированная идемпотентная персистентность parse-output (контентная сторона).** Новый оркестратор `crates/index/src/parse/persist.rs::persist_parse_output(tx, file_revision_id, LanguageId, source_blob, &ParseOutput, candidate_unit_ids: &[String], now)->PersistOutcome` — деривит content_blob по normalized-тексту каждого span, строит path-free `syntax_locator`, вставляет parsed_unit с `parent_unit_id` (канонический порядок гарантирует parent<child), затем идемпотентно refs. Всё внутри одной caller-транзакции → атомарный rollback (частичного графа нет). Новые store-примитивы: `revision.rs` `create_or_reuse_parsed_unit`+`ParsedUnitOutcome`+`parsed_unit_id_by_natural_key` (reuse по UNIQUE `(file_revision_id,unit_kind,syntax_locator,span_start,span_end)`, не по `unit_id`); `membership.rs` `delete_unresolved_references_for_revision` (scoped clear для идемпотентности refs без natural key — сохраняет легитимную кратность). Минт `unit_id` — вызывающего (`candidate_unit_ids`; прод `SystemUuidV7`, тесты `SeqUuids`), используется только на create → retry возвращает те же id (фундамент детерминированного `occurrence_id` группы 05). Реэкспорты через `code/mod.rs` + корневой `lib.rs` + `parse/mod.rs`. `[SPEC]`-amendment spec 03 §2.4 (as-built модель персистентности: create/reuse по natural key, refs delete-per-revision+reinsert, атомарность, стабильность id). Scope-guard: **без** occurrences/generation builder/reconcile (группа 05 — round-trip создаёт occurrences отдельно, вне `persist_parse_output`); **ноль новых зависимостей** (T10 держится тривиально). Отклонений не обнаружено; `DEVIATIONS.md` без изменений. **Группа 04 завершена — остаётся gate G04.** | Claude Opus 4.8 / 2026-07-21 |
| T04-05 | коммит `T04-05: Rust tree-sitter adapter + parser fixtures (closes GAP-01)` (строка evidence в том же коммите) | `cargo test -p local-rag-index --lib parse::` = 60 unit OK (новый adapter `rust::*` 13: **`grammar_loads_and_extracts_symbols`** ABI-guard; file/empty; `extracts_core_declaration_kinds_with_lang_kind` — все 11 видов включая union/macro/`function_signature_item`(trait-сигнатура)→function; byte-spans; **`impl_method_parent_and_route`** (fn в impl → parent=impl, route `impl:Host/function:method`); `mod_nesting_parent`; **`anonymous_impl_self_type_falls_back_to_ordinal`** (`impl X for (u8,u8)` → ordinal); refs classified use→import/pub use→reexport; error→fallback+recover; determinism; unicode-byte-offsets; `locator_and_signature_goldens` sig `2e25289b…330f` + полный сериализованный locator; TS/JS-регресс без изменений) + integration `--test parse_fixtures` 6 OK (многоязычный: `parser_for` +`rust`; `parser_fixtures_match_expected_units_and_refs` теперь 9 TS + 8 JS + 8 Rust, ≥4 на язык; determinism-gate spec 14 §5 по всему корпусу; `fingerprints_are_reconciled_after_linking_the_grammars` +Rust golden; **`every_language_has_a_distinct_fingerprint`** = cross-language fingerprint case; `ambiguous_extension_yields_distinct_file_revisions`; round-trip) + регресс `--test language_coverage` 4 / `--test parse_identity` 2 / `--test classify` 2 / `-p xtask --test adr_links` 4 OK; `python3 fixtures/tooling/validate.py` = OK (parser/index.json **25** кейсов, ids уникальны, no backend keys); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; **T10 guardrail**: grep всех `crates/*/Cargo.toml` = NONE; `git diff Cargo.lock` добавил **ровно 1 источник** `tree-sitter-rust 0.23.3` (транзитив `tree-sitter-language` общий, 0 новых) | **Третий язык v0 — Rust (ADR-0001 dogfooding); закрывает GAP-01.** Новый адаптер `crates/index/src/parse/adapter/rust.rs` (`RustParser`: `LanguageParser` + `LanguageSpec`) над `tree_sitter_rust::LANGUAGE` + query set `rust.scm` (function/`function_signature_item`/struct/enum/union/trait/impl/mod/const/static/type_alias/macro → symbol; `use_declaration` → ref). Rust-специфика: `local_name` для `impl_item` выводит имя из Self-типа (`type`-поле → базовый `type_identifier`, разворачивает 1 слой `generic_type`; иначе ordinal), даёт методам маршрут `impl:<Type>/function:<name>`; локальный `rust_modifiers` (visibility_modifier как **named**-узел + анонимные keyword-токены async/const/unsafe/default/extern/mut); JS/TS-common `field_text`/`body_member_count`/`is_identifier_kind`/`is_safe_segment` переиспользованы из `adapter/mod.rs`. **ABI-решение**: `tree-sitter-rust = "0.23"` (0.23.3, ABI **14**, проверено `grep LANGUAGE_VERSION` в parser.c и runtime-guard) — 0.24+ = ABI 15, который core 0.24 отверг бы → тихий `file_only`. Fixtures: `fixtures/parser/index.json` +8 Rust-кейсов (`language:"rust"`, категории syntax/error/empty/unicode; impl+методы, mod-nesting, use+pub-use refs); схема `parser.schema.json` `language` enum +`rust`; README обновлён; **`manifest.json`: GAP-01 закрыт** — parser `coverage` partial→**full**, `gaps` []→пусто, provenance +Rust, описание GAP-01 → RESOLVED (config/text universal-kinds — отдельный путь групп 05/08, не O4-gap). `[SPEC]`-amendment spec 03 §2.3.1 (Rust-грамматика слинкована, reconciled @1/@1 против `tree-sitter-rust 0.23`, ABI-14 пиннинг). CONTRIBUTING allowlist: строка `tree-sitter-rust` (MIT, 0 новых транзитивов). Scope-guard: **без** минта `unit_id`/`blob_id`, generation-builder, occurrence-политик (T04-06/группа 05); dense/model backend не тронут (T10 держится). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-21 |
| T04-04 | коммит `T04-04: JavaScript tree-sitter adapter + parser fixtures` (строка evidence в том же коммите) | `cargo test -p local-rag-index --lib parse::` = 47 unit OK (новый adapter `javascript::*` 13: **`grammar_loads_and_extracts_symbols`** ABI-guard + file/empty/kinds+lang_kind/byte-spans/method-parent/named-route+ordinal-fallback/top-level-const-vs-value/refs-classified(import+reexport)/error→fallback+recover/determinism/unicode-byte-offsets + `locator_and_signature_goldens` crate-local трипвайр (sig `ef7dab…4a60`, полный сериализованный locator); TS-регресс `typescript::*` без изменений после перевода на общие хелперы) + integration `--test parse_fixtures` 5 OK (**переименован** из `parse_typescript.rs`, многоязычный: `parser_fixtures_match_expected_units_and_refs` маршрутизирует по `case.language` — 9 TS + 8 JS кейсов, ≥4 на язык; `parser_output_is_byte_identical_on_reparse` = determinism gate spec 14 §5 по всему корпусу; `fingerprints_are_reconciled_after_linking_the_grammars` TS+JS goldens; **`ambiguous_extension_yields_distinct_file_revisions`** = spec 03 §2.3.1 / 06 §2.1 `[FIXED]`: одни байты + fp(TS) vs fp(JS) через `create_or_reuse_file_revision` → два разных `file_revision_id`, повтор с тем же fp → `Reused`; `every_produced_unit_kind_is_searchable` round-trip через `StateDb`) + регресс `--test language_coverage` 4 / `--test parse_identity` 2 / `--test classify` 2 / `-p xtask --test adr_links` 4 OK; `python3 fixtures/tooling/validate.py` = OK (parser/index.json 17 кейсов, ids уникальны, no backend keys); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; **T10 guardrail**: grep всех `crates/*/Cargo.toml` на qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss = NONE; `git diff Cargo.lock` добавил **ровно 1 источник** `tree-sitter-javascript 0.23.1` (транзитив `tree-sitter-language` общий с `tree-sitter`, 0 новых) | **Второй язык v0 — JavaScript (ADR-0001).** Новый адаптер `crates/index/src/parse/adapter/javascript.rs` (`JavaScriptParser`: `LanguageParser` + `LanguageSpec`) над `tree_sitter_javascript::LANGUAGE` + отдельный query set `javascript.scm` (function/generator/class/method/module-scope function-class-valued const → symbol; import/reexport specifiers; **без** TS-узлов interface/enum/type_alias/namespace/function_signature/abstract_* — иначе `Query::new` упал бы). JS-профиль `signature_descriptor` без type-полей (name/modifiers/parameters/value-shape/heritage/body-count). **Общий контракт усилен, не ослаблен**: идентичные примитивы (`is_identifier_kind`/`is_safe_segment`/`field_text`/`modifiers`/`heritage_text`/`body_member_count`) подняты в `adapter/mod.rs` как `pub(crate)` и переиспользованы обоими адаптерами (TS переключён, поведение не изменилось — TS goldens зелёные); TS-специфика (`has_type_token`) осталась локальной. **Ключевое решение по ABI**: `tree-sitter-javascript = "0.23"` (0.23.1, ABI 14) — 0.25 = ABI 15, который core 0.24 (max ABI 14) отверг бы → тихий `file_only`; добавлен явный `grammar_loads_and_extracts_symbols` guard. Fixtures: единый корпус (выбор владельца) — `fixtures/parser/index.json` расширен 8 JS-кейсами (`language:"javascript"`, категории syntax/error/empty/unicode), схема `parser.schema.json` `language` const→enum[typescript,javascript], README/manifest (`GAP-01` provenance) обновлены; sig/locator не в нейтральной фикстуре (crate-local). `[SPEC]`-amendment spec 03 §2.3.1 (JS-грамматика слинкована, reconciled @1/@1 против `tree-sitter-javascript 0.23`, пиннинг ABI-14 задокументирован). CONTRIBUTING allowlist: строка `tree-sitter-javascript` (MIT, ABI-обоснование, 0 новых транзитивов). Scope-guard: **без** минта `unit_id`/`blob_id`, generation-builder, occurrence-политик (T04-06/группа 05); Rust остаётся T04-05; dense/model backend не тронут (T10 держится). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-21 |
| T04-03 | коммит `T04-03: TypeScript tree-sitter adapter + parser fixtures` (строка evidence в том же коммите) | `cargo test -p local-rag-index --lib parse::` = 34 unit OK (adapter `typescript::*` — file/empty/kinds/byte-spans/method-parent/named-route+ordinal-fallback/overloads-distinct-sig/const-vs-value/refs-classified/error→fallback/determinism/Unicode-byte-offsets + `locator_and_signature_goldens` crate-local трипвайр; `signature::*` 3; `output::*` 2; `locator::*`/`fingerprint::*`/`language::*` регресс) + integration `--test parse_typescript` 4 OK (`parser_fixtures_match_expected_units_and_refs` по 9 fixture-кейсам, `parser_output_is_byte_identical_on_reparse` = determinism gate spec 14 §5, `fingerprint_is_reconciled_to_at_one_after_linking_the_grammar`, `every_produced_unit_kind_is_searchable` = round-trip через реальный `StateDb`: parse→derive_content_blob→create_or_reuse→insert_parsed_unit→insert_occurrence→read-back file/symbol/fallback_chunk) + `cargo test -p local-rag-core --lib identity::domain` 9 OK (новый `Domain::SignatureFingerprint` + golden-таблица) + `cargo test -p xtask --test adr_links` 4 OK (ADR-0002 well-formed + link-check) + регресс `--test classify` 2 / `--test language_coverage` 4 / `--test parse_identity` 2 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; **T10 guardrail**: grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/reqwest/hyper/tonic/hnsw/faiss = NONE (dense/model/network backends не тронуты); **`tree-sitter` легитимно снят с guardrail-hold** (это парсер, не dense backend) — `tree-sitter 0.24`+`tree-sitter-typescript 0.23`+`streaming-iterator` (prod) + `serde_json` (dev); `Cargo.lock` +8 источников (tree-sitter/tree-sitter-typescript/tree-sitter-language/streaming-iterator/serde_json/itoa/zmij/regex; regex/aho-corasick/memchr переиспользованы из `ignore`), все MIT / MIT OR Apache-2.0, 4 строки allowlist в CONTRIBUTING | **Первый настоящий tree-sitter адаптер (TypeScript, `tsx`-грамматика).** Новый контракт вывода `crates/index/src/parse/output.rs` (`ByteSpan`/`ParsedUnitDraft`/`ReferenceKind`/`UnresolvedRef`/`ParseOutput`, каноническая сортировка), trait `LanguageParser::parse(&[u8])->ParseOutput` (чистая, DB-free — персистентность T04-06), `SyntaxLocatorDraft`+`SyntaxLocator::from_draft` (path-free/blob-free seam). Движок `parse/adapter/mod.rs` (`LanguageSpec` seam для JS/Rust T04-04/05: query→canonical-sort→parent-стек по span-containment→error/fallback→syntax_path/ordinal→refs) + `parse/adapter/typescript.rs`+`typescript.scm` (query_version=1; function/generator/overload-sig/class/interface/enum/type_alias/namespace/method/module-scope function-valued const → symbol; file-unit; ERROR/MISSING→fallback_chunk; import/type_import/reexport specifiers). `parse/signature.rs` — канонический дескриптор→hash. **Закрыт [OPEN] O7 (часть SyntaxLocator)** через новый **ADR-0002** `docs/adr/0002-syntax-locator-derivation.md` (syntax_path = маршрут `<kind>:<name>` по именованным предкам, ordinal-fallback для анонимных/коллизий; `signature_fingerprint` = hashed domain; tsx-выбор; chunk/norm семантика; reconcile grammar_version→@1 без бампа); graph-часть O7 остаётся `[OPEN]`. **core**: `Domain::SignatureFingerprint` + `signature_fingerprint()` helper (аддитивно, `HASH_SCHEMA_VERSION`=1). Fixtures: `fixtures/parser/index.json` (9 кейсов syntax/error/empty/unicode, source→expected units/refs; sig-хеш и полный locator НЕ в нейтральной фикстуре — они crate-local golden), `fixtures/schema/parser.schema.json`, `manifest.json` parser gap-only→partial, `validate.py` маппинг, README. `[SPEC]`-amendment'ы: spec 03 §2.4 (O7 derivation→as-built), §1.2 (домен signature_fingerprint), §2.3.1 (грамматика слинкована/reconcile), 15 §4 (O7 частично RESOLVED). Scope-guard: **без** минта `unit_id`/`blob_id`, generation-builder, occurrence-политики (T04-06/группа 05); tree-sitter — единственная новая native-деп, dense/model backend НЕ тронут (T10 держится). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| T04-02 | коммит `T04-02: parser fingerprint + parser abstraction` (строка evidence в том же коммите) | `cargo test -p local-rag-index` = 35 unit OK (в т.ч. `parse::fingerprint::typescript_fingerprint_is_exact_golden` + `javascript_and_rust_goldens` + `all_languages_have_distinct_fingerprints` + `canonical_kv_is_order_independent` + `fingerprint_keys_are_ascii_sorted_and_complete` + `bumping_any_boundary_version_changes_fingerprint` + `version_constants_and_descriptors_are_pinned` + `parser_fingerprint_is_deterministic`; `parse::language::*` round-trip/selector/case/negatives; `parse::locator::syntax_locator_round_trips_both_anchors` + `serialization_is_canonical_and_path_free` + `parse_rejects_path_like_keys` + `parse_rejects_unknown_and_malformed`; `parse::parser::language_parser_reports_language_and_default_fingerprint`) + integration `--test parse_identity` 2 OK (`language_ids_match_config_language_set`, `every_config_language_parses_back_to_a_language_id`) + регресс `--test language_coverage` 4 OK / `--test classify` 2 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: grep всех `Cargo.toml` на tree-sitter/qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss = NONE, `git diff Cargo.lock` пуст = **0 новых зависимостей** | Новый модуль `crates/index/src/parse/` (реэкспортирующий `mod.rs` + `language.rs`/`fingerprint.rs`/`locator.rs`/`parser.rs`), подключён `pub mod parse;` в `lib.rs`. **`parser_fingerprint` producer** (spec 03 §2.3.1, `[FIXED semantics, format [SPEC]]`): `LanguageId{TypeScript,JavaScript,Rust}` (закрытый набор ADR-0001, `as_str`/`from_str_value`/`ALL`) + `select_language(&Path)` (extension-only, case-insensitive: ts/tsx/mts/cts, js/jsx/mjs/cjs, rs; unknown→None→универсальный путь позже); версии-константы `CHUNK_POLICY_VERSION=1`/`BOUNDARY_NORM_VERSION=1` (последняя **отлична** от store `NORMALIZATION_VERSION`) + per-language `LanguageDescriptor{grammar_name,grammar_version=1,query_version=1}`; `canonical_kv` (сортировка по ASCII-ключу, join `;`, **без хвостовой `;`** — решение владельца, совпадает с примером §2.3.1) + `FingerprintComponents::to_canonical_string` + `parser_fingerprint(LanguageId)`. **Golden**: `chunk=1;grammar=tree-sitter-typescript@1;lang=typescript;norm=1;queries=1`. **`SyntaxLocator`** (spec 03 §2.4 `[FIXED]` field-set): `{language,anchor:SyntaxAnchor(Path|LocalOrdinal),signature_fingerprint,blob_id}` — path-free структурно; каноническая сериализация (allow-set `{anchor,blob,lang,sig}`, anchor `p:`/`o:`) + `parse()` с типизированным `LocatorParseError` (отвергает файловые ключи `path/normalized_path/…` → `PathFieldForbidden`, неизвестные → `UnknownField`); двухслойная path-freedom (структура + guard). **`LanguageParser` trait** — минимальный язык-агностичный seam (`language()` + defaulted `parser_fingerprint()` → таблица дескрипторов), parse-output (spans/parents/refs) НЕ определён (scope T04-03). **`[SPEC]`-amendment'ы** (spec 03, тот же коммит): §2.3.1 (as-built формат: ASCII-sort, separator `;` без хвоста, grammar-имена, реализация версий T04-02, forward-note про rebuild-event при подключении реальной грамматики) + §2.4 (as-built сериализация локатора; явно сохранён `[OPEN]` вывод `syntax_path`/`signature_fingerprint` из дерева — O7, не решаем). Scope-guard: `store` не тронут (fingerprint остаётся opaque `&str`, **новый `Domain`-вариант не добавлен**), tree-sitter/грамматики/адаптеры/spans/parents/unresolved-refs — T04-03+; версии — объявленные константы (T10 держится, 0 SDK). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| T04-01 | коммит `T04-01: ADR-0001 first-release language set (closes O4)` (строка evidence в том же коммите) | `cargo test -p xtask --test adr_links` 3 OK (`adr_relative_links_resolve_on_disk`, `first_release_language_adr_is_well_formed`, `link_target_parser_extracts_and_classifies`); `cargo test -p local-rag-index --test language_coverage` 4 OK (`benchmark_language_is_in_the_selected_set`, `language_set_is_consistent_across_spec_and_code`, `non_corpus_languages_are_acknowledged_in_adr`, `coverage_string_helpers_are_correct`); `cargo test -p local-rag-core --lib config::` 11 OK (в т.ч. **`default_matches_spec_toml`** — verbatim SPEC_CONFIG_TOML синхронна с `Config::default()` после добавления `rust`) + `--test config` 5 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: grep всех `Cargo.toml` на tree-sitter/qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic = NONE, `git diff Cargo.lock` пуст = **0 новых зависимостей** | **Закрыт O4** — набор языков v0 = **TypeScript, JavaScript, Rust** (3 языка, в пределах `[FIXED]` «2–3»; согласовано с владельцем). Новый **ADR-0001** `docs/adr/0001-first-release-language-set.md` — **первый ADR в репозитории**, задаёт конвенцию `docs/adr/NNNN-title.md` (Nygard: Status/Context/Decision/Consequences, English под стать spec) для будущих T10-05/T11/T14-07. Обоснование: TS обязателен — единственный измеримый benchmark/baseline (corpus 100% TypeScript, 544 chunks), иначе gates T12-05 неизмеримы; JS — общая tree-sitter grammar-family с TS (min риск, max reuse); Rust — dogfooding (индексирование своей кодовой базы). Extension-маппинг (ts:.ts/.tsx/.mts/.cts, js:.js/.jsx/.mjs/.cjs, rust:.rs) — точный selector в T04-02. Разграничение scope: YAML/JSON/plain **вне** O4-набора → универсальные kinds `config_section|text_section|fallback_chunk` (06 §2.1), specified позже (T04-06/05/08). Явно зафиксирован known-limit: **нет benchmark-корпуса для JS/Rust** в v0 (TS-gate = основной сигнал; JS/Rust держатся determinism/byte-span goldens 14 §5 в T04-04/05). T04-05 = Rust (не «N/A by ADR»). Сняты все активные `[OPEN]`-маркеры про языки: `02-architecture.md:87` (`languages=["typescript","javascript","rust"]` + as-built note), `15-roadmap.md:40` (снят `[OPEN which]`) + строка O4 реестра (RESOLVED→ADR-0001), `idea.md` §«Открыто» (убран пункт «языки первого релиза»; `[FIXED]`-строка «2–3 языка» не тронута). Синхронизирован config-default `crates/core/src/config/mod.rs` (default vec + verbatim `SPEC_CONFIG_TOML` + doc-комментарии — `[OPEN]` снят, ссылка на ADR-0001; иначе был бы code↔spec deviation). 2 новых теста в стиле `ci_config.rs`, парсинг через substring/typed `Config` (**без `serde_json`**, workspace остаётся dependency-free). Scope-guard: production parser adapters/грамматики/`parser_fingerprint` — T04-02+ (guardrail T10 держится, 0 dense/model/parser SDK). **Не тронут** `03-data-model.md:422 edge_kind [OPEN]` = это O7, не O4. Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| T03-01 | коммит `T03-01: code-storage DDL + typed repositories` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test code` OK (9 integration: happy-path insert+read (exact BLOB round-trip `hello\n`, size/compression, reuse-key/member lookups, occurrence count); revision-reuse-key = `(content_hash, parser_fingerprint)` (дубль пары → `SQLITE_CONSTRAINT_UNIQUE`; тот же content_hash + другой fingerprint → 2 ревизии, §2.3.1 `.c` vs `.cpp`); parsed_unit span-ordered (`span_end==span_start` OK; инверсия → `SQLITE_CONSTRAINT_CHECK`); parsed_unit FK unknown blob/revision → `FOREIGNKEY` + unique-locator → `UNIQUE`; generation_file FK unknown gen/revision → `FOREIGNKEY`; **occurrence-requires-generation_file** (non-member path → `FOREIGNKEY`; после `insert_generation_file` → OK; дубль `(gen,path,unit)` → `UNIQUE`; unknown unit → `FOREIGNKEY`); skipped_file reason round-trip через `skip_reason` + occurrence на skipped-пути → `FOREIGNKEY` (skip⇒no-occurrence структурно) + out-of-domain reason raw SQL → `CHECK`; unresolved_reference/resolved_graph_edge FK + resolution out-of-domain → `CHECK`; code-tables/indexes exist); `--lib code` OK (11 unit: round-trip `SourceCompression`/`NewlineStyle`/`UnitKind`/`SkipReason`/`EdgeResolution` + `skip_reason_rejects_corrupt_enum` → `FromSqlConversionFailure(0,Text,_)` через constraint-free in-memory таблицу); **schema audit** `--test schema_audit` OK (4: `no_foreign_key_targets_a_path_hash_or_stray_path_column` — FK на `*_fingerprint` запрещён абсолютно, FK на path-колонку только на sanctioned `generation_file` (§2.4 composite FK); `path_columns_live_only_on_path_bearing_tables` — path только на ledger+membership; **`content_shared_tables_carry_no_path_or_context_field`** — `file_revision`/`content_blob`/`parsed_unit` без path/context/generation-полей (§5.1); `column_classifiers_discriminate`); регресс `--test migrate` 8 OK (обновлён `state_db_open_bootstraps_and_is_idempotent`: applied 2→3, +8 code-таблиц, строка `(3,code)` — усиление), `--test registry` 11 OK (обновлён `migration_produces_exact_registry_schema`: набор `[(1,registry),(2,worktree),(3,code)]`), `--test worktree` 12 / `--test resolve` 11 / `--test settings` 11 / `--test cache` 10/11 / `--test state` 6 / `--test migrate_resumable` 6/7 OK; `cargo test -p local-rag-store --features failpoints` = 11 lib + все integration OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE (T10 держится); 0 новых зависимостей | Миграция v3 `code` (`code::SCHEMA_V3`, byte-exact §2.3+§2.4: `file_revision`+`UNIQUE(content_hash,parser_fingerprint)` / `content_blob` / `parsed_unit`+`CHECK(span_end>=span_start)`+unique-locator / `generation_file` (PK `(generation_id,normalized_path)`) / `skipped_file`+reason-CHECK / `generation_unit_occurrence`+composite FK на `generation_file` (структурный source-blob инвариант 12 §5) + индексы `occurrence_by_gen`/`occurrence_by_unit` / `unresolved_reference`+`unresolved_by_rev` / `resolved_graph_edge`+resolution-CHECK), добавлена в `migrate::ALL` (frozen checksum). Новый модуль `crates/store/src/code/{mod,revision,membership}.rs`: typed enum'ы (`SourceCompression`/`NewlineStyle`/`UnitKind`/`SkipReason`/`EdgeResolution` — `as_str`/`from_db`, corrupt-read → `FromSqlConversionFailure` как `worktree.state`); typed repositories (write через `&Transaction`, read через `&Connection`, как registry): `insert_file_revision`(+`NewFileRevision`, `now_ms`)/`insert_content_blob`(+`NewContentBlob`)/`insert_parsed_unit`(+`NewParsedUnit`)/`file_revision_id_by_content_key`; `insert_generation_file`/`insert_skipped_file`/`insert_occurrence`(+`NewOccurrence`)/`insert_unresolved_reference`(+`NewUnresolvedReference`)/`insert_resolved_edge`(+`NewResolvedEdge`)/`member_file_revision`/`skip_reason`. Scope-guard: **без** parser/hash/encoding/zstd (T03-03), классификации (T03-02), normalized-text cache (T03-04), детерминированного `occurrence_id`/generation-builder (group 05) — id/хеши подаёт вызывающий. Ре-экспорты `code/mod.rs`+`lib.rs`; crate-doc абзац. **Schema audit уточнён** (в scope карточки «schema audit forbidden path columns», НЕ ослабление; §5.1 + §2.4 нормативны): (1) запрет FK на path-**хеш** (`*_fingerprint`) остался абсолютным, FK на path-колонку разрешён только на sanctioned `generation_file` (единственный composite FK §2.4); (2) path-колонки разрешены на ledger+membership (`PATH_MEMBERSHIP_TABLES`); (3) **новый позитивный тест** `content_shared_tables_carry_no_path_or_context_field` — усиление гарантии §5.1 для content-shared §2.3. Инвариант 01 §5.1 соблюдён: content-shared строки без path/context/generation-полей; occurrence-identity path-scoped by design (§5 identity ladder). Найдено пре-существующее: интермиттентный флап `cache.rs::recreate_is_idempotent_on_retry` под полной параллельной нагрузкой workspace (helper `cache_meta` глотает transient error через `.ok()`→None; cache-код T01-05 корректен, изолированно 8/8 зелёный, финальный CI зелёный) — вне scope T03-01, вынесено пользователю для решения (кандидат в D-003). Отклонений в scope T03-01 не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| T03-02 | коммит `T03-02: file classification + skip reasons` (строка evidence в том же коммите) | `cargo test -p local-rag-core --lib redaction` 8 OK (версия/стабильность `REDACTION_VERSION=1`; PEM private-key; credential-форматы AWS/GitHub/Slack + отказ на bare-префиксе; assigned quoted secret, но НЕ unquoted-код и НЕ substring-ключ; high-entropy base64 flagged / 40-char hex-SHA нет — дискриминация по порогу 4.5 бит/симв; clean-код без находок; ascending-спаны в input; энтропия равномерного алфавита = 1 бит); `cargo test -p local-rag-index` 18 unit + 1 integration OK — детекторы (NUL / binary-ext / extension-edge; LFS pointer + near-miss; UTF-8 gate на `0xFF 0xFE 0x41` — не NUL, не binary-ext → encoding); gitignore (root, nested-deeper-wins via `!`-negation, nested-deeper-adds, configured excludes, malformed→typed `ignore::Error`); classify — clean→Indexed, **fixture per reason** (ignored/huge/lfs/binary/encoding/secret), **huge exact-edge** (`==cap`→Indexed, `==cap+1`→Huge), precedence (ignored≻всё, huge≻binary/secret, binary≻secret), cap из `IndexConfig`; integration `classified_skip_becomes_non_member_with_no_occurrence` — classify→`insert_skipped_file`→`member_file_revision`==None + `skip_reason`==ожидаемый + occurrence на skipped-пути → `SQLITE_CONSTRAINT_FOREIGNKEY` (skip⇒no-occurrence через seam классификатора); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); guardrail T10: grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic/hnsw/faiss = NONE; единственная новая внешняя деп — `ignore` (scope `crates/index`, allowlist CONTRIBUTING) | **Precedence авторски задан и задокументирован** (`[SPEC]`, spec 06 §2.2): цепочка предусловий `ignored → huge → lfs → binary → encoding → secret`, first-match-wins, иначе Indexed (spec фиксирует множество из 6 причин, но НЕ порядок; PK `skipped_file` требует ровно одну причину). Новый общий версионированный secret scanner `crates/core/src/redaction/` (`REDACTION_VERSION=1`, `Scanner::{new,version,scan,has_secret}`, `Finding{kind,start,end}`) — hand-rolled, без regex/новых деп; rule set v0 `[SPEC]` (PEM / credential-префиксы / assigned quoted secret / high-entropy ≥40 симв ≥4.5 бит) — переиспользуем будущими spool (13) / remote (16) flow'ами. Новый классификатор `crates/index/src/classify/{mod,detect,gitignore}.rs`: `classify(path,size,content,&GitignoreSet,&ClassifierConfig,&Scanner)->Classification{Indexed|Skipped(SkipReason)}`; детекторы `is_binary`(NUL 8 KiB + built-in `BINARY_EXTENSIONS`)/`is_lfs_pointer`/`is_valid_utf8`; `GitignoreSet`/`GitignoreSetBuilder` поверх крейта `ignore` (`gitignore` matcher, nested/negation/precedence; синтетический root — тесты без диска). `encoding` = только UTF-8-gate (полное encoding/newline detection — T03-03). Scope-guard: **без** content-hash/zstd/reuse (T03-03), normalized cache (T03-04), tree-walk/`ignore::Walk` (T05-02), payload-redaction transform (13/16). `[SPEC]`-amendment'ы: spec 06 §2.2 (precedence + семантика детекторов), spec 12 §2 (rule set v0 + `redaction_version`). CONTRIBUTING allowlist: строка `ignore` (обоснование spec 06 §2, лицензия Unlicense OR MIT, транзитивный набор globset/aho-corasick/regex-automata/regex-syntax/bstr/memchr/log/same-file/walkdir/crossbeam-*). Отклонений в scope T03-02 не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| D-003 | коммит `D-003: cache read-helper transient-busy robustness` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test cache` OK (12: было 10 + 2 regression `read_helper_classifiers_discriminate`, `read_helpers_distinguish_present_from_absent`; `recreate_is_idempotent_on_retry` зелёный); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); стресс-подтверждение: `cargo test --workspace` 20/20 без падений после фикса (до фикса 1 падение во время `xtask ci`; base `bb683c9` 0/20, current до фикса 0/20 — флап редкий, нагрузочно-зависимый); фикс структурный, поэтому конкретный класс флапа исключён by construction | Устранён недетерминизм тест-helper'ов в `crates/store/tests/cache.rs` (**только тест-код**; продуктовый cache-путь T01-05 корректен — read-коннект уже с `busy_timeout(5000)`, `open.rs:137` — и НЕ тронут). Корень: `cache_meta` (`.ok()`) и `probe_present` (`unwrap_or(false)`) превращали *любую* ошибку в отсутствие → transient `SQLITE_BUSY_SNAPSHOT` (который `busy_timeout` не пережидает) на свежем read-коннекте сразу после recreate маскировался как `None`/`false`. Фикс: helper `read_optional` (+классификаторы `is_transient` по `ErrorCode::DatabaseBusy|DatabaseLocked`, `is_no_such_table` по тексту сообщения) — `None` строго на `QueryReturnedNoRows`, retry transient на свежем коннекте (без wall-clock sleep — busy_timeout пережидает обычную contention, свежий snapshot снимает `BUSY_SNAPSHOT`), «no such table» → легитимный `false` для `probe_present`, любая иная ошибка → громкая паника (не маскируется). Regression: `read_helper_classifiers_discriminate` (transient-семейство vs no-such-table vs generic vs QueryReturnedNoRows) + `read_helpers_distinguish_present_from_absent` (present→Some, absent-key→None, absent-table→false, present→true). Соответствует правилу детерминизма (CLAUDE.md/README DoD); тест не ослаблен (`.ok()`-маскировка убрана, дискриминация усилена). `DEVIATIONS.md` D-003 → resolved | Claude Opus 4.8 / 2026-07-20 |
| T03-03 | коммит `T03-03: exact source_blob/file_revision reuse` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib code::source` OK (7 unit: `content_hash` == `domain::hash(Domain::FileContent,&[raw])` + hex-64 lowercase; non-ASCII/empty дают разные дайджесты + пустой файл = one-empty-field ≠ domain-only `&[]`; `detect_newline_style` lf/crlf/mixed + no-newline/empty/lone-CR→lf; `compress_keeps_smaller_frame` (8 KiB одного байта → `Zstd`, blob<size, decode round-trip); `compress_declines_on_incompressible` (1 байт → `None`, blob==raw); `decode_source` round-trips обоих тегов на lf/crlf/mixed/non-ASCII/empty; `prepared_source_fields_consistent` (size=uncompressed, encoding "utf-8", blob≤raw)); `cargo test -p local-rag-store --test source` OK (6 integration через реальный `StateDb`/`TempHome`/фикс. `now_ms`/`uuid(seed)`: `roundtrip_lf_crlf_mixed_and_non_ascii` — `source_bytes` байт-в-байт == вход + `newline_style`/`source_size` корректны; `compression_roundtrips_in_db` — compressible → колонка `source_compression`=='zstd' ∧ `length(source_blob)`<`source_size` ∧ exact round-trip, incompressible → 'none'; `same_key_reuses_row` — 2-й вызов `Reused(id1)`, другой `new_id` игнорится, `count(*)`==1; `different_fingerprint_separates` — те же байты .c vs .cpp → 2 `Created` с разными id, count==2; `live_file_mutation_does_not_affect_stored` — мутация входного буфера после ingest не меняет хранимое (независимая копия); `reuse_is_idempotent_on_retry` — 3 транзакции → 1 `Created` + 2 `Reused`, count==1); регресс `--test code` 9 OK, `--test schema_audit` 4 OK (DDL/`SCHEMA_V3` frozen — 0 новых колонок), `--test migrate`/`registry`/`worktree`/`resolve`/`settings`/`cache`/`state`/`migrate_resumable` без изменений; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); T10 guardrail: grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic/hnsw/faiss = NONE; единственная новая внешняя деп — `zstd` (0.13, scope `crates/store`, allowlist CONTRIBUTING) | Новый модуль `crates/store/src/code/source.rs` — ingestion-слой над примитивами T03-01, **без изменения схемы**. Pure `prepare_source(&[u8])->PreparedSource` (content_hash + newline + encoding + keep-if-smaller zstd) исполняется вне единственного writer-потока; `content_hash` через generic `local_rag_core::identity::domain::hash(Domain::FileContent,&[raw])` (без typed-конструктора в core — по конвенции `domain.rs` content/deterministic-домены хеширует owning task); `detect_newline_style`/`detect_encoding`; `decode_source`+`source_bytes` (read-back точных байт; corrupt tag/frame — невозможно при штатных INSERT из-за CHECK+`encode_source` — surфейсится как `FromSqlConversionFailure`, не паника); atomic `create_or_reuse_file_revision(&Transaction,&PreparedSource,fingerprint,new_id,now_ms)->RevisionOutcome{Created|Reused}`. Ре-экспорты `code/mod.rs`+`lib.rs`; deferral-доки `revision.rs`/`mod.rs`/`lib.rs`/`detect.rs` теперь указывают на `source`. **`[SPEC]`-решения** (spec 03 §2.3/§2.3.1, задокументированы в docs модуля): (1) политика сжатия — zstd level 3 (`SOURCE_ZSTD_LEVEL`), keep-if-smaller (frame хранится только если строго меньше raw, иначе `none`+raw → blob никогда не растёт), `source_size` всегда несжатый, **сжатие вне identity** (reuse-ключ только `(content_hash,parser_fingerprint)`, фрейм не хешируется → level перенастраивается без миграции); (2) `newline_style`: no-newline/empty → `lf` дефолт, lone-CR (classic Mac) не представим в v0 enum → как no-`\n`→lf (влияет только на метаданные, не на байты); (3) `source_encoding`="utf-8" (`SOURCE_ENCODING_UTF8`) фиксирован в v0 — non-UTF-8 отсеян классификатором T03-02, `prepare_source` несёт `debug_assert`-инвариант, без runtime-транскодирования. **Атомарность**: SELECT-then-INSERT race-free под single bounded-writer (02 §5), идемпотентно на retry (replay → `Reused`, без дубля); rejected-alternative `INSERT ... ON CONFLICT DO NOTHING` (замаскировал бы баг вставки одинакового ключа + потребовал бы менять frozen-стиль `insert_file_revision`, чей T03-01-тест ассертит `SQLITE_CONSTRAINT_UNIQUE`). Зависимость `zstd = "0.13"` (`crates/store`) — vendored libzstd via `zstd-sys`/`cc` (bundled/offline как rusqlite); allowlist-строка CONTRIBUTING (license MIT / BSD-3-Clause OR GPL-2.0; транзитив `zstd`/`zstd-safe`/`zstd-sys` + **build-time** `cc`/`jobserver`(/`getrandom`/`r-efi` на неродных таргетах) — не линкуются в бинарь, не dense/model/network SDK); `Cargo.lock` +6. Scope-guard: **без** построения `parser_fingerprint` (T04-02 — здесь непрозрачная строка), normalized-text cache (T03-04), async `StateWriter`-обёртки/generation-builder/`occurrence_id` (group 05). Отклонений в scope T03-03 не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| T03-04 | коммит `T03-04: normalized text cache regeneration` (строка evidence в том же коммите) | `cargo test -p local-rag-store --lib code::normalize` OK (11 unit: CRLF/lone-CR→LF; leading-BOM снят, interior U+FEFF сохранён; NFC `e+U+0301`→`é` (1 codepoint); trailing-WS trim построчно, leading сохранён; финальный `\n`/число строк сохранены; пустой вход/только-BOM→`""`; **идемпотентность** `normalize(normalize(x))==normalize(x)` на 5 входах; **golden** `content_blob_id(1,"rust",1,"fn main() {}\n")==908f1ded…2357` (пиннит порядок полей и u32-LE ширину); каждое из 4 полей меняет хеш; `integer_fields_use_u32_not_i64_width` (u32≠i64-кодировка); `derive_is_self_consistent`); `--lib cache::text` OK (2 unit: `verify_cached_text` матчит деривацию, отвергает битый текст/чужой язык; `batching_dedups_to_latest_timestamp` — latest-wins, drain очищает); `cargo test -p local-rag-store --test normalized_text` OK (6 integration, реальные `StateDb`+`CacheDb`/`TempHome`/фикс.`now_ms`, D-003-style retry-читалка: `normalized_text_cache_table_exists_after_open` (`CACHE_SCHEMA_VERSION==2`, get на пустом кэше→None доказывает таблицу); `roundtrip_derive_cache_read`; **`cache_delete_reconstructs_identically_from_source_blob`** — ingest→`source_bytes`→derive→cache→**delete**→regenerate из `source_blob`→идентичные `blob_id`/`normalized_text`/`byte_size` (06 §4); **`invalid_row_is_detected_and_regenerated`** — битый текст под валидным blob_id → `verify_cached_text`==false → evict+regenerate → true; `content_blob_state_row_has_no_text_and_reuse_is_idempotent` — 3× create-or-reuse = 1 Created+2 Reused, `count(*)==1`, `PRAGMA table_info(content_blob)` без `normalized_text`; `last_used_batching_seam_flushes` — record(dedup)→drain→`flush_last_used`==2 applied, `last_used_at` обновлён/`created_at` нетронут, evicted-blob пропущен); `cargo test -p local-rag-store --test schema_audit` OK (6: +новый **`no_state_table_stores_normalized_text`** — ни одна таблица `state.sqlite` не несёт колонку `normalized_text`); регресс `--test cache` 12 OK (бамп версии безопасен — не захардкожена), `--test code`/`source`/`migrate`/`registry`/`worktree`/`resolve`/`settings`/`state`/`migrate_resumable` без изменений; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); T10 guardrail: grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic/hnsw/faiss = NONE; `Cargo.lock` +1 строка (ребро зависимости `unicode-normalization` у `local-rag-store`) = **0 новых внешних источников** (крейт уже в lock через `local-rag-core`); стресс `cargo test -p local-rag-store` 5/5 прогонов зелёные | Новый модуль `crates/store/src/code/normalize.rs` (чистые функции, off-writer): `ALGO_VERSION=1`/`NORMALIZATION_VERSION=1` (u32); `normalize(&str)->String` — **v0 pipeline `[SPEC]`** (по согласованию с владельцем): strip leading BOM `U+FEFF` → CRLF/lone-CR→LF → Unicode **NFC** → trim trailing-WS построчно (split `\n`/`trim_end`/rejoin — сохраняет число строк и финальный `\n`), детерминирован+идемпотентен; `content_blob_id(algo:u32,lang,norm:u32,text)->String` через `local_rag_core::identity::domain::hash(Domain::ContentBlob,…)` — **первый int-домен в кодовой базе**, фиксирует «declared width» = **LE u32** (порядок полей строго по §1.2); `DerivedContentBlob{blob_id,normalized_text,byte_size,algo_version,normalization_version}` + `derive_content_blob(lang,src)` (byte_size=UTF-8-длина). `crates/store/src/code/revision.rs`: `BlobOutcome{Created|Reused}` + `create_or_reuse_content_blob(tx,&DerivedContentBlob,lang,now_ms)` (reuse по blob_id-PK, идемпотентно) + `content_blob_exists`. Новый `crates/store/src/cache/text.rs`: repo `normalized_text_cache` — `insert_normalized_text` (`ON CONFLICT(blob_id) DO UPDATE last_used_at`), `get_normalized_text` (`.optional()` → None только на QueryReturnedNoRows, transient BUSY **пробрасывается** — дух D-003), `delete_normalized_text`, `verify_cached_text` (пересчёт identity = «invalid row/checksum path», у таблицы нет checksum-колонки); **батч-seam `[SPEC]`**: trait `LastUsedSink` + `BatchingLastUsed` (in-mem dedup-до-latest, `drain`) + `flush_last_used` (один батчевый UPDATE) — cadence-драйвер (≤5с/500 строк) отложен. `crates/store/src/cache/open.rs`: `NORMALIZED_TEXT_CACHE_DDL` (byte §4.2) создаётся в `seed_binding` вместе с `cache_meta` (одна tx); `CACHE_SCHEMA_VERSION` 1→2 (старый кэш авто-rebuild через §4.4 step 2, миграций у кэша нет). Ре-экспорты `code/mod.rs`+`cache/mod.rs`+`lib.rs`; scope-note `code/mod.rs` обновлён. Зависимость `unicode-normalization="0.1"` добавлена в `crates/store/Cargo.toml` (уже в lock через core → 0 новых источников); allowlist-строка CONTRIBUTING расширена scope на `crates/store`. **`[SPEC]`-amendment'ы** (spec 03, тот же коммит): §1.2 (int-поля домена = LE u32), §3 (батч-seam as-built), §4.2 (нормализация v0 + `normalized_text_cache` создаётся T03-04 + `CACHE_SCHEMA_VERSION=2` + recompute-identity вместо checksum). Scope-guard: **без** `parser_fingerprint` (T04-02), реальных `parsed_unit`/`occurrence_id`/generation-builder (05), фонового флаш-драйвера, `embedding_cache`/FTS таблиц (T08/T11), оркестратора reconcile/search «get-or-regenerate» (05/06/09). Нормализация content_blob не является `[OPEN]` (реестр O1–O8 её не содержит) — задана как `[SPEC]` с документированием, ничей `[OPEN]` не разрешён молча. Отклонений в scope T03-04 не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-20 |
| G02 | этот коммит (gate-сверка + gate-hardening тест + `PROGRESS.md` в одной правке) | узкие тесты группы 02: `cargo test -p local-rag-store --test registry` 11 OK, `--test worktree` 12 OK, `--test resolve` 11 OK, `--test settings` 11 OK; `cargo test -p local-rag-core` 64 unit + 4 doctests OK (identity: uuidv7 6 / domain 9 / path 10 / remote 6 + config 13; включая golden_hashes_for_every_domain, field_boundaries_do_not_collide, hash_is_deterministic_under_retry, most_restrictive_covers_every_pair), `--test config` 5 OK (в т.ч. `config_is_not_read_from_inside_a_repository`); **новый gate-hardening** `cargo test -p local-rag-store --test schema_audit` 3 OK (`no_foreign_key_targets_a_path_or_fingerprint_column`, `path_columns_live_only_on_ledger_tables`, `column_classifiers_discriminate`); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-core -p local-rag-store --no-deps` clean; guardrail T10: grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE, `Cargo.lock` 57 источников, `git diff Cargo.lock` пуст (gate-hardening тест = 0 новых зависимостей) | Сверка G02 (перечитаны spec 01 §5, 02 §3, 03 §1–2.1, 04 §7, 12 §7): построен трейс «требование → код → тест» (блок «G02 — трейс…» ниже), расхождений код↔спека НЕ обнаружено. Schema-аудит (явное требование карточки G02) подтверждён: (a) ни один durable PK/FK не указывает на path-производное значение — все identity-PK суть UUIDv7 (`repo_id`/`worktree_id`/`generation_id`), все FK ссылаются на UUID-колонки, пути живут только в ledger-таблицах `repository_path`/`worktree_path` (composite PK со scope на UUID, не FK-цель), `path_fingerprint` — lookup-only; (b) нет ambient current project (routing только через explicit `RequestRoot`→`resolve`, `session_id` routing-only) и нет второго current path (partial unique index `*_path_current WHERE is_current=1` + clear-then-set); (c) identity-колонки — UUID, remote — nullable non-unique hash-hint. Все затронутые группой `[FIXED]`/`[SPEC]` = as-built+verified; `[OPEN]`-числа 02 §3.1 (`retired_generations_keep`/`_ttl_h`/`languages`) остаются provisional (не закрыты молча). Gate-hardening (только тест + docs; не feature, не D-NNN): `crates/store/tests/schema_audit.rs` — schema-wide DDL-lint, реализующий механизм enforcement #2 из 01 §5.1 («schema lint test that greps the DDL for forbidden column placements»); итерирует все таблицы мигрированного `state.sqlite` через `pragma_table_info`/`pragma_foreign_key_list`, форсит (1) ни один FK не таргетит path/fingerprint-колонку, (2) path-колонки только на ledger-таблицах — покрывает будущие content-shared таблицы групп 03/11 автоматически. Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002 остаются resolved); историческое evidence не переписывалось. Итог: **PASS** — группа 03 разблокирована | Claude Opus 4.8 / 2026-07-17 |
| G03 | этот коммит (gate-сверка + 3 gate-hardening теста + `PROGRESS.md` в одной правке) | узкие тесты группы 03: `cargo test -p local-rag-store --test code` 11 OK (+2 новых: `occurrence_implies_member_file_with_non_null_source_blob`, `source_blob_is_not_nullable`), `--test source` 6 OK, `--test normalized_text` 6 OK, `--test cache` 12 OK, `--test schema_audit` 5 OK; `cargo test -p local-rag-index --test classify` 2 OK (+1 новый: `every_skip_reason_yields_no_occurrence_and_no_source_blob`, все 6 reasons); под `--features failpoints`: `--test code` 11 OK, `--test cache` 13 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; guardrail T10: grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE, `git diff Cargo.lock` пуст (gate-hardening = 0 новых зависимостей) | Сверка G03 (перечитаны spec 03 §2.3–2.4/§4.2, 06 §2/§2.1/§2.2, 12 §2/§5): построен трейс «требование → код → тест» (блок «G03 — трейс…» ниже), расхождений код↔спека НЕ обнаружено. Явные deliverables карточки G03 подтверждены: (a) **property** `occurrence ⇒ generation_file ⇒ file_revision.source_blob` — структурен (composite FK `code/mod.rs:152-154` + `source_blob BLOB NOT NULL` `code/mod.rs:92`) и теперь покрыт end-to-end новым SQL-sweep-тестом (0 сирот, non-vacuous) + негативным NOT-NULL; (b) **для каждого skip reason обратное отсутствует** — новый тест покрывает все 6 (binary/lfs/huge/secret/ignored/encoding: non-member ⇒ нет source_blob + occurrence отвергается membership FK; было только Binary/Ignored); (c) **удалить cache → восстановление normalized text** — `cache_delete_reconstructs_identically_from_source_blob` (идентичные blob_id/text/byte_size из `source_blob`); (d) **schema audit** (обязателен) зелёный — 5 тестов, итерируют все таблицы. «Property test» реализован детерминированным инвариант-тестом (перечисленные кейсы + SQL-обход), НЕ рандомизированным proptest — соблюдён детерминизм (CLAUDE.md), 0 новых зависимостей. Gate-hardening (только тест-код + PROGRESS; продуктовый код не тронут — не feature, не D-NNN, прецедент G01/G02). `[OPEN]` осмотрены: `resolved_graph_edge.edge_kind` (03 §2.4) остаётся open (edge_kind без домена-CHECK), не закрыт молча. Отложено by-design (seam на месте): generation-builder/occurrence_id/reconcile (05), реальный parser_fingerprint (04), search-facing snippet/context/overview + 8 KiB cap (09/12). Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются resolved); историческое evidence не переписывалось. Итог: **PASS** — группа 04 разблокирована | Claude Opus 4.8 / 2026-07-20 |
| T02-05 | коммит `T02-05: config merge + data-policy ordering` (строка evidence в том же коммите) | `cargo test -p local-rag-core --lib config` OK (13: `data_policy_string_round_trips_and_rejects_bogus` — round-trip всех 4 вариантов + `bogus`/`LOCAL_ONLY`/`""` → None; `data_policy_default_is_local_only`; `most_restrictive_covers_every_pair` — все 16 пар = strictest-of-pair + коммутативность + идемпотентность на диагонали + spot-check spec-порядка; `default_matches_spec_toml` — `parse_toml(SPEC §3.1 verbatim) == Config::default()` (страж дрейфа дефолтов); `empty_toml_is_all_defaults`; `partial_toml_defaults_missing_keys_and_sections`; `unknown_keys_are_ignored` (ленивость — незнакомые ключ/секция игнорируются); `unsupported_schema_version_is_rejected` → `UnsupportedSchemaVersion{2,1}`; `invalid_data_policy_is_rejected_not_defaulted` → `InvalidDataPolicy{"yolo_remote"}` (без тихого дефолта); `malformed_toml_is_rejected` → `Toml`; `config_toml_path_joins_filename`); `cargo test -p local-rag-core --test config` OK (5: missing-file→defaults, missing-dir→defaults, present→parsed, present-invalid→typed `InvalidDataPolicy`, **`config_is_not_read_from_inside_a_repository`** — `config.toml`/`.local-rag.toml` подброшены в repo-каталог, `load(config_dir)` вернул defaults `local_only`, доказывая единственный вход `config_dir`); `cargo test -p local-rag-store --test settings` OK (11: generic round-trip; upsert-идемпотентность (2× запись ключа → 1 строка, latest wins); FK-отказ на unknown repo → `WriteError::Sqlite`, 0 строк; typed data_policy round-trip всех 4 + хранение под mirrored `data_policy`; unset→None; corrupt stored → `FromSqlConversionFailure(0,_,_)`; **every-pair** effective = `most_restrictive` (16 комбо через реальный StateDb); `repo_cannot_relax_global` (global=metadata_only_remote + repo=allow_remote_full → metadata_only_remote — end-to-end поведенческая проверка); repo-без-настройки→global; multi-repo tightening order-independent (forward==reverse); `repo_settings` listing ordered-by-key); регресс `--test registry` 11, `--test worktree` 12, `--test resolve` 11, `--test migrate` 8, `--test cache` 10, `--test state` 6, `--test migrate_resumable` 6 — все OK (SCHEMA_V1/V2 checksum не тронут); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-core -p local-rag-store --no-deps` clean; `Cargo.lock` источников 45→57 (+12: toml 0.8.23/toml_edit/toml_datetime/toml_write/serde_spanned/winnow/memchr/indexmap/equivalent + serde/serde_core/serde_derive 1.0.228; proc-macro2/quote/syn/unicode-ident/hashbrown переиспользованы из rusqlite-subtree, не новые); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE (T10 держится) | Новый модуль `crates/core/src/config/mod.rs`: `DataPolicy` (4-вариантный enum без serde: `as_str`/`from_str_value`/`restrictiveness_rank`/`most_restrictive` (strictest-wins, коммут./ассоц.)/`Default=LocalOnly`); типизированный `Config` (+`DaemonConfig`/`StorageConfig`/`ModelsConfig`/`IndexConfig`, `Default` = дословные defaults 02 §3.1) с `parse_toml`/`load(<config_dir>)`/`config_toml_path`; приватные `RawConfig`/`RawModels` (serde `Deserialize` + `#[serde(default)]`, **без** `deny_unknown_fields` → ленивость) → валидация `from_raw` (schema_version==1 иначе `UnsupportedSchemaVersion`; parse data_policy иначе `InvalidDataPolicy`); typed `ConfigError`(`Io`/`Toml`/`UnsupportedSchemaVersion`/`InvalidDataPolicy`)+Display/Error. **«No repo-local lookup»** структурно: единственный вход — `config_dir` (нет API с worktree/repo root). Новый модуль `crates/store/src/registry/settings.rs` (зеркалит `repository.rs`: запись `&Transaction`, чтение `&Connection`): generic `set_repo_setting`(upsert `ON CONFLICT(repo_id,key) DO UPDATE`, FK-enforced)/`get_repo_setting`/`repo_settings`(ordered); typed `set_repo_data_policy`/`repo_data_policy` (ключ `DATA_POLICY_KEY="data_policy"`, corrupt → `FromSqlConversionFailure`); `effective_data_policy(global,conn,repo_ids)` — fold `most_restrictive` (детерминирован, repo только ужесточает). Ре-экспорты `core/lib.rs` (`Config`/`ConfigError`/`DataPolicy`), `registry/mod.rs`+`store/lib.rs`. Без новой миграции (generic `repo_settings` из SCHEMA_V1). Deps (утв. пользователем): `toml`+`serde(derive)` в `core/Cargo.toml`, 2 allowlist-строки в `CONTRIBUTING.md` (обоснование+транзитив+license MIT/Apache-2.0). `[SPEC]`-правки (тот же коммит): 02 §3.1 (validation policy: missing→defaults, version MUST=1, invalid enum→error, unknown keys ignored, `[OPEN]` числа provisional, single config path), 02 §3.2 (effective merge as-built, guard отложен T11/T16), 03 §2.1 (T02-05 as-built note + снята пометка «T02-05»). Guard в provider pool — вне scope (T11/T16). Инвариант 02 §3.2 «repo не ослабляет global» и 02 §6 «nothing degrades silently» соблюдены. Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-17 |
| T02-04 | коммит `T02-04: attach/move/non-git resolution` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test resolve` OK (11: directory_move_preserves_repo_and_worktree_ids — те же repo/worktree id после detach→attach@/new, resolve@/new=Resolved, resolve@/old=GlobalOnly, 1 worktree/1 repo, state Active, /old в истории `is_current=0` обеих сторон; recreated_path_does_not_steal_identity — worktree переезжает /p→/p2 (Active), resolve@/p=GlobalOnly (fp-match отсеян т.к. Active), свежие R2/W2@/p → resolve@/p=Resolved{R2,W2}≠{R,W}, старый цел; linked_ambiguity_requires_id — 2 detached linked одного repo → Ambiguous[WL1,WL2] (sorted), repo-hint тоже Ambiguous, attach(WL1)→resolve@/moved=Resolved{R,WL1}; unknown_root_resolves_global_only — /never/seen и RequestRoot::default() → GlobalOnly; non_git_happy_path — NonGit resolve + move синкает repository_path; attach_unknown_worktree→Ok(Err(UnknownWorktree)) 0 строк; attach_repo_mismatch→Ok(Err(RepoMismatch)) без мутации; attach_removing_is_not_reattachable→Ok(Err(NotReattachable{Removing→Active})) state цел, путь не наблюдён; attach_is_idempotent_under_retry→1 current-row, first_seen цел/last_seen bumped, Active; repo_hint_selects_single_detached_main→hint=Some→Resolved, None→Ambiguous[W]; common_dir_fingerprint_alone_never_resolves→GlobalOnly); юнит-тесты `--lib` 5 (+2: summary_from_row_rejects_corrupt_enum — corrupt kind/state → `FromSqlConversionFailure(2/3,Text)`, summary_from_row_parses_valid_row); регресс `--test registry` 11 OK, `--test worktree` 12 OK, `--test cache` 10 OK (было 9 + D-002 regression), `--test migrate` 8 OK, `--test migrate_resumable` 6 OK, `--test state` 6 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store --no-deps` clean (исправлена redundant-explicit-link в `registry/mod.rs` → `[resolve()]`/`[attach()]`); `git diff --stat Cargo.lock` пуст (0 новых зависимостей); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE (T10 держится) | Новый композиционный модуль `crates/store/src/registry/resolve.rs` над примитивами T02-02/03 (без новых таблиц/миграций): `WorktreeRootFacts` (canonical/display/path_fingerprint/kind + advisory `common_dir_fingerprint`/`remote_fingerprint` — факты подаёт демон, store без git/net), `RequestRoot{worktree_root?,repo_hint?}` (маппинг 02 §3.3, `session_id` routing-only), `Resolution{Resolved,GlobalOnly,Ambiguous{candidates}}`, `Candidate`, `AttachError{UnknownWorktree,RepoMismatch,NotReattachable}` (+Display/Error). `resolve(&Connection,&RequestRoot)` — авто-резолв ТОЛЬКО по current exact-path (`find_worktree_by_current_path`); иначе advisory detached-кандидаты (`detached_candidates`: fp-lookup + remote-hint, фильтр `state=Detached ∧ kind=facts.kind`, dedup `BTreeMap`), `repo_hint` выбирает ровно одного → Resolved, иначе Ambiguous; пусто → GlobalOnly. `attach(&Transaction,...)` — перепривязка existing identity: summary→(UnknownWorktree|RepoMismatch)→transition(→Active, Removing→NotReattachable до записи)→observe_worktree_path→(если stored kind≠Linked) observe_repository_path; `Result<Result<(),AttachError>>` (внешний Err=SQLite/rollback, внутренний=отказ без мутации). Новые read'ы в `worktree.rs`: `WorktreeSummary`, `find_worktree_by_current_path` (is_current=1, `ORDER BY worktree_id LIMIT 1`), `worktree_summary`, `worktrees_of_repo` (parse kind/state с `FromSqlConversionFailure`-fallback). Ре-экспорты `registry/mod.rs`+`lib.rs`. `[SPEC]`-ноты (тот же коммит): 03 §2.1 (resolver/attach композиция, 3 read'а, per-row uniqueness→детерминированный первый, auto-resolve только exact current, fingerprint/remote/common-dir advisory, кандидаты detached+kind, common-dir не хранится, git-пробинг у демона) и 02 §3.3 (маппинг RequestRoot, session_id routing-only, GlobalOnly не ошибка, repo_hint только tie-break, факты от демона/store без git). Найдено отклонение D-002 (source-lint false-positive на имени `attach`), исправлено до T02-04 done. Инвариант 01 §5 соблюдён (identity не выводится из пути; fingerprint advisory). Guardrail T10 держится (0 новых deps) | Claude Opus 4.8 / 2026-07-17 |
| D-002 | коммит `T02-04: attach/move/non-git resolution` (та же правка) | `cargo test -p local-rag-store --test cache` OK (10; было 9 + `source_lint_targets_sql_attach_not_the_rust_identifier`: positive `"ATTACH DATABASE …"`/`ATTACH 'file' AS aux`; negative `pub fn attach(`, `AttachError::…`, `NotReattachable(…)`); `no_writable_cross_db_attach` OK); `cargo xtask ci` = all checks passed | Source-lint `tests/cache.rs::no_writable_cross_db_attach` (T01-05, охрана 03 §1.4 «нет writable cross-DB ATTACH») матчил `line.to_ascii_lowercase().contains("attach")` → ложно срабатывал на введённую T02-04 spec-именованную операцию `repo attach` (04 §7): `pub fn attach`/`AttachError`/`NotReattachable`. Фикс: хелпер `contains_sql_attach(line)=line.contains("ATTACH")` (SQL в крейте — заглавными по строгой конвенции, ловит и многострочный `execute_batch`), Rust-идентификатор больше не матчится. Инвариант НЕ ослаблен (любой uppercase SQL `ATTACH` без маркера `// cross-db: read-only` по-прежнему flagged); добавлен regression-тест дискриминации. `DEVIATIONS.md` → resolved | Claude Opus 4.8 / 2026-07-17 |
| T02-03 | коммит `T02-03: stable worktree operations + path history` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test worktree` OK (12: worktree_id≠fp и не выводится из fp (fp хранится только в `worktree_path.path_fingerprint`, `find_worktrees_by_path_fingerprint` возвращает UUID-lookup, display сохранён), `worktree` без path-производной колонки (ровно 7 §2.1-колонок, нет canonical_path/path/path_fingerprint), composite FK отвергает генерацию чужого worktree (`(g2,w1)` ∉ generation → `WriteError::Sqlite`, откат, `current_generation(w1)==g1`), observe single-current A→B→A, partial-unique index отвергает форс-2nd-current (A остаётся), path history retained across move (`is_current=0`, `first_seen_at` цел), observe идемпотентен под retry (без дубля, first_seen цел, last_seen/display bumped), detach→reattach retains id (Active→Detached→Active, id неизменен, обе истории пути живы), illegal transition Removing→Active → `Ok(Err(Illegal{Removing,Active}))` без мутации, unknown worktree → `Ok(Err(UnknownWorktree))`, create_worktree unknown repo → FK-отказ (0 строк), миграция v2 = ровно worktree-схема (3 таблицы + partial unique `worktree_path_current WHERE is_current = 1` + `worktree_path_fp` + 2-колоночный composite FK на `generation`)); юнит-тесты `--lib` 3 (kind/state round-trip, полная матрица `check_transition`: 4 legal + 3 self-noop + 2 illegal); регресс `--test registry` 11 OK (обновлён `migration_produces_exact_registry_schema`: набор `[(1,registry),(2,worktree)]` — усиление), `--test migrate` 8 OK (обновлён `state_db_open_bootstraps...`: applied 1→2, +проверка worktree-таблиц и строки `(2,worktree)` — усиление), `--test state` 6 OK, `--test cache` 9 OK, `--test migrate_resumable` 6 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store --no-deps` clean; `git diff Cargo.lock` пуст (0 новых зависимостей — только std + уже-разрешённые rusqlite/tokio); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE (T10 держится) | Миграция v2 `worktree` (`registry::SCHEMA_V2`, byte-exact §2.1: `worktree`+composite FK / `worktree_path`+partial-unique `worktree_path_current`+`worktree_path_fp` / `generation`), добавлена в `migrate::ALL`. Циклический FK worktree↔generation в одной миграции (SQLite резолвит FK-родителей лениво; composite-цель валидна через `UNIQUE (generation_id, worktree_id)`). Новый модуль `crates/store/src/registry/worktree.rs`: `WorktreeKind{Main,Linked,NonGit}`, `WorktreeState{Active,Detached,Removing}` + чистая `check_transition` (04 §7; self-переходы — идемпотентный no-op), типы ошибок `IllegalWorktreeTransition{from,to}`/`WorktreeTransitionError{UnknownWorktree,Illegal(..)}` (Display+Error). Операции (write через `&Transaction`, read через `&Connection`, как T02-02): `create_worktree` (state='active', current_gen=NULL, FK на repo), `observe_worktree_path` (clear-then-set single-current в 3 statements + path_fingerprint/display, FK на worktree), `set_current_generation` (worktree-side seam; composite FK), `transition_worktree_state -> rusqlite::Result<Result<(),WorktreeTransitionError>>` (внешний Err = сбой SQLite/rollback, внутренний = доменный отказ без мутации; corrupt state → `FromSqlConversionFailure`); reads `worktree_state`/`current_worktree_path`/`current_generation`/`worktree_path_history`/`find_worktrees_by_path_fingerprint`. `registry/mod.rs`+`lib.rs` ре-экспорты. `worktree_id` caller-minted UUIDv7 (не path-derived, 01 §5); `path_fingerprint` — lookup-only (01 §5, 03 §2.1). `generation`-builder/occurrence/state-machine — group 05 (scope-guard). `[SPEC]`-нота 03 §2.1 (тот же коммит): as-built миграция v2, идемпотентные self-переходы, generation как FK seam. Инвариант 01 §5 соблюдён (нет path-производной identity/FK-цели). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-17 |
| T02-02 | коммит `T02-02: registry schema + repository operations` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test registry` OK (11: one-current across switches, partial-unique index rejects 2nd current (A survives), same-remote→2 repos (hint не unique), path history retained (`first_seen_at` intact, old row `is_current=0`), observe idempotent under retry (`first_seen_at` preserved/`last_seen_at` bumped), FK rejects observe on unknown repo (0 rows), stored fingerprint = 64-hex BLAKE3 (не raw/normalized URL), NULL fingerprint allowed, migration schema exact (3 таблицы + UNIQUE partial index `WHERE is_current = 1` + ровно 1 row `(1,"registry")`), `repository` без `canonical_path` (ровно 4 §2.1-колонки), `find_by_path` матчит только current (после move старый путь→None, но в history остаётся)); регресс `--test migrate` 8 OK (обновлён `state_db_open_bootstraps_and_is_idempotent`: `applied` 0→1, +проверка registry-таблиц и row `(1,"registry")` — усиление, не ослабление), `--test migrate_resumable` 6 OK, `--test state` 6 OK, `--test cache` 9 OK; `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store -p local-rag-core --no-deps` clean; аудит `Cargo.lock` = 45 внешних источников, `git diff Cargo.lock` пуст (0 новых зависимостей vs T02-01 baseline); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/hyper/tonic = NONE (T10 держится) | Новый модуль `crates/store/src/registry/{mod,repository.rs}`. `mod.rs`: `pub(crate) const SCHEMA_V1` — byte-exact DDL §2.1 (`repository`/`repository_path`+partial unique index `repository_path_current`/`repo_settings`), ре-экспорт операций. `repository.rs`: свободные функции — записи через `&Transaction` (композируются в одной `StateWriter::transaction`), чтения через `&Connection`, все возвращают `rusqlite::Result`. `create_repository(tx, repo_id, git_remote_fingerprint: Option, now)`; `observe_repository_path(tx, repo_id, path, now)` — clear-then-set в 3 statements (SQLite без deferred UNIQUE), upsert по PK `(repo_id, observed_path)` (new → `first_seen=last_seen=now`; existing → `first_seen` сохранён, `last_seen` bumped), unknown repo отвергается FK; `find_repository_by_path` (только current), `find_repositories_by_remote` (МНОГО — hint), `current_path`, `path_history` (+ тип `PathObservation`). `migrate/mod.rs`: `ALL = &[Migration::sql(1, "registry", registry::SCHEMA_V1)]` (+doc). `lib.rs`: `pub mod registry` + ре-экспорты. repo_id минтится вызывающей стороной (UUIDv7 вне транзакции; closure `Send+'static`); типизированный `RegistryError` не вводился (семантика resolution/ambiguity — T02-04, `WriteError::Sqlite(ConstraintViolation)` покрывает unknown-repo). `[SPEC]`-уточнение (тот же коммит): 03 §2.1 as-built-нота (миграция v1 `registry`; single-current-path через clear-then-set; repo_id caller-minted/не path-derived; fingerprint — nullable non-unique hint; history retained). Инвариант 01 §5 соблюдён (`repository` без `canonical_path`; identity не выводится из пути). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-17 |
| T02-01 | коммит `T02-01: canonical path + hash/ID primitives (identity module)` (строка evidence в том же коммите) | `cargo test -p local-rag-core` OK (53 unit + 4 doctests; +31 identity: blake3 reference KAT len 0/1/3/1024 (multi-chunk tree), `encode` byte-exact, 12-domain goldens + distinctness, field-boundary collision (`["ab","c"]≠["a","bc"]`), retry-determinism, typed fingerprints; uuidv7 golden layout / version=7+variant=0b10 stamp / 48-бит ts round-trip+truncate / ordering / shape / system source distinct; path relative dot+sep+NFC+fold+display-preserve, absolute drive-upcase/UNC/verbatim-strip/NFC/fold-keeps-drive, symlink resolve via `TempHome`; remote SSH/HTTPS equivalence + creds-strip + host-lowercase + nested + distinct); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-core --no-deps` clean; аудит `Cargo.lock` = 45 внешних источников (+9 vs 36: native-link blake3+arrayref+arrayvec+constant_time_eq, unicode-normalization+tinyvec+tinyvec_macros, casefold; `cpufeatures` — phantom, gated out `pure`, не в `cargo tree -e normal` core); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/net = NONE (T10 держится) | Новый модуль `crates/core/src/identity/{mod,domain,uuidv7,path,remote}.rs`. `domain`: `HASH_SCHEMA_VERSION=1`, `Domain` (12 доменов §1.2, subject-слуги с `/`), `encode` (`utf8(domain)‖0x00‖Σ(le_u32(len)‖f)`) + `hash` (BLAKE3 64-hex), типизированные `path_fingerprint`/`remote_fingerprint`. `uuidv7`: чистый `uuidv7_from(now_ms,[u8;10])→Uuid` (RFC 9562: 48-бит BE ts, version 7, variant 0b10), `Display` 8-4-4-4-12, трейт `UuidSource` + `SystemUuidV7` (unix: `SystemTime` + `/dev/urandom`). `path`: `CaseSensitivity`, `Canonical{canonical,display}`, `normalize_relative`, чистая `normalize_absolute_str` (verbatim/UNC/drive-upcase/NFC/fold), FS-`canonicalize_absolute` (symlink). `remote`: `normalize_remote_url` (каноник `host/path`; SSH/HTTPS/git/https эквивалентность; срез scheme/credentials/port/`.git`/слэшей; host lowercase, path-case сохранён) + `fingerprint`. Deps (утв. пользователем): `blake3`(pure), `unicode-normalization`, `casefold`(github/rust-gems) — 3 строки allowlist в `CONTRIBUTING.md` (обоснование+license+транзитив). `[SPEC]`-уточнения (as-built, тот же коммит): §1.2 field-encoding-конвенции (text→utf8, id→lowercase-hex-ascii, int→le fixed-width) и типизированы лишь 2 lookup-домена (остальные — через generic `hash` в задачах-владельцах, без premature coupling); §1.3 simple case-fold через `casefold::simple_fold` (порядок NFC→fold), FS-чувствительность подаёт вызывающий. Инвариант 01 §5 соблюдён: `worktree_id` НЕ выводится из пути (identity = UUIDv7; path — только `path_fingerprint` lookup). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-17 |
| T00-01 | не коммичено | `python3 fixtures/tooling/validate.py` exit 0 (stdlib); тот же скрипт под venv `jsonschema==4.23.0` exit 0; негативный self-test PASS (denylist/schema/49-count/dup-id); live v1 бенчмарк без ошибок | `fixtures/` — 6 семейств, 114 уникальных id; `search/corpus.json` = 49 запросов; baseline снят (embeddinggemma:300m, code-only): MRR 0.696, Hit@1 0.592, Hit@3 0.796, Hit@5 0.837, 544 чанка; gap-register GAP-01..06; пороги = TBD | Claude Opus 4.8 / 2026-07-16 |
| T00-02 | коммит `T00-02: scaffold Rust workspace + xtask quality gate + CI` (хэш в git-логе; строка edet в том же коммите) | `cargo build --workspace` OK; каждый бинарник `version` → `<имя> 0.0.0`, неизвестная команда exit 2; `cargo xtask ci` (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps) = all checks passed; offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; 12 значимых тестов (3×2 CLI-smoke, 2 core unit, 2 core doctests, 2 xtask ci_config) | Workspace: 6 либ (`core/store/index/projection/memory/protocol`) + 3 продуктовых бинарника (`local-rag`, `local-rag-proxy`, `local-rag-hook`) + dev-only `xtask`; toolchain pin 1.96.1 (`rust-toolchain.toml`), edition 2024, MSRV 1.96; `CONTRIBUTING.md` (единая команда `cargo xtask ci` + dependency policy); `.github/workflows/ci.yml` (ubuntu-latest); `Cargo.lock` без внешних зависимостей (0 registry sources) | Claude Opus 4.8 / 2026-07-16 |
| T00-03 | коммит `T00-03: shared fixture/failpoint test harness` (хэш в git-логе; строка evidence в том же коммите) | `cargo test -p local-rag-test-support` OK (8 integration + 5 unit + 6 doctests); `cargo test -p local-rag --test harness_smoke` OK; `cargo xtask ci` = all checks passed; offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `cargo doc` без warnings; ручная проверка bundle: `status.txt=signal: 6 (SIGABRT)` + command/stdout/stderr сохранены вне temp home, temp homes подчищены на Drop | Dev-only crate `crates/test-support` (`local-rag-test-support`, в `members`, не в `default-members`): `TempHome` (temp `LOCAL_RAG_HOME` под `env::temp_dir()`, RAII-cleanup, `.command()` ставит env только в дочерний процесс), `Clock`/`FixedClock`/`ManualClock`, `IdSource`/`SeqUuids`, `subprocess::run_capturing` + artifact bundle, `Failpoints` + `fail_point!` (registry-strict: неизвестное имя → `FailpointError::Unknown`; `Action::Abort` = crash-точка F/S-матриц), `fixtures` (root/read, std-only). Приёмка: smoke в `local-rag` (dev-dependency); `$HOME` не читается (страж-тест). `Cargo.lock` по-прежнему 0 внешних источников. GAP-06 механизм закрыт; строки F1–F12/S1–S8 остаются за T07-05/T13-06 | Claude Opus 4.8 / 2026-07-16 |
| T01-02 | коммит `T01-02: state.sqlite open policy + bounded writer` (строка evidence в том же коммите) | `cargo test -p local-rag-store` OK (6 integration: pragma-assertions journal_mode=wal/foreign_keys=1/synchronous=2(FULL)/busy_timeout=5000, FK rejection+rollback, closure-error rollback + idempotent retry, 64 concurrent producers serialize, queue-saturation waits+cancel-clean, read-only conn rejects write); `cargo clippy -p local-rag-store --all-targets -- -D warnings` clean; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store --no-deps` clean; `cargo xtask ci` = all checks passed; offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; аудит `Cargo.lock` = 36 внешних источников (rusqlite 0.40.1 + libsqlite3-sys 0.38.1 + cc + tokio 1.52.4 + транзитивные; wasm-*/sqlite-wasm-rs target-gated); grep всех `Cargo.toml` на dense/model/tree-sitter/net SDK = NONE (T10-инвариант держится) | Крейт `local-rag-store`: `crates/store/src/state/{mod,open,writer}.rs`. Open policy (`open_state_rw`/`open_state_read_only`) применяет 03 §2 pragmas (WAL set-and-verify; FK/synchronous=FULL/busy_timeout=5000 per-connection). `StateDb::open[_with_capacity]` спавнит единственный writer-поток, владеющий одним write-`Connection` (`blocking_recv`); публично — только `StateWriter::transaction` (bounded `tokio::mpsc`→`oneshot`, backpressure + чистая отмена, неявная BEGIN/COMMIT с rollback на Err/drop) и read-only `open_read`; writable `Connection` наружу не выходит (спец 02 §5). Метрики очереди `queue_capacity`/`available_slots` (02 §5). Deps добавлены в `CONTRIBUTING.md` allowlist (rusqlite+bundled, tokio lib-фича только `sync`). `[FIXED]`/`[SPEC]` 02 §5, 03 §2/§3 соблюдены; отклонений нет, `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-16 |
| T01-01 | коммит `T01-01: store/config path resolution + permissions` (строка evidence в том же коммите) | `cargo test -p local-rag-core` OK (21 unit + 4 doctests: sha256 NIST-KAT, precedence table data_dir/config_dir, unicode/space + non-UTF-8 paths, wrong-owner refusal `/`, idempotent 0700-tree + 0600-file, pipe-name fixture, endpoint socket); `cargo xtask ci` = all checks passed; offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; аудит `Cargo.lock` = ровно 1 внешний источник (`libc 0.2.186`, unix-target); grep Cargo.toml по dense/model/tree-sitter/sqlite/net SDK = NONE | Модуль `crates/core/src/paths/{mod,perms,hash}.rs`: `Env`/`SystemEnv` (инъекция env), `data_dir`/`config_dir` (02 §2.1 precedence, XDG empty=unset/relative-ignored), `StoreLayout` (все пути layout 02 §2 + идемпотентный `ensure()` 0700/owner-verify), `Endpoint`/`socket_path`/`pipe_name` (vendored std-only sha256), `perms` (0700 dirs/0600 files, symlink-swap reject, owner-verify до chmod; `geteuid` через `libc` в scoped `#[allow(unsafe_code)]`). `[SPEC]` amendment 02 §2.1: `LOCAL_RAG_HOME` override распространён на `config_dir=$LOCAL_RAG_HOME/config`; `CONTRIBUTING.md` — allowlist `libc`. Windows dirs/pipe реализованы под `cfg`, SID-lookup отложен (не в CI). Отклонений нет; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-16 |
| T01-03 | коммит `T01-03: forward-only migration runner` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test migrate` OK (7 тестов: empty→latest, older→latest, checksum-drift reject, newer-store reject, concurrent-migrator exclusion, repeated no-op, `StateDb::open` bootstrap+идемпотентность); `--test state` OK (6, регресс не сломан bootstrap'ом); `cargo test -p local-rag-core` OK (21 unit + 4 doctests — SHA-256 promotion не сломала `paths`); `cargo xtask ci` = all checks passed; offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store -p local-rag-core --no-deps` clean; аудит `Cargo.lock` = 36 внешних источников (без изменений с T01-02 → новых зависимостей НЕ добавлено, всё на std); grep всех `Cargo.toml` на dense/model/tree-sitter/net SDK = NONE (T10-инвариант держится) | Крейт `local-rag-store`: `migrate/{mod,lock}.rs` + `clock.rs`. `migrate::run(conn,&[Migration],lock_path,now_ms)` — под L1 (`migration.lock`, `std::fs::File::lock`, RAII), bootstrap `schema_migrations`/`store_settings` (`IF NOT EXISTS`, точный DDL 03 §2.1), compat-check (`IncompatibleStore`→`INCOMPATIBLE_STORE`), checksum-drift reject, apply-loop по одной tx на миграцию (шов для T01-04 resume). `Migration{version,name,sql}`+`checksum()` (SHA-256 текста SQL). `MigrationReport{applied,store_version}`; типизированный `MigrationError`. `pub const ALL: &[Migration] = &[]` (первая реальная миграция — T02-02). `StateDb::open` прогоняет `migrate::run` между `open_state_rw` и спавном writer (02 §4.1). `OpenError += Migration(Box<..>)`. Core: vendored SHA-256 поднят в `local_rag_core::hash::sha256_hex` (был `pub(crate)` в `paths/hash.rs`); добавлен `StoreLayout::migration_lock()`. `store_instance_uuid` НЕ сеется (defer в T01-05: UUIDv7-генератор — T02-01). `[SPEC]`-уточнения (as-built precision, тот же коммит): 02 §2 layout += `migration.lock`; 02 §6 нота — `ChecksumDrift`/rewritten history мапится под `INCOMPATIBLE_STORE` (protocol-mapping — T15). Clock seam — параметр `now_ms:i64` + `clock::system_now_ms()` (без зависимости от test-support). Отклонений нет; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-16 |
| T01-04 | коммит `T01-04: resumable/destructive migration mechanics` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test migrate_resumable` OK (6 default: resume-after-each-checkpoint, failed-step/retry, backup-pre-change, backup-idempotent-on-resume, destructive-sql-only, finalize-pending-resume); `--features failpoints` → 7 (та же 6 + `resumable_hard_kill_via_sigabrt`: реальный SIGABRT-child через re-exec, parent резюмит); регресс `--test migrate` 7 OK, `--test state` 6 OK; `cargo test -p local-rag-core` 21 unit + 4 doctests OK; `cargo xtask ci` = all checks passed (fmt → clippy `-D warnings` → test --workspace → doc → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store -p local-rag-core --no-deps` (и `--features failpoints`) clean; аудит `Cargo.lock` = 36 внешних источников (git diff vs HEAD = 0 новых; `failpoints` тянет optional path-dep `test-support`, 0 внешних); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/net = NONE (T10 держится) | Крейт `local-rag-store` `migrate/mod.rs`: `Migration` += `destructive: bool` и `steps: &[MigrationStep]` (+ `const fn sql/destructive/with_steps`, `StepFn`); `checksum()` над SQL (drift-тест T01-03 не тронут). Bootstrap += `migration_progress(version,seq,label,done_at)` (03 §2.1). `run()`: простые миграции — атомарный путь T01-03; сложные (destructive/steps) — `apply_complex` пер-юнитовыми чекпойнтами `[backup?][sql?][steps…]`, финализация одной tx (insert `schema_migrations` + очистка прогресса). Backup: `VACUUM INTO <root>/backups/state-<version>-<now_ms>.sqlite` до мутации (dir 0700, файл 0600), progress backup — отдельной tx (VACUUM вне транзакции); resume пропускает закоммиченные юниты. `MigrationError` += `Backup`/`BackupPath`. Restore-шов задокументирован в docstring. Feature-gated seam `#[cfg(feature="failpoints")] fail_point!("migrate:after_backup")` для hard-kill теста; `Cargo.toml` store += optional `test-support` + фича `failpoints`; `xtask ci` += 2 шага под фичей; `CONTRIBUTING.md` обновлён. Core: `StoreLayout::backups_dir()` + создание в `ensure()` (0700). `[SPEC]`-уточнения (тот же коммит): 02 §2 дерево += `backups/`; 03 §2.1 += `migration_progress`. Найдено отклонение D-001 (флейк concurrent-теста), исправлено до T01-04 done | Claude Opus 4.8 / 2026-07-17 |
| T01-05 | коммит `T01-05: cache.sqlite binding + recreation + bounded writer` (строка evidence в том же коммите) | `cargo test -p local-rag-store --test cache` OK (9: pragmas WAL/FK=0/synchronous=1(NORMAL)/busy=5000, matching-reopen-preserves-rows→Reused, uuid-mismatch→Recreated, schema-version-999→Recreated, corrupt-file→Recreated+usable, state-untouched (sha256 state.sqlite до/после rebuild равен + строка store_settings читается), writer-backpressure-cancels-cleanly, recreate-idempotent-on-retry, source-lint no-writable-ATTACH); `--features failpoints` → 10 (+ `cache_recreate_hard_kill_resumes`: реальный SIGABRT-child в `cache:after_delete` между delete и seed, parent резюмит → Recreated bound to B); регресс `--test state` 6 OK, `--test migrate` 7 OK, `--test migrate_resumable` 6/7 OK; `cargo xtask ci` = all checks passed (fmt → clippy `-D warnings` → test --workspace → doc → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store --no-deps` clean; аудит `Cargo.lock` = 36 внешних источников (git diff vs HEAD = 0 новых — только std + уже-разрешённые rusqlite/tokio); grep всех `Cargo.toml` на qdrant/usearch/ollama/onnx/candle/tree-sitter/net = NONE (T10 держится) | Крейт `local-rag-store` новый модуль `cache/{mod,open,writer}.rs` (зеркало `state/`): `CacheDb::open[_with_capacity](path, store_instance_uuid, [cap])`. `open_cache_rw`/`open_cache_read_only` применяют cache-pragmas (03 §4: WAL set-and-verify, FK=OFF, synchronous=NORMAL, busy=5000; отличие от state — FK/synchronous). `open_and_bind`: `inspect_existing` (open RW → read `cache_meta`) → валиден+совпал uuid/`CACHE_SCHEMA_VERSION`(=1) ⇒ `Reused`; иначе (нет файла / mismatch / порча-NOTADB / unreadable) ⇒ `recreate` (unlink `cache.sqlite`/`-wal`/`-shm`, ignore NotFound) → fresh `open_cache_rw` → `seed_binding` одной tx (`cache_meta` DDL + rows uuid/version/created_at) ⇒ `Created`|`Recreated`. Порядок open→validate/recreate→serve (writer получает уже связанный conn). Отдельный `CacheWriter` (02 §5 L4b): выделенный поток `local-rag-cache-writer`, bounded mpsc→oneshot, `transaction`/`queue_capacity`/`available_slots`, writable conn наружу не выходит. Cross-DB (03 §1.4): без ATTACH; source-lint тест охраняет инвариант. Feature-gated seam `#[cfg(feature="failpoints")] fail_point!("cache:after_delete")`; `Cargo.toml`/`xtask` не менялись (фича уже есть). `lib.rs` += `mod cache` + re-exports (`CACHE_SCHEMA_VERSION`,`CacheDb`,`CacheOpenError`,`CacheOpenOutcome`,`CacheWriteError`,`CacheWriter`). Scope-guard: payload-таблицы (`embedding_cache`/`normalized_text_cache`/`fts_*`) НЕ создаются (T03/T08/T11); §4.4 шаги 3–4 (FTS/per-row checksum) — лениво позже. Решение (утверждено пользователем): `store_instance_uuid` подаётся вызывающей стороной, посев в state отложен до T02-01 (UUIDv7)+T15-01 (daemon startup) — не deviation (ни один `[FIXED]`/`[SPEC]` не нарушен, карточка посев не требует). `[SPEC]`-уточнение (тот же коммит): 03 §4.4 as-built-нота (delete-and-recreate + идемпотентная сходимость; `cache_schema_version`=1; uuid — от caller). Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-17 |
| D-001 | коммит `T01-04: resumable/destructive migration mechanics` (та же правка) | `migrate.rs` concurrent-тест: 40/40 прогонов зелёные (`--exact concurrent_migrators_apply_exactly_once --test-threads=1`), до фикса ~4/5 виснул на `Barrier::wait` или паниковал в `raw_conn:51` (`enable WAL`); полный `cargo xtask ci` не виснет | Недетерминизм T01-03-теста: два worker-потока гонялись за однократным `PRAGMA journal_mode=WAL` на свежей БД (эксклюзивная перезапись заголовка → `SQLITE_BUSY`), проигравший паниковал до `Barrier::wait(2)` → deadlock второго worker'а; обнажено добавленной T01-04 параллельной нагрузкой (`migrate_resumable`). Фикс: пред-инициализация БД в WAL в главном потоке до spawn (`drop(raw_conn(&state_path))`), открытие уже-WAL БД идемпотентно → остаётся только целевая гонка за migration lock (L1). Ассерт не ослаблен. Продуктовый flock-код корректен. `DEVIATIONS.md` → resolved | Claude Opus 4.8 / 2026-07-17 |
| G01 | этот коммит (gate-hardening тесты + docs + `PROGRESS.md` в одной правке) | узкие тесты группы 01: `cargo test -p local-rag-core` OK (22 unit + 4 doctests; +`ensure_rejects_symlink_swap`, +pinned pipe_name KAT, +config_dir empty/relative); `-p local-rag-store --test state` 6 OK, `--test migrate` 8 OK (+`malformed_set_is_rejected`), `--test migrate_resumable` 6 OK, `--test cache` 9 OK; под `--features failpoints` → `--test cache` 10 (`cache_recreate_hard_kill_resumes`), `--test migrate_resumable` 7 (`resumable_hard_kill_via_sigabrt` — реальный SIGABRT-child); `cargo xtask ci` = all checks passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps → clippy+test `--features failpoints`); offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed; `RUSTDOCFLAGS="-D warnings" cargo doc -p local-rag-store -p local-rag-core --no-deps` clean; аудит `Cargo.lock` = 36 `source=` (все crates.io), grep всех `Cargo.toml` на dense/model/tree-sitter/сеть = 0 | Сверка G01 (перечитаны spec 02 §2/§2.1/§4–5; 03 §1.4/§2.1/§3/§4/§5; 13 §3): перестроен трейс «требование → код → тест» (блок ниже), расхождений код↔спека НЕ обнаружено. Все нормативные требования группы (layout+directory resolution, state/cache pragmas, bounded single-writer L4a/L4b, L1 migration lock, cross-DB запрет, framework bootstrap DDL, migration runner/compat/drift/resumable/backup, `StateDb::open` order) = as-built + verified. Backend-coupling guardrail держится: 3 allowlisted прямых dep (`libc`/`rusqlite bundled`/`tokio sync`), 0 dense/model/tree-sitter/network. Отложено by-design (seam на месте, не преждевременная реализация): wire-code mapping `INCOMPATIBLE_STORE`/`MIGRATION_IN_PROGRESS` → T15; посев `store_instance_uuid` → T02-01/T15; Windows SID → T17; batching/checkpoint/VACUUM-by-metrics → позже; v1→v2 migration `[OPEN]` не закрыт молча. Gate-hardening (тесты + docs; не features, не D-NNN): закрыты дешёвые пробелы покрытия защитного кода — `MalformedSet`, config_dir empty/relative, реальный symlink-swap reject, pinned pipe_name KAT, синхронизирован allowlist транзитивных deps в `CONTRIBUTING.md`. Отклонений не обнаружено; `DEVIATIONS.md` без изменений | Claude Opus 4.8 / 2026-07-17 |
| G00 | этот коммит (строка evidence в том же коммите; изменён только `PROGRESS.md`) | `python3 fixtures/tooling/validate.py` exit 0 (built-in subset validator; 6 семейств, 114 уникальных id, 20 matrix-строк, no-backend-keys); `cargo xtask ci` = all checks passed; offline `CARGO_NET_OFFLINE=true cargo xtask ci` = passed (fmt --check → clippy `-D warnings` → test --workspace → doc --no-deps; прогнаны все group-тесты: test-support 7 unit + 8 integration + 6 doctests, `harness_smoke`, три пары `cli_version`, `xtask ci_config`, core doctests); аудит `Cargo.lock` = 0 строк `source=`/`checksum=`, 11 пакетов; grep всех `Cargo.toml` по dense/model/tree-sitter/sqlite/сеть = NONE; 10/10 baseline-порогов = `"TBD"`; manifest families = ровно 6, gaps = GAP-01..06 (все `blocking:false`) | Сверка G00: перечитаны spec 01/14/15, каждый маркер `[FIXED]`/`[SPEC]`/`[OPEN]` сопоставлен с as-built (трейс ниже). Fixture families 1–6 учтены (spec 14 §1), deferred scope в workspace отсутствует (spec 15 §3, 01 §2 O1), gaps GAP-01..06 видимы в `manifest.json`. Ни один `[OPEN]` (O1 backend, O2 пороги, O4 языки, O8 split) не закрыт молча. Отклонений не обнаружено → `DEVIATIONS.md` без изменений, D-NNN не заводился. `jsonschema==4.23.0` вариант validate.py — сетезависим (в системе не установлен), авторитетно подтверждён ещё в T00-01; для G00 сетенезависимым путём выступает built-in валидатор | Claude Opus 4.8 / 2026-07-16 |

### G00 — трейс требование → artifact/test

Дата 2026-07-16, исполнитель Claude Opus 4.8. Все команды воспроизводимы из README-плана,
раздел «Проверка». `отложено` = требование нормативно, но реализуется позже по плану; на G00
проверено лишь отсутствие преждевременного нарушения/coupling.

Spec 01 — Overview:

| Требование (маркер) | Artifact / Test | Статус на G00 |
| --- | --- | --- |
| Rust impl `[FIXED]` 01§1 | Rust workspace `Cargo.toml` (9 default-members, edition 2024, MSRV 1.96) | as-built |
| npm distribution `[FIXED]` 01§1 | packaging → T17; `manifest.json → deferred[]` фиксирует границы | отложено, не нарушено |
| No mandatory external daemons `[FIXED]` 01§1 | `Cargo.lock` 0 внешних источников; нет qdrant/ollama SDK | as-built |
| Claude Code only harness `[FIXED]` 01§2 | нет multi-harness кода; multi-harness в `deferred[]` | as-built |
| Spool-only hook ingestion `[FIXED]` 01§2 | бинарник `local-rag-hook` (seam) → логика G13 | отложено, seam есть |
| Platform targets; win32-arm64/FreeBSD deferred `[FIXED]` 01§2 | CI single host (T17 добавит остальные); `deferred[]` | as-built |
| Dense backend выбирается на step 11 `[OPEN]` 01§2 / O1 | нет backend-крейтов (`Cargo.lock`, grep Cargo.toml); только план `ProjectionStore` | не закрыт молча |
| No process-global current project `[FIXED]` 01§3 | кода маршрутизации ещё нет → G02/G15 | отложено, не нарушено |
| No in-place re-embed/migration `[FIXED]` 01§3 | → G11 | отложено |
| `non_rebuildable` отвергается `[FIXED]` 01§3 | → G03 | отложено |
| Correctness budget `[FIXED]` 01§4 | fixtures `memory/`+`fault/` кодируют ожидания; harness `Failpoints` | mechanism готов; реализация G07/G13/G14 |
| Two identity ladders + audit rule `[FIXED]` 01§5 | `validate.py` denylist (no payload/backend поля в fixtures); schema-lint DDL → G01+ | fixtures neutral; DDL позже |
| v1 behavioral contract `[FIXED]` 01§7 | fixtures импортированы implementation-neutral (6 семейств, 114 id) | as-built |

Spec 14 — Acceptance & Testing:

| Требование (маркер) | Artifact / Test | Статус на G00 |
| --- | --- | --- |
| 6 fixture families, implementation-neutral `[FIXED]` 14§1 | `fixtures/{parser,reconcile,search,memory,adversarial,fault}`; `validate.py` форсит множество семейств | учтены (parser gap-only) |
| Acceptance gates existence/shape `[FIXED set]` 14§2 | `fault/matrix.json` (F1–F12/S1–S8 декларативно); пороги `TBD` | shape зафиксирован |
| Baseline numbers `[BASELINE]`/`[OPEN]` 14§2–3 / O2 | `manifest.baseline`: метрики сняты, 10/10 порогов = `"TBD"` | O2 соблюдён |
| Fault-injection harness `[FIXED]`+`[SPEC]` 14§3 | `test-support::Failpoints`/`fail_point!`; `harness.rs` (8); `matrix.json` | mechanism (GAP-06 закрыт); скрипты → T07-05/T13-06 |
| Consistency tests `[SPEC mechanics]` 14§4 | → G07/G08/G09 | отложено |
| Determinism (parser/ids/addlContext/schema-lint) 14§5 `[FIXED]` | `validate.py` denylist; `Clock`/`SeqUuids` в harness; schema-lint → G01+ | partial |
| Adversarial `[FIXED]` 14§6 | `adversarial/index.json` (12); v2-spec injection round-trip → GAP-05 | импортирован subset + gap |
| 49-query benchmark `[FIXED]` 14§7 | `search/corpus.json` (49); `manifest.baseline` снят на v1 | as-built |
| Step-11 spike matrix `[FIXED]` 14§7 | → T10 | отложено |

Spec 15 — Roadmap:

| Требование (маркер) | Artifact / Test | Статус на G00 |
| --- | --- | --- |
| Implementation order `[FIXED]` 15§1 | порядок групп 00→17 в `PROGRESS.md` | соблюдён |
| Backend fixed at step 11 `[FIXED]` 15§1 | нет backend coupling (см. O1) | соблюдён |
| Steps 1–7 без open question `[FIXED]` 15§1 | foundations без hardcoded `[OPEN]` | соблюдён |
| MVP v0 scope `[FIXED]` 15§2 | план покрывает; tree-sitter 2–3 языка `[OPEN which]` → GAP-01/O4 | соблюдён |
| Deferred (all additive) `[FIXED]` 15§3 | `manifest.deferred[]`; grep `Cargo.toml` = deferred SDK NONE | не в workspace |
| Open questions O1–O8 `[OPEN]` 15§4 | O1→нет coupling; O2→пороги `TBD`; O4→GAP-01; O8 (`split now [FIXED]`)→state/cache split запланирован T01-05 | ни один не закрыт молча |

Заметки G00 (не отклонения):

- Имена директорий `fixtures/{memory,adversarial,fault}` — слаги; spec 14 §1 называет семейства
  «Memory-quality», «Adversarial recall», «Fault-injection scripts». Покрытие соответствует;
  недостающая часть каждого явно висит как GAP (rev6 memory-quality op-корпус → GAP-04 → T14-07;
  v2 injection round-trip → GAP-05 → T14-08/T16-04). Это не spec↔code mismatch, D-NNN не требуется.
- Строка evidence `T00-01` выше гласит «не коммичено», хотя задача фактически отгружена в коммите
  `055a27a` (`T00-01: import v1 behavioral fixtures and baseline inventory`); правило «коммить
  каждую задачу» появилось позже (`6d0546d`). Историческое evidence не переписывается (CLAUDE.md,
  «Repository hygiene») — фиксирую факт здесь.

### G01 — трейс требование → artifact/test

Дата 2026-07-17, исполнитель Claude Opus 4.8. Команды воспроизводимы из README-плана и строки
evidence G01 выше. `отложено` = требование нормативно, но реализуется позже по плану; на G01
проверено лишь отсутствие преждевременного нарушения/coupling и наличие seam. Ссылки на код —
`file:symbol`/`file:line` на момент сверки.

Spec 02 — Architecture / lifecycle / locking:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Store layout, все 11 путей `[SPEC]` 02 §2 | `StoreLayout` core `paths/mod.rs:234+` (incl. `migration_lock`, `backups_dir`) | `store_layout_maps_every_path` | as-built |
| Directory resolution precedence `[SPEC]` 02 §2.1 | `data_dir`/`config_dir` + `nonempty_var`/`absolute` (`paths/mod.rs:162/175/144/153`) | `data_dir_precedence_posix`, `config_dir_precedence_posix` (empty/relative добавлены) | as-built |
| `LOCAL_RAG_HOME` overrides all incl config_dir `[SPEC]` 02 §2.1 | early-return `paths/mod.rs:163/176` | `local_rag_home_overrides_data_dir`, `config_dir_under_local_rag_home` | as-built |
| state pragmas WAL(set-and-verify)/FK=ON/FULL/busy=5000 `[SPEC]` 02 §5 ↔ 03 §2 | `apply_state_pragmas` store `state/open.rs:98` (verify 101-104) | `state_pragmas_are_applied` | as-built |
| Bounded single-writer L4a `[FIXED]` 02 §5 | `state/writer.rs` (spawn/transaction/run_transaction); writable conn не публичен | `concurrent_producers_serialize`, `queue_saturation_waits_then_cancels_cleanly`, `foreign_keys_are_enforced`, `closure_error_rolls_back_then_retry_is_idempotent`, `read_only_connection_cannot_write` | as-built |
| Bounded single-writer L4b (cache) `[FIXED]` 02 §5 | `cache/writer.rs` (отдельный поток `local-rag-cache-writer`) | `cache_writer_backpressure_cancels_cleanly` | as-built |
| L1 migration lock, held only while migrating, RAII `[SPEC]` 02 §5 | `migrate/lock.rs` (Drop 50-55), `mod.rs:299` `_guard` | `concurrent_migrators_apply_exactly_once` | as-built |
| `StateDb::open` order (open_rw → migrate под L1 → spawn writer) `[FIXED, mechanics [SPEC]]` 02 §4.1 | `state/mod.rs:53-73` | `state_db_open_bootstraps_and_is_idempotent` | as-built |
| Degraded modes / wire-code taxonomy `[SPEC]` 02 §6 | типизированные ошибки в `OpenError::Migration`; mapping → protocol boundary | — | отложено (T15) |

Spec 03 — Data model / DDL / hash rules:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Cross-DB запрет: no writable ATTACH `[FIXED]` 03 §1.4 | два физически раздельных writer-потока; нет ATTACH в src (только read-only в docstring) | `no_writable_cross_db_attach` (source-lint скан `crates/store/src/**`) | as-built |
| Framework bootstrap DDL байт-в-байт `[SPEC]` 03 §2.1 | `bootstrap()` `migrate/mod.rs:554-578` (`schema_migrations`/`store_settings`/`migration_progress`) | `state_db_open_bootstraps_and_is_idempotent`, `empty_to_latest_applies_all` | as-built |
| state write policy: single bounded queue `[FIXED, numbers [SPEC]]` 03 §3 | L4a (см. 02 §5 выше) | (L4a-тесты) | as-built (queue) / отложено (batched last_used/checkpoint/VACUUM-by-metrics) |
| cache pragmas WAL/FK=OFF/NORMAL/busy=5000 `[SPEC]` 03 §4 | `apply_cache_pragmas` `cache/open.rs:147` | `cache_pragmas_are_applied` | as-built |
| cache binding/recreation `[FIXED principle]` 03 §4.4 (`CACHE_SCHEMA_VERSION=1`) | `cache/open.rs` (`open_and_bind`:173/`inspect_existing`:208/`seed_binding`:251/`recreate`:279) | `matching_reopen_preserves_rows`, `uuid_mismatch_rebuilds`, `schema_version_mismatch_rebuilds`, `corrupt_cache_yields_clean_cache`, `state_untouched_on_cache_rebuild`, `recreate_is_idempotent_on_retry`, `cache_recreate_hard_kill_resumes` | as-built |
| payload-таблицы (embedding/normalized_text/fts) НЕ создаются | только `cache_meta` DDL `cache/open.rs:39` | — | scope-guard (T03/T08/T11) |
| Migration boundaries: hash schema/`occurrence_id`/`worktree_id`/`received_seq` стабильны; deferred additive `[FIXED]` 03 §5 | `ALL: &[] ` `mod.rs:149` (первая миграция T02-02); `store_instance_uuid` не засеян | — | as-built / отложено (посев T02-01/T15) |

Spec 13 §3 — Migration framework:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| numbered/checksummed(over SQL)/forward-only `[FIXED]` | `Migration.version`/`checksum()=sha256_hex(SQL)` `mod.rs:88/132`; `filter(v>store_version)`:348 | `checksum_drift_is_rejected`, `older_to_latest_applies_only_new` | as-built |
| well-formed set (strictly-increasing-contiguous-from-1) | `validate_set` `mod.rs:532` → `MalformedSet` | `malformed_set_is_rejected` (добавлен) | as-built |
| compat-check: refuse newer store `[FIXED]` | `run()` `mod.rs:310-316` `IncompatibleStore` | `newer_store_is_rejected` | as-built (wire-code → T15) |
| checksum drift / rewritten history `[FIXED]` | `mod.rs:319-337` `ChecksumDrift`/`UnknownAppliedVersion` | `checksum_drift_is_rejected` | as-built |
| resumable: idempotent steps, progress row/step, crash⇒resume `[FIXED]` | `apply_complex` `mod.rs:375-433` + `migration_progress` | `resume_after_each_checkpoint`, `resume_from_finalize_pending`, `resumable_hard_kill_via_sigabrt`, `failed_step_leaves_version_unapplied_then_retry_succeeds` | as-built |
| backup before destructive (`VACUUM INTO backups/state-<v>-<ts>.sqlite`) `[SPEC mechanics]` | `mod.rs:387-399`, `take_backup`:442, `backup_path`:469 | `destructive_backup_has_pre_change_data`, `destructive_backup_idempotent_on_resume`, `destructive_sql_only_backs_up_then_applies_sql` | as-built |
| cache never migrated (bump ⇒ drop&rebuild) `[FIXED]` ↔ 03 §4.4 | cache recreate path | `schema_version_mismatch_rebuilds` | as-built |
| migration tests on every prior released schema `[FIXED]` | синтетические наборы M1..M4 (matrix); реальный `ALL` пуст | migrate matrix (8) | as-built (released schema нет до T02-02) |
| v1→v2 data migration `[OPEN]` | — | — | не закрыт молча (решение до GA) |

Backend-coupling guardrail (архитектурный инвариант, T10):

| Проверка | Результат |
| --- | --- |
| прямые внешние deps | ровно 3 allowlisted: `libc` (unix), `rusqlite` (bundled), `tokio` (sync) |
| `Cargo.lock` `source=` | 36 (все crates.io; wasm-подсемейство rusqlite 0.40 target-gated, не линкуется в native) |
| grep dense/model/tree-sitter/network SDK по всем `Cargo.toml` | 0 (qdrant/usearch/ollama/onnx/candle/tree-sitter/reqwest/… = NONE) |

Заметки G01 (не отклонения):

- Все затронутые группой маркеры `[FIXED]`/`[SPEC]` = as-built + verified; единственный
  затронутый `[OPEN]` — O8 (state/cache split, уже `[FIXED]`) соблюдён, а v1→v2 migration
  (`[OPEN]` 13 §3) не закрыт молча. Ни один `[OPEN]` не разрешён имплицитно.
- Спека «single writer *task*» (02 §5 L4a/L4b) реализована выделенным OS-потоком с
  `blocking_recv` (не tokio-task). Семантически эквивалентно (ровно один физический писатель
  на БД); не расхождение — docstring `state/mod.rs`/`cache/mod.rs` использует слово «task».
- Gate-hardening (закрытие дешёвых пробелов покрытия защитного/edge кода; только тесты + docs,
  продуктовый код не тронут — это не features и не D-NNN): `malformed_set_is_rejected` (store
  `tests/migrate.rs`), edge-cases empty/relative `XDG_CONFIG_HOME` в `config_dir_precedence_posix`,
  реальный symlink-swap reject `ensure_rejects_symlink_swap` (core `paths/perms.rs`), pinned
  12-hex KAT в `pipe_name_fixture` (ground truth вычислен независимо), синхронизация перечня
  транзитивных deps `rusqlite` в `CONTRIBUTING.md` с `Cargo.lock`.
- Отложенные by-design seam (проверено наличие, не преждевременная реализация): protocol
  wire-code mapping (T15), посев `store_instance_uuid` (T02-01/T15), Windows SID (T17),
  batched `last_used_at`/WAL checkpoint/VACUUM-by-metrics (позже). Отклонений не обнаружено;
  `DEVIATIONS.md` без изменений; historical evidence не переписывалось.

### G02 — трейс требование → artifact/test

Дата 2026-07-17, исполнитель Claude Opus 4.8. Команды воспроизводимы из строки evidence G02
выше. `отложено` = требование нормативно, но реализуется позже по плану; на G02 проверено лишь
отсутствие преждевременного нарушения/coupling и наличие seam. Ссылки на код — `file:symbol`/
`file:line` на момент сверки.

Spec 01 §5 — Две identity-лестницы + systemic audit rule:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| No durable ID derived from a filesystem path `[FIXED]` 01 §5.1 | identity-PK суть UUIDv7: `repository.repo_id`/`worktree.worktree_id`/`generation.generation_id` (`registry/mod.rs:82/130/158`), минтятся вызывающим (`identity::uuidv7`) | `worktree_has_no_path_derived_identity_column`, `repository_has_no_canonical_path_column`, `worktree_id_is_not_and_does_not_define_path_fingerprint`, `no_foreign_key_targets_a_path_or_fingerprint_column` (new) | as-built |
| Path-derived hash — lookup key only, никогда FK-цель `[FIXED]` 01 §5.1 | `worktree_path.path_fingerprint` «lookup accelerator ONLY» (`registry/mod.rs:147`) + index `worktree_path_fp`; `find_worktrees_by_path_fingerprint` возвращает UUID | `worktree_id_is_not_and_does_not_define_path_fingerprint`, `no_foreign_key_targets_a_path_or_fingerprint_column` (new), `path_columns_live_only_on_ledger_tables` (new) | as-built |
| Content-shared строки не несут path/generation/context-полей `[FIXED]` 01 §5.1 | content-shared таблицы (`content_blob`/`file_revision`/`parsed_unit`/`embedding_cache`) ещё не созданы (группы 03/11); registry-split compliant | `path_columns_live_only_on_ledger_tables` (new — итерирует все таблицы, покрывает будущие автоматически) | отложено by-design (seam есть) |
| Audit rule enforced три способа, механизм #2 = «schema lint test that greps the DDL» `[FIXED]` 01 §5.1 | `crates/store/tests/schema_audit.rs` — schema-wide lint над всеми таблицами мигрированного `state.sqlite` (`pragma_table_info`/`pragma_foreign_key_list`) | `no_foreign_key_targets_a_path_or_fingerprint_column`, `path_columns_live_only_on_ledger_tables`, `column_classifiers_discriminate` (все new) | as-built (gate-hardening) |

Spec 02 §3 — Config / data-policy / routing:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `data_policy` default `local_only` `[FIXED default]` 02 §3.1 | `DataPolicy::default → LocalOnly` (`config/mod.rs:117`) | `data_policy_default_is_local_only`, `default_matches_spec_toml` | as-built |
| missing→defaults / unknown `schema_version`→typed / invalid policy→typed (не тихий дефолт §6) / unknown keys ignored `[SPEC]` 02 §3.1 | `Config::load`/`parse_toml`/`from_raw` (`config/mod.rs:266/256/276`), lenient `RawConfig` без `deny_unknown_fields` | `unsupported_schema_version_is_rejected`, `invalid_data_policy_is_rejected_not_defaulted`, `unknown_keys_are_ignored`, `empty_toml_is_all_defaults`, `present_but_invalid_config_is_typed_error` | as-built |
| No repo-local config lookup (единственный вход `config_dir`) `[SPEC]` 02 §3.1/§3.2 | `Config::load(config_dir)` — нет API с worktree/repo root (`config/mod.rs:266`) | `config_is_not_read_from_inside_a_repository` | as-built |
| Effective policy = most restrictive; repo только ужесточает `[SPEC]` 02 §3.2 | `DataPolicy::most_restrictive` (commut./assoc., `config/mod.rs:108`) + `effective_data_policy` fold (`settings.rs:126`) | `most_restrictive_covers_every_pair`, `effective_policy_is_most_restrictive_for_every_pair`, `repo_cannot_relax_global`, `multi_repo_tightening_is_order_independent` | as-built |
| Settings в `state.sqlite`, «never via files inside the repository» `[SPEC]` 02 §3.2 | `repo_settings` table + `settings.rs`; конфиг только глобальный | `set_setting_on_unknown_repo_is_rejected`, `config_is_not_read_from_inside_a_repository` | as-built |
| Нет ambient current project; unresolvable/None→`GlobalOnly` (не ошибка); `repo_hint` только tie-break; `session_id` routing-only `[FIXED — daemon defines routing]` 02 §3.3 | `RequestRoot` (исключает `session_id`, `resolve.rs:77`) + `resolve` (auto-resolve только exact current-path, `resolve.rs:185`) | `unknown_root_resolves_global_only`, `repo_hint_selects_single_detached_main`, `linked_ambiguity_requires_id`, `common_dir_fingerprint_alone_never_resolves` | as-built |
| `[OPEN]` `retired_generations_keep`/`_ttl_h`, `languages` 02 §3.1 | provisional defaults `StorageConfig`/`IndexConfig` (`config/mod.rs:163/208`), doc-помечены `[OPEN]` | `default_matches_spec_toml` | не закрыт молча |

Spec 03 §1–§2.1 — Conventions / hashes / cross-DB / registry DDL:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Column conventions (UUID TEXT lowercase; ts INTEGER ms) `[SPEC]` 03 §1.1 | DDL-типы `SCHEMA_V1/V2`; identity через UUIDv7 (`identity::uuidv7`) | `migration_produces_exact_{registry,worktree}_schema`, uuidv7 unit (6) | as-built |
| Hashing domain-separated/version-tagged; deterministic IDs стабильны под retry, независимы от порядка `[FIXED principle, encoding [SPEC]]` 03 §1.2 | `identity::domain` (`encode`/`hash`, 12 доменов, `local-rag/1/<slug>`) | `golden_hashes_for_every_domain`, `field_boundaries_do_not_collide`, `hash_is_deterministic_under_retry`, `encode_produces_exact_bytes` | as-built (deterministic-ID домены хэшируются владельцами T03/T05/T08/T11/T14 через generic `hash`) |
| Path canonicalization NFC/case-fold/display preserved `[FIXED principle, details [SPEC]]` 03 §1.3 | `identity::path::Canonical{canonical,display}` (`path.rs:38`) | path.rs unit (10: NFC, case-fold, symlink, display-preserve, drive/UNC) | as-built |
| Cross-DB: no writable `ATTACH` `[FIXED]` 03 §1.4 | нет `ATTACH` в src; два физически раздельных writer-потока (state/cache) | `no_writable_cross_db_attach` (source-lint) | as-built (сверено на G01, держится) |
| Registry DDL byte-exact; `repository` без `canonical_path` `[FIXED]`; `worktree_id` UUIDv7 «NEVER path-derived» `[FIXED]`; `path_fingerprint` не identity/не FK-цель; composite FK `[SPEC]` | `SCHEMA_V1`/`SCHEMA_V2` frozen-checksum (`registry/mod.rs:80/128`), `migrate::ALL=[v1 registry, v2 worktree]` (`migrate/mod.rs:152`) | `migration_produces_exact_{registry,worktree}_schema`, `repository_has_no_canonical_path_column`, `worktree_has_no_path_derived_identity_column`, `composite_fk_rejects_cross_worktree_current_generation`, schema_audit (new, 3) | as-built |
| Один current path: partial unique index + clear-then-set 03 §2.1 | `repository_path_current`/`worktree_path_current` (`WHERE is_current=1`); `observe_repository_path`/`observe_worktree_path` | `partial_unique_index_rejects_two_current` (reg+wt), `observe_sets_and_switches_single_current`, `path_history_retained_across_move` | as-built |
| `retiring` никогда для routing `[FIXED]` 03 §2.1 / 04 §1 | `generation` поставлена как composite-FK seam; builder/routing = группа 05/12 | — | отложено by-design (seam; routing 05/12) |

Spec 04 §7 — Worktree state machine:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Illegal transitions MUST фейлить tx, «never silently coerce» (преамбула) | `check_transition` + `transition_worktree_state` (`worktree.rs:113/326`) — мутирует ничего при отказе; self-переходы идемпотентны | `illegal_transition_is_typed_error`, `unknown_worktree_transition_is_typed_error`, unit `check_transition_covers_the_whole_matrix` | as-built |
| `active⇄detached`, `active\|detached→removing`; move ≠ новый worktree; reattach via `attach`; fingerprint hint, никогда sole ID `[FIXED]` 04 §7 | `resolve`/`attach` (`resolve.rs:185/292`): auto-resolve только exact current-path, advisory fingerprints, `common_dir_fingerprint` не хранится (нет колонки) | `directory_move_preserves_repo_and_worktree_ids`, `recreated_path_does_not_steal_identity`, `detach_and_reattach_retains_worktree_id`, `linked_ambiguity_requires_id`, `common_dir_fingerprint_alone_never_resolves`, `attach_removing_is_not_reattachable`, `attach_is_idempotent_under_retry` | as-built |

Spec 12 §7 — Remote fingerprint `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Credentials стрипаются, SSH/HTTPS нормализуются, хранится только хэш; remote URL никогда не единственный идентификатор | `identity::remote::normalize_remote_url`/`fingerprint` (`remote.rs:21/63`); `repository.git_remote_fingerprint` nullable non-unique (`registry/mod.rs:83`) | `credentials_are_stripped`, `transports_for_one_repo_share_a_fingerprint`, `host_is_lowercased_path_case_preserved`, `same_remote_maps_to_two_repositories`, `remote_fingerprint_stored_is_the_hash`, `null_remote_fingerprint_allowed` | as-built |

Заметки G02 (не отклонения):

- Gate-hardening (только тест + docs, продуктовый код не тронут — не feature, не D-NNN):
  `crates/store/tests/schema_audit.rs` реализует названный в 01 §5.1 механизм enforcement #2
  («schema lint test that greps the DDL for forbidden column placements»). Где два table-specific
  негативных теста фиксируют точные колонки `repository`/`worktree`, новый lint итерирует ВСЕ
  таблицы мигрированного `state.sqlite` и форсит два структурных правила 01 §5.1: (1) ни один FK
  не таргетит path/fingerprint-колонку, (2) path-колонки только на ledger-таблицах
  (`repository_path`/`worktree_path`). Так любая будущая миграция (content-shared таблицы групп
  03/11) проверяется автоматически. Поведение и до этого было корректным и покрытым — потому это
  усиление защиты, а не исправление расхождения (D-NNN не заводился), по прецеденту G01.
- Per-worktree (не глобальная) уникальность current path — соответствует норме «один current path
  на worktree» (03 §2.1 партиальные индексы per-row); auto-resolve только по exact current-path,
  `find_worktree_by_current_path` детерминирован (`ORDER BY worktree_id LIMIT 1`), демон держит
  одного current-обитателя на путь (спека 03 §2.1 T02-04). As-built, не отклонение.
- `session_id` в routing — только routing/telemetry, не часть identity: структурно исключён из
  `RequestRoot` (`resolve.rs:74-87`) и не участвует в `resolve`. Подтверждено.
- Отложено by-design (seam на месте, не преждевременная реализация): content-shared audit-поверхность
  и `retiring`-routing (группы 05/12); consumer effective-policy — central remote-policy guard
  provider pool (T11/T16); deterministic-ID домены хэшируются задачами-владельцами (T03/T05/T08/T11/T14)
  через generic `hash`. Ни один `[OPEN]` не разрешён имплицитно.
- Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002 остаются `resolved`);
  historical evidence не переписывалось.

### G03 — трейс требование → artifact/test

Дата 2026-07-20, исполнитель Claude Opus 4.8. Команды воспроизводимы из строки evidence G03
выше. Ссылки на код — `file:line`/`file:symbol` на момент сверки. `отложено` = требование
нормативно, но реализуется позже по плану; на G03 проверено лишь отсутствие преждевременного
нарушения/coupling и наличие seam. Тесты, помеченные `(new)`, добавлены этим gate.

Spec 03 §2.3 — Code side (path-independent, shared by content):

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `source_blob BLOB NOT NULL` — exact original bytes `[FIXED: strict invariant]` 03 §2.3 | `file_revision.source_blob BLOB NOT NULL` (`code/mod.rs:92`, `SCHEMA_V3` frozen-checksum) | `source_blob_is_not_nullable` (new), `roundtrip_lf_crlf_mixed_and_non_ascii`, `happy_path_insert_and_read` | as-built |
| `content_hash=H(file_content)`; `UNIQUE(content_hash,parser_fingerprint)`; compression/encoding/newline/size `[SPEC]` 03 §2.3 | `create_or_reuse_file_revision` (`code/source.rs:218`), `content_hash` (`code/source.rs:117`), `UNIQUE` (`code/mod.rs:98`) | `revision_reuse_key_is_content_hash_and_fingerprint`, `same_key_reuses_row`, `different_fingerprint_separates`, `compression_roundtrips_in_db` | as-built |
| `content_blob` = identity+metadata, text lives in cache `[SPEC]` 03 §2.3 | `content_blob` без text-колонки (`code/mod.rs:101-107`) | `content_blob_state_row_has_no_text_and_reuse_is_idempotent`, `no_state_table_stores_normalized_text`, `content_shared_tables_carry_no_path_or_context_field` | as-built |
| `parsed_unit` spans = byte offsets, `CHECK(span_end>=span_start)`, NO path `[SPEC]/[FIXED]` 03 §2.3/§2.3.1 | `parsed_unit` (`code/mod.rs:109-122`) | `parsed_unit_span_must_be_ordered`, `parsed_unit_foreign_keys_and_unique_locator`, `content_shared_tables_carry_no_path_or_context_field` | as-built |
| Byte-identical `.c` vs `.cpp` → разные ревизии (язык по extension) `[FIXED]` 03 §2.3.1 | reuse-ключ содержит `parser_fingerprint`; classify precedence по extension (`classify/mod.rs`) | `different_fingerprint_separates`, `revision_reuse_key_is_content_hash_and_fingerprint` | as-built (реальный `parser_fingerprint` — group 04, seam) |

Spec 03 §2.4 — Generation membership (path-dependent):

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `generation_file` PK `(generation_id,normalized_path)`, `file_revision_id NOT NULL REFERENCES file_revision` — среднее звено `[SPEC]` 03 §2.4 | `generation_file` (`code/mod.rs:124-130`) | `generation_file_requires_generation_and_revision`, `occurrence_implies_member_file_with_non_null_source_blob` (new) | as-built |
| skipped files NEVER get occurrences; `reason CHECK IN` 6 значений; нет `source_blob`-колонки `[FIXED §10 invariant]` 03 §2.4 | `skipped_file` (`code/mod.rs:132-140`, без `source_blob`) | `skipped_file_reason_and_no_occurrence`, `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new), `classified_skip_becomes_non_member_with_no_occurrence` | as-built |
| occurrence — структурный инвариант: composite FK → `generation_file` `[FIXED]` 03 §2.4 | `generation_unit_occurrence` composite FK (`code/mod.rs:152-154`) | `occurrence_requires_generation_file_member`, `occurrence_implies_member_file_with_non_null_source_blob` (new) | as-built |
| occurrence → unit → `parsed_unit.file_revision_id` → `source_blob`; path/context живут только на occurrence `[FIXED]` 03 §2.4 | `unit_id REFERENCES parsed_unit` (`code/mod.rs:146`), `qualified_name`/`context_hash` на occurrence | `occurrence_implies_member_file_with_non_null_source_blob` (new, SQL-sweep), `content_shared_tables_carry_no_path_or_context_field` | as-built |
| `resolved_graph_edge.edge_kind` `[OPEN: final graph semantics]` 03 §2.4 | `edge_kind` свободный текст, `resolution CHECK IN('heuristic','syntax','lsp')` (`code/mod.rs:170-171`) | `references_and_edges_constraints` | `[OPEN]` не закрыт молча (домен `edge_kind` не ограничен CHECK; только `resolution`) |

Spec 03 §4.2 — Text cache (cache.sqlite):

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `normalized_text_cache` derived from `source_blob`; `blob_id` PK, `normalized_text NOT NULL` `[SPEC]` 03 §4.2 | `NORMALIZED_TEXT_CACHE_DDL` (`cache/open.rs:52`), `normalize` (`code/normalize.rs:76`), `derive_content_blob` (`code/normalize.rs:143`) | `roundtrip_derive_cache_read`, `cache_delete_reconstructs_identically_from_source_blob` | as-built |
| Normalized text хранится ТОЛЬКО в кэше, не в `content_blob`; state-строки path-/text-free `[SPEC]` 03 §4.2/01 §5.1 | derive/verify (`cache/text.rs:121`) | `no_state_table_stores_normalized_text`, `content_blob_state_row_has_no_text_and_reuse_is_idempotent` | as-built |
| Кэш полностью rebuildable; на mismatch delete+regenerate из `source_blob` `[SPEC]/[FIXED]` 03 §4.2/§4.4 | `verify_cached_text`+`delete_normalized_text` (`cache/text.rs`) | `invalid_row_is_detected_and_regenerated`, `cache_delete_reconstructs_identically_from_source_blob`, `state_untouched_on_cache_rebuild` | as-built |

Spec 06 §2/§2.1/§2.2 — Reconcile / parsing / skip policy:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| pipeline создаёт `file_revision(+source_blob)` на изменённых файлах; occurrences только для members `[FIXED]` 06 §2 | seam `insert_file_revision`/`insert_generation_file`/`insert_occurrence` (`code/{revision,membership}.rs`) | `happy_path_insert_and_read`, `occurrence_implies_member_file_with_non_null_source_blob` (new) | as-built (orchestration builder — group 05, seam) |
| spans byte offsets; unsupported encoding → `skipped_file(reason='encoding')` `[FIXED]` 06 §2.1 | classify encoding-gate (`classify/mod.rs:113`) | `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new, encoding), classify unit `one_fixture_per_reason` | as-built |
| skipped files — нет occurrences, отсутствуют в searchable generation; 6 reasons `[FIXED §10]` 06 §2.2 | `classify` (`classify/mod.rs:89`), `insert_skipped_file` (`code/membership.rs:131`) | `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new, 6/6), `classified_skip_becomes_non_member_with_no_occurrence` | as-built |
| precedence ignored→huge→lfs→binary→encoding→secret, first match; huge строго `>` cap `[SPEC]` 06 §2.2 | `classify` (`classify/mod.rs:97-122`), huge `>` (`classify/mod.rs:102`) | classify unit `precedence_*`, `huge_exact_size_edge`, `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new) | as-built |
| Детерминизм parser output per `(content_hash,parser_fingerprint)`; structural sharing без дублей `MUST`/`[FIXED gate]` 06 §2.1 | reuse по ключу; `create_or_reuse_file_revision` (`code/source.rs:218`) | `reuse_is_idempotent_on_retry`, `same_key_reuses_row` | as-built (сам parser — group 04, seam) |

Spec 12 §2/§5 — Redaction & source-blob policy:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Общий версионированный redaction scanner (`redaction_version`) перед записью; secret-файлы → `skipped(secret)`, нет `source_blob`/occurrences `[FIXED]/[SPEC]` 12 §2/§5 | `redaction::Scanner` (`core/redaction/mod.rs`, `REDACTION_VERSION=1`), classify secret-gate (`classify/mod.rs:118`) | `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new, secret), core redaction unit (8) | as-built |
| Strict invariant: `no source_blob ⇒ file is not part of the canonical indexed generation (no occurrences)` `[FIXED]` 12 §5 | composite FK + `source_blob NOT NULL` (structural biconditional) | `occurrence_implies_member_file_with_non_null_source_blob` (new), `occurrence_requires_generation_file_member`, `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new) | as-built |
| binary/lfs/huge/secret/ignored/encoding → `skipped_file(path, reason, optional content_hash)`, нет searchable occurrences `[FIXED]` 12 §5 | `skipped_file` (path, reason, nullable `content_hash`) (`code/mod.rs:132-139`) | `every_skip_reason_yields_no_occurrence_and_no_source_blob` (new), `skipped_file_reason_and_no_occurrence` | as-built |
| `non_rebuildable` (чтение диска на query-time) отвергнут; canonical reproducibility = local copy каждого индексированного source (zstd) `[FIXED]` 12 §5 | `source_blob` хранит точные байты (zstd keep-if-smaller, `code/source.rs:162`); read-back из state, не с диска | `roundtrip_lf_crlf_mixed_and_non_ascii`, `compression_roundtrips_in_db`, `live_file_mutation_does_not_affect_stored` | as-built (search-facing consumers 09/12 отложены) |

Заметки G03 (не отклонения):

- Gate-hardening (только тест-код + `PROGRESS.md`, продуктовый код не тронут — не feature,
  не D-NNN, по прецеденту G01/G02): 3 новых теста закрывают явные deliverables карточки G03.
  `occurrence_implies_member_file_with_non_null_source_blob` (`crates/store/tests/code.rs`) —
  property-инвариант через SQL-sweep всей цепочки `occurrence → generation_file →
  file_revision` с `source_blob IS NOT NULL` (0 сирот, non-vacuous: 2 occurrence в фикстуре);
  `source_blob_is_not_nullable` (там же) — негативный NOT-NULL через raw INSERT.
  `every_skip_reason_yields_no_occurrence_and_no_source_blob` (`crates/index/tests/classify.rs`)
  — «обратное отсутствие» для всех 6 skip reasons (было покрыто только Binary/Ignored на
  уровне store).
- «Property test» из карточки реализован **детерминированным** инвариант-тестом (перечисленные
  кейсы + SQL-обход всех occurrences), НЕ рандомизированным proptest: соблюдён запрет
  недетерминизма (CLAUDE.md/README DoD) и не введена новая dev-зависимость (`git diff
  Cargo.lock` пуст).
- Явные deliverables карточки G03 подтверждены: (a) property occurrence⇒generation_file⇒
  source_blob — структурен и покрыт end-to-end; (b) для каждого skip reason обратное
  отсутствует — 6/6; (c) удаление cache → идентичное восстановление normalized text из
  `source_blob`; (d) schema audit (обязателен) — зелёный, 5 тестов итерируют все таблицы
  мигрированного `state.sqlite`.
- `[OPEN]` осмотрены и не закрыты молча: `resolved_graph_edge.edge_kind` (03 §2.4) остаётся
  open (домен `edge_kind` не ограничен CHECK; только `resolution`). Прочие `[OPEN]` (O4 языки)
  относятся к group 04 и вне scope 03.
- Отложено by-design (seam на месте, не преждевременная реализация): generation-builder /
  deterministic `occurrence_id` / reconcile orchestration (group 05), реальный
  `parser_fingerprint` (group 04), search-facing snippet/context/overview и 8 KiB snippet-cap
  (12 §2, spec 09/12). На G03 проверено лишь отсутствие преждевременного нарушения/coupling.
- Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются
  `resolved`); историческое evidence не переписывалось. Итог: **PASS** — группа 04
  разблокирована.

### G04 — трейс требование → artifact/test

Дата 2026-07-21, исполнитель Claude Opus 4.8. Команды воспроизводимы из строки evidence G04
выше. Ссылки на код — `file:line`/`file:symbol` на момент сверки. `отложено` = требование
нормативно, но реализуется позже по плану; на G04 проверено лишь отсутствие преждевременного
нарушения/coupling и наличие seam. Все перечисленные тесты уже существовали в группе 04 — gate
не добавлял ни продуктового кода, ни новых тестов.

Spec 03 §2.3.1 — `parser_fingerprint` `[FIXED semantics, format [SPEC]]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Canonical sorted `key=value` над `chunk/grammar/lang/norm/queries`, ASCII-сортировка, `;` — разделитель без хвостового терминатора `[FIXED sem, [SPEC] format]` | `parser_fingerprint`/`canonical_kv`/`FingerprintComponents` (`parse/fingerprint.rs`) | `parse::fingerprint::typescript_fingerprint_is_exact_golden`, `canonical_kv_is_order_independent`, `fingerprint_keys_are_ascii_sorted_and_complete`, `parse_fixtures::fingerprints_are_reconciled_after_linking_the_grammars` | as-built |
| Любой bump boundary-версии меняет fingerprint; версии/дескрипторы запиннены `[SPEC]` | `LanguageDescriptor`+`CHUNK_POLICY_VERSION`/`BOUNDARY_NORM_VERSION` (`parse/fingerprint.rs`) | `bumping_any_boundary_version_changes_fingerprint`, `version_constants_and_descriptors_are_pinned` | as-built |
| Язык по extension; byte-identical `.c`/`.cpp` (лок. аналог `.ts`/`.js`) → разные `file_revision` `[FIXED]` | `select_language` (`parse/language.rs`) + reuse-ключ с `parser_fingerprint` (`code/source.rs`) | `every_language_has_a_distinct_fingerprint`, `ambiguous_extension_yields_distinct_file_revisions`, `revision_reuse_key_is_content_hash_and_fingerprint` | as-built |
| Spans = байтовые смещения в точный `source_blob`; unsupported encoding ⇒ файл skipped `[FIXED]` | `ByteSpan` (`parse/output.rs`), encoding-gate (`classify/mod.rs`) | `byte_spans_are_exact`, `unicode_spans_are_byte_offsets`, `every_skip_reason_yields_no_occurrence_and_no_source_blob` (encoding) | as-built |

Spec 03 §2.3/§2.4 — `SyntaxLocator`, `parsed_unit`, `content_blob`, `unresolved_reference`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `SyntaxLocator = {language, syntax_path\|local_ordinal, signature_fingerprint, blob_id}` — NO path; двухслойная path-freedom (тип + отвержение path-ключей) `[FIXED]/[SPEC]` | `SyntaxLocator`/`SyntaxAnchor`/`parse()` (`parse/locator.rs`) | `parse::locator::serialization_is_canonical_and_path_free`, `parse_rejects_path_like_keys`, `parse_rejects_unknown_and_malformed`, `syntax_locator_round_trips_both_anchors` | as-built |
| Derivation: named route `<lang_kind>:<name>/…` иначе `o:<ordinal>`; `signature_fingerprint` = domain-separated hash canonical descriptor; каноническая сортировка юнитов `[SPEC]` (ADR-0002, O7 locator-half) | движок `adapter/mod.rs` + `signature.rs` (`Domain::SignatureFingerprint`) | `named_route_and_ordinal_fallback`, `locator_and_signature_goldens`, `overloads_differ_by_signature`, `signature::{descriptor_is_order_sensitive_and_hex,identical_descriptors_match}` | as-built |
| Adapters дают `symbol/file/…/fallback_chunk`, byte spans, parents, unresolved refs; **все kinds индексируются** `[FIXED]` 06 §2.1 | `adapter/{typescript,javascript,rust}.rs` (+ `.scm`), 5-kind CHECK (`code/mod.rs:114`) | `parse_fixtures::parser_fixtures_match_expected_units_and_refs` (≥4/язык), per-adapter unit-тесты (`extracts_core_declaration_kinds…`, `method_parent…`, `error_input_yields_fallback_chunk…`), `every_produced_unit_kind_is_searchable` | as-built (`config_section`/`text_section` — universal-file путь групп 05/08, вне O4, отложено by-design) |
| `content_blob` = identity+metadata, dedup по content-derived PK; text только в кэше `[SPEC]` | `content_blob` (`code/mod.rs:103`), `create_or_reuse_content_blob`/`derive_content_blob` | `persist::content_blobs_are_deduped`, `schema_audit::no_state_table_stores_normalized_text` | as-built |
| `parsed_unit` reuse по natural key `UNIQUE(file_revision_id,unit_kind,syntax_locator,span_start,span_end)`, НЕ по `unit_id` `[SPEC]` | `create_or_reuse_parsed_unit`/`parsed_unit_id_by_natural_key` (`code/revision.rs`) | `code::create_or_reuse_parsed_unit_reuses_by_natural_key`, `code::parsed_unit_foreign_keys_and_unique_locator` | as-built |
| Content-shared строки без path/context/generation-полей `[FIXED]` 03 §2.3 / 01 §5.1 | `SCHEMA_V3` (`code/mod.rs:89`): `file_revision`/`content_blob`/`parsed_unit`/`unresolved_reference` | `schema_audit::content_shared_tables_carry_no_path_or_context_field`, `path_columns_live_only_on_path_bearing_tables`, `persist::shared_rows_have_no_path_or_context` (incl. `unresolved_reference`) | as-built |
| Persistence: атомарная tx (нет частичного графа 06 §2.1), идемпотентный create/reuse, refs clear-then-reinsert, те же `unit_id` под retry, parent перед children `[SPEC]` (T04-06) | `persist_parse_output` (`parse/persist.rs`), `delete_unresolved_references_for_revision` (`code/membership.rs`) | `persist::{persist_is_idempotent_under_retry_with_no_duplicates,persisted_row_set_is_input_order_independent,rollback_leaves_no_partial_graph,references_carry_file_source_and_kind,parents_link_to_persisted_ids}`, `code::delete_unresolved_references_for_revision_is_scoped` | as-built |

Spec 06 §2.1 — Parsing rules `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| tree-sitter; язык по extension/path (`.c`/`.cpp` consequence) `[FIXED]` | adapters + `select_language` | `ambiguous_extension_yields_distinct_file_revisions`, `every_language_has_a_distinct_fingerprint` | as-built |
| Parser output детерминирован для `(content_hash, parser_fingerprint)`; fixtures это утверждают `MUST` (14 §5) | pure `parse(&[u8])->ParseOutput` (adapters) | `parse_fixtures::parser_output_is_byte_identical_on_reparse` (весь корпус ×2), per-adapter `parse_is_deterministic` (TS/JS/Rust) | as-built |
| Structural sharing: правка одного файла НЕ дублирует юниты неизменённых `[FIXED gate]` | `parsed_unit` keyed per `file_revision`; `content_blob` dedup; natural-key reuse | `persist::content_blobs_are_deduped`, `create_or_reuse_parsed_unit_reuses_by_natural_key`, `persist_is_idempotent_under_retry_with_no_duplicates` | as-built (кросс-generation sharing — group 05, здесь заложен фундамент content-стороны) |

Spec 14 §1/§5 — Fixtures & determinism:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Fixture-семейство 1: source → expected units (kind/span/locator) по языкам `[FIXED]` | `fixtures/parser/index.json` (25), `fixtures/schema/parser.schema.json`, `validate.py` | `parser_fixtures_match_expected_units_and_refs`, `python3 fixtures/tooling/validate.py` (25 кейсов, no backend keys) | as-built |
| Parser determinism: тот же `(content, parser_fingerprint)` ⇒ byte-identical unit set | adapters | `parser_output_is_byte_identical_on_reparse` | as-built |
| Deterministic IDs стабильны под retry reconcile `[FIXED]` | `persist_parse_output` возвращает те же `unit_id` на reuse | `persist_is_idempotent_under_retry_with_no_duplicates` | as-built (repository-move-часть — group 05, отложено) |
| Schema audit lint: нет path/context на content-shared; нет durable FK на path-производное значение (01 §5.1) | `schema_audit.rs` (итерирует все таблицы) | `content_shared_tables_carry_no_path_or_context_field`, `no_foreign_key_targets_a_path_hash_or_stray_path_column`, `path_columns_live_only_on_path_bearing_tables` | as-built |

Spec 15 — Roadmap O4/O7:

| Маркер | Статус | Artifact / verifying test |
| --- | --- | --- |
| O4 — набор языков первого релиза | **RESOLVED — ADR-0001** (TypeScript, JavaScript, Rust) | `language_coverage::{benchmark_language_is_in_the_selected_set,language_set_is_consistent_across_spec_and_code,non_corpus_languages_are_acknowledged_in_adr}`, `adr_links` |
| O7 — final `SyntaxLocator` / graph semantics | **SyntaxLocator-часть RESOLVED — ADR-0002**; graph-семантика = design follow-up | locator/signature-goldens (выше); `resolved_graph_edge.edge_kind` остаётся свободным текстом `[OPEN]` — не закрыт молча (`code::references_and_edges_constraints`) |

Заметки G04 (не отклонения):

- **Gate не писал ни продуктового кода, ни gate-hardening тестов** (в отличие от G01/G02/G03,
  где добавлялись schema-audit/invariant-тесты): все требуемые карточкой G04 проверки —
  двойной прогон корпуса с byte-compare, span-против-blob, системный schema/code audit — уже
  полностью покрыты тестами группы 04 (T04-02…T04-06). Правка gate = только `PROGRESS.md`.
- **Двойной прогон корпуса** подтверждён на двух уровнях: (1) внутрипроцессный —
  `parser_output_is_byte_identical_on_reparse` парсит все 25 кейсов дважды через свежие
  parser-instance'ы и сравнивает `ParseOutput` побайтно; (2) межпроцессный — `cargo xtask ci`
  выполнен дважды (обычный + `CARGO_NET_OFFLINE=true`), `test --workspace` зелёный оба раза.
- Content-shared audit для `unresolved_reference` разнесён по двум тестам (таблица не входит в
  `CONTENT_SHARED_TABLES` из `schema_audit.rs`, т.к. объявлена в §2.4): path-колонки ловит
  `schema_audit::path_columns_live_only_on_path_bearing_tables` (итерирует **все** таблицы, а
  `unresolved_reference` не в path-bearing множестве), context/generation — явный forbidden-list
  в `persist::shared_rows_have_no_path_or_context`. Фактических нарушений нет; таблица path-free.
- `[OPEN]` осмотрены и не закрыты молча: `resolved_graph_edge.edge_kind` (03 §2.4, graph-часть
  O7) остаётся open — домен `edge_kind` без CHECK, `find_usages`/`get_dependencies` не реализованы.
- Полнота language coverage (условие карточки для разблокировки generation builder): TS 9 / JS 8 /
  Rust 8 кейсов, каждый язык ≥4, все категории syntax/error/empty/unicode. Сам generation builder
  (group 05) — вне scope gate.
- Отложено by-design (seam на месте, не преждевременная реализация): generation-builder /
  deterministic `occurrence_id` / reconcile (group 05); универсальные `config_section`/
  `text_section` kinds (universal-file путь групп 05/08, вне O4-набора языков — T04-01);
  graph-семантика O7 (design follow-up). На G04 проверено лишь отсутствие преждевременного
  нарушения/coupling (guardrail T10: 0 backend/model/network SDK, `git diff Cargo.lock` пуст).
- Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются
  `resolved`); историческое evidence не переписывалось. Итог: **PASS** — группа 05
  разблокирована.

### G05 — трейс требование → artifact/test

Дата 2026-07-21, исполнитель Claude Opus 4.8. Команды воспроизводимы из строки evidence G05
выше. Ссылки на код — `file:line`/`file:symbol` на момент сверки. `отложено` = требование
нормативно, но реализуется позже по плану; на G05 проверено лишь отсутствие преждевременного
нарушения/coupling и наличие seam. Все перечисленные тесты уже существовали в группе 05 — gate
не добавлял ни продуктового кода, ни новых тестов.

Spec 04 §1 — Generation lifecycle `[FIXED]/[SPEC]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Легальный набор `building→projection_ready→active→retiring` + `building`/`projection_ready→failed`, self = идемпотентный no-op; `active→failed` и `building→active` **отвергаются** `[SPEC]` | `GenerationState::check_transition` (`registry/generation.rs:96`) | `check_transition_covers_the_whole_matrix` (полная 5×5 матрица), `generation_state_round_trips` | as-built |
| Guarded read-then-write в одной tx, typed `GenerationTransitionError`, **no mutation** на reject, corrupt enum → `FromSqlConversionFailure` (не тихий default) `[SPEC]` | `transition_generation`/`generation_state` (`registry/generation.rs:217`/`260`) | `illegal_transition_is_typed_error_and_rolls_back`, `unknown_generation_transition_is_typed_error`, `generation_state_rejects_corrupt_enum`, `happy_path_walk_succeeds` | as-built |
| `∅→building` allocate: per-worktree monotone `MAX(number)+1` над всеми строками, `UNIQUE(worktree_id,generation_number)` backstop, single-writer сериализует гонку `[SPEC]` | `allocate_generation` (`registry/generation.rs:177`) | `allocation_is_monotone_per_worktree`, `allocation_numbers_are_per_worktree`, `concurrent_allocation_yields_unique_numbers` (24 конкур.), `allocate_unknown_worktree_rejected` | as-built |
| `retiring`/`failed` **никогда** не роутятся — reader отдаёт только `state='active'` `[FIXED]`; one-active инвариант процедурный (asserted) | `active_generations` (`registry/generation.rs:289`) | `one_active_invariant` (позитив+негатив), `cross_worktree_current_generation_rejected` | as-built |

Spec 06 §1 — Triggers & scheduling `[FIXED principle]/[SPEC]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| **watcher = hint, reconcile = truth** `[FIXED]` | pure `watch_event_to_trigger` (`reconcile/watcher.rs`) + `Debouncer` (`reconcile/schedule.rs`) | `reconcile::watcher::tests::*` (4), `reconcile::schedule::tests::*` (13) | as-built |
| debounce `[SPEC: 500 ms quiet window]`, coalescing в один pending, `Strict` wins эскалация | `DEBOUNCE_MS=500` (`schedule.rs:26`), `Debouncer::{record,take_due}` | `debounce_resets_on_each_fs_event`, `coalescing_collapses_burst_into_one`, `startup_periodic_manual_git_coalesce_into_one_strict`, `scan_mode_selection_table` | as-built |
| periodic strict `[SPEC: every 6 h while open]`, self-inject backstop | `PERIODIC_MS=6h` (`schedule.rs:30`) | `periodic_self_injects_every_interval` | as-built |
| watcher **overflow → mandatory strict** (never resync from events) `[FIXED]` | `TriggerKind::WatcherOverflow`→Strict; `Rescan→WatcherOverflow` (`watcher.rs`) | `overflow_forces_strict_immediately`, `rescan_maps_to_overflow_regardless_of_git` | as-built |
| Fast-path cache `(mtime,size,file_id)` **advisory only**, mismatch/doubt → hashing; Strict хеширует всё `[SPEC]` | `resolve_content_hash` (`scan/mod.rs:235`, `Strict⇒None`), `StatKey::is_trustworthy_match` (`stat_cache.rs:55`, `file_id.is_some()`) | `stat_mismatch_escalates_to_hashing`, `fast_trusts_full_match_but_strict_rehashes_all`, `unknown_cached_file_id_never_reuses`, `reconcile_mode_fast_uses_cache_strict_rehashes` | as-built |
| Canonical `(normalized_path,display_path)`-sorted manifest; детерминизм-guards `git_global(false)`/`parents(false)`/`follow_links(false)`/regular-only/`.git` pruned | `scan` + `WalkBuilder` (`scan/mod.rs:157`) | `manifest_is_deterministic_across_scans`, `nested_gitignore_and_negation`, `symlinks_are_excluded_and_not_followed`, `git_directory_is_never_indexed`, `prune_roots_excludes_nested_worktree_subtree` | as-built |
| Один writer на worktree; concurrent triggers → **ровно одно** следующее поколение; cancellation drop-safe на tx boundary `[SPEC]` | `WorktreeReconciler::run` biased `select!` (`driver.rs:251`) | `concurrent_triggers_make_one_next_generation`, `driver_runs_a_reconcile_on_shutdown_flush`, `cancellation_leaves_active_generation_valid` | as-built |

Spec 06 §2 (+§2.1/§2.2) — Reconcile pipeline `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| **Structural sharing:** правка одного файла НЕ дублирует юниты неизменённых `[FIXED gate]` | pre-check `file_revision_id_by_content_key` reuse без read/parse (`build.rs:290`) | `editing_one_file_shares_untouched_units`, `retry_produces_no_duplicate_content` | as-built |
| rename переиспользует контент, но минтит fresh path-scoped occurrences `[FIXED]` | `persist_member_from_revision` + `occurrence_id(gen,path,unit)` (`build.rs`) | `rename_shares_content_but_changes_occurrences` | as-built |
| Каждый файл — одна tx (bounded phase); `building→projection_ready` **только** при полной сборке | `build_generation`/`run_build` (`build.rs:148`/`225`) | `complete_build_reaches_projection_ready_without_activating` | as-built |
| На любой ошибке → `failed` (best-effort), distinct row set, прежнее поколение не мутируется; retry без дублей `[FIXED]` | error-funnel (`build.rs:204`), `create_or_reuse_*` | `read_failure_marks_failed_and_leaves_prior_untouched`, `retry_after_failure_builds_valid_generation_without_duplicates`, `failpoint_at_each_build_phase_marks_failed_and_leaves_active` (все 3 фазы) | as-built |
| skip reasons `binary\|lfs\|huge\|secret\|ignored\|encoding`; skipped → **нет occurrences** `[FIXED §10]`; huge stat-only unread | `write_skipped`/`classify` (`build.rs`), huge-gate (`build.rs:274`) | `huge_file_is_skipped_without_reading`, `supported_extension_binary_content_is_skipped` | as-built (полная precedence-цепочка `ignored→huge→lfs→binary→encoding→secret` — тесты `classify` группы 03) |
| deterministic occurrence_id per unit; unsupported language → **deferred** (не index/не skip) `[SPEC]` | `occurrence_id`; `select_language`→None→defer (`build.rs:282`) | `unsupported_language_file_is_deferred` | as-built |

Spec 06 §6 — Non-git roots:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| non-git reconcile идентичен минус git-триггеры (watcher+periodic); `.gitignore` чтится `require_git(false)` | `GitHead` dropped non-git (`schedule.rs`/`watcher.rs`), `scan` require_git (`scan/mod.rs:171`) | `git_head_is_dropped_for_non_git_worktrees`, `git_meta_path_is_a_git_trigger_only_on_git_worktrees`, `non_git_and_git_modes_produce_the_same_manifest` | as-built |

Spec 03 §1.2/§2.4 — deterministic `occurrence_id` `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `H(occurrence_id: generation_id, normalized_path, unit_id)`, length-prefixed framing, стабилен под retry/reconcile, независим от порядка вставки `[FIXED]` | `code::occurrence_id` (`code/membership.rs:39`, `Domain::OccurrenceId`) | `occurrence_id_matches_domain_hash_golden` (64-hex golden), `occurrence_id_is_retry_and_order_independent`, `occurrence_id_field_boundaries_are_unambiguous` | as-built |

Spec 14 §2/§4/§5 — Acceptance/consistency/determinism `[FIXED set]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| §2 **sharing** gate: изменение одного файла не дублирует юниты неизменённых | (06 §2.1 выше) | `editing_one_file_shares_untouched_units` | as-built |
| §2 **reliability**: watcher overflow пойман strict reconcile | (06 §1 overflow выше) | `overflow_forces_strict_immediately` | as-built |
| §2 **idempotency**: retried reconcile ⇒ нет дублей (deterministic IDs) | `create_or_reuse_*` + `occurrence_id` | `retry_produces_no_duplicate_content`, `retry_after_failure_builds_valid_generation_without_duplicates` | as-built |
| §5 deterministic IDs стабильны под retry reconcile и repository move `[FIXED]` | occurrence_id зависит только от кортежа (path — `normalized_path`, не durable id) | `occurrence_id_is_retry_and_order_independent` + retry-тесты выше | as-built (полный repository-move flow — path-independence уже структурна в §2.4; end-to-end move — позже) |
| §4 generation-mixing / FTS staleness / two-axis interleaving `[SPEC mechanics]` | — | — | отложено — read-lock/hybrid/FTS (группы 08/09/12); на G05 seam «projection не активируется» доказан `complete_build_reaches_projection_ready_without_activating` |

Заметки G05 (не отклонения):

- **Gate не писал ни продуктового кода, ни gate-hardening тестов** (в отличие от G01/G02/G03):
  карточка G05 (A→B fixtures, retries, concurrent scans, structural sharing, strict-overflow,
  immutable active, no-routing-by-retiring) полностью покрыта тестами T05-01…T05-05. Правка
  gate = только `PROGRESS.md`.
- Спека уже несёт as-built заметки по T05-01 (04 §1, 03 §2.4), T05-02/T05-04 (06 §1), T05-03
  (06 §2); T05-05 отражён forward-ref в 04 §1 («error edge fires from `building`… never from the
  already-serving `active`, T05-05») и 06 §1 (typed failure/backoff bookkeeping) — сверено
  построчно с кодом, расхождений нет. Observability-слой T05-05 (`ReconcileFailure`,
  backoff-счётчики `driver.rs:116`/`schedule.rs:36`) намеренно **in-memory**; персист `last_error`
  в `worktree_projection_state` — группа 07 (задокументировано в `driver.rs`).
- `[OPEN]` осмотрены и **не закрыты молча**: retention `K`/`T` window (06 §5) — группа 06;
  `[BASELINE]` числа acceptance gates (14 §2) — фиксируются после v1-run, post-v0.
- Отложено by-design (seam на месте, не преждевременная реализация): projection switch /
  activation / `worktree_projection_state` / retire N + promote N+1 (05 §5 — группа 07; карточка
  G05 запрещает имитировать активацию обходным update, доказано
  `complete_build_reaches_projection_ready_without_activating`); read-lock и hybrid
  generation-mixing (06 §3, 14 §4 — группы 09/12); live `spawn_watcher` в детерминированном CI
  (группа 15); Retention/GC `retiring/failed→deleted` (06 §5 — группа 06).
- guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей), grep запрещённых
  backend/model/network SDK = NONE; git-free: `crates/index/src/{scan,reconcile}` без
  git2/gix/libgit/`Command::new`.
- Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001/D-002/D-003 остаются
  `resolved`); историческое evidence не переписывалось. Итог: **PASS** — группа 06
  разблокирована.

### G06 — трейс требование → artifact/test

Дата 2026-07-22, исполнитель Claude Opus 4.8. Команды воспроизводимы из строки evidence G06
выше. Ссылки на код — `file:line`/`file:symbol` на момент сверки. `отложено` = требование
нормативно, но реализуется позже по плану; на G06 проверено лишь отсутствие преждевременного
нарушения/coupling и наличие seam. Все перечисленные тесты уже существовали в группе 06 — gate
не добавлял ни продуктового кода (кроме 1 строки doc-precision, поведение не изменено), ни новых
тестов.

Spec 06 §5 — Retention/GC: pin roots `[FIXED]`, `K`/`T` `[OPEN: O6]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Pin: `active` + `building`/projection-target безусловно `[FIXED]` | `mark_pins` `retention.rs:194-205` | `state_roots_pin_active_building_and_target_only` `retention.rs:773` | as-built |
| Pin: last `K` retired **OR** окно `T` (union) `[OPEN: K,T]` | `mark_pins` `retention.rs:211-230`, `RetentionParams` `:107` | `last_k_boundary_keeps_highest_numbers:792`, `window_boundary_is_inclusive_lower_edge:833`, `k_and_t_are_a_union:873`, `oversized_window_saturates_and_pins_all:856` | as-built |
| Pin: memory/audit/export references | `ExternalPins` `retention.rs:149`, `mark_pins:233-235` | `external_references_pin_regardless_of_state:947`, `referenced_file_revisions_pass_through:988` | отложено — seam empty; реальные refs → группы 14/16 |
| Pin: активные job leases (temporary) | `JobLease` `retention.rs:135`, `mark_pins:238-242` | `only_unexpired_leases_pin:897`, `lease_pins_generation_absent_from_slice:931`, `lease_pins_over_db_path tests/retention.rs:222` | отложено — seam; lease-таблица → reconcile/embedding |
| `failed` НЕ пиннится retention'ом (04 §1 = GC target) `[SPEC]` | `mark_pins` `retention.rs:202-203` | `failed_is_never_pinned_by_retention:968` | as-built |
| mark детерминирован, worktree-isolated | `mark_pins:185` (pure, `BTreeSet`), `pinned_generation_roots:295`, `generation_meta_for_worktree:257` | `mark_is_order_independent:1004`, `last_k_is_isolated_per_worktree tests/retention.rs:148`, `db_roots_match_pure_mark_and_cover_state_roots tests/retention.rs:107`, `window_over_created_at_from_real_rows tests/retention.rs:196`, `empty_worktree_pins_nothing tests/retention.rs:256` | as-built |
| `K`/`T` из config; дефолты provisional/не нормативны | `RetentionParams::from_storage_config` `retention.rs:119` | `params_from_default_storage_config:1026`, `from_storage_config_saturates_absurd_ttl:1034` | as-built (O6 open — см. Spec 15 ниже) |

Spec 06 §5 — sweep: delete order `[FIXED]` + bounded tx `[SPEC ≤ 500 rows/tx]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Candidate = `retiring`/`failed` AND not pinned; store-wide pin union | `store_wide_pinned` `retention.rs:506`, `setup_scratch:431` | `pinned_rows_survive tests/retention.rs:462`, `empty_sweep_is_a_noop:834` | as-built |
| Delete order occurrences/edges → membership/skips → generation → revisions/units/blobs `[FIXED]` | `PHASES` `retention.rs:543-597` (edge до occurrence, FK `foreign_keys=ON`) | `orphan_graph_fully_removed tests/retention.rs:564` (9 таблиц → 0) | as-built |
| Shared revision retained until final ref (reachability closure) | `sweep_orphan_rev` `setup_scratch:478-486` | `shared_revision_retained_until_final_ref:620`, `external_revision_pin_retains_shared_content:703` | as-built |
| Bounded tx ≤ 500 rows/tx `[SPEC]`; портируемый `LIMIT`-в-подзапросе | `SWEEP_BATCH_ROWS=500` `retention.rs:314`, `delete_batched:718` | `sweep_batch_ceiling_is_500:1044`; функц. при ceiling=1 `deep_unit_tree_deleted_leaf_first_across_batches:793` | as-built |
| leaf-first `parsed_unit` (self-FK across batches) | `PHASES` `delete_pred` `retention.rs:576-582` | `deep_unit_tree_deleted_leaf_first_across_batches:793` | as-built |
| Resumable без progress-таблицы (батч = committed tx, пересчёт из live) | `run_sweep:670`, `run_sweep_with_batch:682`, comment `:265-269` | `interruption_between_batches_resumes retention_sweep.rs:225`, `resumable_hard_kill_via_sigabrt:277` (SIGABRT) | as-built |
| Dry run (`plan_sweep`) mutates nothing (temp-only) | `plan_sweep:624`, `setup_scratch` doc `:413-418` (уточнён) | `dry_run_mutates_nothing tests/retention.rs:737` | as-built |
| Shard/FTS retired-строки исчезают через desired-set, НЕ sweep'ом | sweep их не трогает (`retention.rs:273-274`) | — | отложено — desired-set reconciliation 05 §8 (группа 07) |

Spec 06 §5 / 03 §3 — checkpoint/VACUUM policy by metrics `[FIXED]`, числа `[SPEC]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| checkpoint/VACUUM by metrics, **never by schedule** `[FIXED]` | нет вызова в `retention`/`housekeeping` (grep VACUUM/wal_checkpoint = NONE); метрики-seams `StateWriter::queue_capacity/available_slots` `writer.rs:88/:94`, `SweepReport`/`ShardSweepReport` counts | — | отложено — драйвер daemon (группа 15); проверено отсутствие VACUUM-by-schedule и преждевременного checkpoint |

Spec 05 §8 — Shard lifecycle: orphan shard dirs `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Orphan shard dir (no worktree row) GC'd at startup sweep `[FIXED]` | `sweep_orphan_shard_dirs housekeeping.rs:117`, `run_orphan_shard_sweep:177`, `all_worktree_ids worktree.rs:560` | `orphan_removed_and_live_retained housekeeping.rs:216`, `orphan_shard_swept_worktree_backed_retained tests/housekeeping.rs:85` | as-built (startup/periodic-триггер → daemon 15) |
| orphan = нет строки; `detached`/`removing` (строка есть) сохранены | `housekeeping.rs:141-151`, module doc `:13-16` | `tests/housekeeping.rs:107-113` | as-built |
| idempotent / dry-run / resume; консервативно (только директории, не-UTF-8 сохранены) | `sweep_orphan_shard_dirs` (pure fs + live set) | `repeated_sweep_is_idempotent housekeeping.rs:241` + `tests/housekeeping.rs:138`, `dry_run_reports_without_removing:229`/`tests:118`, `non_directory_entries_are_left_alone:256`, `non_utf8_dir_name_is_retained:296`, `missing_projection_dir_is_empty:268`, `all_live_removes_nothing:277`, `empty_projection_is_a_noop tests:155` | as-built |
| Quarantine ≤ 2 rebuild cycles `[SPEC]`; grace-destroy 7d `remove`/`detach` `[SPEC]` | — | — | отложено — quarantine → T07-04; grace-destroy (нужна миграция `removed_at`) → 07/09 (D-004) |

Spec 07 §6/§5 — Spool GC `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Session spool GC: > 14d absent AND fully committed cursor; uncommitted-retained; delete-after-commit `[FIXED]` | — (нет `spool_import_cursor`; `local-rag-hook` = stub) | — | отложено — T13-05 (spool import/cursor группа 13, D-004; дубль spool-части T06-03) |

Spec 03 §2.1 / §4.4 — инварианты (опора):

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| `retiring` GC/audit only, **never** routing `[FIXED]` | candidate-guard `retention.rs:251-253` (свипаются только `retiring`/`failed`; `active`/`building` защищены от stale pin snapshot) | (покрыто G05: `active_generations` `generation.rs:289`, `one_active_invariant`) | as-built |
| Rebuildability `[FIXED]`: projection/cache reconstructible из `state.sqlite` | sweep мутирует только canonical `state.sqlite`; shard/FTS через desired-set (не durable-барьер) | (acceptance `rebuild`-gate 14) | отложено — rebuild-gate группы 07/14 |

Spec 15 §4 — Roadmap O6:

| Маркер | Статус | Artifact / verifying test |
| --- | --- | --- |
| O6 — Retention `K`/`T` для retired generations (resolved by **usage metrics**; blocks **GC defaults**) | `[OPEN]` — **НЕ закрыт гейтом** | `RetentionParams::from_storage_config retention.rs:119`; дефолты `2`/`168 h` provisional/не нормативны (`core::config::StorageConfig`); `params_from_default_storage_config:1026`, `from_storage_config_saturates_absurd_ttl:1034`. 15 §1: «Steps 1–7 depend on no open question» — шаг 6 (retention/GC pin roots) от O6 не зависит |

Заметки G06 (не отклонения):

- Gate не писал ни продуктового кода (кроме одной строки doc-precision в `setup_scratch`,
  `retention.rs` — комментарий про «read-only connection» приведён в соответствие со спекой
  06 §5 as-built note T06-02 п.5; поведение не изменено, `dry_run_mutates_nothing` зелёный), ни
  новых тестов. Все gate-требования карточки уже покрыты существующими тестами группы 06.
- Явные пункты карточки G06 подтверждены: pin-root truth table полна; crash sweep tests есть
  (вкл. реальный `SIGABRT` в дочернем процессе); transaction batch ceiling `[SPEC ≤500]` = as-built;
  metrics-driven checkpoint/VACUUM policy — deferred seam без преждевременного нарушения.
- `[OPEN]` осмотрены и НЕ закрыты молча: **O6** (`K`/`T`) остаётся open — provisional дефолты не
  нормативны, закрываются usage metrics; условие карточки «O6 не считать закрытым без данных»
  соблюдено.
- Наблюдение (non-blocking): `setup_scratch`/`plan_sweep` материализуют scratch-набор одной
  транзакцией без ≤500-потолка — нормативный потолок относится к sweep-DELETE-батчам, temp-таблицы
  connection-local и не пишут в `state.sqlite` WAL; `[FIXED]` не нарушен, кандидат на будущую
  оптимизацию.
- Отложено by-design (seam на месте, указывает на owning-карточки, D-004 `resolved`):
  ExternalPins/leases (14/16); startup/periodic wiring + production `now_ms`-supplier (daemon 15);
  quarantine-rotation → T07-04; grace-destroy + миграция `removed_at` → 07/09; spool-GC → T13-05.
- guardrail T10: `git diff Cargo.lock` пуст (0 новых зависимостей); grep запрещённых
  backend/model/network SDK в `crates/*/Cargo.toml` = NONE; в `retention`/`housekeeping` нет
  VACUUM/checkpoint (dense/maintenance coupling отсутствует).
- Отклонений не обнаружено; `DEVIATIONS.md` без изменений (D-001…D-004 остаются `resolved`);
  историческое evidence не переписывалось. Итог: **PASS** — группа 07 разблокирована.

### G07 — трейс требование → artifact/test

Дата 2026-07-22, исполнитель Claude Sonnet 5. Команды воспроизводимы из строки evidence G07
выше. Ссылки на код — `file:line`/`file:symbol` на момент сверки. `отложено` = требование
нормативно, но реализуется позже по плану; на G07 проверено лишь отсутствие преждевременного
нарушения/coupling и наличие seam. Все перечисленные тесты уже существовали в группе 07 — gate
не добавлял ни продуктового кода, ни новых тестов.

Spec 05 §1 — `ProjectionStore`/`ShardHandle` contract `[FIXED abstraction, signatures [SPEC]]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Backend-neutral traits, verbatim signatures | `ProjectionStore`/`ShardHandle` `contract.rs:249-289` | (структурно; типы упражняются каждым тестом крейта) | as-built |
| `write_head` — LAST write of any delta/rebuild | `switch.rs:411-421` (head после upsert/delete), `rebuild.rs:391-402` (head после upsert) | `add_change_delete_point_sets`, `unopenable_shard_is_quarantined_and_rebuilt` | as-built |
| Backend chosen at roadmap step 11 `[OPEN]` | единственный имплементор — `FakeProjectionStore` (`fake.rs`, T07-01) | — | НЕ закрыт молча — see guardrail T10 ниже |
| Deterministic point IDs `H(projection_point, worktree_id, occurrence_id, model_space_id, representation_kind)` (05 §3) | `identity::projection_point_id` `identity.rs:102` | `identity::tests::projection_point_id_is_exact_golden`, `representation_kind_changes_the_point_id` | as-built |
| Expected point set — pure fn of `state.sqlite` (05 §4) | `expected::expected_point_ids`/`expected_points` | `expected::tests::*` (5 тестов: empty/cross-product/generation-change/model-change/ids-match) | as-built |

Spec 04 §2 / 05 §5 — Two-axis projection state + write-ahead switch `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Status FSM `clean⇄updating`, `dirty→rebuilding→clean`, `rebuilding→dirty`, self-transitions legal | `ProjectionStatus::check_transition` `projection_state.rs:156-174` | `check_transition_covers_the_whole_matrix` (4×4 полная truth-table, T07-02, не тронут гейтом) | as-built |
| `status='clean'` ⇒ `active==projected` ∧ `target=NULL`; `status='updating'` ⇒ `target`+`op_id` заданы | `check_invariants` `projection_state.rs:279-309` | `clean_invariants`, `updating_invariants` | as-built |
| Одна ось за раз (`BothAxesMovedAtOnce`), NULL-active init — не нарушение | `check_invariants:298-304` | `updating_invariants` (both-axes case + NULL-active init case) | as-built |
| Write-ahead: одна tx, `status→updating`, target tuple, свежий `projection_op_id`, ДО backend-мутации | `switch::write_ahead` `switch.rs:202-228` | `unknown_worktree_is_a_typed_write_ahead_error`, любой `switch()`-тест (write-ahead — первый шаг) | as-built |
| Desired-set reconcile: `expected\existing` upsert, `existing\expected` delete, head последним; НЕ command-log replay | `switch()` `switch.rs:340-449` | `add_change_delete_point_sets`, `unchanged_vectors_not_recomputed` (вектор запрашивается только для отсутствующих точек), `retry_from_arbitrary_partial_fake_set_converges_and_is_idempotent` | as-built |
| Commit: одна tx ПОСЛЕ backend, `active:=target`, `projected:=target`, generation-переход в той же tx | `commit_switch` `switch.rs:241-316` (pre-flight `GenerationState::check_transition` до первой записи) | `final_tuple_is_exact`, `generation_transitions_in_the_same_commit`, `model_axis_only_switch_leaves_generation_active` | as-built |
| Crash между write-ahead и commit ⇒ `status='updating'`, детектируемо | нет explicit commit-вызова при ошибке — функция просто возвращает `Err` | `missing_vector_is_typed_and_leaves_status_updating`; F1–F4 (`fault_matrix.rs`/`switch_faults.rs`) | as-built |
| Per-worktree WRITE lock (05 §5 шаг 3) | — (ответственность вызывающего, doc `switch.rs:29-31`) | — | отложено — T09-01 (lock hierarchy) |

Spec 05 §6 — Validate-on-open `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Предикат-таблица в заданном порядке (status→active/projected→head-missing→op_id→head-tuple→count→manifest) | `validate::validate` `validate.rs:83-118` | `not_clean_fires_alone`, `active_projected_mismatch_fires`, `missing_head_fires`, `op_id_mismatch_fires`, `head_tuple_mismatch_fires`, `point_count_mismatch_fires`, `manifest_mismatch_fires_on_equal_count_different_ids`, `earlier_predicate_wins_over_later_ones` | as-built |
| Выполняется на КАЖДОМ open (daemon start/LRU reopen/post-crash), без исключений | `open_and_validate` `rebuild.rs:426-506` — единая точка входа, вызывающая `validate` для любого открытия | `valid_shard_stays_valid_and_second_open_is_a_true_no_op` (2-й open — истинный no-op), F5–F10 (`fault_matrix.rs`, post-clean corruption ловится на СЛЕДУЮЩЕМ open) | as-built |
| Manifest hash — сильная проверка (equal count, different IDs) | `manifest_hash` пересчёт `rebuild.rs:464-473`, `validate.rs:114-116` | `manifest_mismatch_fires_on_equal_count_different_ids` (unit), F8 (`fault_matrix.rs`, e2e) | as-built |
| Результат валидации записан (`status→dirty` до rebuild), крах во время валидации сам re-enter'ит путь | `mark_dirty` `rebuild.rs:188-209` (легален из ЛЮБОГО статуса) | покрыто структурно — `mark_dirty` вызывается первым шагом `rebuild()` до любого I/O над шардом | as-built |

Spec 05 §7 — Rebuild — единственный recovery-путь `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Full rebuild — ВСЕГДА destroy/quarantine-then-recreate, никогда diff | `rebuild()` `rebuild.rs:309-417` (свежий шард, полный upsert expected-сета) | `stale_head_tuple_triggers_rebuild`, `unopenable_shard_is_quarantined_and_rebuilt` | as-built |
| `dirty→rebuilding` — единственный легальный прямой переход (FSM), псевдокод spec = 3 committed tx | `mark_dirty→begin_rebuild→finish_rebuild` `rebuild.rs:188-264` | покрыто всеми `rebuild.rs`/`rebuild_faults.rs`/`fault_matrix.rs` (F11/F12) тестами | as-built (as-built note зафиксирована в 05-projection.md T07-04) |
| Rebuild таргетит ТОЛЬКО active tuple, никогда не резюмирует прерванный switch-target | `begin_rebuild` `rebuild.rs:215-236` (`target_generation_id`/`target_model_space_id: None`) | `bootstrap_before_any_switch_is_no_active_tuple`, `stale_head_tuple_triggers_rebuild` | as-built |
| Missing vectors → recompute перед head-write; шард НИКОГДА не `clean` с partial set | `rebuild()` `rebuild.rs:375-387` (весь `Vec` собран ДО первого `upsert`) | `missing_vector_during_rebuild_leaves_status_rebuilding_and_no_partial_shard` | as-built |
| Rebuild идемпотентен (детерминированные IDs + desired-set) | destroy/quarantine-then-recreate — deterministic point IDs (05 §3) | `valid_shard_stays_valid_and_second_open_is_a_true_no_op`, F11 (`rebuild_faults.rs::crash_during_rebuild_leaves_rebuilding_and_retry_converges`) | as-built |
| Quarantine (не destroy) ТОЛЬКО при unopenable (F12); ≤2 rebuild cycles `[SPEC]` | `quarantine_shard`/`rotate_quarantine` `rebuild.rs:270-303`, `QUARANTINE_RETENTION=2` `:58` | `unopenable_shard_is_quarantined_and_rebuilt`, `quarantine_rotation_keeps_at_most_two` | as-built |

Spec 05 §8 — Shard lifecycle follows registry lifecycle `[FIXED]`:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Orphan shard dirs GC'd at startup sweep | `housekeeping::sweep_orphan_shard_dirs` (T06-03/D-004) | (см. G06) | as-built — покрыто G06, вне scope T07 |
| Quarantined shards kept ≤2 cycles `[SPEC]` | см. выше (§7 строка) | `quarantine_rotation_keeps_at_most_two` | as-built |
| Grace-destroy `removing`/`detached` worktree, 7 days `[SPEC]` | — (нужна миграция `removed_at`, которой нет) | — | отложено — группа 07/09 per D-004 (не эта карточка) |
| Dormant worktree model migration — offline worktree мигрирует на default space при следующем open `[FIXED]` | — (нет production wiring в `crates/projection`; требует daemon open-path + real model-space registry) | — | отложено — daemon open-path (группа 15) + T11-01 (real registry); `switch()` сам протокол уже готов и был бы переиспользован |

Spec 05 §10 / 14 §3 — Fault-detection matrix F1–F12 `[FIXED]`:

| # | Injected fault | Artifact/test | Статус |
| --- | --- | --- | --- |
| F1 | kill между write-ahead и первым backend-op | `switch_faults.rs::backend_error_leaves_detectable_updating` (расширен T07-05 до `open_and_validate` → `NoActiveTuple`) | as-built |
| F2 | kill mid-upsert batch | `fault_matrix.rs::f2_kill_mid_upsert_leaves_status_updating_and_stale_head` | as-built |
| F3 | kill после point-ops, до `write_head` | `fault_matrix.rs::f3_kill_before_write_head_leaves_stale_op_id` | as-built |
| F4 | kill после `write_head`, до SQLite commit | `fault_matrix.rs::f4_kill_before_final_commit_leaves_head_ahead_of_active` (новый seam `projection.switch.before_commit`) | as-built |
| F5 | shard WAL loss после clean commit | `fault_matrix.rs::f5_post_clean_point_loss_detected_at_next_open` | as-built |
| F6 | partial point deletion, intact catalog | `fault_matrix.rs::f6_partial_point_deletion_detected_at_next_open` (честная заметка: fake обновляет count вместе с манифестом — `PointCountMismatch`, не «intact catalog»-специфичный) | as-built |
| F7 | missing/stale head | `fault_matrix.rs::f7_missing_head_detected_at_next_open` | as-built |
| F8 | equal point_count, different ID set | `fault_matrix.rs::f8_equal_count_different_ids_detected_at_next_open` (+ unit `validate.rs::manifest_mismatch_fires_on_equal_count_different_ids`) | as-built |
| F9 | failed upsert/delete reported ok | `fault_matrix.rs::f9_backend_reported_success_but_corrupted_content_caught_at_next_open` (`switch()` возвращает `Ok`, расхождение — на следующем `open_and_validate`) | as-built |
| F10 | backend flush failure swallowed | `fault_matrix.rs::f10_same_as_f5_backend_flush_failure_swallowed` (буквально «same as F5» по тексту спеки) | as-built |
| F11 | crash during rebuild | `rebuild_faults.rs::crash_during_rebuild_leaves_rebuilding_and_retry_converges` | as-built |
| F12 | corruption делает шард unopenable | `rebuild.rs::unopenable_shard_is_quarantined_and_rebuilt` | as-built |
| — | Механическое покрытие 12/12 (reusable artifact) | `fault_matrix_coverage.rs::every_declared_fault_row_has_exactly_one_executable_test` — сверяет `fixtures/fault/matrix.json` ↔ `FAULT_MATRIX_COVERAGE` без `serde_json` (dependency-free scan) | as-built |

Spec 14 §2/§3 — Acceptance gates / fault-injection suite (опора):

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| reliability: ANY projection divergence detected at open → rebuild БЕЗ manual clear | `open_and_validate` автоматически чинит через `rebuild` при любом `Divergence` | все F1–F12 + `stale_head_tuple_triggers_rebuild` | as-built |
| consistency: validate-on-open matrix (05 §10) fully green | см. таблицу F1–F12 выше | 12/12 | as-built |
| Named `fail_point!`-style kill points, каждая строка — специфичный сигнал (не «eventually rebuilt») | `local_rag_test_support::fail_point!` (T00-03) + `Divergence`/`RebuildCause` типизированные варианты | каждый F-тест матчит конкретный `Divergence`/статус | as-built |
| Generation-mixing под конкурентной нагрузкой (14 §4) | — (нет real per-worktree read-lock над hybrid pipeline) | — | отложено — T09-01/T09-03 (locking), группа 12 (hybrid search) |

Guardrail T10 — No premature backend coupling:

| Требование (маркер) | Artifact | Verifying test | Статус |
| --- | --- | --- | --- |
| Единственный имплементор `ProjectionStore` — `FakeProjectionStore` (T07-01), не «просто test double» | `fake.rs` module doc `lib.rs:19-22` | `fake_shard.rs` 9 OK, `fake_faults.rs` 7 OK | as-built |
| `Cargo.lock` без новых внешних зависимостей от группы 06 к 07 | `git diff --stat Cargo.lock` = пусто на момент гейта | — | as-built |
| Нет qdrant/usearch/onnx/candle/ollama/reqwest/hyper/tonic/hnsw/faiss в `crates/*/Cargo.toml` | grep по всем манифестам крейтов = NONE | — | as-built |
| `optimize()` — policy-driven по метрикам, thresholds `[OPEN]` (05 §9) не закрыты | `fake.rs::optimize` — no-op stub | (не упражняется ни одним F-тестом — корректно, thresholds не существуют до T10) | НЕ закрыт молча — T10 |

Заметки G07 (не отклонения):

- Gate не писал ни продуктового кода, ни новых тестов. Все 70 тестов группы 07 (26 lib + 44
  integration через 8 test-бинарников, полный набор виден только под `--features failpoints`)
  уже существовали и были зафиксированы построчно в evidence T07-01…T07-05/D-005 выше.
- Явные пункты карточки G07 подтверждены: F1–F12 matrix 12/12 (мехнически сверено); two-axis
  serialization (полная truth-table T07-02, не расширялась); no durable-barrier assumption
  (F5/F9/F10 доказывают detectability без durability-претензии к бэкенду); validate on every
  fake reopen (`open_and_validate` — единственная точка входа, без «доверенного» пути); recovery
  only from active canonical state (`begin_rebuild` явно очищает `target_*`); backend-specific
  crate dependency отсутствует (guardrail T10 таблица выше).
- `[OPEN]` осмотрены и НЕ закрыты молча: backend choice (05 §1 roadmap step 11) и `optimize`
  thresholds (05 §9) остаются open — единственный имплементор всё ещё `FakeProjectionStore`,
  что и требуется до T10.
- Отложено by-design (seam на месте, указывает на owning-карточки, не backlog): dormant-worktree
  model migration (05 §8) → daemon open-path группа 15 + T11-01; grace-destroy `removing`/
  `detached` (нужна миграция `removed_at`) → группа 07/09 per D-004 (эта карточка её не
  расширяла); generation-mixing под нагрузкой (14 §4) + real per-worktree WRITE/READ lock → T09/
  группа 12.
- Отклонений не обнаружено; `DEVIATIONS.md` D-001…D-005 остаются `resolved`; историческое
  evidence не переписывалось. Итог: **PASS** — группа 08 разблокирована.

Примечания к T00-03:

- Крейт `test-support` — dev-only по образцу `xtask`: добавлен в `members`, но не в
  `default-members`, поэтому продуктовый `cargo build`/дистрибуция его не трогают, а
  `cargo test --workspace` (внутри `cargo xtask ci`) покрывает. Потребляется только как
  `[dev-dependencies]` (сейчас — из `local-rag`).
- Изоляция temp store — по путям: каждый `TempHome` владеет своим каталогом; `LOCAL_RAG_HOME`
  ставится только в окружение дочернего процесса (`Command::env`), в родительский процесс env не
  пишется (иначе параллельные тесты гонялись бы за глобал). `$HOME` не читается нигде — база
  `std::env::temp_dir()`; страж-тест проверяет, что путь не под `$HOME`, `.command()` дополнительно
  делает `env_remove("HOME")` для дочернего процесса.
- Failpoints реализованы registry-strict (карточка: «неизвестный failpoint отвергается»): имя
  надо объявить (`register`) до `arm`; `arm`/`eval`/`disarm` неизвестного имени → `Err(Unknown)`.
  `Action::Abort` = crash-точка (симуляция kill), `Panic`, `Error` (early-return через 2-арг
  `fail_point!`). Механизм закрывает GAP-06; сами строки F1–F12 (T07-05) и S1–S8 (T13-06) здесь
  НЕ писались (scope-guard).
- Осознанное решение по зависимостям: харнесс на чистом `std`, без `tempfile`/`uuid`/`serde`,
  чтобы сохранить свойство T00-02 «`Cargo.lock` = 0 внешних источников». `fixtures` отдаёт пути и
  сырые байты/строки; типизированный JSON-парсинг отложен до задачи-потребителя, которая обоснует
  зависимость по dependency policy (`CONTRIBUTING.md`).
- Doctest edition 2024: `gen` — зарезервированное слово, переменная в примере переименована.
- Отклонений не обнаружено; DEVIATIONS.md без изменений; `fixtures/manifest.json` GAP-06 не
  переписан (историю не трогаем).

Примечания к T00-02:

- Три бинарника соответствуют spec 13 §1 (`local-rag` = daemon+CLI, `local-rag-proxy` = stdio
  MCP proxy, `local-rag-hook` = spool writer); бизнес-логики нет — единственная команда `version`.
- Единый full-check = `cargo xtask ci` (dev-only крейт `xtask`, `.cargo/config.toml` alias); тот же
  command вызывает CI. `xtask` исключён из `default-members`, но покрыт `cargo test --workspace`.
- CI config lint реализован как `xtask/tests/ci_config.rs`: текстовые проверки, что
  `.github/workflows/ci.yml` вызывает `cargo xtask ci` на одном host, а toolchain запинен на 1.96.1.
- Deferred scope не заведён: нет dense/model SDK, tree-sitter, rusqlite и сетевых крейтов.
  Мультиплексирование бинарников через argv0 (spec 13 §1) отложено до дистрибуции (T17).
- Отклонений не обнаружено; DEVIATIONS.md без изменений.

Примечания к T00-01:

- Формат fixtures: JSON + JSON Schema (Draft 2020-12) в `fixtures/schema/`; валидатор
  `fixtures/tooling/validate.py` покрывает 4 проверки карточки (schema validation, ID
  uniqueness, runner dry-run, no backend-specific fields). Это временный bootstrap: Rust-harness
  из T00-03 переиспользует те же fixtures и схемы.
- Baseline v1 снят живым прогоном `scripts/benchmark.ts` (Qdrant 1.18.2 + Ollama на localhost,
  docker не использовался). Правки применялись только к сборочному артефакту
  `dist/scripts/benchmark.js` (форс code-only; исключение вендоренных `node_modules` из обхода
  корпуса) и **возвращены** после прогона; исходники v1 не менялись. Сырые артефакты —
  `fixtures/search/baseline/`.
- Общая workspace quality-команда ещё не существует (создаётся в T00-02), поэтому она не
  запускалась; выполнены доступные проверки: `validate.py` (stdlib + jsonschema), негативный
  self-test и well-formedness всех JSON. Rust/cargo проверки неприменимы (production-кода нет).
- DEVIATIONS.md без изменений: gap-register разрешён карточкой («manifest OR explicitly
  registered blocking gap»), нормативных расхождений спека↔v1 не обнаружено.
