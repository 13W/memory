//! `local-rag` code indexing / reconcile layer.
//!
//! Scaffold only (T00-02): no indexing logic yet. Re-exports the workspace
//! version so downstream crates share a single source of truth.

pub use local_rag_core::VERSION;
