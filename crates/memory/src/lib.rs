//! `local-rag` durable memory + observations layer.
//!
//! Scaffold only (T00-02): no memory logic yet. Re-exports the workspace
//! version so downstream crates share a single source of truth.

pub use local_rag_core::VERSION;
